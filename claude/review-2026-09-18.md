# The 2026-09-18 list — what is already live, and what is left

Addie sent fifteen things in one message. **Eight of them were already built and merged
to main before this review started** — several of them the day before, by parallel
sessions working the same message. This file is the comparison she asked for
("compare it to my current repo"), so the first job is to say which is which: chasing a
bug that was fixed yesterday is the most expensive thing a session can do here.

Measured against `origin/main` at `52cd392`. Baseline when this review started: **7059 passed, 0 failed**. After the work below, and
after merging main on 2026-09-18: **7183 passed, 0 failed**.

⚠ **This file is the state at the end of that session.** Several items moved while it was
being written — parallel sessions were working the same message — so re-read `git log`
before trusting any line of it.

---

## A. Already on main — go and look before reporting these again

| # | What she asked for | Where it landed | Notes |
|---|---|---|---|
| 1 | Portal light change should show in Customers | `f7cb297` | Edit Customer was drawing `lightColors` while the portal writes `lightsDescription`. It reads `houseLightsText` now — the same answer the warehouse and both printed sheets have always used. **The warehouse was never wrong**; only this one screen was. |
| 3 | A warehouse note on the customer | `3b7acad` | **Notes for the warehouse** on Add and Edit Customer (`warehouseNote`), plus a one-time note cleared when the bundle is made. Prints in the Notes column of **both** build sheets — the Printing tab's sheet had no Notes column at all, which is why a note read as not working. |
| 4 | Linda Hunley still scheduled after switching her to pending | `9e22c7e` | `dropHousesWhoLeftSeason` now runs on the five-minute sync. ⚠ **See question 3 below — her sentence and this fix disagree.** |
| 7 | People scheduled but saying no (Miko Johnson) | `9e22c7e` | Same fix. The rule existed since 2026-08-22 but only ⚙ Recalculate everything ran it. |
| 9 | Madi Blackhurt scheduled for Oct 22, Oct 22 not on the schedule | `scheduledDayIsReal` ([[SCH-73]]/[[SCH-74]]) | The row checks the day is real, and an orphaned booking is cleared by the sweep rather than only flagged. |
| 12 | A timer coming out should show in the warehouse | [[WH-34]] | **Remove Timer** is its own block under Timers, each row with its own Done. |
| 13 | Re-quotes awaiting response belong under Awaiting Response | `786acce` | Only once it has actually gone out — a re-quote still being written stays in Re-quotes. Reverses half of the old folder ruling and says so. |
| 15 | Re-quote emails are not sending | `e925055` ([[QT-42]]) | `getEmailTemplateByName` is exact and case-sensitive and was asked for the literal `"ReQuotes"`. A first quote falls back to a built-in body and still sends; a re-quote **refused outright** — which is why it reads as re-quotes being broken rather than a setting being wrong. It warns now instead of refusing. |

⚠ **Item 15 is worth her checking herself.** The code no longer refuses, but if her
Templates list has no template named exactly `ReQuotes`, the send now goes out on the
built-in re-quote wording and says so. That is a setting, not a bug.

---

## B. Done in this session

| # | What she asked for | Ruling | Where |
|---|---|---|---|
| 6 | Paying should not confirm somebody who hasn't replied | **[[SCH-82]]** — narrows her own [[SCH-79]] of the day before | `housePaidThisSeason`, `paymentSeasonYear`; Suites 338/339 |
| 11 | Instruct them to pick wire colour from their gutter | **[[OPT-21]]** — narrows [[OPT-12]]'s removal; picker back, Any writes nothing | portal + `portalSave` guard; Suite 340 |
| 10 | The RSVP question should go away once answered | **[[RS-62]]** — collapsed, never removed | `renderPortalRsvp`; Suite 341 |
| 8 | Everything into the Inbox; folders she deletes stay deleted | **[[MSG-28]]** — reverses half of [[MSG-07]] | `MESSAGE_HOME_FOLDER`, `inboxFolderSeed`; Suite 342 |
| 4 | Pending comes off the schedule | **[[SCH-83]]** — confirms [[SCH-78]], no code change | — |

Every one red-checked. **36 sabotages across the four, all caught.** Seven of them missed
on a first pass and *every one of those was a weak check of mine rather than a bad
sabotage* — a fixture that could not reach the new state, a match satisfied by a
declaration spelled the same as the thing being tested, a word found in a comment rather
than in the page. That ratio is the argument for red-checking at all.

⚠ **Three of these reverse or narrow a ruling she gave within the last 48 hours.** Each
old row is marked `Superseded → #ID` rather than edited, and each new row carries the old
reasoning, because in every case the earlier answer was right about something the newer
one does not repeal.

---

## C. Still open

### 2 · "Waiting on light colours"
**Landed on main while this was being written** — `ca93296`, those houses read *Check
lights* now. She was doing it in another tab. Nothing owed here.

### 14 · The nudge ladder: text at 10 days, email at 20, archived at 30
Today `runQuoteNudgeBatch` sends **email only**, `QUOTE_NUDGE_WAIT_DAYS = 10`,
`QUOTE_NUDGE_MAX = 2`, and nothing is ever archived. So the first rung exists and is the
wrong channel, the second exists, and the third does not exist at all.

⛔ **The SMS rung needs her before it is built, and this is why.** It is a real Twilio send
to a real phone, on a schedule, to people who have not replied — the one rung here that
costs money per message, can annoy a customer, and cannot be taken back once sent. §5 of
`CLAUDE.md` names spending real money as one of the few things that genuinely needs the
owner. The email and archive rungs carry none of that and can go first.

⚠ **And archiving after 30 days is a data decision, not a display one.** A quote moved to
archived leaves the Awaiting Response list for good; if the customer answers on day 31 the
office needs a way to find them. Worth settling before it is built rather than after.
