# Questions Map — Addie's rulings

**What this is.** Every judgement call Addie has made about how Highlighting Utah should
work, in one place, so it is never asked twice and never answered two different ways.

**What this is NOT.** Not how the system works (`system-map.md`), not conventions
(`CLAUDE.md`), not facts about the code. If a machine could derive the answer by reading
the code, it does not belong here. Only decisions a person made.

**Where it lives.** `claude/questions-map.md`, committed to the repo alongside
`CLAUDE.md`, `system-map.md` and `docs/RULES.md`. ⚠ This paragraph used to say there was
no `claude/` folder on `main` and that only two docs were committed. That stopped being
true and nothing noticed — which is the exact rot this file exists to prevent, in its own
header. `git ls-files claude/` is the answer, not this sentence.

**Three rules for keeping it true:**

1. **Every session that gets a ruling appends to it before it ends.** Same standing
   obligation as the hole-hunting job — not something to do later.
2. **A superseded answer is never edited in place and never deleted.** It is marked
   `Superseded → #X` and left where it is. The old reasoning is usually why the new
   ruling is right, and deleting it invites somebody to rebuild the old one.
3. **The "Where it's proved" column is the point.** A row with a code anchor can be
   checked. A row with only a doc name is a claim. Prefer the first; mark the second.
4. **An answered question in `docs/open-questions.md` names its row here.** That file's
   *Resulting map change* line is the link, and from 2026-08-26 it must name a row ID
   (`MON-01`) when the answer was a ruling. `questions-map.test.js` enforces both ends.

**What checks this.** `npm run test:questions` — part of `npm test`, so it gates every
merge. It cannot tell that a ruling was given and never written down; nothing can. What
it does catch is the map rotting: a half-written row, a duplicate ID, a status outside
the set, and above all a `Superseded → #X` pointing at a row somebody deleted, which is
how the reasoning behind a current answer goes missing.

**Reading the columns.** *Decided* is the date on the message. A date in (brackets) means
the docs record it as settled but no dated quote survives — treat those as weaker.
*Status:* **Standing** (live), **Superseded** (answered differently since),
**Closed** (settled and explicitly not to be revisited), **Decided — not built**
(ruled on, code not written yet).

---

## Money and fees

| # | Question | Her answer | Decided | Where it's proved | Status |
|---|---|---|---|---|---|
| MON-54 | There is no way to waive a late invoice fee. How should waiving work? | **An × beside the line, in both Invoices and Customers.** *"right now we don't have a way to waive a late invoice fee. Can we make a way to waive that?"*, then on the shape: *"we can just have a spot we push x on the late fee and can be place both in invoice and costumers under fee."* ⚠ NO LATE FEE IS CHARGED TODAY and none was built here — that rule is decided and unbuilt ([[PROC-32]]: $25 if they have paid something, $40 if they have not). ⭐ SO THE × IS BUILT AGAINST THE LEDGER, NOT AGAINST A NAMED FEE. Every charge on a bill is already a `changeFeeNotes` entry, so waivability is decided by NOT being the carried debt rather than by a list of kinds — which means a late fee written by Phase 5 arrives with its × already working and nothing to remember to change. A whitelist would fail silently: the new fee simply has no ×, and the screen looks like one where nobody was charged. ⚠ IT REPLACED A BUTTON THAT CLEARED THE LOT. "Remove light-change fee(s)" wrote `changeFeeNotes: []`, and that array had long since stopped being light-change fees alone — see [[MON-55]]. | 2026-09-02 | `ledgerWaiveUpdates`, `waiveLedgerLine`, `ledgerLinesHtml`; fee-waive.test.js, 13 sabotages red-checked | Standing |
| MON-55 | May the office waive last season's carried debt with the same ×? | **No — that line alone has no ×.** Not asked as a question; it is the consequence of MON-54 and it is recorded because the answer is already hers. The carried debt rides in the same fee ledger as every other charge, and `arrearsOutstanding` is what holds an unpaid customer off the schedule — so an × on that row is the *hang them anyway* button she was offered and turned down ([[MON-38]]; js/money.js says it in as many words: a credit lifts the hold because it is money, on the invoice, visible to the customer, and "Addie asked for no 'hang them anyway' button and there is none"). ⚠ THE OLD REMOVE-ALL BUTTON WAS EXACTLY THAT BUTTON WEARING ANOTHER LABEL: one press wrote off a real debt AND released the hold, silently, from a control that said it removed a light-change fee. ⭐ THE ROW STILL SAYS WHERE IT IS EDITED rather than showing an empty gap — a row with no × beside rows that have one reads as a bug instead of a rule. It is corrected in Edit Customer › Owed from a previous season, which names the season and says where the figure came from. | 2026-09-02 | `ledgerLineIsWaivable`; fee-waive.test.js — the refusal is the check with its own named CI step | Standing |
| MON-56 | Should the × be on discounts as well as fees? | **Yes, on both.** *"to be honest we should have an x next to all discounts and fees to get rid of those if necessary."* ⭐ ONE RULE OVER TWO LEDGERS, not two removers: `creditNotes` sums to `credits` exactly as `changeFeeNotes` sums to `changeFees`, and both totals land in the same `computeInvoiceStatus` — two implementations is two chances for a bill to stop adding up. ⚠ NOTHING ON THE DISCOUNT SIDE IS PROTECTED, deliberately. The one refusal exists for the carried DEBT ([[MON-55]]); a credit can only ever be money coming off, so refusing one would be protecting the business from its own office. ⚠ AND A REFERRAL CREDIT IS DERIVED, NOT STORED — `referralCount` on the customer is rebuilt into a credit on every Edit Customer save, so taking the line off without zeroing the count is an × that visibly works and is undone by the next save, from a screen that never showed the count. The count is cleared in the same press. ⚠ ONE PRESS CHANGES ONE THING: the untouched ledger's total is taken as stored rather than re-summed, so crossing off a discount cannot quietly undo an amount the Invoices panel set by hand. | 2026-09-02 | `LEDGERS`, `ledgerWaiveUpdates`, the referral branch of `waiveLedgerLine`; fee-waive.test.js §9 | Standing |
| MON-63 | What is the set-up fee? | **$25.** *"make the set up fee $25."* It was $30. ⚠ IT WAS A BARE LITERAL IN TEN PLACES IN admin.html AND TWO IN functions/index.js, plus a dozen sentences on screen that said "$30" in words — so moving a price meant finding all of them, and missing one meant the office quoting one figure while the nightly run charged another. It is now `NEW_MEMBER_FEE`, once in `js/money.js` and once in `functions/index.js`, and **money-parity.test.js fails the build the moment the two differ**. Every sentence that prints it reads it, including the invoice line and the emailed one; the two static checkbox labels are stamped from it at start-up. ⚠ IT IS THE JOIN FEE, NOT THE LIGHT-CHANGE FEE. Both were $30 and they are separate charges — a new member who changes colours late pays both — so moving one must never move the other. The change fee is untouched at $30. ⚠ AND IT IS NOT HISTORY: an invoice already carrying the old fee is recomputed against the new number the next time it syncs, so anybody charged $30 comes out at $25. One price for the season. ⚠ The one place that is not a recompute is Start New Season, which STRIPS the fee out — an old $30 invoice has $25 taken off and carries $5 too much. Written down at that line rather than fixed, because the fix is storing the amount on the invoice when it is charged, and the exposure today is nil: nobody has been billed this season. If the fee moves again mid-season, store it. | 2026-09-03 | `NEW_MEMBER_FEE` in js/money.js and functions/index.js; money-parity compares them, 6 sabotages red-checked | Standing |
| MON-57 | The portal shows a balance months before anyone is billed. What should it say? | **That it is their price, and that nothing is due until the invoice arrives.** *"Also for this I want to make it clear to the member that this is there payment however they do not need to pay until after they get an invoice from us."* ⚠ THE CARD READ "CURRENT BALANCE" FROM THE MOMENT A HOUSE WAS PRICED, which is months before the nightly run bills anybody — so a customer signing in during October to check their colours read a number that looks due today, above a pay button, while the banner directly above it promised we would be in touch with their install date. ⭐ THE LABEL MOVES TOO, not just the paragraph: "Current Balance" is the half somebody skims, and a note under a heading loses to the heading. It reads **Your Price This Season** until the bill goes out. ⚠ IT DOES NOT HIDE THE PAY BUTTONS. Paying early is real — `prepaid` is a value the master sheet already carries — so refusing money somebody is trying to give us would be a worse bug than the confusion. ⚠ AND IT IS SILENT WHEN THEY OWE FOR LAST SEASON, which is the one case where this message would cost money: a carried debt is on this year's bill before this year's bill is issued, so the flag is honestly false while the money is payable NOW and that customer is being held off the schedule until they pay it ([[MON-55]]). ⚠ DRIVEN BY `invoicedAt`, THE STAMP THAT ALREADY DECIDES THE DUE DATE and that Start New Season clears — not a second "have we billed them" answer derived from the houses, which would eventually disagree with it on a multi-house bill. ⚠ AND READ AS `=== false`, NEVER `!billIssued`: a field that never arrived means *we do not know*, and telling somebody holding a real invoice that they need not pay it is the expensive way to be wrong. ⚠ THE WORDS AND THE GATE STAND; THE CONTAINER MOVED — it is a pop-up now, and only on RSVP Approve ([[MON-59]], [[MON-60]]). Read the inline-box description below as history. | 2026-09-02 | `record.billIssued` in portalInvoice, `renderNotBilledYetNotice`; test/not-billed-yet.spec.js drives the real portal | Standing |
| MON-58 | A customer is stamped Confirmed and owes from last season. Does Confirmed win, or the money hold? | **Money hold wins.** Asked as one narrow question against Aaron Gardner's row — Confirmed, and $311.61 outstanding from 2025 — she answered in three words. ⭐ THAT BOUNDS HER EARLIER INSTRUCTION, given the same evening: *"if it is stamped confirmed than keep that and keep it scheduled if it has an on hold stamp or pending stamp lets fix that."* Confirmed persists **except** where money is owed; the arrears hold is the one thing that outranks a real reply. ⚠ NO CODE CHANGED — this is a ruling confirming what already ships, which is why it is worth writing down: `isOutForSeason` tests `houseOwesFromLastSeason` BEFORE it reaches the confirmed branch, so `seasonBadgeKey` (which delegates to it, [[MON-47]]) already returns `pending` for a debtor whatever their RSVP says, and `seasonHold` already names the reason as *"owes $311.61 from 2025"* rather than *"no RSVP yet"*. A future session reading her Confirmed instruction alone would have moved that test and released every debtor onto the routes. ⚠ AND THE ORDER IS THE WHOLE RULE, WITH NOTHING ELSE GUARDING IT: swapping the two branches returns the identical answer for every customer except this one, so a revert reads as a tidy-up. ⚠ IT IS NOT `houseIsOnTheBill` — a held customer is still CHARGED ([[MON-32]]); this decides only who gets a crew | 2026-09-03 | the arrears branch of `isOutForSeason` sits ahead of the `seasonRuleIsLive()` branch; `seasonBadgeKey`; `arrears-hold.test.js` runs the invariant over both rule states × both money states | Standing |
| MON-62 | A colour change made in the member portal never appears under the customer's fees. Where did it go? | **Onto next season's bill, because this season's had already gone out.** *"when you do color change or any other fees/discounts it should come up under their account fees and we should be able to waive it but when they make a change in member portal it doesnt show it on fees in the edit customer spot."* ⚠ WHERE THE FEE GOES DEPENDS ON WHETHER THE BILL HAS BEEN SENT — her own [[MON-09]] ruling. Unsent, `portalSave` puts it on the invoice as a `changeFeeNotes` line, and those have shown in Edit Customer all along. Already sent, it becomes `carryoverCharge` on the CUSTOMER, to be collected next season — and Edit Customer's fee lines only ever read the invoice, so that kind was invisible on the one screen anybody would waive it from. ⭐ There is a third ledger now, `carried`, drawn under the other two with the same ✕. It is the only one whose money lives on the customer record rather than the invoice, so the ✕ writes to `jobAddresses` — but the PLAN is built by the shared `ledgerWaiveUpdates`, because a second copy of "drop this line and re-total the rest" is how one ledger starts disagreeing with the others about what a ✕ does. ⚠ AND IT IS NOT REFUSED FOR WANT OF AN INVOICE: it exists precisely BECAUSE the bill had gone out, and a customer who has never been invoiced can carry one too. Refusing it there would be the same invisibility one screen further on. | 2026-09-03 | `LEDGERS.carried`, `waiveCarriedCharge`, `renderEditCustCarriedLines`; run-all.js suite 298 runs the arithmetic, 7 sabotages red-checked | Standing |
| MON-59 | MON-57 shipped as an inline box under the price. Should it be louder? | **Yes — a pop-up over the portal, not an inline box.** *"make it more obvious... like a popup instead."* Seeing a mockup with a second "Pay now" button beside the dismiss, she asked for one dismiss button only: the real pay buttons already sit on this same screen, and a second one that closes a dialog rather than charging anything reads as broken, not helpful. ⚠ SUPERSEDES ONLY THE PRESENTATION, NOT THE GATE — every condition in [[MON-57]] (`billIssued===false`, silent on arrears, silent on a $0 card, fails open on a missing field) is unchanged; only where the message is drawn moved, from `#invNotBilledYet` to `#portalPriceModal`. ⚠ SHOWS AT MOST ONCE PER PAGE LOAD. `renderCustomerInvoicePage` can re-run mid-visit — most notably right after the RSVP gate-code step closes — and a dialog popping back up after being dismissed would read as a broken close button; `portalPriceModalShown` gates that, and the modal is still force-closed if the condition goes false regardless of the flag. ⚠ SITS BELOW THE GATE-CODE DIALOG AND THE ARREARS LOCK (z-index 399 against 400/401) — both can be raised on the same load and both outrank it. | 2026-09-03 | `renderNotBilledYetNotice`, `#portalPriceModal`; test/not-billed-yet.spec.js rewritten for the modal | Superseded → MON-60 |
| MON-60 | MON-59 popped up on every ordinary sign-in, not only the RSVP flow it was designed alongside. When should it actually open? | **Only when they push RSVP Approve — never just from opening the member portal.** *"This should only pull up when they push RSVP Approve. It should not pop up every time they open there member portal."* ⚠ "RSVP APPROVE" IS `openPortalAfterYes`, reached only from `handleRsvpLink`'s yes branch — the RSVP email link — because that is the one action in this codebase with no other name and the same flow the gate-code confirmation already lives behind ([[RS-44]] territory). It is NOT the post-payment "are you having lights this year?" ask (`rsvpAskModal`) and NOT the in-portal Changes-tab RSVP toggle — both call `portalRsvp` through the same shared `[data-portalrsvp]` click handler but never touch `openPortalAfterYes`, so neither one raises this pop-up. ⚠ THE FLAG TRAVELS AS `showPriceModalOnLoad`, set in exactly one place (`openPortalAfterYes`'s call to `loadPortalByToken`) and read in exactly one place (`renderNotBilledYetNotice`'s third argument). Every other caller of `renderCustomerInvoicePage` passes nothing, so the safe default is "does not show" rather than "shows every time" — being wrong about a forgotten flag costs nothing here, where being wrong the other way costs a support call. ⚠ THE LABEL IS NOT GATED BY THIS. "Your Price This Season" still switches correctly on an ordinary sign-in; only the interrupting dialog is restricted to the RSVP-approve moment. ⚠ EVERYTHING FROM [[MON-59]] STILL HOLDS underneath this — the popup shape, the single dismiss button, showing at most once per page load, sitting below the gate-code dialog and the arrears lock — this ruling only narrows WHEN it is allowed to open at all. | 2026-09-03 | `openPortalAfterYes`, `renderNotBilledYetNotice(record, totalDue, allowPopup)` | Standing |
| MON-01 | New member who changes their lights after the first 48 hours — one fee or two? | Two, and they stack. $30 join + $30 change = $60. "new member fee and change light fees are seperate but should get charged seperetly for this so if new member changes lights again after 48 hours of being a new member than they should get charged for light change and new member fee which would put them at 6[0] dollars. So new member should not be assigned on routes within 48 hours." | 2026-08-21 | `applyLightChange` / `applyLightChangeServer`, money-parity | Standing |
| MON-02 | Where does the $30 change fee land if the invoice already went out? | Not sent → `changeFees` on the current invoice. Already sent → `carryoverCharge` on the customer, collected next season. "if invoice has already been sent out but they change there lights after invoice is sent out than the 30 dollars will be charged for next season but if invoice hasn't been sent out than it will be charged on there current invoice." | 2026-08-21 | `applyLightChange` → `feeDestination` / `carryoverCharge` | Standing |
| MON-03 | Charge a change fee silently, or ask? | Ask. `askLightChangeFee` is a popup with more than two answers — deliberately not a `confirm()` | 2026-08-21 | `askLightChangeFee` | Standing |
| MON-04 | Does waiving the fee also waive the route lock? | No. The lock is about the crew, not the money — waiving still sets the window and still raises the note | 2026-08-21 | `lightsLockedUntil` write | Standing |
| MON-05 | When does the 48-hour window open, and does a free change extend it? | Two events open it: becoming a customer, and a *charged* change. A free change does not extend it. The free window and the route lock are the same 48 hours, one field | 2026-08-21 | `lightsLockedUntil` | Standing |
| MON-06 | Does importing 960 customers open 960 windows? | No — only Add Customer opens one on joining | 2026-08-21 | `lightsLockedUntil` — only Add Customer opens one | Standing |
| MON-07 | Who counts as a new member? | By quote, not by date and never by bulk. And it expires — a 2026 quote is not new in 2027 | 2026-08-21 | `chargeNewMemberFee`, cleared by Start New Season | Standing |
| MON-08 | Charge a fee when a takedown is marked done? | "No we don't need that we'll just add on a fee if we need that." Removal is included in the price; `removal` stays unsettable; `removalDone` touches no invoice | 2026-08-21 | `computeInvoiceStatus` | Standing |
| MON-09 | Whose name goes on a shared bill? | The lowest customer number — the longest-standing account | 2026-08-19 | `custInvoiceKey` grouping | Standing |
| MON-10 | Can one payer cover several houses, and can they see it? | Yes — one member portal, one invoice, still two separate customers | 2026-08-19 | `billToPhone`, who-pays-for-whom screen | Standing |
| MON-11 | Does changing the price in Edit Customer raise a re-quote? | It asks, with an optional reason box. Feet and address still raise one without asking — the house is measurably different | 2026-08-21 | `askPriceRequote` | Standing |
| MON-12 | Does the app ever recalculate a converted quote's price from feet? | **No, never.** The price they agreed to becomes `housePrice` — they are billed the number they saw and agreed to, and any hand-typed price survives conversion | 2026-08-26 | `housePrice` | Standing |
| MON-14 | Does difficulty change the price? | **No — feet is the price.** Charging more for a hard house was raised and deliberately left alone: it is rare enough not to be worth money code today. ⚠ NOT rejected, only deferred — if it is ever wanted it is a money change, and MON-12 already protects a hand-typed price through conversion | 2026-08-26 | `difficulty` is used for day-sizing only, never money | Standing |
| MON-15 | Several people on one bill and one of them sits the season out — when does that bill go? | "After the last persons house is done if there are multiple people on one bill is when they will be charged." The wait was already right; who counted was not. A house sitting the season out is never visited, so it can never be done — it comes OFF the bill rather than being waited for, or the bill never goes at all. ⚠ A house hung and only THEN answering back-next-year still owes — and since MON-21 that is true of a flat "no" too, which this ruling deliberately left alone | 2026-08-26 | `houseIsOnTheBill` / `houseIsOnTheBillServer`, money-parity | Standing |
| MON-16 | A house billed to somebody else — what payment status does its own row show? | **The status of the bill it was added to.** "if Kyle didn't say no or back next year and his bill wasn't already paid or partially paid but it added to Dana's bill than should be paid in full by dana." ⚠ It also removes a false reading: the zeroed leftover invoice under that house's own key computes to Paid in Full on its own | 2026-08-26 | `getLiveInvoiceStatus` keys on `billToPhone` else `custInvoiceKey`, Suite 275 | Standing |
| MON-17 | A house moves onto somebody else's bill — what happens to money it had already paid? | **The money follows the house.** Fully paid → the payer owes nothing more for it; half paid → "Just pay what hasn't been paid yet"; unpaid → a fresh bill, "they will get another bill showing what they still owe". ⭐ All three are ONE mechanism — if it ever needs three branches it has been implemented twice | 2026-08-26 | `carriedPaymentOnBillToChange` → `paidBeforeBillTo` → a `kind:'carried'` credit in `syncPayerInvoice` | Standing |
| MON-18 | Is somebody who said Back Next Year still billed for this season? | **No — take them off the bill.** ⭐ Same rule as MON-15, asked from the other side in a parallel session and answered the same way — ONE implementation, `houseIsOnTheBill`. They were already off every crew day and out of the build queue since 2026-08-22 and were still being invoiced. ⚠ Deliberately NOT `isOutForSeason`, which also means `needsLightRecycle` and would empty the whole book once `SEASON_ELIGIBILITY` flips | 2026-08-26 | `houseIsOnTheBill` / `houseIsOnTheBillServer` (folded into MON-16 on the merge), money-parity | Standing |
| MON-19 | What does each person's invoice ask them for? | **What that person is paying.** "if dana is paying for kyle than she gets her bill and kyles bill on the invoice." The payer's itemises a row per house; a house billed elsewhere shows its own price, **Due from you $0.00**, who is paying — and no status and no how-to-pay block at all | 2026-08-26 | `buildInvoiceDocHtml`, Suite 278 | Standing |
| MON-20 | When Who Pays for Whom is retired, does its Excel export survive? | **Keep the Excel export somewhere.** The whole-book view goes with the tab; the export does not. Where it lands is a placement decision, not a rule — the Invoices tab is the obvious home | 2026-08-26 | — retirement is deliberately after a season of the house tabs | Decided — not built |
| MON-21 | A house that was HUNG and afterwards said no — does it still get charged? | "Any house hung no matter what should be charged. This will only be overuled if it is our fault... if we hung the lights and there is no reason to not charge them than we will charge them still." So `completed` is tested FIRST, ahead of every status. ⚠ "Our fault" is the office writing it off with a credit — never an automatic test, which would be the app guessing at fault | 2026-08-26 | `houseIsOnTheBill` / `houseIsOnTheBillServer`, money-parity | Standing |
| MON-22 | Who is in the season once the RSVP has gone out? | **"Only people that RSVP are yes and should be scheduled and invoiced."** ⚠ RECORDED, NOT YET SWITCHED ON — `SEASON_ELIGIBILITY` already implements it and flipping it before the RSVP is sent puts every unanswered customer out of the season, which is nearly everybody. Needs a date from her (Q-010a). ⚠ It also widens the rule to INVOICING, which today is a separate question by design; MON-21 is what makes that safe, since anyone whose lights are up is billed whatever they answered | 2026-08-26 | `SEASON_ELIGIBILITY`, `isOutForSeason` | Superseded → RS-17 |
| MON-26 | Should only people who answered the RSVP be scheduled and invoiced — and how do we stop crews reaching non-repliers? | **Hardcode it AND warn me.** `SEASON_ELIGIBILITY` defaults to `confirmed-only` rather than being a switch she must remember. ⚠ Gated by `seasonRuleIsLive`: the RSVP must have gone out AND the reply window closed, or hardcoding empties the season on day one. ⚠ The BILLING is not where this is fixed — MON-21 says a hung house is charged; the only place to break the chain is not sending a crew. ⭐ Health Check `seasonRuleDrops` is the warning half, silent until the RSVP has gone out | 2026-08-26 | `seasonRuleIsLive`, `RSVP_REPLY_DAYS`, Health Check `seasonRuleDrops` | Standing |
| MON-27 | Should everything that can be changed on, or added to, a member account carry a date? | **Yes.** "Everything that can be changed for members or added to members account including 30 dollars fees or 25 dollar referall discount, anything member portal should be dated." ⚠ MOST OF IT ALREADY WAS, and checking first is what kept this small: the $25 referral, manual discounts, carried credits, manual fees, the automatic $30 change fee and the carryover charge each carry a `date` on their own note. ⚠ TWO DID NOT. The recycle queue stamped at two of its six places, so a house carried a date only if it reached the list through one particular button; and the $30 JOIN fee had none at all, because it is folded straight into `install` rather than listed like the change fee — the flag said IF, never WHEN. ⚠ AND THE EDITS ARE DELIBERATELY NOT DATE FIELDS: an address, timer or sides change belongs in the activity log, which records WHAT it changed to. A bare date would say the address moved and not where to, which is the half she actually asked about. | 2026-08-28 | `stampRecycleRequested` / `stampRecycleRequestedServer`; `newMemberFeeAppliedAt` in runInvoiceBatch; queue-date.test.js | Standing |
| MON-13 | ~~One fee or two for a new member changing lights~~ | ~~Only one — the change fee is folded into the join fee~~ | 2026-08-19 | — | Superseded → MON-01 |
| MON-23 | A stored phone the invoice rebuild cannot match — measure it first, or just fix it? | **"yes"** — do the query-side fix, asked right after she said she does not read Health Check (HC-03), which is where the measurement was parked. `syncPayerInvoice`'s phone branch now falls back to the loaded `jobAddresses` list when the equality query resolves nobody — the same fallback the email branch already had. ⚠ It asks `custInvoiceKey`, never compares phone fields. ⚠ The STORED phone is deliberately not normalised: that changes what the office reads on every screen and export. ⚠ A mixed group (some digits-only, some not) still resolves only the digits ones — carried as Q-019a, not smuggled in | 2026-08-26 | `syncPayerInvoice`, Suite 10 | Standing |
| MON-25 | Q-019a — should the invoice fallback run always, or only when the lookup found nobody? | **"yes"** — always. Conditional, it fixed a payer whose ONLY house is stored formatted and missed the worse case: one payer with SEVERAL houses, one typed `(801) 111-2222` and the rest digits. The query finds the digits ones, the fallback never ran, and the formatted house left the bill — total and `billedHouseIds` both omit it, so the invoice adds up and is wrong. UNDERCHARGING, and it lands on the ~17 shared-phone households. ⚠ The EMAIL branch was widened too: identical shape, identical hole, and it was the precedent the phone fix was copied from. ⚠ Safe because `addHouse` dedupes by id and every added house already passed the same `custInvoiceKey` rule. ⚠ Stored phones/emails are still not normalised | 2026-08-26 | `syncPayerInvoice`, Suite 10 | Standing |

| REF-01 | When does a referral earn its $25, and is there a limit? | **When the referred person actually becomes a customer, with no limit.** Her plan: *"$25 applies automatically once the referred quote **converts to a paying customer** (i.e. reaches `jobAddresses`, whether via new-customer conversion or a re-quote applied to an existing customer) — not at the point they merely request a quote."* ⚠ THE EXISTING BOX COULD NOT DO IT, which is why this is a second mechanism and not a setting on the first — she wrote that down too: *"Bumping `referralCount` with a background write does **nothing to the invoice** until someone opens that customer's record and hits Save."* A referral earned through a link has nobody opening anything. ⚠ SO `referralCount` IS NOW DERIVED from `referralCredits[]` rather than hand-typed, and the credit line is written at the moment the friend joins. | 2026-09-03 | `creditReferralIfAny`, `applyReferralCreditLine`, `referralCreditNote`; run-all.js suite 299 | Standing |
| REF-02 | How is somebody stopped from referring themselves? | **Either the phone or the email matching is a hard refusal.** *"The referred person's phone AND email must both differ from the referrer's on file. This is a hard requirement, not a soft flag"*, and *"any single match on either field is enough to reject — this is not 'both must match'"*. ⚠ AND NOTHING VERIFIES EITHER FIELD, decided and on the record: *"No identity verification of phone/email — considered and declined 2026-09-03"*, with the gap named — *"a referral can still be gamed by using a different but real friend's contact info... an accepted, documented gap, not an oversight."* ⚠ THE REFERRER'S OWN RECORD IS REFUSED FIRST BY DOCUMENT ID, before any field is compared: an existing customer re-quoting through their own link is the easy $25 this would otherwise hand out, and they match themselves on every field anyway. | 2026-09-03 | `referralIsSelfReferral` and the id check in `creditReferralIfAny`; suite 299, 3 sabotages | Standing |
| REF-03 | If the referred person cancels, does the referrer keep the $25? | **Only once the lights are up.** *"if the referred customer **cancels (`rsvpStatus` → `'no'`) before their lights are installed (`completed !== true`)**, the $25 is removed from the referrer. If they cancel **after** `completed: true`, the referral already did its job and the credit stands."* ⚠ AND NEVER OFF A SETTLED BILL: *"Paid-in-full invoices are never charged back... Clawback only applies while there's still an open balance to remove the $25 from."* That is the same reasoning as [[MON-55]] — a charge arriving after somebody has paid is the outcome nobody forgives. ⚠ THE ENTRY IS KEPT, MARKED REVOKED, never deleted; a list that quietly shortens is one nobody can audit. ⚠ AND BOTH DOORS OUT OF THE SEASON DO IT, the office dropdown and the customer's own portal — a decline through the RSVP link never reaches the office screen. | 2026-09-03 | `referralClawbackAllowed`, `clawBackReferralIfAny`, `clawBackReferralServer`; suite 299 §6–§8 | Standing |
| REF-04 | Does a refused referral leave any trace? | **Yes — an Inbox note, and the quote is marked so it is not retried.** She left this open and leaned that way: *"Should a **blocked self-referral attempt** also drop its own Inbox note, or stay fully silent? Leaning toward 'note it' for the same reason as the main audit trail, but flagging rather than assuming."* Built as noted, and flagged to her rather than buried. ⚠ IT IS ALSO WHY THE AUDIT TRAIL EXISTS AT ALL, in her words: *"otherwise the only way to see this happened is opening two customers' records side by side."* ⚠ THE MARK MATTERS AS MUCH AS THE NOTE — without it every save of that quote runs the refusal again and raises the note again. ⚠ All three referral notices sit in the MONEY section of the System tab; $25 off a bill belongs beside the other money notices rather than in Other, where nobody looks. | 2026-09-03 | `referralBlocked`, `referralMarkQuote`, `SYSTEM_NOTICE_SECTION_OF`; suite 299 | Standing |
| REF-05 | Where does the customer get their link, and what is in it? | **A "Refer a Friend" tab in their own portal:** *"a unique, unguessable link per existing customer, generated from inside the Member Portal, in a new **Refer a Friend** tab."* ⚠ NOT THE CUSTOMER NUMBER, and she said why: *"`customerNumber` is NOT used as the link identifier — it's printed on invoices and bins, and therefore guessable."* ⚠ AND NOT THE PORTAL LOGIN TOKEN EITHER. A portal token signs somebody in; this one is pasted into a group chat, so reusing it would hand the customer's own account — address, balance, the ability to cancel — to everybody they shared it with. Two tokens, two jobs. ⚠ **THE LOCK CLAUSE HERE IS SUPERSEDED → [[REF-07]]** (2026-09-04). It read: *the tab is locked behind unpaid arrears like every tab except Payment, Contact and Cancel, because earning credit towards a bill they are behind on is not one of the three that stay open.* That was MY inference and not her ruling, and it is kept because it is the argument somebody will make again: the tab is now OPEN to a customer who owes. | 2026-09-03 | `portalRenderReferral`, `ensureReferralToken`, `PORTAL_TAB_NAMES`; suite 299, portal-fields.test.js | Standing |
| REF-10 | The office screen says "make the link" — can every customer just have one? | **Yes, and nobody presses anything.** *"in customers it says refer a friend and make the link but we want the link to automatically be made for every customer can you fix that."* Three changes, no button left: every customer created from today is **born with one**, riding in the same write as their portal token at all four places a record is made; opening a customer in Edit Customer **makes one if they have none**; and one press in Bulk Updates gives the ~960 who were already on file. ⚠ THE ARGUMENT THE BUTTON WAS BUILT ON WAS WRONG, and correcting it matters more than overruling it: it said minting on open would "write to Firestore every single time anybody opened Edit Customer". It would not — `referralTokenFor` returns early the moment a token exists, so it is ONE write per customer for the life of the record, and none at all for anybody created after today. ⚠ THE GUARD THAT SURVIVES IS THE ONE THAT WAS ALWAYS RIGHT: it must not mint twice. That row is redrawn on a tab switch and after every save, so an unguarded mint writes a second token and the link the office has just copied is the one that stops counting. ⚠ AND THE BACKFILL ONLY EVER ADDS — anybody who already has a token is skipped — which is why it needs no typed confirmation the way the Danger Zone sweeps do: nothing it does can be undone because nothing it does is destroyed. | 2026-09-04 | the four creators, `editCustRenderReferLine`, `backfillReferralLinks`; run-all.js suites 276 and 299, both RUN rather than read | Standing |
| REF-11 | Can the referral link be shortened so it looks right in the RSVP button and in a text? | **`/r/<token>`, 39 characters instead of 61.** *"make sure the link isnt crazy long try to shorten it so in the rsvp refferal button it looks nice in the email/text."* ⭐ EXACTLY THE `/q/` QUOTE-LINK MECHANISM, which exists for the same reason and was built for the same complaint ("I don't want a long link"): a Netlify 200-rewrite, and the segment after `/r/` IS the token — no short-code table, no extra read, nothing to keep in step. ⚠ AND THE TOKEN ITSELF IS EIGHT CHARACTERS, from its own generator. `generatePortalToken` stays 20 because it signs somebody INTO their account; shortening THAT to fit a link in a text would be an account-security change nobody meant to make. A referral token only says who sent a friend — the worst a guess does is credit the wrong person's $25 — and the alphabet drops l/1/0/o, the pairs people get wrong reading a link aloud. ⚠ `?ref=` STILL WORKS FOR EVER: every link already pasted into somebody's messages carries the old spelling, and a referral that quietly stops counting is the one failure nobody reports — the customer simply never gets their $25. ⚠ AND IT IS PROVED BY LOADING THE PAGE, not by reading the rule. The `/q/` link passed a source check for its rewrite AND its path pattern and had never once worked, because a relative import died under that path and took every script with it. A browser spec caught a second one here: `REFERRAL_LINK_KEY` was a hoisted `var` declared below the reader, so the token was stored under the key "undefined" — right page, right hash, referral worth nothing. | 2026-09-04 | `_redirects`, `_headers`, `referralLinkFromToken`, `generateReferralToken` in admin.html and functions/index.js; test/quote-link.spec.js loads `/r/<token>` for real | Standing |
| REF-09 | Why is the referral link not showing up anywhere, and may a customer who owes for last season use it? | **Two faults, and the bigger one was mine.** Addie, 2026-09-04: *"its not showing up anywhere for some reason and i told you to have it in member portal and rsvp."* ⭐ **THE ARREARS LOCK WAS WHY.** I held the Refer a Friend tab behind unpaid arrears when it shipped ([[REF-05]], now marked superseded). Most of the book carries a 2025 balance this season, so the tab she had just asked for was greyed out on nearly every record she opened — confirmed on the live site before anything was changed: signed in with $311.61 owing, Refer a Friend greyed out beside Information, Light Colors, Sides and Changes. **Unlocked.** Dax's rule holds a customer to paying before anything goes INTO the system ([[MON-38]]); referring a friend puts nothing in — it writes to somebody else's record months later — and the only thing it can do to this bill is take $25 OFF it. ⚠ **THE RSVP HALF IS [[REF-08]]'s TOKEN, PUT IN THE DEFAULT BODIES.** That row settled the token and deliberately left the wording to her, because `etEnsureMaybeTemplateExists` only ever fills a BLANK body ([[MON-24]]). Both built-in RSVP bodies now carry the offer, which changes nothing about a template she has already written — so **an RSVP template that already exists in Firestore still needs `{{referral_button}}` adding by hand, once.** Said plainly rather than left as an assumption, because it is the difference between her seeing it and not. ⚠ **AND THE SERVER HAD TO LEARN THE TOKEN TOO.** [[REF-08]] resolves it in admin.html only; `runArrearsRsvpBatch` in functions/index.js sends the Not Paid RSVP with no browser involved, so the moment that template carried the token the automatic chase would have posted a literal `{{referral_button}}` to a real customer. The {{photo}} failure, in a new place. | 2026-09-04 | `PORTAL_UNLOCKED_TABS`, the `{{referral_button}}` branch in `runArrearsRsvpBatch`, the two RSVP template bodies; run-all.js suite 299, arrears-lock.spec.js | Standing |
| REF-06 | What happens if the office crosses a referral discount off with the × ? | **It stays off — the entries are marked waived, not just the count zeroed.** Not a question she was asked: it is [[MON-56]] meeting [[REF-01]], and it is recorded because her answer was already given and the code stopped honouring it. The × zeroed `referralCount` alone, which was complete while the count WAS the record — her own plan is what changed that: *"`referralCount`... now DERIVED, not hand-typed for this path"*. Derived, the next referral through a link recomputed the count from the entries and put the whole discount back on the bill, from a path nobody in the office was looking at. ⚠ `waived` IS KEPT APART FROM `revoked` ON PURPOSE — revoked means the friend cancelled, waived means we crossed it off. Both stop the count; only one is anybody's fault, and a season later the difference is the only thing that explains the bill. | 2026-09-03 | `referralLiveCount`, the referral branch of `waiveLedgerLine`; suite 299 §9 plus a structural check on the write | Standing |
| REF-07 | Where does the OFFICE see a customer's referral link? | **At the top of Edit Customer, in Customers.** Addie, 2026-09-04, with a screenshot of the bill line: *"can we put referal link under at the top"*, then *"in costumers"*. That is the screen somebody is on when a member rings and asks how to send their link to a neighbour, and until now the office had no way to see it at all — only the customer's own portal ever drew it. ⚠ THE TOKEN WAS MINTED IN ONE PLACE AND IT WAS NOT REACHABLE FROM HERE: `ensureReferralToken` lives inside `portalLookup`, so a customer got one on their FIRST PORTAL SIGN-IN and never before it, and most of the book has never signed in. So the row MINTS one — but on the BUTTON, not on the render: minting on open would write to Firestore every time anybody opened the most-opened form in the app, for a link most of those opens will never use. ⚠ AND IT IS KEYED TO THE RECORD, never resolved from the phone: seventeen numbers in the real book are shared and fourteen are two households, and the money follows the token. | 2026-09-04 | `editCustRenderReferLine`, `editCustReferClick`, `referralTokenFor`; suite 305 | Standing |
| REF-08 | Can the referral link be sent through the RSVP? | **Yes — `{{referral_link}}` and `{{referral_button}}`, offered in the token picker.** Addie, 2026-09-04: *"how these can be sent through RSVP?"* The RSVP is the one email a customer definitely opens and definitely acts on, so it is the cheapest chance all season to put their own link in front of them — and until now the only way to see it was to sign in and find the Refer a Friend tab, which is the tab locked behind unpaid arrears ([[REF-05]], [[MON-38]]). ⚠ HER OWN RSVP TEMPLATE IS NOT REWRITTEN, and that is deliberate rather than an omission: `etEnsureMaybeTemplateExists` only ever FILLS A BLANK body, never replaces words somebody has written ([[MON-24]]). The tokens exist; putting one in the email is her edit to make. ⚠ THE CUSTOMER IS RESOLVED BY `hlxEmailCustomerItem`, which refuses a phone matching two customers rather than picking the first — through a `.find()` a parent is emailed the child's link and every $25 that friend earns lands on the child's bill for the season. ⚠ AND AN UNRESOLVED CUSTOMER EMITS NOTHING AT ALL: a dead `<a href="">` in a bulk send is a customer tapping something we sent them and landing nowhere, which they cannot tell from the scheme being broken. | 2026-09-04 | the `{{referral_link}}` block in the template renderer, `hlxEmailCustomerItem`, `referralLinkFromToken`; suite 305 | Standing |

## Warehouse — build, recycle, bins

| # | Question | Her answer | Decided | Where it's proved | Status |
|---|---|---|---|---|---|
| WH-24 | Confirming a customer for the season sent them to the warehouse. When SHOULD a returning customer be built for? | **Only when something actually changed.** *"I recently confirmed jeff rasmensons light in the costumers tab cause they asked for it to be approved for this season. So I think that is why it sent this to warehouse. But it should only be sent to warehouse if there is any sort of change from last year. If nothing changes than nothing is affected."* ⚠ SHE DIAGNOSED IT AND SHE WAS RIGHT. Four places asked `was 'no' && !needsLightRecycle` and read the CLEARED flag as proof the warehouse had pulled the bundle apart — functions/index.js said so outright, that such a record "is the ONE signal that the recycle actually happened". It is not: the flag is equally clear when nobody ever queued a recycle, which is the ordinary state of somebody marked no by hand or by an import. ⭐ `lightsRecycledAt` IS THE POSITIVE SIGNAL and did not exist when the rule was written. Both paths that COMPLETE a recycle stamp it, and a plain Mark Recycled removes the record entirely — so nobody rejoins down that road. `rejoinNeedsBuild` / `rejoinNeedsBuildServer` ask for the stamp instead of inferring. ⚠ HER OWN OBSERVATION CLOSED IT TOO: *"It looks like these are not changed if there is no badge. Unless member portal badge is not working."* The badge was working — no badge means `lightsChangedAt` is unset, so his colours had never been touched by anyone. The badge was right and the rule was wrong. ⚠ THE STRICT DIRECTION HAS A COST, TAKEN KNOWINGLY: a recycle completed before the stamp existed leaves no evidence, so that customer returns with no build queued — a crew at an empty bin, which is the worse failure. Accepted because the population is empty in practice (a plain no deletes the record, the keep-them path has always stamped, and the crew portal is unused this season). | 2026-09-03 | `rejoinNeedsBuild`, `rejoinNeedsBuildServer`; rejoin-build.test.js runs both copies, 10 sabotages red-checked | Standing |
| WH-25 | How do you take a house off the build list when it should never have been on it? | **Its own button, which records that nothing was built.** ⚠ THE DESIGN HERE IS NOT HERS AND THE ROW SAYS SO — what she gave was the go-ahead on the rule, *"Okay I recently confirmed jeff rasmensons light... if you could fix this that would be awesome"*, and the button is what that fix needed to be usable, because Jeff was already queued by the time the rule changed. If she wants a different answer here, hers replaces this one and this row is superseded rather than argued with. ⚠ MARK DONE WAS THE ONLY WAY OFF AND IT STAMPS `lightsMarkedBuiltAt`, so clearing a wrongly-queued house meant dating a bundle that was never made, on the one field that answers *when was this built* — which the warehouse audit box reads back as fact. ⭐ **Not needed** sits beside Mark Done, clears the flag and the queue date, stamps nothing, and writes the reason to the activity log. It asks first, because the two buttons are side by side and the wrong one leaves a house that genuinely needs a bundle with nothing telling anybody. ⚠ AND IT LEAVES `buildTopUpFromFeet` AND `binLabelNumber` ALONE, unlike Mark Done: those describe a bundle that was actually made or a bin that was actually labelled, and nothing here made or labelled anything. | 2026-09-03 | the `data-whnotneeded` handler in `renderWarehouseQueue`; build-stamp.test.js names it as a non-stamping clear site | Standing |
| WH-01 | One button for recycle-and-build, or two? | Two. "dont make it one button have one button for build and another for recycle." Neither may quietly do the other's half | 2026-08-21 | `recycleKeepingCustomer` / `buildTopUpFromFeet` | Standing |
| WH-02 | Can somebody who moved be recycled without being deleted? | Yes — `recycleKeepingCustomer`. Mark Recycled has a mover branch that keeps them and their number | 2026-08-20 | `recycleKeepingCustomer`, mover branch of Mark Recycled | Standing |
| WH-03 | Should an add-on look different from a house on the sheet? | Yes, three signals: `Type: ADD-ON`, a note saying it goes in the bin they already have, and a badge | 2026-08-20 | `WH_BUILD_COLUMNS`, `whPutIntoLabel` | Standing |
| WH-04 | Should the warehouse be told *why* a bundle is being built? | Yes — NEW / OLD-REBUILD / MEMBER PORTAL / REQUEST, in that order, most-work-first. "There should be a badge by each person on warehouse that say's new, Old-Rebuild or Member Poral or Request" | 2026-08-24 | `whBuildReasonKey`, build-reason.test.js | Standing |
| WH-05 | Does that badge print? | Yes — "I need paper to carry badge too." A **Why** column on both build sheets | 2026-08-24 | `whBuildReasonLabel` | Standing |
| WH-06 | Does every sheet say how many bins? | Yes, unconditionally. "Everyone needs to know how many bins there are for each house" | 2026-08-25 | `whBinsForHouse`, both build sheets + recycle sheet | Standing |
| WH-07 | ~~Does the sheet need a bin count?~~ | ~~No — warehouse people know 5000 means 2 bins~~, then ~~only if more than 2~~ | 2026-08-24 | — | Superseded → WH-06 |
| WH-08 | Which number does the warehouse need on a recycle row? | The **old** number painted on the bin — "thats how they find it" — shown separately from how many bins | 2026-08-21 | `binLabelNumber` | Standing |
| WH-09 | Does the recycle queue need colours and patterns? | No. "they just need the customer number and name." One flat list ordered by name | 2026-08-21 | `whArchivedPending`, flat list by name | Standing |
| WH-10 | Who appears on the printed needs-building sheet? | Everyone on the warehouse tab — the same flag the tab asks, `needsLightBuild` alone. "everyone on the warehouse tab should be printed." | 2026-08-24 | `printNeedsBuildList` | Standing |
| WH-11 | Split the build sheet by badge, or by colour group? | By colour group — one page per group, because the group *is* the pile | 2026-08-24 | `whSheetRowsForBuild` | Standing |
| WH-12 | Is "soft" its own colour? | It counts as Warm White everywhere. She was asked and told the cost before reversing | 2026-08-24 | `houseLightsText`, `warehouse-colours.test.js` | Standing |
| WH-13 | ~~Is "soft" its own colour?~~ | ~~Yes — kept as `soft(recycled)` so those houses can be found and switched~~ | 2026-08-19 | switching list survives via `allCustFilterLights` on the raw record | Superseded → WH-12 |
| WH-14 | Colour shorthand — what does each form mean? | `rr` = two reds (a repeated letter is a count); WW and PW are **initials**, never counts; `rrgg` = `rr/gg`; anything multi-something = Multi; `pur` = Pure White, **not** Purple; clear / cool white / bright white = Pure White; plurals → singular; `mc` is skipped for good | 2026-08-24 | `rbLetterRun`, `rbLooksMulti`, `WH_COLOR_WORDS` | Standing |
| WH-15 | Does a colour list keep its order? | A set sorts; a strand keeps its order — `rrgg` and `rgrg` are two different builds | 2026-08-24 | `whOrderColors` | Standing |
| WH-16 | Can the build tab be asked about one house? | Yes — "i clicked build ashley wray but shes not here" was four different causes looking identical | 2026-08-21 | `whHouseBuildStatus` | Standing |
| WH-17 | Should a blank colour field clear the build flag? | No. "big problem, she went to the recycle but not to the build." Blank colours mean the build can't be *done*, not that it isn't *owed* | 2026-08-21 | `needsLightBuild` on the Edit Customer save | Standing |
| WH-18 | Should a house with no colours be hidden from the queue? | No — its own blocked block with an Add colours button, and marked Blocked on the sheet | 2026-08-21 | `whBuildQueueGroups` | Standing |
| WH-19 | Buffer stock — does it carry a badge? | No. No customer, so no claim | 2026-08-24 | `whBuildReasonKey`, `build-reason.test.js` | Standing |
| WH-20 | Should an imported house be queued for a build only if it has colours? | No — **ungated**. "we want to build everyone." A row with no colours goes to the blocked "Waiting on light colours" block, which is visible and has an Add colours button; gating the flag makes those houses invisible instead, which is the bug being closed | 2026-08-26 | `needsLightBuild` in `rbImportBtn` / `ibImportBtn` add branches, `import-build-flag.test.js` | Standing |
| WH-21 | Does an import that MATCHES an existing customer re-queue their build? | No, never. These tools write hundreds of records a press, so a matched row that re-queued would put every house already built back on the warehouse list and somebody would make a second set for it | 2026-08-26 | update branches of `rbImportBtn` / `ibImportBtn`, `import-build-flag.test.js` | Standing |
| WH-22 | The All Customers panel was clearing the build flag on blank colours — fix it, or keep reporting it? | **Fix it now.** WH-17 already ruled the behaviour in 2026-08-21; the same rule had two writers and only one was repaired. Nothing left to decide | 2026-08-26 | `attachAddressRowHandlers`, `import-build-flag.test.js` | Standing |
| WH-23 | Should the record show when a bundle was SENT to the warehouse, not just when it was built? | **Yes.** "Okay what about when bundle got sent to warehouse" — asked straight after being told a customer history would show dates, and it was the gap. ⚠ `lightsMarkedBuiltAt` recorded when a bundle was MADE; nothing recorded when one was ASKED FOR, so "built on 14 Oct" could not say whether it waited two days or five weeks, and a house queued and forgotten looked identical to one queued that morning. ⚠ STAMPED ON THE TRANSITION, never on every write: two callers write the flag on EVERY save keeping whatever it held, so stamping there would reset the clock whenever anybody opened a record to fix a phone number. ⚠ A RE-QUEUE IS A NEW DATE — the warehouse waits on the newest request — and it is NOT cleared when the build is marked done, because "queued on the 2nd, built on the 9th" is the whole point. | 2026-08-28 | `stampBuildQueued` (admin.html) / `stampBuildQueuedServer` (functions/index.js); queue-date.test.js keeps a census of all fifteen queueing places | Standing |

## Customer numbers

| # | Question | Her answer | Decided | Where it's proved | Status |
|---|---|---|---|---|---|
| CN-01 | Is a pooled number somebody still holds "available"? | No — shown as shadowed, because a number can leave the pool without the pool being told | 2026-08-20 | `cnFreePool` | Standing |
| CN-02 | Should converting ask before moving the number? | No — it moves automatically. The confirm existed only to protect the bin label, and `binLabelNumber` covers that now | 2026-08-21 | `binLabelNumber`, `requoteBeingConverted` | Standing |
| CN-03 | Does a re-quote keep the old number? | Held only if feet stay below 260 with the addition — but the old number must stay findable either way | 2026-08-18 | `whBinNumberFor` / `binLabelNumber` | Standing |
| CN-04 | Does Schedule ever touch customer numbers? | No. Numbers return to the pool in Customer Numbers, driven by customer status. Schedule is routing only | 2026-08-09 | routes-dashboard-plan.md | Standing |

## Quotes and re-quotes

| # | Question | Her answer | Decided | Where it's proved | Status |
|---|---|---|---|---|---|
| QT-01 | Delete the customer and let the re-quote convert them back? | No — re-quotes get their own folder, and converting one *updates* | 2026-08-19 | `quoteFolder` | Standing |
| QT-02 | How many kinds of re-quote, and who says which? | Three, stated not guessed: addition to house / change of address / price only | 2026-08-21 | `askRequoteKind`, `requoteKind` | Standing |
| QT-03 | Price-only re-quote — does the warehouse do anything? | Nothing. Same house, same lights, same feet. It offers a box to explain the price change | 2026-08-21 | `requoteBuildChoice` | Standing |
| QT-04 | Applying a re-quote — one press or two? | One. "after I fill everything out I just want to convert them" | 2026-08-21 | `applyRequote(applyNow)` | Standing |
| QT-05 | What does the card say for an existing customer? | **Apply Re-Quote**, not Convert to Customer — "its converting them rather than updating them" | 2026-08-21 | `showConvertQuoteChoice` | Standing |
| QT-06 | Can the office mark a quote approved without emailing it? | Yes — that *is* the override. Taken back if the answer is later changed | 2026-08-21 | `approvedByOffice` | Standing |
| QT-07 | What does the popup ask the warehouse after a re-quote? | Recycle old & build new / build the add-on (footage **typed**, not calculated) / nothing. The add-on is always offered | 2026-08-21 | `requoteBuildChoice`, `houseBundleNeed` | Standing |
| QT-08 | ~~When is the add-on option offered?~~ | ~~Only when the sum comes out — known old footage and a bigger new one~~ | 2026-08-20 | — | Superseded → QT-07 |
| QT-09 | Does deleting a re-quote delete the customer? | No — the quote only, and the message names which of the two is going | 2026-08-20 | `attachDeleteHandlers` | Standing |
| QT-10 | Does a re-quote carry colours onto a customer who has none? | Yes. "Ashley and Rachel should have lights in warehouse so we can build them" | 2026-08-21 | `showApplyRequoteChoice` | Standing |
| QT-11 | Does the measure tool save feet as well as price? | Both, in one press. "I need no guessing I need feet to be correct" | 2026-08-25 | `rmCommitPayload` | Standing |
| QT-12 | Does the difficulty grade save when it's set? | Yes, automatically on change — not as a passenger on a commit button | 2026-08-25 | `#rmDifficulty` listener | Standing |
| QT-13 | Does the satellite alignment have to be redone per house? | No — it belongs to the photograph, remembered per area, inherited within a mile, and says on screen when it was inherited. ⚠ THE FEATURE THIS RULES ON WAS REMOVED on 2026-08-27 and the reasoning is kept because it is sound and explains why it existed: Google's aerial imagery is not shot from overhead, so anything with height is drawn displaced from its own footprint — about six feet on the house that prompted it — and a dot placed from above landed beside the house in Street View. That displacement was the price of ONE DOT having to mean the same place in two pictures, which MR-06 stopped requiring. ⚠ FOUND BY A GATE, NOT BY A PERSON: it still read Standing while naming a Firestore document that no longer exists anywhere in the code. | 2026-08-25 | settings/measureAlign (gone — quoted, not pointed at; the tombstone is above `rmMapPixelToWorld` in admin.html) | Superseded → MR-06 |
| QT-14 | Sides of the house — four names or a count? | A count, 1–4, so it matches the sheet. Front/left/right/back was a guess nobody could confirm. "in the website its called front left right and back side, we need it to say 1, 2, 3, or 4 sides of the house so then it can just be connected and we dont have to guess if its the left or right side." | 2026-08-19 | `houseSideCount` | Standing |
| QT-15 | Are sides a second price input? | No — only measured feet sets the price. Sides raise a re-quote because the *footage* changes | 2026-08-21 | `docs/what-we-can-record.md` | Closed |
| QT-16 | Nothing changed on a re-quote — is that a valid answer? | Yes, and it must not be a dead end. "to the system it looks like nothing changes" | 2026-08-21 | `requoteBeingConverted` | Standing |
| QT-17 | Re-quote approval — how does the customer answer? | **Three buttons: fill out a fresh form / change one thing in the portal / keep everything.** The fresh form pre-fills from file. ⭐ RE-CONFIRMED 2026-08-29, in her own words: *"Requotes work different we should just have a fill out form or keep info already in system kind of thing... So give them an option if that makes sense."* ⚠ AND THIS ROW SAID "not built" WHILE IT WAS BUILT — `offerMemberChangeChoice` renders all three, and `showQuoteDetailFormFresh` opens the same form and the same submit a new customer uses, pre-filled through `qdPrefillFromMember`, greeting them with "Here's what we have on file — change anything that's different this year." A status column that has gone stale is worse than a missing one: it sent a session to design a fork that already existed. Checked at the code, not from the row. | 2026-08-29 | `offerMemberChangeChoice`, `showQuoteDetailFormFresh`, `qdPrefillFromMember` | Standing |
| QT-18 | Can the quote link sit behind her own words instead of printing the whole address? | **In an email, yes, both shapes** — `{{link:See your home and approve here}}` for a sentence with the link behind part of it, or `{{link_button:See your home and approve}}` for the gold block wearing her words instead of "View & Respond". **In a text, no**: SMS is plain characters, there is no such thing as a link behind a word, so the address stays visible — the words survive and the link goes on its own line underneath | 2026-08-26 | `applyQuoteLinkLabel` / `applyQuoteLinkButton` + their Server twins, run-all.js Suite 279 | Standing |
| QT-19 | The quote link is too long for a text — what instead? | A short address on our own domain, `highlightingutah.com/q/<token>`, rewritten to the same page. Not bit.ly: carriers filter shortened links and the message is dropped silently. A visible address is also right for a text — people do not tap what they cannot see | 2026-08-26 | `quoteShortLink`, `_redirects`, run-all.js Suite 280 | Standing |
| QT-31 | What should come off the free quote form? | **The photo uploader entirely, and the side count to the details form.** *"a lot of info in the free quote shouldnt be there including upload picture and side of house, upload picture should not be there and side of house should be in detail form, 1 side should be default."* ⚠ **THE FORM'S OWN PROMISE IS "about two minutes"**, and photographing four walls of a house — and of every outbuilding — was the most expensive thing on it, asked of somebody who has not been given a price yet. ⭐ **SIDES MOVED RATHER THAN WENT**: it drives the footage and so the price, so it is asked on the Install Details form instead, after they have approved. ⚠ **ONE SIDE IS PRE-PICKED THERE AND DELIBERATELY NOT ON THE ADMIN FORMS**, where run-all.js asserts the opposite — Edit and Add Customer sit over ~956 records nobody has ever asked, so a default there stamps a made-up answer on all of them ("a count nobody gave is not a count"). One customer looking at the question for their own house is a different case, and the front only is the commonest answer by far. ⚠ **AND A FRESH QUOTE STORES NO COUNT AT ALL** — absent, not 1 — because every reader turns absent into 1 through `portalSideCount`, so the default holds without claiming somebody answered. ⚠ **THE SERVER HAD TO BE TOLD**: `quoteSaveDetails` keeps a whitelist and the emailed-link path goes through it, so a field the browser sends and the function drops is lost with nothing wrong on screen. | 2026-09-03 | the quote form markup, `qdSidesRow`, `quoteSaveDetails`; run-all.js Suite 302, S62 and S80 repointed | Standing |
| QT-32 | Keep the option to add another property? | **Yes, and it must be undoable.** *"there should keep the option to add another property but make sure there is a way to delete the extra property in case they accidentally push on it."* ⚠ **THERE WAS NO WAY BACK.** "+ Add another property" is one press from being pressed by accident and the block simply stayed, so a stray empty building went to the office as a real one. Each extra row now carries **Remove**; the main house does not, because a quote with no house is not a quote. ⚠ **THE ROW LEAVES THE ARRAY, NOT JUST THE PAGE** — removing the element alone would still submit a building the customer can no longer see, and read its detached box as a blank one. ⚠ **AND A NAMELESS EXTRA IS NOT SENT**, for somebody who presses Add and just leaves it empty. | 2026-09-03 | `removeQuoteBuilding`; Suite 302 | Standing |
| QT-33 | Can the office add a building later? | **Yes — and asking for it uncovered a silent loss.** *"in edit customer we need to have add a building set up there as well in case they come around later and want another building."* ⭐ **THE QUOTE FORM HAS COLLECTED `buildings` FOR MONTHS AND THE WORD DID NOT APPEAR ANYWHERE IN admin.html.** So a customer who told us about their shop at quote time had it recorded on the quote and then dropped the moment they became a customer, with nothing said and nothing to look at. Building the control is what made that visible; `addCustBuildingsFromQuote` is the half that pays. ⚠ **THE MAIN HOUSE IS NEVER IN THE LIST.** On a quote it is one of the buildings, because the quote has no record of its own; on a customer the record IS the main house, so carrying it across would show the same address twice and invite an edit to the wrong one. ⚠ **THE FIELD IS LABELLED, WHICH IS WHAT GIVES IT A READER** — CLAUDE.md §1 wants written, read AND declared, and the change-log gate refused it until it had words. Its own `buildings` kind, not `list`: these are objects, and `join()` would print `[object Object]` into somebody's history, which is worse than no entry because it still looks like one. ⚠ **AND NOTHING ELSE READS THEM YET** — they do not print on a crew sheet. Said plainly rather than left to be discovered. | 2026-09-03 | `editCustReadBuildings`, `addCustBuildingsFromQuote`, `CUSTOMER_FIELD_LABELS.buildings`; Suite 302 | Standing |
| QT-20 | When a member is re-quoted, what should the office be able to see about the house they already have? | **Everything the old record knew, so nothing is lost while they are in the quote.** *"we should be able to find what their old # was no matter what"*, *"hold everything as long as they stay below 260 feet"*, and *"if they go above 260 feet hold what you can"*. The re-quote is where the measurement changes, so whether the old number still fits cannot be decided when the quote is raised — what CAN be done is carry the number, the feet, the bin count and the lights across, and let whoever prices it hand back whatever still works. ⚠ THE RULING IS FROM 2026-08-18 AND WAS NEVER IN THIS MAP UNTIL 2026-08-30 — it lived in CLAUDE.md and in three code comments, which is exactly the state R-023 exists to prevent; the docs gate caught it on the change that acted on it. ⚠ AND IT WAS NOT ACTUALLY TRUE ON THE PORTAL SIDE. A member changing which sides of their house are lit raised a re-quote carrying five BLANKS: those values are read off the record `sanitizeRecord` returns, and `customerNumber`, `measuredFeet`, `numberOfBins`, `lightColors` and `housePrice` are deliberately not in `PORTAL_READ_FIELDS` — pricing, numbers and bins never leave the server. So the office card read "On file: no number" above an instruction saying "same number, same bin". ⭐ THE WHITELIST WAS NOT WIDENED: `requoteOnFile` derives them from the LIVE customer at render time, which also fixes the staleness a stored snapshot always had. | 2026-08-18 | `requoteOnFile`; run-all.js S64; `portal-fields.test.js` | Standing |

## RSVP and season status

| # | Question | Her answer | Decided | Where it's proved | Status |
|---|---|---|---|---|---|
| RS-01 | Does approving a quote count as yes to the season, even over a recorded "no"? | Yes — "go with option 2", asked directly and told the trade | 2026-08-22 | `seasonYesUpdates` | Standing |
| RS-02 | ~~Does approving override a recorded no?~~ | ~~No — a recorded "no" is a deliberate answer and one inferred from a price should not outrank it~~ | 2026-08-19 | kept as the argument against | Superseded → RS-01 |
| RS-03 | Does a later "no" still win? | Yes. Approving is one more way to say yes, not a lock. She asked for that specifically | 2026-08-22 | `seasonYesUpdates` | Standing |
| RS-04 | Does approving clear a Maybe Next Year badge? | Yes — "we shouldn't have to clear a badge to get somebody updated" | 2026-08-22 | `maybeNextYear` clear | Standing |
| RS-05 | Does "Back Next Year" recycle? | No. Never sets `needsLightRecycle`; it is its own status value, not a flavour of "no" | 2026-08-14 | `isOutForSeason`, `season-state.test.js` | Standing |
| RS-06 | Is there a tag for never-asked vs asked-and-silent? | Yes — a third tag, **Unanswered**, distinct from blank. Everyone moves to Unanswered right before the RSVP goes out | 2026-08-20 | `rsvpStatusLabel` — the one place a status is named | Standing |
| RS-07 | Does an RSVP go to somebody who has never had lights? | No — choosing an RSVP template sets the audience to Returning by itself. A default, not a lock | 2026-08-19 | `etTemplateIsRsvp` | Standing |
| RS-08 | Existing member approves — form, or portal? | Ask "do you want anything changed?" Yes → straight into the portal. No → a short message. A new member not yet converted still gets the form | 2026-08-19 | `alreadyMember` (functions/index.js) | Standing |
| RS-09 | Can the season be switched to answered-Yes-only before the RSVP is marked sent? | It must refuse. That guard is the one thing between one click and an empty season | 2026-08-25 | manual test 215 | Standing |
| RS-11 | How many days before install should a confirmation go out? | The question does not apply — **there is no pre-install confirmation.** It is the RSVP email, sent once at the start of the season: "This is for emails we don't have twillo and this would be for RSVP emails." Earlier than any N-days answer, and better, because it lands before anything is built | 2026-08-21 | Q-005; Automation Emails | Standing |
| RS-12 | What happens to a job whose confirmation comes back disputed? | **There is no disputed state.** The RSVP takes exactly three answers and `portalRsvp` rejects anything else. ⚠ A catch-all "something is wrong" reply was considered and **rejected — do not rebuild**: an answer that sometimes means *don't come* and sometimes means *my colours are wrong* is how a crew skips a house that was fine. A problem is a phone call; a wrong detail is the portal | 2026-08-21 | Q-006; R-005 retired | Closed |
| RS-13 | Does an assumed yes count as a reply? | **No.** "They need a reply either through email or approving through the button. We should be able to approve for them in costumers as well." Converting a quote writes `yes` with no `rsvpRespondedAt` — the office knowing they want lights, nobody having asked them about this season. All four routes stamp the date, including the office answering on their behalf | 2026-08-22 | Q-010b; `isOutForSeason` confirmed-only branch | Standing |
| RS-14 | What counts for the season once RSVPs are live? | "straight yes is the only thing to count for RSVP for the season." ⚠ The switch exists and **must not be flipped yet** — no RSVP has gone out, so it would put nearly everybody out of the season. *When* to flip it is still open as Q-010a | 2026-08-21 | Q-010; `SEASON_ELIGIBILITY` | Standing |
| RS-15 | Does a customer who never replies eventually count as scheduled, or not? | Not — and elapsed time is not a factor at all. "It doesn't matter how long ago house isn't sent RSVP a house won't be a yes or no because of how long they haven't responded for. They are just unresponsive and we won't do there house unless we get a yes from them." The 14-day reply window is REMOVED: the rule goes live the moment the RSVP is sent. Accepted cost — the season empties on send day and refills as replies arrive | 2026-08-26 | `seasonRuleIsLive`, `season-state.test.js` | Standing |
| RS-16 | ~~Should non-repliers get a grace period before the rule bites?~~ | ~~Yes, 14 days — otherwise the season empties the second she presses send and every reply re-shuffles the routes~~ | 2026-08-26 | `RSVP_REPLY_DAYS`, removed | Superseded → RS-15 |
| RS-17 | Is who gets scheduled a setting the office turns on, or the rule? | **The rule. There is no switch.** "I tried to make it clear I didn't want a switch and that RSVP Approved in any of the approval ways should be hardcoded and anyone who didn't respond is not approved and should not be scheduled." ⚠ Supersedes MON-22, which recorded the same intent and shipped it as a DEFAULT with a Dashboard control that could turn it off — and a stored setting that could turn it off silently. `const SEASON_ELIGIBILITY = 'confirmed-only'`; nothing assigns it, no settings value is read, the control is gone. ⚠ WHAT IS STILL NOT HARDCODED IS WHEN IT BITES, and that is not a loophole — you cannot fail to answer a question nobody sent, so the rule waits on `rsvpSentAt`, which the send stamps by itself. Without it the season empties the day this ships. ⚠ SO THE MARK IS NOW THE TRIGGER, not a precondition: setting it takes every unanswered customer off the routes, and clearing it puts them back. Both now warn, count, and redraw — the clear dialog used to promise "this only changes what this banner says", which stopped being true the moment the rule was tied to the mark. ⚠ THE WARNING HALF OF HER EARLIER "Both — hardcode it AND warn me" survives and is now the Dashboard row's only job | 2026-08-27 | `SEASON_ELIGIBILITY`, `seasonRuleIsLive`, `seasonRuleChanged`, `season-state.test.js` | Superseded → RS-22 |
| RS-18 | Somebody with no email address cannot answer the RSVP. Are they an exception? | **No. "Anyone that doesn't have an email I don't want you to worry about those people. We need a yes in order to do there christmas lights if they do not RSVP there are no exceptions to do there lights."** ⚠ NO CODE CHANGE — the app already behaves this way, and an exemption was drafted and NOT built on the strength of this answer. They are skipped by the send (counted, not silent), never answer, and so are never scheduled. ⚠ THE COST IS ACCEPTED KNOWINGLY: a returning customer whose only fault is having no email gets no lights unless the office marks their answer by hand. They are findable under Customers → Filters → Email | 2026-08-27 | `custCanBeEmailed`, `isOutForSeason` | Standing |
| RS-19 | Which routes count as a yes? | **Three, and all three already stamp a real reply**: the member portal, the RSVP email link, and the office marking it for them. "They RSVP through member portal, email, or we approve that for them." ⚠ A bare stored `rsvpStatus: 'yes'` with no reply date is NOT one of them — that is an import, a hand-edit, or the assumed yes written at conversion, and `effectiveRsvpStatus` deliberately distrusts it | 2026-08-27 | `portalRsvp`, `quoteRespond`, the Edit Customer dropdown, `effectiveRsvpStatus` | Standing |
| RS-20 | Is a customer converted from a quote this year an exception to needing a yes? | **Yes — the only one, and for two independent reasons she gave.** "Once we convert a quote to costumer that is the only exception... One they are already approved through converting to costumer this year and 2 they don't get send an RSVP." ⚠ EITHER REASON ALONE WOULD DO, which is why this is safe: converting IS an approval, and requiring an answer to a question deliberately never sent is a test nobody can pass. ⚠ IT EXPIRES ON ITS OWN — Start New Season clears `chargeNewMemberFee` and the quote-join year is compared against the current year, so in 2027 they answer like anybody else. ⚠ NOT AN EXCEPTION SHE HAS TO REMEMBER: `audienceNeverAsked` is one predicate, asked by the season rule, the RSVP audience and now the badge | 2026-08-27 | `audienceNeverAsked`, `effectiveRsvpStatus` | Standing |
| RS-21 | How does the office see that a converted customer is already approved? | **A badge beside their RSVP pill — "Approved — new this year".** She offered it: "if we need to put a badge on new member for converted costumer to make this easier that they are approved than we can do that." ⚠ IT EXPLAINS, IT NEVER DECIDES — `approvedOnJoinBadge` asks `audienceNeverAsked`, the same predicate the season rule asks, because a badge with its own idea of "new this year" is worse than no badge: a confident wrong label on the one screen somebody checks before ringing a customer. ⚠ ALL THREE RSVP PILLS CARRY IT, and the count is asserted. ⚠ IT ALSO EXPOSED A REAL DRIFT: the All Customers row printed the RAW `rsvpStatus`, so a bare stored yes read "RSVP: Yes" there while the Dashboard said Pending and the season left them off every route | 2026-08-27 | `approvedOnJoinBadge`, `season-state.test.js` | Standing |
| RS-22 | If nobody may be scheduled without a yes, what happens to the people who have not answered? | **The rule applies always, and they get a list instead of disappearing.** "We cannot schedule people that haven't RSVP. could we make another section in schedule were it just has pending people?" ⚠ SUPERSEDES RS-17 ON ONE HALF ONLY: the hardcoding stands, but the rule no longer waits for the RSVP to be marked sent. That gate existed so the season would not empty on the day it shipped — and it still does empty; what changed is that the ~960 are NAMED, in **Schedule › Waiting on RSVP**, by town, with phone numbers. ⚠ SO `rsvpSentAt` DECIDES NOTHING NOW. It is still worth setting: it is the difference between "they have not replied" and "we have not asked them", which is the only thing that makes the list actionable. Both mark dialogs were rewritten to stop claiming they move anybody. ⚠ OUT OF THE SEASON IS NOT THE SAME AS WAITING — somebody who said no, said Back Next Year, or is queued for recycle is not waiting on anything and is deliberately absent from the list | 2026-08-27 | `seasonRuleIsLive`, `waitingOnRsvpHouses`, `renderWaitingRsvp` | Standing |
| RS-27 | What should the subject line on the RSVP email say? | **Her call, delegated, with one requirement: they must be able to tell it is about Christmas lights before they open it.** "Whatever you think is best subject is great we just need them to know it a christmas light RSVP." Shipped as **"Your Christmas lights this year — a quick yes or no"** — names the thing and reads as a question, which is what an RSVP is. ⚠ THE REQUIREMENT IS THE RULE, NOT THE WORDING: the subject box on each template overrides it, so this is the default rather than a fixed string, and `email-subject.test.js` asserts the REQUIREMENT (that an RSVP subject names Christmas lights and reads as a question) rather than the exact sentence — pinning the words would fail the moment she reworded it herself. ⚠ AND A BILLING TEMPLATE MUST NOT INHERIT IT: a receipt headed "a quick yes or no" is worse than a blank subject, so the fallback is chosen by what the template IS | 2026-08-31 | `defaultEmailSubject`, `email-subject.test.js` | Standing |
| RS-28 | Which emails need a subject? | **All of them.** "I need a subject on all them." ⚠ SIXTEEN OF TWENTY `emailjs.send` calls in admin.html passed none — twelve are dead UI (KNOWN_MISSING_IDS), and the four live ones plus both server sends now carry one. Only the quote emails ever had one, which is why quotes looked right in an inbox and the RSVP did not. ⚠ ONE EMAILJS TEMPLATE SERVES EVERY SEND (they all read `#emailjsTemplateId`), so the subject cannot be set per-email in the EmailJS dashboard — it has to travel with each send, and their template's Subject field must say `{{subject}}` or none of it arrives | 2026-08-31 | `emailSubjectFor`, `templateSubjectOr`, `email-subject.test.js` | Standing |
| RS-29 | When in the RSVP should the gate code be asked for? | **Before the changes question.** "Lets do gate code before changes." Only on a **yes** — somebody sitting the season out is never asked, because no crew is coming. ⚠ IT CONFIRMS WHAT WE HOLD RATHER THAN ASKING: "do you have a gate code?" put to somebody who gave us 4417 last year invites them to tap No, which reads as them removing it — so a code on file is quoted back for confirmation, and confirming writes nothing. ⚠ IT FAILS OPEN EVERY WAY OUT (missing markup, refused save, thrown call) because the RSVP answer is already recorded before the step is shown, so nothing here can cost them their reply. ⚠ AND IT IS **NOT** `portalSave`: `gateCode` is in that function's `info` whitelist, but that section sets `seasonStatus: 'needs_changes'` — the RE-QUOTE state — so reusing it would park every customer who typed a gate code in Needs Changes for ever, waiting on a question nobody asked | 2026-08-31 | `showRsvpGateCodeStep`, `portalSetGateCode`, `gate-code.test.js` | Standing |
| RS-10 | The three RSVP answers, and what each does | **No** → records immediately, Cancel tab with an optional reason box, triggers `needsLightRecycle`. **Back Next Year** → homepage, "We look forward to seeing you next year!", no recycle logic, separate status. **Yes** → "Do you want changes?" → yes: portal logged in; no: thank-you message, then stops | (chat) | not in repo yet | Decided — not built |
| MON-24 | Q-010a — how long do people get to reply before the season counts only a straight yes? | **No grace window: "any RSVP should be a straight yes starting when emails are sent."** The trigger is the SEND, not a calendar date — which is what the `rsvpSentAt` mark already records, and matches how she described it in August ("a box I tick once I send out RSVP emails"). ⚠ THE MECHANISM IS UNCHANGED and deliberately still a deliberate act: `noteRsvpSendHappened` records the send and now SAYS the season is still counting everybody and names the switch, but does not throw it — flipping takes every unanswered customer (~960 on send day) off the routes, the schedule and the build queue at once, and the Dashboard switch shows that count before acting. ⚠ Answers the open half of MON-22 | 2026-08-26 | `noteRsvpSendHappened`, `seasonEligibilityWouldDrop`, `SEASON_ELIGIBILITY` | Standing |

## Schedule and routes

| # | Question | Her answer | Decided | Where it's proved | Status |
|---|---|---|---|---|---|
| SCH-01 | Is the day or the town the unit? | The day. Each working day the free crews go to the towns with the most houses still waiting, recounted before the next day | 2026-08-17 | `planNewCrewDays`, Suite 47 | Standing |
| SCH-02 | How many towns can a crew have? | One dominant town plus at most one genuinely nearby — and the rule belongs to the **crew**, not the day | 2026-08-20 | `MAX_TOWNS_PER_CREW`, `townsAreNeighbours` | Standing |
| SCH-03 | Top up from the nearest town, or the most urgent? | Most urgent | 2026-08-17 | `fillableCount` | Standing |
| SCH-04 | What is the priority order? | New hangs outrank everything, then October, then a named day, then no preference, then November | 2026-08-17 | `houseInstallPriority` | Standing |
| SCH-05 | Is a named day urgent? | No — a named day is a **wait**, not a hurry. It is a floor: not before, and from there they take their turn, ahead of the people who don't mind | 2026-08-19/20 | `houseInstallPriority` = 2 | Standing |
| SCH-06 | Can anyone be hung before the month they asked for? | "under no circumstance." Enforced on the saved plan, not just at build time | 2026-08-17 | `houseAllowedFrom` | Standing |
| SCH-07 | Is October a deadline? | Yes — everyone who asked for October is done in October; November only if there is literally no other way, and then 1 November | 2026-08-18 | `houseDeadline` | Standing |
| SCH-08 | Thanksgiving? | Nobody works Thanksgiving Day, and Thanksgiving-labelled houses go as close to it as possible. The date is computed from the year, never written down | 2026-08-18 | `isWorkingDay` | Standing |
| SCH-09 | How big is a day? | 8 or fewer = one man. 9–19 = one crew. 20+ = two crews. **Eight is hers, not a formula** | 2026-08-20 | `ONE_MAN_MAX_HOUSES` | Standing |
| SCH-10 | Should the crew split be evened? | Yes, but only back to the cap — 19/21 becomes 20/20, 30/4 stays lopsided rather than breaking the cap | 2026-08-20 | `dayCrewHouses` | Standing |
| SCH-11 | Tidy the tail by filling one-man days to 15? | No — rejected after two attempts were built and shown. Prioritise every day having 40 over the ending days having 15; one-man days recalculate into crews | 2026-08-20 | `wastedDay` | Closed |
| SCH-12 | The dribble at the end — leave it or sweep it? | Sweep it up. But nobody is ever moved *later* than the day they already had, and a finished house never moves | 2026-08-18 | `packTailCrewDays` | Standing |
| SCH-13 | Which towns count as "close"? | No more than two per town, and actually close — "we dont want every city bordering each other" | 2026-08-20 | `DEFAULT_NEARBY_TOWNS` | Standing |
| SCH-14 | Do one-man installs get their own tab? | Yes, and it stays in sync as the season moves | 2026-08-20 | `oneManDays` | Standing |
| SCH-15 | When does a pinned day hold? | Only if the day falls within the next two business days. The pin is ignored, never deleted, and starts holding again on its own. "a day should only be set if the day falls within the next two business days." | 2026-08-20 | `effectivePin` | Standing |
| SCH-16 | Does ticking a stop on Schedule mark the customer complete? | Yes — one stop marks that customer, not the whole day. And it can trigger billing | 2026-08-21 | `hlxMarkJobDone` | Standing |
| SCH-17 | Does marking a route complete mean the takedown is done? | No. Install, takedown and fix are independent everywhere; nothing nests under anything | 2026-08-21 | `hlxMarkJobDone` | Standing |
| SCH-18 | Where does the route generator live? | In Schedule, and one button recalculates everything — two routes, one per crew, generated automatically | 2026-08-20 | `generateDayRoutes` | Standing |
| SCH-19 | Does the plan learn about customers added elsewhere? | Yes — the sync adds people it has never heard of, and pulls changes from all customers periodically | 2026-08-17/20 | `customersMissingFromSeason` | Standing |
| SCH-20 | Keep the New Members tab in Schedule? | No — removed. It answered a question only a re-import could raise | 2026-08-20 | pane removed, `syncTabs` | Closed |
| SCH-21 | Keep the Calendar tab? | No — confirmed delete. Schedule's day sidebar is the calendar | 2026-08-09 | routes-dashboard-plan.md | Closed |
| SCH-23 | Who sets "Before/After Thanksgiving", and where? | "that should only be assigned by admins in admin portal through customers and then that should go into reassign for that member." Before Thanksgiving is its own key (`prethx`), not swallowed by the November prefix | 2026-08-21 | Q-011; `prefKey` | Standing |
| SCH-24 | A house is past the day it asked to beat and no earlier day has room — move it or leave it? | **Leave it where it is and report it**, matching what the too-early branch has always done. The message says which way they are stuck; it used to say "no later day has room" to everybody, which is backwards for a missed deadline | 2026-08-21 | Q-011; `enforceInstallTiming` | Standing |
| SCH-25 | Does it matter to the crew that all their houses are in one town? (asked as: is a crew-day a patch of map, or a town?) | **A patch of map. What matters is that the houses are next to each other.** "what matters is that all the houses are next to each other. So if there not all in Lehi that is okay just as long as the houses are next to each other." Adjacency between HOUSES is the rule; the town name is not. ⚠ Settles Q-023, a same-tier contradiction that had been left open on purpose: the *ruling* for map was newer (2026-08-22, "city lines arent a concern") and the *code* for town was newer (2026-08-24 to 26). This answers it for the map side, so every rule that tests a TOWN where it means a DISTANCE is superseded — `townsAreNeighbours`, `MAX_TOWNS_PER_CREW` and the neighbour list. ⚠ The rulings those came from are NOT wrong and are kept: one crew in one place, no three-town days, and never stranding a rim house are all still what she is asking for. Only the test changes, from a name to a distance. | 2026-08-28 | `js/grid.js` (`planBlocks`, parked); grid-parked.test.js. ⚠ NOT WIRED — the builder on main still groups by town | Decided — not built |
| SCH-26 | Is every step of the path a customer takes dated, not just the ones I listed? | **Yes — and it took being asked twice.** "Are you sure you have everything that can be changed or moved within the system can be dated?" then "Okay so all paths are dated?" ⚠ BOTH TIMES THE HONEST ANSWER WAS NO, and both times the gap was found by ENUMERATING rather than by me judging my own list complete. Two steps had no date: being put on a crew sheet (`scheduledDate` is the day they are booked FOR, never when the booking was made) and a fix being RAISED (`fixDoneAt` recorded the mend, so nobody could tell a fault outstanding three weeks from one reported that morning). ⚠ AND THE FIELD EDITS ARE STILL NOT DATED — 46 of them — deliberately: they belong in the activity log, which records WHAT changed, not 46 more stamps that each say only that something moved. | 2026-08-28 | `queue-date.test.js` lists all nineteen steps and fails if one loses its stamp | Standing |
| SCH-27 | The 46 field edits are not dated — how do I get it so everything has stamps? | **They are logged, not stamped.** "Okay how can we get it so everything has stamps" — asked after every STATE had a date. ⚠ A DATE CANNOT ANSWER AN EDIT: "the address changed on 3 Oct" without saying what it changed FROM is the question restated, and 46 more stamps would each say only that something moved. So the Edit Customer save diffs what it is about to write against what the record held and writes ONE activity entry naming every field — "Timer: off → on; House price: $400.00 → $450.00". ⚠ ONE ENTRY PER SAVE, because a save is one event; a row per field turns an afternoon of tidying into a wall nobody scrolls. ⚠ EVERY FIELD IS LABELLED OR DELIBERATELY QUIET and the census fails on a new one that is neither — a field with no label does not throw, it just never appears in anybody's history, which reads exactly like the field never being edited. ⚠ AND THE VAGUE "Edited customer X" ENTRY IS GONE rather than left beside it: it fired on every save including ones that changed nothing. Answers the half SCH-26 left open. | 2026-08-28 | `describeCustomerChanges`, `CUSTOMER_FIELD_LABELS`, `change-log.test.js` | Standing |
| SCH-28 | Did Connections pick up the new dates and the change log? | **Half of it.** "you added on connections members and dates that things were changed right?" ⚠ THE RULES TAB HAD, because it is parsed from this map every build — WH-23 and SCH-27 were already on it. THE GRID HAD NOT: it is hand-written, so a field nobody declared cannot fail anything, and a green grid is never evidence that new work is wired. ⭐ WRITING THE SPINES IS WHAT FOUND THE REAL ANSWER: R-010 refuses a writer with no reader, and all five dates are written by real code and read by nothing — because the customer history that will read them is not built. So they are on the NOT-WATCHED list with that reason rather than forced onto the grid with an invented reader, which would be the false green the page exists to prevent. ⚠ AND THE CHANGE LOG'S LABEL TABLE was being read as writing the ten watched fields it names; the scanner blanks it now, the same way it blanks comments. | 2026-08-29 | `NOT_WATCHED` in `connections/manifest.js`, `DECLARATION_TABLES` in `connections/scan.js`, `connections.test.js` | Standing |
| SCH-29 | Build the customer history — the thing that reads all the dates? | **Yes** — "Okay go ahead", against her own definition of it: "journey map is dating when everything is done for each costumer through the system we have set". ⚠ IT IS WHAT MAKES THE DATES WORTH HAVING: five of them were written by real code and read by nothing, which Connections refused to declare under R-010, so the entries went on the grid in the same change that gave them a reader. ⚠ FIVE SOURCES INTERLEAVED BY DATE, never grouped by source — grouped, the day something happened stops being the thing you read down the page. ⚠ AND AN UNDATED STEP IS SHOWN AS UNDATED: dropped it claims the thing never happened, sorted to one end it invents an order somebody will act on, and it is the common case because every customer on file before the stamps has flags and no dates. | 2026-08-29 | `customerHistory`, `HISTORY_STEPS`, `history.test.js` | Standing |
| SCH-30 | Is the path quote → email → approve → form → convert → warehouse → schedule → crew → invoice → paid, and can Connections show it? | **The path is right; the page cannot show it.** Her words on the limit: "this can only show connection one level in" — the grid answers where ONE field goes, and the customer history is one customer's chain, not the chain. The order exists only in her paragraph. ⚠ THREE PLACES THE CODE DIFFERS, none of them a bug, all worth knowing: the install-details form is for NEW customers only (an existing member approving a re-quote is asked what is changing instead); the build does NOT gate the schedule (`customersMissingFromSeason` asks `isOutForSeason`, never the build, so the warehouse works in parallel — her model says it comes first and NOTHING enforces either, which is unasked); and there is no automatic payment chasing (`sendQuoteNudges` chases an unanswered QUOTE, and chasing a bill is a manual send). | 2026-08-29 | `system-map.md` §10d; `quoteRespond`, `customersMissingFromSeason`, `sendQuoteNudges` | Standing |
| SCH-31 | Should a house wait for its bundle to be built before it goes on the schedule? | **Yes.** "I do want it to wait for the build before sending it to schedule." ⚠ IT DID NOT: `customersMissingFromSeason` asked `isOutForSeason` and the 48-hour lock and nothing about the build, so a customer joined the season the moment they were in it and the warehouse worked in parallel — a crew could be sent to a house whose lights had not been made. ⚠ THE TEST IS "STILL WAITING" (`needsLightBuild === true`), NOT "has a built date": `lightsMarkedBuiltAt` only exists from 2026-08-27, so gating on it would hold back the entire book silently — the SEASON_ELIGIBILITY shape this repo has already been bitten by. ⚠ HELD BACK, NOT DROPPED, and COUNTED on screen: a house absent for a good reason looks exactly like one absent for a bad one, so the rebuild says how many are waiting and names the warehouse. | 2026-08-29 | `customersMissingFromSeason`, `customersWaitingOnBuild`, `rebuildSeasonDays` | Standing |
| SCH-32 | Which events should carry a date on a customer's history? | **Her own list, in her words**: "a cancelled button or a be back next year date. or maybe next year date or requoted on. Or asked for different lights on this date or changed timer settings this date. Changed address this date." ⭐ THE FIRST ITEM ON IT WAS THE ONE MISSING. `lightsChangedAt` had existed for a while — written by the portal, by Edit Customer and by the sheet sync, read by the Color Changes tab and the warehouse badge — and was on no path and in no history, so the event she named first was absent from the page built to answer her and nothing was ever red about it. ⚠ A FIELD WRITTEN EVERYWHERE AND NAMED ON NO ROUTE is the shape of hole the two censuses exist to catch, and neither could see it while the field was not listed as a step. ⚠ AND THE HISTORY SAYS WHO CHANGED IT: `lightsChangedVia` exists precisely because the customer doing it in their own portal and the office typing it in after a call are the same event from opposite ends, and the warehouse badge already tells them apart. ⚠ AN UNRECORDED ORIGIN CLAIMS NEITHER — every change made before 2026-08-24 carries no via, and a coin toss prints a confidently wrong claim beside a real date. | 2026-08-29 | `HISTORY_STEPS`, `historyLightsWords`, `history.test.js`, `journey.test.js` | Standing |
| SCH-33 | How many more of the dates she asked for were written and shown to nobody? | **Twelve, found by sweeping the code rather than the list.** No new ruling — this is SCH-32's answer applied properly. The colour change was found by hand; scanning every field in the four source files written with a real timestamp turned up 35 more on no path, twelve of them plain stages with the field already written. ⭐ NEITHER EXISTING CENSUS COULD HAVE FOUND THEM: both prove things about fields ON the list, and a field never put on it is absent from the question rather than answered wrongly — so the sweep runs from the code back to the list, the opposite direction from everything else. ⚠ THE PAIRS WERE THE REAL MISS — replied vs we-were-told, bill raised vs bill sent, asked back vs actually back, re-quoted vs agreed — each the easy half answered while looking complete. ⚠ AND "not a journey date" is now said out loud with a reason for all 27 of them; silence is how these sat unnoticed. | 2026-08-29 | `historyRequoteRows`, `HISTORY_STEPS`, `queue-date.test.js`, `history.test.js`, `journey.test.js` | Standing |
| SCH-34 | Where does "changed timer settings this date" get recorded, and does it work when the customer does it themselves? | **A log line, not a stamp, and the customer's half was missing entirely.** No new ruling; this is SCH-32 finished. A timer switched on in Edit Customer produced "Timer: no → yes"; the same switch flicked by the CUSTOMER in their own portal produced nothing at all, because the activity log is only ever written from admin.html. ⭐ THE OFFICE HALF LOOKING COMPLETE IS WHY NOBODY NOTICED — the same asymmetry `lightsChangedVia` closes one level up. ⚠ TWO COPIES, SO A PARITY TEST, the answer this repo already gives the invoice maths; what keeps it small is that the portal can only write PORTAL_WRITE_FIELDS, so the server table is exactly that set and both copies are run over every field in six shapes. ⚠ THE DIFF IS TAKEN BEFORE THE WRITE AND POSTED AFTER IT: taken afterwards it compares the record with itself, posted before it claims a failed save happened. ⚠ AND IT SAYS IT WAS THEM — every other row in that log is one of the four people who share the dashboard. | 2026-08-29 | `describePortalChanges`, `logPortalChange`, `change-log.test.js` | Standing |
| SCH-35 | Should Start New Season clear `removalDone`, the way it clears `completed`? | **Yes.** *"Oh so if we removed lights from someone's house that should reset for new season."* ⚠ IT WAS THE ONE JOB-DONE FLAG LEFT STANDING — nothing else in the app ever cleared it, the only other writer of `removalDone: false` being the Mark Done toggle unticked by hand — so a customer whose lights came down last December read **Removed** all the way through the new season until somebody opened their record. ⚠ THE SAME SHAPE AS `completed`, which is why it belongs beside it: both say a job was finished THIS season, and neither is true of the season about to start. ⚠ THE DATE STAYS. `removalDoneAt` is untouched, like every other date in that write — the history needs it to say when last season's takedown happened, and `seasonResetAt` is the line that stops it reading as this season's. Clearing the flag and keeping the date is the whole design. ⚠ IN THE SAME WRITE as the rest of the reset, asserted: a separate write can fail on its own and leave half the book reset, and Start New Season rewrites every customer in one press with no undo. | 2026-08-29 | Start New Season customer write; `queue-date.test.js` | Standing |
| MON-28 | How should a customer with no email on file be invoiced? | **Bill them anyway; the sending is hers.** *"How invoice bills. So if no email on file than invoice by phone for member portal. I'll send invoices that only have phone number on file myself."* ⚠ THEY WERE NOT BEING INVOICED AT ALL — the nightly run gave up BEFORE the invoice document was written, so their member portal (reached with a phone and a last name) had nothing to show them and no record existed of what was owed. ⭐ THE FIX IS AN ORDERING: the no-email block moved below the invoice write and the carryover handling, so only the SEND is skipped. ⚠ IT MUST STAY BELOW THE CARRYOVER DRAWDOWN — bailing out above it leaves the invoice holding a credit the customer still has in full, and the next run applies it twice. ⚠ `invoiceEmailSent` IS DELIBERATELY NOT SET: the bill has not gone out, so they stay on the nightly list until she sends it, which is what "I'll send those myself" needs. Re-running is safe — the fee, the carried charge and the credit are each guarded. ⚠ AND THREE PIECES OF WORDING MOVED WITH IT (the nightly text, the Inbox note, and a check pinning the first literally): they are billed now, and calling it impossible is the one thing that would stop her doing her half. | 2026-08-30 | `runInvoiceBatch`; run-all.js Suite 135 | Standing |
| MON-29 | Where should a card payment that found no bill show up? | **Health Check.** *"Put that in health check."* ⚠ THE MONEY WAS INVISIBLE — `recordUnmatchedPayment` files a capture that succeeded with no invoice to apply it to, no screen read that collection, nothing ever wrote `resolved: true`, and the rules forbid writing to it. Real money in a place with no way in and no way out, while the customer's portal reads Paid in Full. ⚠ THE INVOICES TAB WAS ARGUED FOR AND SHE CHOSE HERE; the HC-03 objection is largely spent since approve/deny shipped 2026-08-27. ⚠ NO FIX BUTTON — applying, refunding and marking-seen are three different answers about somebody's money, and only WHERE has been settled. ⭐ "Not a problem" clears a row and writes to `healthCheckDecisions`, never to `unmatchedPayments`, so the collection stays write-forbidden and NO RULES DEPLOY IS NEEDED. ⚠ A failed read reports nothing rather than a false all-clear. | 2026-08-30 | `hcLoadUnmatched`, `hcRunChecks`; run-all.js health checks | Standing |
| MON-30 | Should an unmatched payment reach her before an invoice goes out, and should the invoice screen say so? | **Both — a note in the System inbox, and a warning on the invoice screen.** *"we need unmatched invoice to come up in system inbox before we send it out."* Offered the choice between holding the bill back and flagging it, she chose **"Note it, and warn on the invoice screen"**. ⚠ SO THE BILL IS NOT HELD. That was the other option and it was turned down for a good reason: a payment we cannot match is OUR bookkeeping problem, and stopping somebody's invoice over it means they are not billed at all — a worse outcome for a customer who has done nothing wrong. ⭐ MON-29 SETTLED WHERE IT LIVES AFTERWARDS; this settles when she FIRST hears about it. Health Check is a panel she has said she rarely opens (HC-03), so a capture that lands at eleven at night and an invoice that goes out at seven the next evening could pass each other with nothing said. ⚠ THE INBOX NOTE IS WRITTEN BY THE SERVER, at the moment the capture is filed, and is deduped on the capture id — the same payment must not raise a note every time anything re-reads it. ⚠ AND IT CANNOT THROW: it runs after the money has already been taken, so a failed note must never unwind a successful capture. ⚠ THE BANNER SAYS THE MONEY IS SAFE. "Unmatched payment" reads as money lost; it is a payment that arrived and could not be filed, so the banner says so in those words and names the customers it might belong to. | 2026-08-30 | `recordUnmatchedPayment`; `renderUnmatchedPaymentBanner`; run-all.js | Standing |
| MON-31 | Start New Season wipes what an unpaid customer still owed. Should that debt carry into the new season? | **"Carry it onto this year's bill."** ⚠ IT WAS BEING WRITTEN OFF IN SILENCE. The reset writes `install: newInstall, deposit: 0` over every invoice, so an unpaid customer opened the new season owing this year's charge and nothing else — for all ~967 customers, with the only surviving record inside a `yearlySnapshot` nothing bills from. ⭐ IT RIDES IN THE EXISTING FEE LEDGER as a `changeFeeNotes` entry with `kind: 'arrears'`, NOT as a new term in the invoice formula. That ledger already reaches everywhere the money must: `syncPayerInvoice` leaves it alone and still counts it, the Edit Customer save rebuilds only the `manual` kind and keeps every other, the printed and emailed invoice renders one row per note from its own `reason`, and both fields are already in `INVOICE_READ_FIELDS` so the customer can see and pay it. A sixth argument to `computeInvoiceStatus` would have meant 47 call sites, a second server copy and a rewritten parity sweep to buy exactly that. ⚠ THE `kind` IS LOAD-BEARING — without it the line reads as a light-change fee to every existing reader and the hold never finds it. ⚠ THE AMOUNT IS THE WHOLE OUTSTANDING BALANCE via `balanceDueAmount`, so last season's own fees and credits are already settled inside it | 2026-08-31 | `ssnBuildPlan`, the Start New Season write, `arrears-hold.test.js` | Standing |
| MON-35 | When a carried debt is shown, which year does it name? | **The year they FELL BEHIND, pinned to the debt — not today's year.** "if it is 2028 but they haven't paid in 2025 than that does not change every year." ⚠ THE YEAR IS WRITTEN ONCE AND PRESERVED, never recomputed: Start New Season and the Arrears Backfill stamp `year` on the arrears note the first time a debt is carried, and every reset after that keeps it while the debt stands. Relabelling it each season would erase how old the debt actually is — which is the one thing that tells the office whether to chase it or write it off. ⚠ EARLIEST YEAR ONLY, for the whole lump sum, on her ruling the same day: a customer unpaid across several seasons shows the year they first fell behind. That falls out for free from there being exactly ONE arrears note per invoice — Start New Season REPLACES it rather than appending — so "the note's year" and "the earliest unpaid year" are the same fact as long as it is preserved. ⚠ AND A NOTE WRITTEN BEFORE THE FIELD EXISTED reads its year out of the four digits already in its own sentence, rather than showing blank on an invoice nobody has touched since | 2026-08-31 | `arrearsYearOnInvoice`, `houseArrearsYear`, `arrears-hold.test.js` | Standing |
| MON-32 | Once last year's debt sits on this year's bill, how do we know whether a payment cleared last year? | **The whole of last year's amount, or it is not cleared.** *"If they were billed 400 last year and paid 400 last year than it is cleared but if they needed to pay 800 and paid only 400 than bill is not cleared and we cannot schedule them."* ⭐ OLDEST DEBT FIRST is what makes it answerable: one bill now holds two debts, and a payment is just `deposit` going up — PayPal, Venmo, cash and the importer all look identical by the time the rule sees them. Money pays off the carried amount before this year's charge, so the question is one subtraction that never has to know where the money came from. ⚠ A PART-PAYMENT RELEASES NOBODY; the comparison is against the whole carried figure, never a proportion. ⚠ TWO ALTERNATIVES WERE PUT TO HER AND TURNED DOWN: two separate bills (perfect attribution, but the nightly run, the portal, PayPal capture and the payer-group logic all assume one bill per payer) and tagging each payment by hand (PayPal, Venmo and imported payments arrive untagged, so they still need a default rule — which is oldest-first with extra typing). ⚠ CREDITS COUNT, and that is the one way a hold lifts without cash — a credit is the office deciding the money is not owed, recorded as money rather than as a hidden override flag | 2026-08-31 | `arrearsSettled`, `owesFromLastSeason` in js/money.js | Standing |
| MON-33 | The season was already reset under the old write-off behaviour. Do last year's debts get recovered? | **Yes, by a one-off backfill.** *"The new season started but RSVP hasn't been sent out."* So Start New Season ran BEFORE MON-31 shipped: it wrote `install: newInstall, deposit: 0` over every invoice, and each unpaid balance is gone from the books, surviving only in `yearlySnapshots` — which nothing bills from. ⭐ **Invoices › Start New Season › Carry last season's unpaid bills** reads that snapshot and puts each balance back as the same `kind: 'arrears'` line the reset now writes itself. ⚠ IT FOLLOWS THE MASS-WRITE RULES: a dry run that writes nothing, a report naming everybody and every amount, a typed CARRY, and the apply writes ONLY what the dry run showed — re-finding rows at write time would charge people she never saw. ⚠ IDEMPOTENT BY THE LEDGER, NOT A FLAG: a bill already carrying a line is skipped, and that is re-checked against a FRESH read at write time, so running it twice — or twice at once from two machines — cannot double a debt. ⚠ IT READS THE SNAPSHOT FRESH rather than `yearlySnapshotsCache`, which only the `money` panel group fills and the Invoices panel never loads — the cache would report "nothing to carry" while real debts sat in the snapshot. ⚠ AND IT NEVER GUESSES AT A MISSING INVOICE: a customer re-keyed since the reset is reported, never matched by name onto a stranger's bill. ⚠ TIMING IS FORGIVING because the RSVP has not gone out — everybody is held by the RSVP rule anyway, so nothing about who is scheduled changes until replies start arriving | 2026-08-31 | `arrearsBackfillPlan`, `lastSeasonSnapshotFrom`, `arrears-hold.test.js` | Standing |
| MON-38 | Is "carry" a step the office has to take? | **No — and she was right to question it.** "I still don't see the use for carry I just think that should just stay on there account until they pay." That IS what happens: carrying is the MECHANISM by which a debt stays on the account, run automatically inside Start New Season's invoice reset. Without it the reset zeroes the debt with everything else. ⚠ THE ONE-OFF **Carry** BUTTON is not an ongoing tool: before 31 August, Start New Season wrote the debt off, and that button repairs only seasons reset in that window. Once used, or once it reports nothing to repair, it is finished | 2026-09-01 | `arrearsBackfillPlan`, Start New Season's invoice reset | Standing |
| MON-39 | Which customers' cards should show as red? | **Anyone who owes from an earlier season, or is 60 days past their payment.** "turn everyone that hasn't paid from last year or is 60 days over there payment as red for there card." ⚠ SIXTY DAYS FROM THE **INVOICE**, not from the due date — asked directly, she said *"no 60 days after invoice goes out."* The first version counted terms-plus-60 and reddened at 90 days; that reading was put to her and rejected. Since terms are 30 days, a card goes red 30 days after the payment was actually due. ⚠ ITS OWN THRESHOLD, NOT `OVERDUE_DAYS` — that one is 30 and drives the ordinary Overdue flag; sharing it would turn most of the book red in November and say nothing. ⚠ AND IT ASKS `houseOwesFromLastSeason`, the same rule that holds them out of the season, so the red card and the hold can never name different people. ⚠ A LEFT BAR AND A TINT, NOT RED TEXT: the row already uses colour for the RSVP and invoice pills, and recolouring those makes an overdue customer's answers unreadable | 2026-09-01 | `houseCardIsRed`, `red-card.test.js` | Standing |
| MON-40 | How prominent should Venmo be on the payment panel? | **A last resort, behind a closed dropdown, and the QR code gone.** "I don't want a venmo QR code anymore and I want venmo under other payments not showing at all cause I want venmo to be a last resort", then "venmo will be under other payments which will need to be a dropdown to see venmo." ⚠ A `<details>` RATHER THAN A SCRIPTED TOGGLE — closed by default with no JavaScript, so Venmo cannot flash on screen before a handler hides it, and it still opens if a script has thrown. ⚠ SHUT ON EVERY RENDER, not just the first: the panel redraws whenever the amount changes (a tip, an instalment), and a dropdown the customer opened would otherwise stay open and turn the last resort back into a visible equal option. ⚠ THE QR IS DELETED, NOT HIDDEN — a ~30KB base64 image shipped to every customer on every load, which asked them to type the amount in themselves; that is how a payment arrives for the wrong figure. ⚠ AND THE "&mdash; or pay with Venmo &mdash;" DIVIDER WENT WITH IT: announcing Venmo as an equal alternative is the opposite of what she asked for | 2026-09-01 | `otherPaymentsPanel`, `test/arrears-portal.spec.js` | Standing |
| MON-44 | Last year's unpaid balance sat in the same list as this year's charges. How should the bill read? | **Its own card, above this season's, each with its own subtotal.** *"I need the unpaid last year to look more obvious but still nice and organized"*, and, on the panel underneath, *"it says pay 1,146 but should only show 200."* ⚠ THE SECOND HALF WAS A REAL FAULT, NOT A LAYOUT ONE: `currentServiceDue = totalDue` had never been repointed when MON-37 made the button charge the arrears, so the page printed **Total Payment $1,146.00** directly above a button taking $200 — and the Venmo link, which is typed by hand at the other end, opened pre-filled with the wrong figure. That is money collected wrongly rather than a label. ⭐ MONEY ALREADY RECEIVED IS SHOWN ON THE CARD IT WENT TO, applied oldest-debt-first — the same order MON-32's hold reads it in — so a payment spanning both seasons cannot appear against this one and leave a subtotal that misses. ⚠ ONLY WHEN THERE IS A DEBT: a customer carrying nothing, which is nearly all of them, gets exactly the single list this page has always shown. ⚠ AND THE CARD KNOWS WHICH LEDGER LINE IS LAST SEASON'S BY IMPORTING `ARREARS_KIND`, never by retyping it — what is still OUTSTANDING remains the server's answer (MON-37), because a third copy of that rule would be the copy the customer reads. | 2026-08-31 | `renderInvoiceBreakdown`, `portalPayableNow`; `arrears-hold.test.js` | Standing |
| MON-43 | A customer paying last season alone adds a tip. Where does it go? | **To the crew — it was going onto the bill.** Put to her with the cost of leaving it, she chose to close it in the same change: *"Yeah lets fix it."* ⚠ FOUND WHILE BUILDING MON-38, LIVE SINCE MON-37 SHIPPED, and nobody could have seen it from the screen: `paypalCaptureOrder` split a payment as `serviceAmount = min(captured, balanceDue)` and called only the surplus a tip. That was correct for as long as the button charged the whole bill, and wrong from the moment it started charging last season alone — $200 plus a $30 tip arrives as $230, which is under a $1,146 balance, so the whole $230 was booked against the bill and the tip recorded as **$0**. ⭐ THE SPLIT IS INFERRED FROM THE INVOICE, STILL NEVER TAKEN FROM THE BROWSER: the original reason for recomputing is unchanged — a caller supplying `tipAmount` could book a real payment as 100% tip and leave somebody who paid still showing as owing — and createOrder only ever charges one of two figures plus a tip, so a capture landing on or above either of them settles that figure and the remainder is the tip. ⚠ WHOLE CENTS, or $230 against a $230 debt misses by 2.8e-14 and books the customer's whole payment as a gratuity. | 2026-08-31 | `paypalCaptureOrder`'s split, `arrearsOutstandingServer`; `arrears-hold.test.js` | Standing |
| MON-42 | Last season is the only amount offered. What about somebody who wants to clear the whole account? | **Two buttons — last season, or everything owing.** *"2 button options for last year and full payment."* ⚠ IT DOES NOT REOPEN WHAT SHE RULED OUT IN MON-37: *"I don't want them to type there amount because then we can't trust if someone paid in full"* still holds, because neither figure is typed — both are worked out by `paypalCreateOrder` from the invoice, and the customer only chooses between them. Last season stays pre-selected, so the default path is exactly the behaviour MON-37 shipped. ⚠ THE CHOICE TRAVELS AS A FLAG, NEVER AS AN AMOUNT (`payAll`), which is what keeps it safe: the worst a forged request can do is pay more of the sender's own bill than the page offered. ⭐ AND IT CLOSES THE OTHER HALF OF MON-37'S HOLE: that one let somebody pay last season when they could not before; this lets somebody clear the lot without ringing the office or falling back to Venmo. ⚠ THE TIP FOLLOWS THE AMOUNT — a percentage picked against the balance and left sitting on a $200 payment is a tip nobody agreed to — but a typed figure is left exactly as typed. | 2026-08-31 | `paypalCreateOrder`'s `payAll`, `portalPayableNow`, `renderPayChoice`; `arrears-hold.test.js` | Standing |
| MON-34 | Should a customer held for last season's debt be chased automatically? | **No — "I'll send those emails/text myself."** ⚠ THE GAP IS REAL AND IS ACCEPTED KNOWINGLY: the nightly run only emails a bill once every house on it is `completed`, and a held customer is never scheduled, so they are never completed and never sent their invoice. Nothing goes out on its own. ⭐ WHAT MAKES THAT WORKABLE: the bill EXISTS with the carried line on it and they can see and pay it in their portal; **Schedule › Owes from last year** names them with the amount and a phone number; and Automation Emails' Unpaid/Partial audiences now include them, because the carried line counts toward the balance. ⚠ SO DO NOT BUILD AN AUTOMATIC CHASE without asking again — and note PROC-32's late-fee chase ($25 / $40) is separately Decided-not-built, so the two would want designing together | 2026-09-01 | `runInvoiceBatch`'s completed gate; `owesFromLastYearHouses` | Standing |
| MON-36 | This is the first year on the system — how do 2025's unpaid bills get in? | **A box on each customer.** *"how to we add not paid 2025"*, answered **"A box on each customer"** over a bulk paste. **Edit Customer › Owed from a previous season** takes an amount and a season and writes a real carried line. ⚠ THE FEES BOX COULD NOT DO THIS JOB, and that is why a new box exists rather than reusing one: a manual fee raises what they owe and does **not** hold them out of the season — verified by running it — so a crew would still have gone out. ⚠ IT WRITES `kind: 'arrears'` (which is what holds them and what the money counts) PLUS `source: 'office'` (which is what lets this box rebuild ITS line while leaving the one Start New Season worked out alone). Each rebuild in that handler owns exactly one thing and keeps every other — the same split the manual fee and the carried credit already use. ⚠ A bulk paste was offered and not taken; if 2025 turns out to be dozens of people, ask again rather than typing them all | 2026-09-01 | `editCustArrears`, the Edit Customer fee rebuild, `arrears-hold.test.js` | Standing |
| MON-37 | PayPal charges the whole balance. How does somebody pay only last season? | **One season at a time, and never a typed amount.** *"I don't want them to type there amount because then we can't trust if someone paid in full. Can we do a one year payment than next years payment will show up after they paid that year?"* ⭐ SO THE CUSTOMER IS ONLY EVER SHOWN ONE FIGURE, AND IT IS ONE WE CHOSE — **that clause is Superseded → #MON-38**, she asked for a second button on 2026-08-31, and the half that survives is the one that mattered: neither figure is ever typed.  while last season's carried balance is outstanding the button charges exactly that; once it is paid the same button comes back offering this year's. `paypalCreateOrder` recomputes it from the invoice every time, so nothing is stored and nothing can go stale. ⚠ IT CLOSES A HOLE THE CARRY OPENED: the season rule releases somebody who pays only last season, and until this the only way to pay was the whole bill or Venmo. ⭐ AND IT SAYS THE YEAR — *"we need to emphasize that is last years payment so someone doesn't get mad and think they are charged twice."* A button charging less than the balance with nothing explaining why reads as a mistake or a double charge, so the portal names the season, says it is **not** a second charge for this year, and promises this year's amount next. The invoice line itself carries the year too, because that is what the printed and emailed bill prints. ⚠ THE PORTAL IS SENT THE FIGURE, never its own sum of the ledger — a third copy would sit outside money-parity and would be the copy the customer reads. ⚠ AND IT EXPOSED A REAL BUG: `onApprove` set **Paid in Full** and $0 unconditionally, which was right while the button always charged everything and wrong the moment a part payment exists. Told they were paid in full a customer stops, and the rest is never asked for again | 2026-09-01 | `arrearsOutstanding` / `arrearsOutstandingServer` (money-parity), `paypalCreateOrder`, `renderArrearsNotice` | Standing |
| RS-30 | Somebody pays off last season but has still never answered the RSVP — how does the office find out? | **A System inbox note.** *"if someone pays that didn't pay last year, but also didn't approve email than that needs to show in system inbox so we can send them an email asking if they approve for us to put there lights up for this year."* ⚠ IT IS THE ONE MOMENT NOTHING ELSE WOULD REPORT: paying clears the money hold, so they quietly leave **Owes from last year** — and they are still not scheduled, because they never answered. Without the note they move from one silent list to another with nothing prompting the call that is now the last thing between them and their lights. ⭐ IT HANGS OFF THE INVOICES LISTENER, the one place every payment lands whatever door it came in by — the Paid/Partial dropdown on either screen, a PayPal capture written by the server, and the payment importer. Detecting it at each would be four copies of one rule. ⭐ ONLY FOR THE SILENT, AND SHE CONFIRMED IT UNPROMPTED (2026-09-01): **"if they said back next year or no we don't need a system email."** It shipped that way as a design call and is now her ruling, which is the difference that matters — do not widen it to everybody who has paid. Somebody who said no or Back Next Year has answered, and a note asking whether they approve would argue with a decision already given. A bare stored `yes` with no reply behind it DOES count as silent (RS-19) — that shape is an import or the assumed yes at conversion. ⚠ RAISED ONCE PER CUSTOMER PER SEASON via `arrearsPaidNoticeAt`, which Start New Season clears; a failed write retries rather than losing the note | 2026-09-01 | `arrearsPaidNotApproved`, `noticeArrearsPaidNotApproved`, `arrears-hold.test.js` | Standing |
| RS-31 | A yes on the RSVP ended on "Do you want to make any changes?" with the portal behind a button. Where should it end? | **In their member portal, automatically.** Dax, 2026-09-01: the RSVP buttons "dont do anythng we need but it needs to change the customers badge and it should also automatically send the customer to their member portal." ⭐ THE BADGE HALF WAS ALREADY TRUE and was checked before anything was built — all three buttons call `portalRsvp`, which writes `rsvpStatus` through `seasonYesUpdates`, and `seasonBadgeKey` derives the badge from it; Ashley Wray and Rachel Oslund both read `RSVP: Yes — CONFIRMED` on the live book. So only the routing changed. ⚠ THE GATE CODE STEP SURVIVES AND IS THE ONLY THING LEFT IN BETWEEN — Dax, asked: "gate codes change so make sure it asks if theres a gate code", which is [[RS-29]] reaffirmed rather than reversed; that step already asks every time and quotes a stored code back for confirmation. ⚠ YES ONLY. "No" still gets its message before the Cancel tab, which is Addie's own 2026-08-19 ruling ("put a message in front of it first so they know why they've landed there"), and Back Next Year still ends on its own card. ⚠ AND THE DEBT WARNING WAS NOT LOST WITH THE SCREEN IT LIVED ON: the removed card told a debtor we could not book them, so that sentence moved onto the portal's own "<year> season — still owing" card, where it is true whenever they open the page rather than only after an RSVP. | 2026-09-01 | `openPortalAfterYes`, `showRsvpGateCodeStep`, `renderPortalInvoice`; test/rsvp-arrears.spec.js, rsvp-gate-code.spec.js, rsvp-link.spec.js, signed-in-links.spec.js — 62 browser tests green | Standing |
| RS-32 | Why does the second answer on the RSVP cards blend into the white, and why does the card look uncentred? | **A white-on-white button, and it was hiding a real dead end.** Dax, 2026-09-01: "the no/ not applicable button blends in with the white and also the whole thing is not centered and pretty fix that." ⚠ `.btn-outline` is built for the DARK hero — `color:#fff` on a 35%-white border — so on the light confirmation cards it rendered invisible: present, taking its space, unclickable-because-unfindable. ⭐ NOT ONLY COSMETIC: on a customer who already has a gate code the two answers are "Yes, that's right" and **"It has changed"**, and that second one is the ONLY route to the entry box — so a gate code that had changed could not be reported at all, which is exactly the case Dax had just asked to protect ([[RS-31]], "gate codes change so make sure it asks"). The No path's "That's all, thanks" was the same class and equally invisible. ⚠ AND THE "not centered" HALF WAS THE SAME BUG: the invisible button wrapped onto its own line, leaving a strip of blank card under a lone gold button. ⚠ SCOPED TO THE THREE CARDS BY ID, never `.btn-outline` at large — the class is correct as it stands on the hero and in the mobile panel, and repainting it globally is this bug pointing the other way. ⭐ THE SAVE PATH WAS CHECKED AND IS SOUND, unchanged: Yes opens the box, `portalSetGateCode` (deployed) writes `gateCode` + `gateCodeUpdatedAt` to the customer, and the office reads it. | 2026-09-01 | `.btn-outline` overrides in index.html; test/rsvp-gate-code.spec.js measures COMPUTED CONTRAST in a real browser (jsdom applies no stylesheet), 1 sabotage red-checked | Standing |
| RS-33 | Should an RSVP "no" still stop for a confirmation before the member portal? | **No — straight into the portal, and tracked either way.** *"can no go straight to member portal but will track it even if they don't get to member portal."* ⚠ **THIS REVERSES HER 2026-08-19 RULING**, kept here because it is the argument against: *"keep the Cancel tab, but put a message in front of it first so they know why they've landed there"* — a no used to drop them into an account page with a reason box and nothing saying what had happened. R-024: the later answer wins, and it is applied out loud rather than quietly. That earlier ruling never got a row of its own; it lived only in a code comment, which is exactly the gap R-023 exists to close. ⭐ **THE TRACKING HALF IS THE ONE SHE NAMED AND IT IS NOT NEW** — `portalRsvp` runs BEFORE any navigation, so closing the tab, a portal that will not load, or a dead network cannot cost the answer. The ORDER is the whole guarantee, so it is asserted as an order (portalRsvp must be the first call) rather than as "both happened". ⚠ **AND IT FAILS BACK, NEVER BLANK**: if the portal cannot be opened they get the old confirmation and the offer to say why, which is precisely the flow this replaced | 2026-09-01 | `handleRsvpLink` no-branch, `test/rsvp-link.spec.js`, `test/signed-in-links.spec.js` | Standing |
| RS-34 | The Back Next Year confirmation reads as a loading screen. What should it look like? | **A pop-up message.** *"back next year it looks like a loading page so if they don't read it they might be a little confused. Can we make it more of a pop up message."* A tree over one centred line on the page background is what a spinner looks like, so it said "still working" rather than "we are done". It is a white card on a dimmed backdrop now, with an uppercase heading stating the answer was recorded. ⚠ **THE HEADING WAITS FOR THE WRITE.** It reads "that's recorded", which is a claim: shown while the call is in flight it would promise something we do not yet know, and shown after a failure it would contradict the error underneath it. Hidden until the answer comes back, and left hidden on every way out | 2026-09-01 | `handleBackNextYear`, `#backNextYearCard` | Standing |
| RS-35 | Where should the gate-code question be asked — before the member portal, or inside it? | **Inside it, as a pop-up over the portal.** Dax, 2026-09-02: *"after they answer the gate code question it should just put them into their member portal"*, then *"make it so the gate code question is just a pop up in the member portal but keep the buttons exactly as is instead of an entire page."* ⚠ **THE ASKING IS NOT REVERSED, ONLY THE PLACE.** [[RS-29]] (ask before anything else) and the half of [[RS-31]] that kept the step both still hold — every confirming customer is still asked every year, and a code already on file is still quoted back for confirmation rather than being asked about from scratch. What changed is that the question stopped being a screen standing in front of the account: a yes now lands in the portal and the dialog arrives on top of it. ⭐ **THE BUTTONS ARE UNTOUCHED** — same two answers, same ids, same wording, same swap when a code is held ("Yes, that’s right" / "It has changed"), same entry box, same `portalSetGateCode`. He asked for that explicitly and it is also what keeps [[RS-32]]’s contrast fix and every spec that clicks them meaning what they meant. ⚠ **IT LIVES OUTSIDE `#rsvpConfirmCard`, WHICH IS NOT A DETAIL.** That card is torn down on the way into the portal, so a dialog nested inside it would be hidden by the step that opens it. Its own top-level `position:fixed` box is the whole reason it can float over a page it is not part of — and the backdrop has to out-stack the portal’s fixed phone tab bar, or a customer taps "Changes" straight through it. ⭐ **AND IT CANNOT STRAND ANYBODY BY CONSTRUCTION NOW**, rather than by the fail-open paths inside the step. The question is the LAST thing to happen, after the destination: `renderCustomerInvoicePage` fires `opts.onPortalReady` as the final line of a successful render, so a customer whose portal did not open (deactivated, wrong last name, no invoice) is left reading why instead of answering a gate question over an error. Escape and the backdrop close it and write nothing — a dialog with no way out locks somebody out of their own account, and an unanswered question must never look like an answer. ⚠ **THE SAME POP-UP SHAPE [[RS-34]] CHOSE** for Back Next Year, for the same reason: a card on a dimmed backdrop reads as a thing to deal with, where a line on the page reads as the page still loading. | 2026-09-02 | `#rsvpGateCodeModal`, `showRsvpGateCodeStep`, `openPortalAfterYes`, `opts.onPortalReady` in `renderCustomerInvoicePage`; test/rsvp-gate-code.spec.js (6 new browser guards), button-contrast.spec.js, rsvp-link.spec.js, signed-in-links.spec.js — 71 browser tests green, 6 sabotages red-checked | Standing |
| RS-36 | Should a customer who owes for last season be able to use the portal before they pay? | **No — a pop-up with no way out, and the writes refused on the server.** Dax, 2026-09-02: *"make sure it forces them to pay for their last year lights before they can do anything and before anything goes into the system"*, then the shape: *"it should be a pop up that doesnt go away until they pay in full that comes after the gate code and says pay for last season before we put you on the schedule for this one"*, and on the arrangement *"organize that the way you think is best as long as you make it obvious they need to pay first and make it easy for them to pay for"*. ⭐ **THREE THINGS HOLD IT AND THE SCREEN IS THE WEAKEST.** The shut tabs are the polite half; `portalSave` refuses the write, which is what makes *"before anything goes into the system"* true, because a callable is public and a hidden tab is not a lock. The pop-up is only what a customer meets. ⚠ **WHAT IS EXEMPT, AND EACH FOR ITS OWN REASON.** The RSVP ANSWER is never held — [[RS-33]] turns on it being recorded whatever else happens, and a customer who owes is exactly the one whose yes or no is most needed. CANCELLING is never held: somebody trying to leave must not be told to pay first, or they stop replying and the office never learns why — Dax’s own call when the scope was put to him. CONTACT stays open because the card itself points at it, and a dispute route that is locked turns a disagreement into silence. THE GATE CODE still saves, because it is still asked, and it is asked FIRST. ⚠ **AND IT FAILS OPEN**, the opposite direction to the season hold and for the same reason `arrearsForCustomer` answers nought on an unreadable invoice: refusing a save because we could not READ a bill would lock out somebody who may owe nothing, and the office still holds them out of the season either way. A change slipping through costs a form field; a customer locked out of their own account costs a phone call and their trust. ⚠ **PAID-OFF IS NOT OWED.** The carried line stays on the invoice for ever; what decides the hold is what is still OUTSTANDING against it, and the figure is the server’s (`arrearsOutstanding`) rather than a fourth copy of the maths in the page. | 2026-09-02 | `arrearsHoldBlocks` / `arrearsHoldError` in functions/index.js, `applyArrearsLock` and `#arrearsLockModal` in index.html; test/arrears-lock.spec.js (9 browser guards) and arrears-hold.test.js (8 server-side), 11 sabotages red-checked | Standing |
| RS-37 | What does the office send somebody who owes for last season and has still not RSVP’d? | **Its own template, "Not Paid RSVP".** Dax, 2026-09-02, asked for the email and then *"can you actually put it directly into the admin page"*. ⚠ **THE PLAIN RSVP EMAIL CANNOT DO THIS JOB**: it asks whether they want lights without mentioning that they will not be scheduled whatever they answer, so a yes from a held customer reads as a booking and is not one. It also asks them to check their gate code and outlet, which is a question about a crew visit that is not happening yet — deliberately not carried over. ⚠ **IT NAMES NO FIGURE, AND THAT IS THE TRAP TO KNOW ABOUT.** There is no token for the carried balance, and `{{amount_due}}` on a MANUALLY sent email resolves to THIS year’s install price — so adding it here would print the wrong number in an email about an old debt and read as exactly the double charge [[MON-37]] exists to prevent. The portal holds the one figure we computed and a yes now lands them on it ([[RS-31]], [[RS-35]]). ⚠ **THE YEAR IS TYPED, not merged**, for the same reason — re-word it next season. ⭐ **AND IT HAD TO REACH AN INSTALL THAT ALREADY HAS TEMPLATES.** `etSeedPrebuiltTemplates` only ever runs on a completely empty library, so a prebuilt added later is just a name in the source nobody can find. The one-time top-up handled exactly one template by name and is now a list — recorded in `settings/emailTemplateSeed`, once per name ever, so a template the office deletes stays deleted instead of coming back every morning. | 2026-09-02 | `ET_PREBUILT_TEMPLATES`, `ET_TOP_UP_NAMES`, `etEnsureMaybeTemplateExists` | Standing |
| RS-38 | Why did the Sides tab show an empty card? | **It was missing from `PORTAL_TAB_NAMES` and had never once opened.** Nobody reported it — it turned up while building [[RS-36]], and it is here under Dax's standing instruction of 2026-09-01: *"i need you to look for things in the system we dont want to find out later when we get to it"*, alongside *"go through every single peice of the admin page and make sure everything will work"*. `activatePortalTab` hides every panel it names and shows the one asked for — so clicking **Sides** hid all six tabs it did name and never showed `#tabPanel-sides`, leaving the customer looking at the tab strip above a blank card. ⚠ **NO SOURCE READ COULD HAVE CAUGHT IT**: the button, the panel and the handler were each correct on their own, and the fault was a name absent from a list. Verified in a real browser before and after. ⭐ **THE GUARD IS THE GENERAL FORM, not the one name** — every `data-tab` on the page must appear in `PORTAL_TAB_NAMES`, so the next tab added cannot repeat this silently. ⚠ **AND IT MATTERED MORE THAN IT LOOKS**: the sides question is the one that changes the PRICE (a requote), so the tab that could not open is the tab the office most needs answered. | 2026-09-02 | `PORTAL_TAB_NAMES`; test/arrears-lock.spec.js runs the tab, arrears-hold.test.js checks the list against the buttons | Standing |
| RS-49 | Can we drop **No** from the RSVP Status dropdown, since it means the same as Back Next Year? | **Yes — the option goes, the VALUE stays.** *"we can just get rid of the no under rsvp because it means the same thing as back next year."* ⚠ **THEY DID NOT MEAN THE SAME THING, AND THE DIFFERENCE WAS DESTRUCTIVE.** Picking No set `needsLightRecycle` — the warehouse queued to pull their bundle apart and hand the customer number back — while [[RS-05]] records that Back Next Year deliberately never does. So one answer in a list of answers also started physical, irreversible work. ⭐ **THAT IS WHY REMOVING IT IS RIGHT RATHER THAN MERELY REQUESTED**: a recycle should be a button that says so, and it already is — *Recycle Their Old Set* in Edit Customer writes the same flag with a label describing what it does. **No capability is lost**, only the side effect. ⚠ **THE EXCEL DESTINATION IS UNAFFECTED**, which was the thing worth checking before agreeing: `HLX_STATE_TABS` keys the Recycle sheet on `needsLightRecycle`, never on the RSVP answer, so the button still sends somebody there and Back Next Year still goes to Contact 2027. ⚠ **THE VALUE `no` IS NOT GONE AND MUST NOT BE.** `portalRsvp` still writes it when a customer taps No in an email, so the badge, `isOutForSeason`, the Members filter, the exports and the Yes sheet all still read it — and the Members-tab FILTER keeps its No option, because those records exist and have to be findable. What went is only the office's ability to CHOOSE it. ⚠ **A STORED `no` SHOWS AS BACK NEXT YEAR AND NORMALISES ON SAVE.** Without that the form shows a blank select — which reads as *Pending (never asked)* — and the next save writes that blank over their answer. One state, one spelling. ⚠ **[[RS-10]] IS NOT SUPERSEDED, ONLY NARROWED**: it describes what each of the three answers DOES, and all three are still values the system holds and reads. What changed is which two the office may pick. Said out loud per R-024 rather than marked Superseded, because marking a row dead that is still accurate is its own kind of drift. | 2026-09-03 | the `#editCustRsvp` options, `openEditCustomerModal`'s stored-`no` mapping, and the removed recycle branch in the customer save; run-all.js Suite 301 ⚠ **SUPERSEDED ON ONE HALF, 2026-09-04.** The OPTION stays gone — the office still cannot choose No, and the reason is unchanged: choosing it is what queued the warehouse to take a bundle apart. What no longer holds is *"it means the same thing as back next year"*: RS-50 splits them on the badge so the two follow-up emails have something to aim at. The one behaviour that had to change with it is the normalisation in `openEditCustomerModal` — showing a stored No as Back Next Year was free while they were one answer, and became a silent move onto the wrong mail-out the moment they were two. It now SHOWS the value without OFFERING it. | Superseded → RS-50 |
| RS-48 | The Confirmed / Maybe Next Year / Pending badge stopped updating once everything moved under RSVP Status. What broke? | **Not the badge — the only screen that can feed it.** *"the badges on the page that says confirmed, maybe next year or pending, that doesnt update it used to its because we got rid of the thing that was confirmed and maybe next year badges in add customer and put all that under RSVP Status but its not working can you fix that for me"*. `seasonBadgeKey` delegates to `isOutForSeason`, which under `confirmed-only` wants a yes **and a date on it** — and the RSVP Status dropdown stamped `rsvpRespondedAt` only when the dropdown **value moved**. ⚠ **SO THE ONE STATE THE OFFICE HAS TO REPAIR BY HAND WAS THE ONE STATE IT COULD NOT**: a record already carrying `rsvpStatus` 'yes' with nothing dating it — the ASSUMED yes written when a quote is converted, or carried in by an import. The dropdown already reads Yes, so picking Yes changes nothing, so nothing is stamped, so the badge stays Pending for ever. ⭐ **AND `seasonHold` TELLS THEM TO DO EXACTLY THAT** — *"a yes is on file but nothing dated it — confirm it on their record"* — so following the instruction on screen did nothing, which is worse than no instruction. ⚠ **MEASURED, NOT IMAGINED**: on the live book that day, 5 customers said yes and 3 were dated. Two were stuck. ⚠ **THE FIX IS IN THE SAVE, NOT THE BADGE.** Softening `isOutForSeason` to accept an undated yes would put a Confirmed badge on people every scheduler in the app still refuses — the exact disagreement [[SCH-38]] and the badge's own note exist to prevent. ⚠ **AND IT NEVER RE-STAMPS A DATED ANSWER**: that date is what the Yes sheet and the customer history read, so editing a phone number must not move the day they replied. | 2026-09-03 | the `oldRsvpForRecycle` block in the customer save; run-all.js Suite 297, red-checked, and Suite 78 still owns the value-changed path | Standing |
| RS-50 | A customer who answers **No** lands on the Back Next Year badge. Should it? | **No — No gets its own badge.** *"when they click no they go to maybe next year but actually we want them to just go to no for the badge in case we want to send two different emails for each."* ⚠ **THE TWO ANSWERS WERE ALREADY STORED APART** — `portalRsvp` has always written `no` and `backnextyear` as separate statuses, and the Email Tool's RSVP filter has always offered them as separate audiences, which is exactly the two mail-outs she wants. `seasonBadgeKey` was the one line that folded them together, so All Customers showed one yellow chip for two different decisions. ⚠ **NEITHER MOVES IN OR OUT OF THE SEASON** — `isOutForSeason` is untouched, so routing, billing and the warehouse see exactly what they saw before; only the word on the chip and the value the Season Badge filter matches changed. ⚠ **AND THE CUSTOMER'S LATEST WORD WINS OVER THE OFFICE FLAG**: `maybeNextYear` is only ever written alongside `backnextyear`, so holding the flag while reading `no` means they answered no afterwards — the rule `isOutForSeason` already states. ⚠ **THIS IS THE SECOND DELIBERATE OVERLAP** with the RSVP pill's own vocabulary, alongside Back Next Year: the guard in `season-state.test.js` forbids one word answering two *different* questions, and here it answers the same one. | 2026-09-04 | `seasonBadgeKey`, Suite 303 in `run-all.js`, `season-state.test.js`, `arrears-hold.test.js` | Standing |
| RS-39 | Should the unpaid-last-season email send itself? | **Built, wired, and shipped switched OFF.** Dax, 2026-09-02: *"i dont have the automated email set up i told you to do that for the unpaid."* ⛔ **THIS RUNS INTO [[MON-34]], WHICH IS ADDIE'S OWN STANDING RULING** — asked the day before whether somebody held for last season's debt should be chased automatically, she answered *"I'll send those emails/text myself"*, recorded with *"DO NOT BUILD AN AUTOMATIC CHASE without asking again"*. So the machinery exists and does nothing: `sendArrearsRsvpEmails` returns immediately unless `settings/arrearsRsvpAutomation.enabled` is true, and an absent document is off. Ticking that box is a person deciding to override her — **her sentence is printed on the card beside the switch** so whoever ticks it reads it with their hand on it. R-024 says the later answer wins, but the later answer has to actually be given. ⭐ **AND THERE IS A HAND-SEND THAT TURNS NOTHING ON.** *Send Once, Now* runs the identical batch on demand, which is the closest thing to what she said she wanted to do herself. ⚠ **WHO IT WRITES TO: owes for a previous season AND has never answered the RSVP.** Not somebody who said no or back next year — [[RS-30]] settles that shape (*"if they said back next year or no we don't need a system email"*), and they have answered. Not somebody who said yes either: the template opens by asking whether they want lights this year, which to somebody who has already said so reads as us losing their answer. They are held by the money regardless, and the portal says so ([[RS-36]]). ⚠ **ONCE PER CUSTOMER PER SEASON, EVER** — `arrearsRsvpEmailAt`, cleared by Start New Season. A daily schedule with no stamp is a daily email to somebody who owes money. **And it stamps only after the send succeeds**, so one bad response from the mail service cannot silently drop a customer for the whole season. ⚠ **THE TEST RECORD CARRIES ADDIE'S OWN PHONE**, so skipping it is not housekeeping — it is the difference between a dry run and mailing the owner a chase for a debt she does not have. | 2026-09-02 | `runArrearsRsvpBatch`, `sendArrearsRsvpEmails`, `runArrearsRsvpNow`; Admin › Invoices › Unpaid Last Season; run-all.js Suite 288 RUNS the shipped batch against a fake book, 10 sabotages red-checked | Standing |
| RS-40 | A customer pays in the portal. What should happen on the office screen, and what if they still have not RSVP’d? | **Everything already moved except the screen — and now they are asked on the spot.** Dax, 2026-09-02: *"make sure that after they pay in the member portal that it updates in all customers so it doesnt say they are unpaid and moves them to confirmed(if they already clicked yes) if they went into the member portal and paid and havent actually clicked yes on the rsvp it should move them to pending after they pay"*, and then *"you could just have it ask them after they pay too and if they dont answer it then it should pop up in our system where we can see it so we know to email them"*. ⭐ **THE RULES WERE ALREADY RIGHT AND IT WAS PROVED BEFORE ANYTHING WAS BUILT.** Every figure on that row is derived live — `computeInvoiceStatus` reads the deposit, `houseArrearsTag` asks `houseOwesFromLastSeason`, `seasonBadgeKey` moves between On hold and Confirmed off the same invoice — so a payment already did all three. **What was missing was anybody telling All Customers to draw again**: the invoices listener repainted the invoice list, the routes, the takedowns and the dashboard, and not the one screen the office watches. So the row went on saying *Unpaid 2025* until somebody navigated away and back. One line. ⚠ **MONEY IS NOT CONSENT, and that is the half the season turns on.** Somebody who pays and has never answered stays **Pending**, whether they paid last season’s carry or this year’s bill in full. Confirmed would send a crew to a house nobody asked us to do. A bare stored `yes` with no reply behind it does not count either ([[RS-19]]). ⭐ **SO THE QUESTION IS PUT TO THEM THE MOMENT THE PAYMENT LANDS** — a card over their own portal, with the three answers. Its buttons carry `data-portalrsvp`, so they run the SAME delegated handler the portal’s own RSVP block uses rather than a second copy that can drift. ⚠ **AND IT CLOSES, unlike the arrears lock ([[RS-36]]).** That one withholds something until they pay; this is asked of somebody who has just paid, and trapping them after they did what we asked would be the wrong way round. ⭐ **THE BACKSTOP IS THE SYSTEM NOTE, widened from [[RS-30]]**: any customer with money in and no answer raises one, not only somebody who cleared an old debt. Addie’s own line survives untouched — *"if they said back next year or no we don’t need a system email"* — because an answer is an answer. | 2026-09-02 | the invoices listener’s `renderAllCustomersTable()`, `paidButNotApproved`, `#rsvpAskModal` / `showRsvpAskIfUnanswered`, `rsvpRespondedAt` added to `PORTAL_READ_FIELDS`; run-all.js Suite 290 RUNS the season rules, test/paid-then-rsvp.spec.js drives the card, 9 sabotages red-checked | Standing |
| RS-41 | A prebuilt template exists but is empty. Who fills it in? | **The top-up does, and only ever the blanks.** Dax, 2026-09-02, over an Edit Template screen showing a name, a folder and nothing else: *"i dont know what you did but this is still not filled out"*. ⚠ **THE FAULT WAS THE SKIP, NOT THE SEEDING.** He had made the template himself from the wording he was given — typed the name, picked the folder, stopped. The top-up then did exactly what it was written to do: found a template of that name, left it alone, and recorded the name as handled. The result was a correctly named template in the right folder that would have sent an **empty email** to everybody it reached, and a seed record saying the job was done. ⭐ **SAYING "it is in the admin page" WAS TRUE OF THE CODE AND FALSE OF THE THING HE NEEDED**, and he was the one who found that out — the fix below is what makes the two the same claim. ⚠ **IT FILLS A BLANK AND NEVER REPLACES WORDS.** Each field is patched only when empty, so a body the office has written is untouched and the repair is idempotent — once filled it stops for ever. ⚠ **AND IT IS NOT GATED ON THE SEED RECORD, unlike creating.** By the time this fault is visible the name is already recorded as handled — that is precisely why it stuck — so gating the repair on the same record would guarantee it never ran. Creating stays gated, so a template the office deletes stays deleted. ⭐ **linkedTokens ARE PART OF THE PATCH**, or the RSVP buttons arrive in somebody’s inbox as raw `{{tokens}}` — invisible until it is sent. | 2026-09-02 | `etEnsureMaybeTemplateExists`; run-all.js Suite 289 RUNS the shipped function against a fake library, 6 sabotages red-checked | Standing |
| RS-44 | Should the gate-code question be put to every confirming customer, or only to the ones we already hold a code for? | **Only the ones we already hold one for.** Dax, 2026-09-02: *"make it so the gate code question after they accept gate code only applys to people that already have a gate code in the system so not everyone is seeing it."* ⚠ **THIS NARROWS [[RS-29]] RATHER THAN REVERSING IT**, and the half that survives is the half the question was worth having for: a code we hold going stale over the summer is what puts a crew at a locked gate, and it is still confirmed on the one screen every confirming customer sees. What is dropped is asking the majority — who have no gate at all — a question whose honest answer is *no*, as a toll on the way into their own portal. ⭐ **NOTHING IS LOST FOR THE PEOPLE WHO NOW SKIP IT.** Gate code is on My Info in the portal and on the record in the office, so somebody who fits a gate later can still tell us. This only decides who is **interrupted** to be asked. ⚠ **IT RETURNS BEFORE ANYTHING IS DRAWN, AND STILL HANDS ON.** The dialog must never flash up and vanish — and the gate step is what raises the arrears pop-up behind it ([[RS-36]]), so an early return that forgot to call `thenFn` would silently switch that money hold off for everybody without a gate code. That is its own test. ⚠ **AND THE ENTRY BOX IS NOW REACHED ONLY BY "It has changed"**, which makes [[RS-32]]’s contrast fix load-bearing rather than cosmetic: if that second answer cannot be seen, a changed gate code cannot be reported at all. | 2026-09-02 | `showRsvpGateCodeStep`; test/rsvp-gate-code.spec.js (the two "no code on file" tests inverted, plus a hand-on check), button-contrast.spec.js repointed, 3 sabotages red-checked | Standing |
| RS-46 | An All Customers row carried two RSVP stamps. Which one stays, and where is the season changed? | **The yellow season badge stays; the switch lives in the customer.** *"we have two badges or stamps for RSVP we only need the yellow one and I don't know why it shows maybe next year underneath. We can just switch this inside there costumer."* ⚠ THE TWO WERE ANSWERING THE SAME QUESTION TWO WAYS, which is exactly the drift [[MON-47]] closed at the badge: the season badge delegates to `isOutForSeason`, while the Route column printed a second pill straight off `effectiveRsvpStatus` — so a debtor's row read **ON HOLD** above **RSVP: Pending**, and a Maybe Next Year row said it twice. The raw pill is gone; the badge is the row's one answer. ⚠ AND THE IN-ROW TOGGLE WENT WITH IT, at her word — a one-click season change on a list of ~960 rows, beside a badge that no longer explains itself, with no confirmation. Edit Customer's dropdown does the same work and more: it stamps `rsvpRespondedAt` ([[RS-13]] — an answer taken over the phone is still an answer) and calls `removeCustomerFromUpcomingRoutes` on the way out, which the row button never did. ⚠ `setCustomerSeason` IS KEPT, not deleted — it is still in the build-stamp census, and the fix for a badge is never to delete the thing that writes it | 2026-09-03 | the season cell in `renderAllCustomersTable`, the Edit Customer RSVP dropdown; run-all.js asserts the row prints no RSVP status, raw or derived | Standing |
| RS-42 | The office screens said Maybe Next Year, Pending and "No — Skip This Year". What should they say? | **The RSVP's own three answers, everywhere.** *"its back next year the quotes is maybe next year"*, and, reading the dropdown, *"I noticed there isn't a no"* — there was, spelled "No — Skip This Year", and the explanatory tail hid it. ⭐ **THE CUSTOMER PRESSES Yes / Back Next Year / No**, so any other wording on the office side is a second vocabulary for one set of answers and somebody has to translate. **Maybe Next Year belongs to QUOTES** and is untouched there — its approvalStatus, its response buttons and the Quote Maybe Next Year Follow-up template all keep it. ⚠ **PENDING AND UNANSWERED KEEP THEIR EXPLANATORY TAILS**, and that is not an inconsistency: they are not answers and have no button — they say who has been ASKED, which is her own RS ruling of 2026-08-20 (*"add a third for unanswered"*) and what makes a stale Yes impossible to mistake for this year's. ⚠ **AND THE BADGE KEYS DID NOT MOVE** — only the words. `r.badge` is what the season filter matches on, so renaming a key would have silently broken filtering | 2026-09-02 | `seasonBadgeKey` cell, `editCustRsvp`, `season-state.test.js` | Standing |
| RS-43 | Two controls in Edit Customer set one state — a *This Season* radio pair and the RSVP dropdown. Which one is right? | **The dropdown, and the radio is gone.** Addie, looking at the pair: *"I don't understand why this is seperate. It should not have two seperate RSVP spots in costumer"*. ⭐ **THE DUPLICATION WAS NOT UNTIDINESS, IT WAS THE BUG SHE REPORTED THE DAY BEFORE.** With two controls the save has to guess which one the office meant, and it guessed wrong: an UNTICKED radio was read as *"bringing them back in"*, so it blanked `rsvpStatus` — throwing away a Back Next Year she had just picked in the dropdown. The blank then read as **Yes**, because a quote-converted customer with no answer on file is treated as approved (`audienceNeverAsked`). She set an answer and the save silently stored its opposite. ⚠ **NOTHING WENT WITH IT.** The dropdown carries five states against the radio's two, and picking Back Next Year there already did everything the radio did — the flag, the timestamp, off the build queue, off the schedule, off any upcoming route; the radio could not express No, Unanswered or Pending at all. ⚠ **AND THE FIRST FIX WENT WITH IT TOO, ON PURPOSE.** [[RS-42]]'s change compared the dropdown against the STORED value so a fresh choice could be told from a stale one — needed only while a second control could contradict it. Kept, that test would now be **actively wrong**: an ordinary re-save of a Back Next Year customer changes nothing, so it computes false and strips the flag off somebody nobody touched. ⚠ **THE STALE-DROPDOWN RESCUE IS DELETED RATHER THAN LEFT AS DEAD CODE** — it could only fire while two controls could disagree, and restoring it beside a single control would blank the RSVP of everybody the office brings back in. | 2026-09-02 | `seasonMaybeChosen` in the Edit Customer save; the run-all check that proved the radio pair existed, repointed to prove no second control has returned; `season-state.test.js` RUNS the block; 6 sabotages red-checked | Standing |
| SCH-36 | The System inbox fills up with route messages faster than she can read them. What should happen? | **One digest a day.** *"system inbox always has a bunch of schedule messages and it's to many to keep up with. How can we fix this"* — offered four options she chose one note a day. ⚠ NOTHING WAS BROKEN, WHICH IS WHY NOTHING WAS RED. The route sweep runs every fifteen minutes, every notice it wrote was true, and `reconcileNoteIsRepeat` already suppressed the word-for-word identical ones. A day on which the routes genuinely keep changing is up to ninety-six DIFFERENT true notices, and true-and-unreadable is still unreadable — an inbox nobody can keep up with is one where the note that matters is missed. ⭐ SO THE SWEEP BANKS AND THE DAY POSTS: lines go into `settings/routeDigest` against today's date and the note is written on the first sweep of the NEXT day. ⚠ THE COST SHE ACCEPTED, said plainly: a date that moves this afternoon is in tomorrow's digest, not this afternoon's inbox. The toast and the activity log still fire on the spot, and both were repointed at Routes rather than at a note that will not exist for hours. ⚠ IT IS A DOCUMENT, NOT A VARIABLE — the dashboard is closed and reopened all day and runs on more than one machine. ⚠ A REFUSED NOTE CARRIES THE DAY FORWARD rather than deleting the day it was reporting, and the "nobody has been told about any date that moved" line survives whatever else is trimmed. | 2026-08-30 | `routeDigestBank`, `routeDigestFlush`; `route-digest.test.js` | Standing |
| SCH-41 | The Season start box says 1 October and the plan underneath it says Tue Sep 22. Which is right? | **Neither — and that is the fault.** *"this is very wrong because the season start date is oct 1"*, reading both lines of her own season bar at once. They are drawn from the same plan and computed differently: the box is `BASE_START + globalDelta`, a day is `BASE_START + base + globalDelta + cascade`, and **nothing clamped the second one** — so a day carrying a negative base or cascade simply rendered before the season had started, with no way from outside to tell which half to believe. ⭐ **THE FLOOR GOES IN THE LAYOUT, NOT IN THE BUTTON THAT CAUSED IT**, because a plan already saved in that state then heals itself on the next draw rather than needing the data repaired. ⚠ **TWO WAYS A DAY GETS THERE, AND THIS CATCHES BOTH**: *◀ Pull this + rest earlier* decrements `cascade` until the date moves and had no floor of its own, so every press walked the plan further into September; and a rebuild keeps the days it does not re-lay, which go on carrying a `base` measured against whatever `BASE_START` was before it moved. ⚠ **A PIN IS STILL EXEMPT.** *Force exact date* is the office overriding the layout on purpose — the same reason it may place a day on a weekend — so it is honoured before the floor is ever consulted. ⚠ **AND TAKEDOWNS KEEP THEIR OWN SEASON**: they run off `TAKE_BASE_START`, and lending them the install floor would haul every one of them into October. ⭐ This is the rule `seasonFirstDate` already states for the BUILDER (*"with no floor, building the season in August books Christmas installs for August"*); it had simply never been applied to a plan that already existed. | 2026-09-03 | `layoutSequence(days, floor)`, `computeDates`; run-all.js Suite 295, red-checked by removing the floor | Standing |
| SCH-40 | Some days we only have one crew, or one man. Can a day be told that, without changing how every other day is worked out? | **Yes — an exception list, not a new rule.** *"there are some days we will need to have 1 crew or 1 man if a crew doesnt show or if a crew takes time off ect, make an option in the day where you can force there to be one crew in a day but by default it doesnt care and it keeps the math the same with get as many houses in a day as possible."* The day panel carries a **Crews on this date** choice — Normal / One crew only / One man only — and **Normal is what every date has until somebody says otherwise**, so the ordinary maths is untouched on every other day of the season. ⚠ **STORED AGAINST THE DATE, NOT THE DAY.** A crew being away on the 10th is a fact about the 10th, not about whichever houses happen to land on it — and it is the only shape that survives the button: the rebuild throws every day object away, which is exactly why `soloCrew` (SCH-19, stored on the day) does not. ⚠ **THE TAIL SWEEP HAD TO BE TOLD TWICE OVER.** `packTailCrewDays` would have relocated a second crew-day onto a marked date, topped a crew-day up past that date’s cap, and dissolved the one-man day of eight as a *wasted morning* — leaving that man with nothing to do and the office looking at a blank day where they had just put the mark. A marked date is not dribble the builder left behind; it is the size somebody asked for. ⚠ **IT SHAPES THE NEXT REBUILD, IT DOES NOT MOVE HOUSES BY ITSELF**, and it can never reach a day inside the 48-hour lock (SCH-30) — the control says both out loud, because a mark that silently did nothing would read as a broken button. For a crew that has already failed to turn up this morning the tool is still *not done — reschedule*. ⭐ **AND THE BUILDER IS NEVER TOLD WHAT "ONE MAN" MEANS**: `dayShapeOn` hands it `{crews, cap}`, so the number one person can hang stays in `ONE_MAN_MAX_HOUSES` (SCH-24) where the One Man Installs list already reads it. | 2026-09-03 | `dayLimitOn` / `dayShapeOn` / `setDayLimitOn`; `planNewCrewDays` + `packTailCrewDays` `dayShape`; `dayCrewLimitControlHTML`; run-all.js Suite 294 — whose first check is that an UNMARKED season is byte-for-byte the season the builder made before | Standing |
| SCH-42 | The whole schedule is one day and three houses. Is it broken? | **No — that is `confirmed-only`, and three people have answered.** Measured on the live book on 2026-09-03 rather than reasoned about: **956 customers, 950 with a blank `rsvpStatus`, 5 saying yes, and only 3 of those carrying an `rsvpRespondedAt`.** `isOutForSeason` ends in the `SEASON_ELIGIBILITY = 'confirmed-only'` branch, which keeps somebody in only if they said **yes AND the reply is stamped** (or they are a new hang nobody was ever asked — `audienceNeverAsked`). So 952 are out and the builder is correctly allowed to place three. ⭐ **THIS IS [[SCH-38]] WORKING**, her own ruling: *"play with it until the only customers in the schedule are the confirmed ones."* The season fills up as the RSVPs come back; it is not a fault to be fixed in the scheduler. ⚠ **AND IT IS THE NORMAL STATE EVERY SEPTEMBER**, which is worth writing down because the screen looks catastrophic: an empty-looking schedule in early autumn is the rule working, not the plan being lost. ⚠ **TWO WRONG THEORIES WERE MEASURED AND DISCARDED FIRST** — that the arrears hold was emptying it (only 19 customers owe from last season), and that a date bug was to blame ([[SCH-41]] was real but unrelated). Both looked convincing; neither survived counting. | 2026-09-03 | `isOutForSeason` final branch, `SEASON_ELIGIBILITY`, `audienceNeverAsked` | Standing |
| SCH-43 | Recalculate everything threw "Write stream exhausted maximum allowed queued writes". Why, and what fixed it? | **It asked for 951 individual writes in one press.** She pasted the error and said *"also fix this"*, and then, when the first answer stopped at a diagnosis, *"figure it out or ask me a question dont just stop trtying to fix it"* — which is why this row carries counted numbers rather than a theory. `clearStaleInstallBookings` clears the booking stamp off everybody who is out of the season — and by [[SCH-42]] that is 952 people, 951 of whom still carry a stamp from last season. It wrote them one at a time; the SDK queues 500 and then refuses, which is the error she saw, and because the run died partway the next press tried the same nine hundred again. ⭐ **THREE FAULTS, ALL THREE FIXED**: the customer records go in `writeBatch` chunks of 400 (951 records → 3 commits, and this file had no batched write anywhere before); the routes are swept **once for everybody** by `removeCustomersFromUpcomingRoutes` instead of walking every upcoming route 952 times and rewriting a shared route once per stop; and an in-flight guard stops an impatient second press starting a second pass over the same nine hundred. ⚠ **THE CACHE IS ONLY UPDATED AFTER THE COMMIT LANDS** — assigning first makes a failed chunk look cleared, so the row stops contradicting itself on screen while Firestore still says booked. ⚠ **THE BUTTON STILL DOES NOT AWAIT IT**, deliberately ([[SCH-38]]'s note): the press must not sit there looking hung, and the count arrives in its own toast. | 2026-09-03 | `clearStaleInstallBookingsRun`, `removeCustomersFromUpcomingRoutes`, `clearStaleBookingsInFlight`; run-all.js Suite 296, and Suite 286 still proves WHO gets cleared | Standing |
| SCH-44 | The schedule can see the weather. What does it do with it? | **A town at 35° or below is not given that day — unless nothing warmer has anybody waiting, and then it is.** Dax, 2026-09-03: *"we need it to see the weather in the area and prioritize warmer temperatures and a rule is dont schedule for an area 35 degrees or lower unless there is no area that wants to be hung in that time that is warmer than that."* ⭐ THAT SENTENCE HOLDS TWO RULES AND ONLY ONE IS HARD. The cutoff is a **veto** — the town is not offered the date at all — and it is written as two passes rather than one more clause, because the "unless" is not a tiebreak: when every town with houses waiting is freezing the crew goes out anyway, which is what stops the calendar stalling for a week and looking like the builder being broken. Warmth above the cutoff is only a **tiebreak, in ten-degree bands**, and it sits BELOW urgency and below how full a day the town can make — which is the other half of what he said in the same breath: *"dont make a route that is 100 miles longer because you were to worried about priority... a higher goal is to shrink total miles."* Two Wasatch Front towns on one day are a degree or two apart, so an unbanded comparison would overrule "the town with the most houses waiting" — the rule the whole season is built on — on the strength of forecast noise. ⚠ **NO FORECAST IS NOT A COLD FORECAST.** Open-Meteo answers ~16 days and the season runs into December, so most dates have no number and a town with no located house has nowhere to ask about; every one of those is "no opinion" and the plan comes out exactly as it did before any of this existed. Failing the other way would veto the whole back half of the calendar on data nobody has. | 2026-09-03 | `COLD_DAY_MAX_F`, `WARMTH_BAND_F`, the two-pass pick and `betterTown` in `planNewCrewDays`, `loadSeasonForecast`/`forecastHighFor`; run-all.js Suite 300 runs the builder against a cold snap | Standing |
| SCH-45 | A crew did not reach a house on the day it was promised. What happens to it? | **It goes to the front of its own tier — never out of it.** Dax, 2026-09-03: *"anyone that was scheduled for a day but didnt get done should take higher priority for where needs to be routed."* ⭐ IT MOVES THE TOWN AS WELL AS THE HOUSE, which is the half he actually asked for: a town's urgency is the best number in it, so a town holding houses the crew drove past yesterday wins its next crew-day ahead of an equal town that is on schedule. ⚠ **WITHIN THE TIER, AND THAT IS WHY THE TIERS WERE RESPACED.** They used to run 0,1,2,3,4 with nothing between them, so the only way to move somebody up was into the next tier — which would have let a missed October house outrank a new hang and a missed Any house jump the whole October queue. Ten apart leaves room for a bump of five that reaches the front of a tier and no further. Same instruction, from the same message: *"dont do someone in a month they dont want to be hung though."* ⚠ **RECORDED AS DATES, NOT AS A COUNTER.** Recalculate everything gets pressed twice in a row and Undo puts the plan back so it can be pressed again; a counter would climb every time and quietly turn one missed morning into a customer who outranks the book. A list of the dates it happened on is idempotent, and it lets somebody missed three times go before somebody missed once. | 2026-09-03 | `markHouseMissed`/`houseMissedDays`, the bump in `houseInstallPriority`, the `missed` term in `planNewCrewDays`'s queue sort; written by `rebuildSeasonDays` and by `applyLeftoverPicks`; Suite 300 | Standing |
| SCH-46 | A customer rings up and asks to be hung sooner. How far up does that move them, and does it outrank the automatic rules? | **To the very front — ahead of new hangs — and it never moves their month.** Dax, 2026-09-03: *"we should be able to manually add priority to specific customers if they directly ask if they can be hung sooner than later"*, and, on where it should sit: *"as far as where they should be prioritized ill let you decide based on what makes sense logically and what makes sense in the system."* ⭐ SO THE PLACEMENT IS A DELEGATED RULING RATHER THAN HIS OWN, and the reasoning is that **an override that cannot override the automatic rule is not an override**. *"The very top priority is new hangs"* ([[SCH-15]], 2026-08-17) is a rule the system applies to everybody by itself; this is a person in the office deciding, after a phone call, that one customer comes first. It is a checkbox somebody has to tick, so the volume is a handful and it cannot bend a season out of shape. If that reading is wrong, this is the row to change. ⚠ **IT DECIDES THE ORDER, NEVER THE MONTH.** Same message: *"dont do someone in a month they dont want to be hung though."* `houseAllowedFrom` and Don't Install Before This Date are untouched by it, so a November customer who asks to go sooner is taken first on the first November day and not one day earlier — and the box says so on screen, because somebody ticking it expecting October would otherwise be let down by a rule they could not see. | 2026-09-03 | `rushInstall` on the customer; read by `houseInstallPriority` and by `installPriority` so the sweep agrees; the box in Edit Customer; Suite 300 and S50 | Standing |
| SCH-39 | A row says "Scheduled — Hang Tue, Oct 20" and "Not scheduled — no RSVP yet" at the same time. Which is right? | **Neither on its own — the stored booking outlived the season.** *"shes not in the schedule but on here it says shes scheduled make sure that this updates when you recalculate everything too"* (Addie Eriksen #210). `scheduled` / `scheduledDate` / `assignedCrew` are STAMPED when somebody is put on a crew's sheet and cleared when they are taken off it — but a customer who simply stops being in the season is never taken off anything, so the stamp outlives the booking and the row goes on promising a crew that is not coming. ⭐ Recalculate everything now clears them, and takes the customer off any upcoming route in the same pass — clearing the flag while the stop survives is the same contradiction the other way round. ⚠ INSTALL FLAGS ONLY. A takedown or a fix is work on lights that are already up, and somebody sitting the season out still needs theirs taking down; cancelling those would leave their lights up all winter. ⚠ AND ONLY WHERE THERE IS SOMETHING TO CLEAR — writing false over false on ~950 records says nothing and stamps `updatedAt` on every one of them. ⚠ NOT AWAITED: it is a round trip per stale record, so the count arrives in its own toast rather than leaving the button looking hung. | 2026-09-01 | `clearStaleInstallBookings`, the `recalcBtn` handler; run-all.js suite 286 runs it, 6 sabotages red-checked | Standing |
| SCH-38 | One customer is badged Confirmed and the schedule holds sixteen houses. Why won't Recalculate clear them? | **They are not customers.** *"play with it until the only customers in the schedule are the confirmed ones and also that all confirmed customers are in the schedule because currently its not working."* ⚠ MEASURED ON HER OWN PLAN, not reasoned about: Marcus Nicolls #968, Tamara Jensen #464 and Jamie Harmon #360 are rows left over from an imported schedule file and match nothing in a book of 956. `customerForHouse` answered null, the *no record, no opinion* guard kept them, and no Recalculate could ever shift them — so the season rule was working perfectly and the sixteen never reached it. ⛔ THAT GUARD'S REASONING IS KEPT, because it is what this has to be careful about: *"Imported CSV rows need not match a record at all, and treating 'no customer found' as 'not coming' would empty most of the plan on the first press."* True when the plan WAS the season; the wrong way round now that every house arrives through `customersMissingFromSeason`. ⭐ So a movable row that matches no customer now comes off — **gated on the customer book having loaded**, which is the half of the old reasoning that still holds absolutely: `jobAddresses` is empty for a moment after login and empty again if the listener fails, and an ungated version would wipe the season on a slow morning. ⚠ Counted and reported SEPARATELY from the ones who left the season: "somebody said back next year" and "this row is not a person" need different actions from the office. | 2026-09-01 | `rebuildSeasonDays`; run-all.js S48 runs both book states, 4 sabotages red-checked | Standing |
| SCH-37 | Why is the schedule changing so many times a day? | **A street typed into somebody's town box, and the fix is to stop the builder acting on one.** *"I don't even know why there are so many changes being made in schedule and it is concerning"*, then *"Okay go ahead and fix that"*. ⚠ THE COUNT WAS REAL AND THE CAUSE WAS ONE RECORD. `extractCleanCity` strips zips and `UT`/`Utah` and drops any part carrying a digit, so `S Summit Crest Ln` survived cleaning and read as a town; `planNewCrewDays` gave that invented town a crew-day of its own, the day could not fill itself so it borrowed twelve real Herriman houses, and `stopProblem` — which judges a stop against the day's allowed towns — evicted those twelve on the very next pass. Placed, evicted, placed, every fifteen minutes, for as long as the record stood. ⭐ `townIsPhantom` IS THE ONE RULE and three places ask it: the builder skips such a town so no day is ever seeded from one, the sweep collects those houses into `stranded.badTown` and the notice names them with the value quoted, and Health Check's row reports the same houses. ⚠ IT REPORTS, IT NEVER CORRECTS — about sixty places read what `extractCleanCity` returns, so silently returning something else would re-file houses between towns everywhere at once; the notice line IS the fix, because the town is corrected on the record and the customer sync carries it across. ⚠ AND THE OFFICE'S OWN LIST IS AN EXEMPTION: a town in `NEARBY_TOWN_LIST` is a town however oddly it is spelled, because she typed it and paired it up herself. ⚠ THE SUFFIX LIST DELIBERATELY OMITS Grove, Springs, Hills, Heights/Hts, Mountain, Fork, View, Cross and City — each is a real street type AND the tail of a real Utah town, and with them in it flags nine real towns, six of them ones she listed. | 2026-08-31 | `townIsPhantom`, `planNewCrewDays`, `stranded.badTown`; Suite 22c and Suite 18 in `run-all.js` | Standing |
| PROC-30 | How should "Where things go" show the journey rather than one level? | **Chose: "The path becomes the page"**, over adding it beside the grid or turning the grid sideways. Her shape for it: "I was thinking we push on quotes than approve and it will show the different routes in can go from there. So we can figure out the different navigations by clicking on how things can go." ⚠ SO IT IS A GRAPH YOU WALK, not a list you read — every step offers what can happen next, and the route you have walked stays behind you, which is what makes two routes out of one step comparable. ⚠ THE GRID IS NOT REPLACED, IT IS PUT UNDERNEATH: each step names the fields that record it and clicking one lands on that field's row, the same one level in it always was. ⚠ AND A STEP THAT IS NOT BUILT SAYS SO — the two payment chases are her spec and neither runs today; drawn as working, the page would be a wish rather than a map. | 2026-08-29 | `connections/journey.js`, `drawPath` in `connections/behaviour.js`, `journey.test.js` | Standing |
| PROC-31 | Is a quote the only way a customer arrives? | **No, and drawing it that way was the first thing she caught**: "Just to make sure you are currently adding all routes in the Were things go. Right?" Somebody typed into Add a Customer, or arriving through the master sheet, has no quote, no email and no approval — they join at converted and skip the first six steps. ⚠ THE GATE WAS ENFORCING THE GAP: it demanded exactly one start, so a check written to protect the page was keeping it wrong. It asks for at least one now and walks from all of them. ⚠ AND THE PAGE OPENS BY ASKING HOW THEY ARRIVED rather than at one door, because opening on one quietly claims everybody came through it. | 2026-08-29 | `STEPS` starts in `connections/journey.js`, `drawPath` chooser, `journey.test.js` | Standing |
| PROC-32 | How much is the late fee on the second chase? | **Already in the repo — "That should be in repo"**, and it was: $25 if they have paid something, $40 if they have paid nothing, sitting in admin.html as the Phase 5 rule marked "preview only, not built". ⚠ THE ANSWER TO A QUESTION LIKE THIS IS A SEARCH, NOT A QUESTION — asking her to re-decide something already written down is exactly the re-answering the questions map exists to stop. ⚠ NEITHER CHASE IS BUILT: two things run on a schedule, the 7pm invoice and the quote nudge, and the nudge chases an unanswered QUOTE rather than an unpaid bill. | 2026-08-29 | `fcLateFee` in admin.html; `chase1` / `chase2` in `connections/journey.js` | Decided — not built |
| PROC-33 | Are the rules and connections accurate and complete, and where are the holes? | **Checked mechanically for the first time**: "all connections and rules should be worked on to make sure there all accurate, and there all there along with marking were the errors or holes are at within the rules and connections based on patters within the maps." ⭐ THE FIRST PATTERN TURNED INTO A GATE: every ruling names the code that proves it, and nothing had ever checked those names were real. It found two on the first run — MR-20 pointing at a constant that has never existed in this repo, and QT-13 still reading Standing while naming a Firestore document removed on 2026-08-27 with the feature it belonged to. ⚠ A RENAME IS THE COMMON CASE AND IT IS SILENT: functions get renamed, the map is prose and never moves with them, so it drifts one rename at a time and the day somebody needs a ruling is the day they find out. ⚠ STANDING ROWS FAIL, SUPERSEDED ONES ONLY NOTE — naming gone code is what superseded MEANS. ⚠ AND THE FIRST VERSION CRIED WOLF ON ALL THREE OF ITS FINDINGS (an element id, a Firestore path, a value on a record), which on a gate whose whole job is to be believed is worse than finding nothing. | 2026-08-29 | the anchor census in `questions-map.test.js` | Standing |
| FIX-05 | Two of the four ways to raise a fault recorded no date — and one was holding bills. Fix? | **Yes, and it was found by the pattern rather than by anybody noticing.** Her instruction was to mark the holes "based on patters within the maps", and the map's commonest recorded shape is one rule with several writers where one is out of step. ⚠ THE CUSTOMER-ROW DROPDOWN WROTE `needsFix` BARE five lines under a comment saying all three of its fields come from the shared rule — `completed` and `removalDone` did, the third did not — and the Routes tab toggle did the same. So raising a fault there recorded no `fixRaisedAt` and mending one recorded no `fixDoneAt`. ⚠ AND IT IS MONEY, NOT TIDINESS: a fault on a completed house stops that payer's WHOLE group being invoiced, and the hold is recomputed from the flag nightly — so a bill held six weeks looked identical to one held this morning and nothing could sort the queue. ⚠ THE OLD CHECK COULD NOT SEE IT because it asserted the shared rule is CALLED somewhere, which was true while two of four callers went round it — presence is not coverage. | 2026-08-29 | `HLX_DONE_KINDS`, `attachAddressRowHandlers`, `renderRouteOrderedList`; the fix census in `queue-date.test.js` | Standing |
| RS-23 | A customer asks to cancel from their portal — is that dated, and is it on the path? | **Neither, until now.** Found by working her instruction to mark the holes "based on patters within the maps": `seasonStatus` carries four answers — a cancellation asked for, an address changed, changes needed, changes settled — and a search for a date on it across every source returned NOTHING. ⚠ THE CANCELLATION IS THE ONE THAT COSTS: they sit there with their old set queued to come back and off every upcoming route, still a customer until the office acts, and the queue could not be sorted by how long anybody had waited — October read exactly like this morning. ⚠ THE PREVIOUS VALUE TRAVELS WITH THE DATE, because "changed on the 4th" cannot say whether they were cancelling or correcting an address, and those need opposite actions. ⚠ AND IT WAS THE BIGGEST HOLE IN THE PATH TOO — a door out of the season distinct from declining a quote and from an RSVP no, with no step drawn for it at all. | 2026-08-29 | `stampSeasonStatusServer`, `historySeasonWords`, the `cancelrequest` step in `connections/journey.js` | Standing |
| RS-24 | Should somebody who did not pay last year be scheduled if they RSVP yes? | **No. "If they didn't pay last year they should not be scheduled to be hung."** ⭐ A YES IS NOT A PAYMENT, so the hold sits OUTSIDE the RSVP branch of `isOutForSeason` and applies to a customer who answered. ⚠ IT ALSO HOLDS THE BUILD, which is the point rather than a side effect: a bundle made for a house nobody is going to is glass and wire spent on a debt. ⚠ IT IS NOT `houseIsOnTheBill` AND MUST NEVER BE FOLDED INTO IT — that rule decides who is CHARGED, and the answer for these customers is emphatically yes; this one decides only who gets a crew. Merging them would write the debt off again by the back door. ⚠ IT READS THE BILL THE HOUSE IS ON, not the house's own key: if Dana pays for Kyle and Dana did not pay, Kyle's lights were not paid for either. ⚠ AND IT FAILS TOWARDS KEEPING PEOPLE IN — no invoice, no key or an unloaded cache all answer "they do not owe", because holding somebody who paid costs them their lights while carrying somebody who did not costs one bundle | 2026-08-31 | `houseOwesFromLastSeason`, `isOutForSeason`, `arrears-hold.test.js` | Standing |
| RS-25 | Is there a way to hang somebody anyway while they still owe? | **No. "If they didn't pay last year they should not be scheduled to be hung."** Offered a per-customer release button, she declined it. ⚠ SO THE HOLD LIFTS ONLY WHEN THE DEBT IS NOT OUTSTANDING, and there are two ways for that to be true: they pay it, or the office credits it off. ⭐ THE CREDIT IS NOT A LOOPHOLE SNEAKED BACK IN — it is the same mechanism MON-21/Q-013 already names for an "our fault" write-off, it is money on the invoice rather than a hidden flag, the customer sees it, and the books count it. What there is NO way to do is schedule somebody while the money is still recorded as owed. ⚠ THE COST, ACCEPTED KNOWINGLY: a payment we failed to record holds a customer who really did pay, and the only way out is to correct the invoice — which is the correct fix anyway. ⚠ THEY ARE NEVER HELD INVISIBLY — **Schedule › Owes from last year** names every one with the amount and a phone number | 2026-08-31 | `arrearsSettled`, `owesFromLastYearHouses`, `renderOwesLastYear` | Standing |
| RS-26 | How does the office tell a paid-up customer from a held one, and see why somebody is not going out? | **A note on the row and in Edit Customer, added automatically.** *"how do we measure if someone paid for last year so than they can be marked approved for the year and be scheduled?"* — and the screen could not answer her: `allCustRouteStatus` says **"Unscheduled"** for a customer who owes money, one who has never replied, and one simply not yet placed on a day. Three different phone calls, one word. ⭐ `seasonHold` is **gated on `isOutForSeason`** — the same rule the routes, the build queue and the schedule ask — so it can never name somebody a crew is on its way to, nor stay silent about somebody left out; everything below that gate only chooses WORDS for a decision already made. ⚠ THE ORDER MIRRORS `isOutForSeason`'s OWN, so somebody who said no AND owes reads "they said no" — that is what is actually keeping them out, and chasing them for money about lights they refused is the wrong call. ⚠ ONLY THE MONEY REASON IS DRAWN LOUD: the RSVP pill on the line above already says no/back next year/pending, so putting those in the warning colour on ~960 rows would bury the one line somebody has to act on. ⚠ DERIVED EVERY DRAW, never stored — a payment recorded at four o'clock takes the line off at five past. ⚠ APPENDED, never merged into `routeStatus`, which the Route filter matches exactly. ⚠ AND CLEARED when a customer is not held, because the house tabs repoint the form without closing it and a stale note would be a confident lie | 2026-08-31 | `seasonHold`, `seasonHoldReason`, `arrears-hold.test.js` | Standing |
| SCH-47 | Can the office add crews and name them? | **Yes — as many as she has.** Addie, 2026-09-04: *"on schedule can you make it so we can add on crews and name them?"* ⚠ NAMING ALREADY WORKED; ADDING DID NOT, and the ceiling sat in two places at once — `normalizeCrews` mapped over a two-entry `CREW_FALLBACK_NAMES`, and `loadSchedulingSettings` clamped the saved count to `(n === 1) ? 1 : 2`. Either alone makes a third crew impossible, and the second is the quieter: it reads a third crew back as two on the NEXT LOGIN, so the season silently rebuilds for two and a named crew simply gets no day. ⭐ THE COUNT IS `settings/scheduling.crewsPerDay` AND NOTHING ELSE — that number was already what the builder, the tail sweep and `surplusCrewDays` read, so making the crew LIST a second answer would put two numbers in two collections in charge of one fact. The names ride with the plan as they always have; the length is padded from the setting. ⚠ AND REMOVING A CREW THAT IS HOLDING HOUSES IS REFUSED: nothing would be lost, but the houses would land on somebody else's sheet with nothing said, and a sheet that changes under a crew already holding paper is what the 48-hour lock exists to prevent. | 2026-09-04 | `normalizeCrews`, `crewCount`, `crewIndexes`, `setCrewCount`, `crewSlotOf`; run-all.js suite 304 | Standing |
| SCH-22 | Does Schedule run recycle or number logic? | No. Marking a takedown done means the lights are physically off, nothing more | 2026-08-09 | routes-dashboard-plan.md | Standing |

## Printing

| # | Question | Her answer | Decided | Where it's proved | Status |
|---|---|---|---|---|---|
| PR-01 | What does Print Today print? | Three papers: the warehouse list for **two days from now**, plus both crew lists for the next working day, each on its own page. "lets make a tab for printing, a main button for print today, which would be the warehouse list (for two days from now) and both crew lists for tomrrow morning (should be on a seperate page) so it prints 3 papers" | 2026-08-20 | `printToday` | Standing |
| PR-02 | Does the Printing tab replace the per-day print buttons? | No — "keep that their." The tab is the morning routine, not a replacement | 2026-08-20 | `printDaySheet`, `printCrewSheet` | Standing |
| PR-03 | Is the crew portal in use this season? | No — "were not using the employee portal this year... we are only printing on schedules and warehouse." `employee.html` stays in the repo, dormant | 2026-08-21 | `printCrewColumns`, `fix-sheet.test.js` | Standing |
| PR-04 | Does the crew get told what's wrong on a fix? | Yes — a **What's wrong** column, and the fault photo in its own block under its own heading, never merged with the new-hangs photos | 2026-08-25 | `printCrewColumns` | Standing |
| PR-05 | A flagged house with no note — blank or something? | "?" — never blank. Blank reads as nothing-wrong-here on a row that is on the sheet *because* something is | 2026-08-25 | `printYesNo` | Standing |
| PR-06 | Do new house photos print on route schedules? | Yes | (chat) | built for crew sheets; missing from Print Whole Plan | Standing |
| PR-07 | Two flags are sent to the customer's own page "so the portal cannot contradict the office", and the page never reads either. Should it show something, and what? | **Yes — build the protection.** *"Okay make a protection"*. ⚠ THE PROTECTION WAS ASSERTED AND NEVER BUILT. `askSameAsLastYear` and `cannotBillNoEmail` each went into `PORTAL_READ_FIELDS` on 2026-08-21 with a comment naming what it prevented, and neither string appeared anywhere in `index.html` — so both flags arrived on every sign-in and nothing looked at them, for five months. The P-001 shape: a rule stated as fact about a guard that does not exist. **NO EMAIL ON THE BILL** now shows a notice on the invoice card. ⚠ IT ASKS FOR THE ADDRESS RATHER THAN ANNOUNCING A PROBLEM — "we cannot bill you" is alarming to somebody who has done nothing wrong and gives them nothing to act on; it says nothing is wrong with their account and names the tab and the phone number. ⚠ AND IT IS DERIVED, NOT JUST STORED: the flag is only cleared by the next nightly pass, so an email typed at nine in the morning would otherwise be nagged about until seven at night. ⚠ THE FLAG IS ONLY EVER SET ON THE PAYER, so this can never tell a tenant to fix their landlord's record. **A DECLINED RE-QUOTE STILL OPEN** now replaces the strip's *"We'll be in touch with your install date"* — a promise about a season they are not booked for — with a line saying we are working out whether last year's setup will do and that nothing is needed from them. ⚠ BELOW THE SCHEDULED-DATE BRANCH: a house with a date was decided in practice, and saying otherwise is the same contradiction pointing the other way. ⚠ The wording is ours, not hers — change it freely. | 2026-08-30 | `renderNoEmailNotice`, `renderScheduleStrip`; `portal-fields.test.js` | Standing |
| PR-08 | Where does a returning customer's path start, and are the old-customer routes drawn? | **At the RSVP, and they were not.** *"for old costumers are starting point is just at RSVP can we work on those paths to"*. ⚠ `rsvpasked` WAS A THROUGH-STEP, reachable only by walking the whole first-season path from a quote — so the ~960 people already on the books had no starting point of their own and their season could only be read as a footnote to somebody else's first one. It is a start now, beside the three first-season doors. ⚠ AND IT HAD NO DATE ON IT AT ALL, which is its own finding: the send stamps `rsvpSentAt`, and that stamp is what `seasonRuleIsLive` reads to decide whether anybody may be dropped for not answering. ⭐ THE RETURNING PATH WAS ALSO THIN — a yes went straight to the warehouse. It now draws what a yes really does (cancels a queued recycle, re-queues a build only where that recycle happened, clears the badge in both fields, stamps both the planner's instruction and the office's badge), the price-only re-quote where the warehouse does nothing, and the SECOND door out of the season: the office badging somebody Back Next Year, which is a different field from the customer answering the link. | 2026-08-30 | `connections/journey.js`; `connections.test.js` | Standing |
| PR-09 | How should the path be drawn? | **Like a pedigree.** *"make the path look more like a pedigree"*, and *"make a pedigree branch for each of the following tabs — Quote, Costumers, Routes, Schedule, Warehouse, Invoices"*. ⭐ TWO GENERATIONS ARE DRAWN: every way out of here, and under each of those, what THAT leads to — so a branch that fans into four looks different from one that runs straight on, before anything is clicked. A stacked list of buttons answers "what can I press" and hides the shape, which is the whole thing a pedigree is for. ⚠ TWO GENERATIONS AND NO MORE IS NOT A STYLE CHOICE: the graph has real cycles (a re-quote returns to the quote email, back-next-year to the RSVP) so an unbounded draw never terminates. ⚠ A GRANDCHILD IS A LABEL, NOT A BUTTON — clickable, it would let somebody skip the middle step and leave a trail claiming a route they did not take. ⭐ THE SIX TAB PEDIGREES ARE VIEWS OF ONE GRAPH, never six graphs, and their hand-offs form a CHAIN — each one's hand-off is the next one's root. ⚠ The chain order is the order work really happens in (a bundle is built before a day is planned), not the order the tabs were listed. | 2026-08-30 | `TAB_ROOTS`, `connections/behaviour.js`; `connections.test.js` | Standing |
| PR-10 | Does the page show all the automation? | **No, and it could not have.** *"I think where things go does not have a complete representation of the automation. There is still things missing there."* ⚠ THE GRID IS FIELD × SCREEN AND AUTOMATION IS NOT A SCREEN. The only automatic run with a column was the 7pm billing; Automation Emails was folded into the Portal column, which is simply wrong — an automation email lands in a customer's inbox. `connections/automation.js` lists all seven with cadence, what each writes, and what it would COST IF IT STOPPED, which is the row that earns the list: what a timer does is guessable from its name, what its silence costs is not. ⚠ FIVE OF THE SEVEN ARE NOT WATCHED by the grid and the page says so — a page claiming to cover the automation while quietly leaving some out is the fault it exists to catch. ⭐ GATED FROM THE CODE BACK TO THE LIST, and an ANONYMOUS long-lived timer is now refused outright: the sweep finds a timer by the variable it is assigned to, and the ten-minute re-read of the nightly billing log had none — the one alarm on the most expensive automatic run in the app, invisible to the list that exists to say what runs by itself. | 2026-08-30 | `connections/automation.js`; `connections.test.js` | Standing |

## The master sheet and the importers

| # | Question | Her answer | Decided | Where it's proved | Status |
|---|---|---|---|---|---|
| SH-01 | Which workbook is the master? | `2026 Client List.xlsx` — "use the new one." The old one's phone column is detached from its rows and has been renamed so it can't be picked | 2026-08-19 | CLAUDE.md | Standing |
| SH-02 | Is Bulk Updates still the tool to reach for? | No — retired in practice. "we should no longer use bulk so this is no longer a concern." The path is Use my master sheet → Compare → Sync | 2026-08-21 | `rbApplyTickedAdds` + ledger | Standing |
| SH-03 | How many rows per batch? | A dial she turns, not a settled number — 250 → 150 → 50 in one afternoon. It remembers its place across a reload | 2026-08-17 | `BULK_CHUNK_SIZE` | Standing |
| SH-04 | Does the sheet flip names by itself? | Yes — "excel is last first but the website is first last." A fresh paste ticks the box itself and shows three names as they will be saved | 2026-08-17 | `rbFlipNames` | Standing |
| SH-05 | One sync button, or one per row? | One. Anything she doesn't want synced she fixes by hand first | 2026-08-19 | `rbRenderLedger` | Standing |
| SH-06 | What sorts to the top of the comparison? | Things needing a manual decision that would fail to sync, then exists-on-one-side-only, then address, then price | 2026-08-19 | `RB_FIELD_RANK` | Standing |
| SH-07 | Who may the comparison offer as a customer? | Not a row with no name, and not a row that is sheet-only bookkeeping. The count stays so nothing silently vanishes | 2026-08-19 | `rbCollectMissingCustomers` | Standing |
| SH-08 | Are new quotes written back into the sheet? | Yes — into the first open row of **that exact file**, never a re-downloaded copy | 2026-08-19 | `hlxAppendRowsToSheet` | Standing |
| SH-09 | What is the `$$` column? | The standard price with no fees. "that will be website only" — Set Up Fee is left blank by us | 2026-08-19 | `rbMiscParse` | Standing |
| SH-10 | What is Up Plug? | The eaves question. "?" is not a no — blank leaves the record alone | 2026-08-19 | `useEaves` mapping | Standing |
| SH-11 | Where does outlet information live? | Sometimes in Notes — and when it does it goes to the outlet preference in All Customers. "sometimes they give plug or outlet information in notes and if they do that should go to the outlet preference we already have set up in all customers." | 2026-08-19 | `rbOutletFromNote` | Standing |
| SH-12 | Where do gate codes appear, and in what format? | Misc **and** Notes, in many formats — including the bare `#0754` the office actually types. "make sure you format it in many formats" | 2026-08-19/20 | `rbGateCodeFromText` | Standing |
| SH-13 | "paid" written anywhere — note or tag? | The **tag** changes, and the season in the cell is deliberately ignored | 2026-08-20 | `rbRowSaysPrepaid` | Standing |
| SH-14 | Is the customer list one sheet? | Four. Confirmed, Color Change, Contact 2027 and Recycle are their own **tabs**, not columns — and a customer in two of them is not a second customer | 2026-08-20 | `hlxWorkbookRowsAllSheets`, `HLX_STATE_TABS` | Standing |
| SH-15 | Should the master-sheet choice follow her between computers? | The **choice** does — it is information, written to `settings/masterSheet`. The browser's **permission** cannot, and needs one press per machine | 2026-08-19 | `settings/masterSheet` | Standing |
| SH-16 | A payer given as an email instead of a name? | Kept as `billToEmail` and preferred — an address is exact where a name is a guess | 2026-08-19 | `rbResolveBillTo` | Standing |

## Duplicates and test records

| # | Question | Her answer | Decided | Where it's proved | Status |
|---|---|---|---|---|---|
| DUP-01 | Merge duplicates, or delete them? | "if you can make it so they rather merge more than just delete" — by name and CU# | 2026-08-18 | `findMergeableCustomers` | Standing |
| DUP-02 | When are two records the same person? | "two different names are never duplicates unless one is surname firstname and the other is firstname surname." Griner Lauren and Lauren Griner are one; Chafffetz and Chaffetz are not | 2026-08-18 | `dupNormName` sorts the words | Standing |
| DUP-03 | Group duplicates by phone, email or street as well as name? | **Rejected — do not rebuild.** Measured on the real book: 17 phone numbers are held by two people and 14 of those are genuine households | 2026-08-19 | CLAUDE.md | Closed |
| DUP-04 | Should sync tick spare copies for deletion by default? | Yes — she asked twice. Safe because of DUP-02, and because merge happens before delete | 2026-08-19 | `mergeFieldsFrom` | Standing |
| DUP-05 | What do you call a website record the sheet doesn't have? | "instead of all those words where it says not on the sheet just call it duplicate" | 2026-08-19 | `dupNormName` | Standing |
| DUP-06 | Should the test customer use a fake address? | No — a real one, 209 S 850 W, Lehi. A fake address can't be geocoded, so a test record could never be routed or mapped, which is most of what it's for | 2026-08-21 | `isTestRecordData` | Standing |
| DUP-07 | Can test records be removed in one press? | Yes — customers, quotes, invoices, routes, schedule, warehouse and the recycle list, with the number put back. "a button in bulk updates for deleting all test customers from customers and quotes and invoices and routes and schedule and put their number back in the system" | 2026-08-21 | `testSweepFind` / `testSweepDelete` | Standing |

## The inbox

| # | Question | Her answer | Decided | Where it's proved | Status |
|---|---|---|---|---|---|
| MSG-01 | What does the number beside a folder count? | Unread only. "I only want the number to show how many are unread" | 2026-08-25 | `folderUnread` | Standing |
| MSG-02 | Should the inbox look like Gmail? | Yes — "I want this to be very similar to gmail layout." Closed folder rolls its children up, open one does not; a zero draws as nothing | 2026-08-25 | `renderFolderSidebar`, `folderUnread`, Suite 273 | Standing |
| MSG-03 | Does Mark Responded also mark it read? | Yes. One direction only — Mark Awaiting does **not** put it back to unread | 2026-08-25 | `msgBulkApply` | Standing |
| MSG-04 | Replace drag-and-drop with tick-and-move? | No — add it. Drag and right-click are kept, because somebody already used to dragging must not lose it | 2026-08-25 | `msgMoveTo` | Standing |
| MSG-05 | Can the Billing, Quotes and Text Messages folders be renamed or deleted? | No — other parts of the app look them up by name | (chat) | not in repo yet | Standing |
| MSG-06 | The daily route digest still buries the notices that need acting on. Hide it? | **Fold it away, never delete it.** *"these almost spam messages flood the system messages making it so we cant see messages we actually need to do something with, make it so we dont see these spam ones but we still see important messages that we need to see"* (Dax). ⚠ NOT A REPEAT OF [[MSG-05]]'s neighbour, the 2026-08-30 digest ruling — that one answered VOLUME (ninety-six notices a day became one) and worked. This is about the FOLDER that one note lands in: `renderSystemMessagesTab` drew every System notice in one flat list, so the digest sat beside *A Background Check Has Stopped* and *Customer Number Needs Fixing* and, being the longest thing there, is what the eye lands on. ⭐ THE SIGNAL ALREADY EXISTED: `routeDigestFlush` partitions its own lines on a leading ⚠ (`urgent`), so a digest that could not write a record, or that caught a line looping all day, carries one — `noticeIsRoutine` reads that same mark and those still come through at full size. ⚠ NOTHING IS DELETED OR MARKED READ: these record days that moved under customers who may already have been told a date, which is the closing line of every digest. Folding is a view, not a write. ⚠ AND THE NAV BADGE WAS COUNTING MESSAGES ITS OWN LIST HAS NEVER SHOWN — `renderMessagesList` has always filtered System out of the customer list while the badge beside it counted them, which is why it read 91 over a much shorter list. | 2026-09-01 | `noticeIsRoutine`, `renderSystemMessagesTab`, `systemNoticeRow`; run-all.js suite 287 RUNS the classifier, 4 sabotages red-checked | Standing |
| MSG-07 | Cancellations, schedule notices and everything the system raises arrive in one pile. Where should they go? | **Cancellations and the member portal get their own folders; the System tab gets sections, one of them Schedule.** *"we need a place for cancelation messages to go. Can we have it go to inbox but these will have it's own no section and schedule also has it's own folder. Can we make folders in system messages. instead of just having it all in one spot. Also can we have inbox for member portal."* ⭐ **DERIVED FROM THE TOPIC, NEVER STORED, AND THAT IS WHAT MAKES IT WORK ON DAY ONE.** Every cancellation already in the book was written `folder:'Inbox'`, so routing only NEW messages would have left her existing ones in the undivided pile — the feature would have looked broken on the only data she has. Nothing is migrated and nothing is rewritten; the same rows simply sort. Same argument as One Man Installs. ⚠ **BUT THE OFFICE'S OWN FILING ALWAYS WINS.** `filedByHand` is stamped by every route that MOVES a message — the drag, the right-click, Move to…, and a folder being deleted out from under it — so a message dragged anywhere stays there. Without it a dragged message springs back on the next render, which reads as the drag not working. ⚠ **A RENAME IS DELIBERATELY NOT A FILING** for a message STORED in the folder: it follows the folder rather than overruling the topic, so renaming does not pin everything inside it for ever. ⭐ **BUT RENAMING OR DELETING ONE OF THESE FOLDERS WOULD HAVE MADE MESSAGES DISAPPEAR, and that hole was found by asking what happens when the office does it.** A message sitting in a folder only BY DERIVATION still reads `folder:'Inbox'`, so both handlers' stored-field match missed it — and once the folder was renamed or gone the topic map went on naming the old one, which now had no row in the sidebar. That message was then in no list at all and counted nowhere. Both handlers ask `messageFolderOf` now: delete files the derived ones into Inbox, and rename PINS them so they follow it — the one place a rename IS a filing, because being pinned is exactly what stops the topic dragging them back to a name that no longer exists. ⚠ **AND `folder:'System'` IS UNTOUCHED BY ALL OF IT.** That field is not a folder in this sidebar — it is what makes a message a system notice at all, and the customer list excludes it unconditionally. Deriving it would let a topic quietly move a notice between the two tabs. ⚠ **AN UNMAPPED TOPIC KEEPS THE FOLDER IT WAS WRITTEN TO, AND AN UNMAPPED NOTICE FALLS IN "Everything else"** — the fail-safe direction in both places. A topic added later must still appear somewhere visible; a notice that exists, counts towards the badge and is drawn in no section is worse than one in the wrong section. ⚠ **THE ROUTINE ROUTE DIGEST STAYS FOLDED BELOW THE SECTIONS** ([[MSG-06]]) rather than filling the Schedule one — those are the notices that need nobody, and merging them would undo the fix that stopped them burying the ones that do. | 2026-09-02 | `MESSAGE_HOME_FOLDER` / `messageFolderOf`, `SYSTEM_NOTICE_SECTIONS` / `systemNoticeSection`, `filedByHand`; run-all.js Suite 292 RUNS both rules, 15 sabotages red-checked | Standing |
| MSG-08 | Should a folder that messages are filed into automatically be deletable? | **No, and the refusal names the topics.** Not a ruling of Addie's — a hole found reviewing MSG-07 the same night, recorded here because it changes what the office can do. ⚠ **DELETING ONE WOULD HAVE HIDDEN THE NEXT MESSAGE.** The delete handler files everything currently in the folder into Inbox, but `MESSAGE_HOME_FOLDER` goes on naming it — so the next cancellation would resolve to a folder with no row in the sidebar and be in no list at all, counted nowhere. ⚠ **AND IT WOULD HAVE LOOKED LIKE A BUG EITHER WAY**: `loadMessageFolders` re-seeds any missing default on the next login, so the folder reappears by itself and the office is left deleting something that keeps coming back, with a window in between where new messages are invisible. Refusing is the honest answer. ⚠ **IT REFUSES BEFORE THE CONFIRM, NOT AFTER** — asking and then refusing teaches the office the button is broken rather than that the folder is protected. ⚠ **AND IT NAMES THE TOPICS**, because a bare "you cannot delete this" is the kind of refusal people work around by making a duplicate folder. ⚠ **RENAMING IS STILL ALLOWED**, and the derived residents follow it — see MSG-07. | 2026-09-03 | the `data-delfolder` handler's `homedTopics` guard; run-all.js Suite 292, 2 sabotages red-checked | Standing |
| RS-45 | The Test account reads as a new customer, so the RSVP cannot be tried on it. What should the two test buttons make? | **Quotes makes the new one, Customers makes the old one.** *"need to be able to make test person for new person in quotes and costumer for old person"*, after *"I need to see if the RSVP works using this Test account or I'm going into this blind and don't know if my badges work."* ⭐ **THE STORED `rsvpStatus: 'yes'` WAS THE WHOLE FAULT, and it was invisible because the badge was right.** A bare yes with no `rsvpRespondedAt` is normalised away by `effectiveRsvpStatus`, so the row read **Pending** — but `etRsvpAnswered`, which decides the send audience, reads the **RAW** field and counts anything except `unanswered` as answered. So the one record she made to test the RSVP with was the one record the RSVP would skip, and a send that skips somebody looks exactly like a send that worked. `buildTestPerson` writes `'unanswered'` now. ⚠ **`'unanswered'` RATHER THAN BLANK** is her own 2026-08-20 ruling: blank means nobody ever asked, unanswered means we asked this season and they have not replied — which is what a returning customer looks like when the RSVP goes out. ⚠ **AND THEY STAY A FINISHED, ROUTABLE CUSTOMER**, which is what the stored yes was there for: `'unanswered'` sets no flag and is not `no`, so `isOutForSeason` keeps them in and the price-edit trial is unaffected. ⭐ **A FIRST ATTEMPT EXEMPTED TEST RECORDS INSIDE `audienceNeverAsked` AND WAS REVERTED, which is the more useful half of this row.** It would have made every test record read as returning — including the one built through Quotes, which is the NEW customer she needs to test the other path with. Her follow-up is what caught it. A production predicate special-casing test data is the wrong shape anyway: the two builders now differ for the ordinary reasons, so the test records are new and old the same way real customers are. ⚠ **AND ONE CHECK WAS REPOINTED, NOT WEAKENED**: it required `rsvpStatus === 'yes'` while its own sentence said *finished customer*. The RSVP answer has nothing to do with whether a price edit can be tried, and the value it pinned was the bug. | 2026-09-03 | `buildTestPerson`; **Customers → Reset Test for RSVP** for re-testing all three answers; run-all.js S115 asserts it through `etRsvpAnswered` and `isOutForSeason` rather than by matching the string, 6 sabotages red-checked | Standing |
| RS-47 | The badge still says On hold after approving through the emailed link. Is the approval broken? | **No — the hold was right and the line under it was wrong.** *"I approved this again through the approval link in email however the badge still says on hold"*, then *"945 is unpaid but can show as yes on the badge since this isn't unpaid from last month its unpaid from this month. or this year."* ⭐ **SHE IS RIGHT ABOUT THE MONEY AND IT WAS NEVER THE CAUSE.** The portal's hold reads `arrearsOutstanding` — LAST season's debt — so a current bill does not touch the season badge, and `seasonHold` never named it. ⭐ **THE HOLD WAS THE CONFIRMED-ONLY RULE, WHICH NEEDS A *DATED* REPLY.** `rsvpStatus: 'yes'` with no `rsvpRespondedAt` is the ASSUMED yes written when a quote is converted, or carried in by an import — nobody actually answered — so `isOutForSeason` holds them and the badge reads On hold. Proved by running the shipped rules: a dated yes gives **confirmed**, an undated one gives **pending**. ⚠ **AND THE CODE PATH IS SOUND**: driving the emailed Yes in a real browser, with a bill outstanding, stamps the date and the badge goes Confirmed. Her record simply never received that write. ⭐ **WHAT WAS ACTUALLY FIXED IS THE SENTENCE.** The line read *"no RSVP yet"* for both states, so she went hunting for a broken approval. They have different remedies — nothing on file means chase them; a yes nobody dated means confirm it on the record, which stamps the date — and told they had never replied, the office chases somebody who may already have approved. ⚠ **It reads the RAW field**, because `effectiveRsvpStatus` normalises a bare yes away to `''`, which is exactly why the line could not tell them apart. ⚠ **It changes no decision**, only words. | 2026-09-03 | `seasonHold`'s undated-yes branch; arrears-hold.test.js RUNS all three states, test/rsvp-unpaid-this-year.spec.js drives the emailed Yes with a bill outstanding; 2 of 3 sabotages caught, the third documented as unreachable | Standing |

## Health-check notices

| # | Question | Her answer | Decided | Where it's proved | Status |
|---|---|---|---|---|---|
| HC-04 | Approve and deny, built at last — where do they live and what does each do? | **On the Health Check panel, per finding.** She chose it over putting the findings somewhere she already looks: "build the approve/deny you asked for." **Not a problem** hides that finding until the data moves; **Fix this one** runs the check's own fix for that row, where it has one. ⚠ THE DECISION IS KEYED ON A FINGERPRINT of check + label + detail (`hcFindingKey`), so it lapses by itself the moment that customer's values change — nothing expires it and nobody remembers it. ⚠ IT CANNOT LIVE ON A NOTICE: `messages` caps at 5,000 characters, so a check with fifty rows could never carry fifty answers. Own collection, staff-only. ⚠ NINETEEN OF TWENTY-FIVE CHECKS HAVE NO FIX and get no Fix button — offering one that writes nothing is a lie about what pressing it does. ⚠ THE LIMIT, SAID PLAINLY: two customers with the same name AND identical detail share one fingerprint. They are indistinguishable on the panel anyway; if rows ever carry a customer id, key on that | 2026-08-27 | `hcFindingKey`, `hcDecide`, `health-decisions.test.js` | Standing |
| HC-01 | What does **Deny** mean on a health-check notice? | Not until the data changes — the exception is scoped to a fingerprint and comes back on its own when the values move. **And it is per member:** "I should be able to choose what member I'm denying for and approve for all other members if we run into that situation." ⚠ So the decision cannot live on the notice — `messages` caps at 5,000 characters — it is keyed on check + member + fingerprint | 2026-08-21 | `hcDecide` / `hcFindingKey` / `hcRender`; health-decisions.test.js | Standing |
| HC-02 | Which health checks may raise a notice? | All of them. "I want to be able to approve or deny it. But after approve it can auto write." Approve means the finding is real, and where a check has an auto-fix, approving RUNS it. ⚠ That makes a notice a bulk write path — one click on "43 invoices have drifted" is 43 money writes — so the notice must carry its own preview, R-007's cap and R-008's log | 2026-08-21 | `hcDecide` / `hcFixRow`; health-decisions.test.js | Standing |
| HC-03 | Health Check is not being read — rebuild the panel? | **No — "don't do anything with design."** Her reason, unprompted: "I don't check health check very often cause the design is weird and I can't mark anything as completed or outside of policy." Both halves verified in code: there is no dismiss/acknowledge anywhere in `hcRunChecks`/`hcRender`, and the badge counts ROWS, so lists she has already ruled on (`sharedPhone` alone is 14 genuine households out of 17) hold it permanently above zero — a number that can never reach nought is one nobody reads. ⚠ THIS IS A "NOT NOW", NOT A REVERSAL of HC-01/HC-02, which are the same marking mechanism asked for from the notice end and still Decided — not built. ⚠ THE CONSEQUENCE IS REAL AND MUST NOT BE FORGOTTEN: Health Check is the delivery route four findings are currently queued behind (Q-019's `phoneNotDigits`, `strandedPayment`, `townIsStreet`, and the colour wipe), so a new row there is not reporting a problem — it is the quiet failure one level up. Until this changes, a finding that matters is reported to her directly, not filed in the panel | 2026-08-26 | `hcRender` (badge = totalRows); no dismiss state exists | Standing |

## Customer-facing options

| # | Question | Her answer | Decided | Where it's proved | Status |
|---|---|---|---|---|---|
| OPT-01 | What is the real customer-facing option set? | The fourteen in `js/options.js`, corrected by her from a placeholder that had seven invented field names in it: measuredFeet, lightsDescription, wireColor, outletTimer, useEaves, specificOutlet, gateCode, houseSides, installPreference, notes, oneTimeNote, wantsMailedInvoice, numberOfBins, difficulty | 2026-08-21 | Q-007; `options-audit.test.js` frozen AGREED map | Standing |

## Process, testing and delivery

| # | Question | Her answer | Decided | Where it's proved | Status |
|---|---|---|---|---|---|
| PROC-01 | How much manual testing should there be? | As little as possible — "it should minimize since you are testing too." The checklist is only for what a human or a live environment must verify | 2026-08-17 | `MANUAL_ONLY_IDS` | Standing |
| PROC-02 | Bump a test's version to force a retest of something the suite covers? | No. The suite passing **is** the sign-off | 2026-08-17 | CLAUDE.md §0 | Standing |
| PROC-03 | Where does new coverage go? | The automated suite — not a new manual row, unless it genuinely can't be automated (a real charge, a real email, a physical crew step, a print check) | 2026-08-17 | CLAUDE.md §0 | Standing |
| PROC-04 | Do tests ship with the change? | Yes, in the same commit. She asked for this rule by name | 2026-08-14 | CLAUDE.md §9.9 | Standing |
| PROC-05 | Canary / percentage rollout with automatic rollback? | **Considered and rejected, with her agreement. Do not build it and do not suggest it again without new information** | 2026-08-14 | CLAUDE.md §9.10 | Closed |
| PROC-06 | Add `data-testid` everywhere in one sweep? | No — as you go. A mass edit of the file is risk without benefit | 2026-08-14 | CLAUDE.md §9 | Standing |
| PROC-07 | Repeat the approve/maybe/decline buttons when there are several photos? | No — "just one approved, Maybe later, Decline." She read the doubled set as a mistake. Do not reintroduce it as a fix for a long email | 2026-08-17 | Suite 33 | Closed |
| PROC-08 | ~~Should the buttons be repeated below a stack of photos?~~ | ~~Yes, so Approve is never far down a phone~~ | 2026-08-13 | `repeatQuoteButtonsServer`, removed | Superseded → PROC-07 |
| PROC-09 | Remove an existing feature while doing something else? | No — features don't get removed unless she asks. Archive over delete | (standing) | project rules | Standing |
| PROC-10 | Write code straight away, or explain first? | Explain first — the feature, the files, the cross-part impact — and wait for the go-ahead. Mockups before major UI changes | (standing) | project rules | Standing |
| PROC-11 | ~~Deliver snippets or whole files?~~ | ~~Whole files, through the file system — only the files she needs to upload~~ | (standing) | project rules | Superseded → PROC-15 |
| PROC-13 | Is "write the ruling down" an active rule or a proposal? | Active — **R-023**, `read` + `code`, tier 5. Filing it as proposed was wrong: R-005 was retired because the state it governed did not exist, not because it was unenforceable, and this rulebook already has a tier for rules that can only be honoured | 2026-08-26 | `docs/RULES.md` R-023 + amendment log | Standing |
| PROC-12 | What does a test failure have to say? | Enough to be understood from the pasted text alone — "failures should read legibility so you can understand it when we ask you questions about failures." The file, the line, which row, **what that row says**, and the fix. Grouped by row, not one line per check | 2026-08-26 | `questions-map.test.js` reporter, 13 sabotages | Standing |
| PROC-14 | How does a ruling reach the map when the work is done in Claude Code? | R-023 restated at the TOP of `CLAUDE.md`, not only as item 5 of §9.9 forty thousand words down. Chat-side work is covered by the Project instructions. Nothing can check that a ruling was given and never written down, so the placement IS the enforcement | 2026-08-26 | `CLAUDE.md` head section, `docs/RULES.md` R-023 | Standing |
| PROC-15 | Does Claude Code hand back files to upload, or do the work in the repo? | Do it in the repo — branch, edit, run `npm test`, open the PR. **Do not hand back files to upload.** Reverses PROC-11, which was written for chat delivery where there was no repo to work in | 2026-08-26 | task briefs, 2026-08-26 | Standing |
| PROC-16 | Where does the connections map live — a file you open, or inside admin? | **Inside admin**, under Project › Connections. "will I need to open this everytime cause if so maybe it's better if we add it to admin under checklist." ⚠ She does NOT have to open it to be safe — a broken connection fails the build. The tab is for looking something up, and it is where she already is | 2026-08-26 | `projtab-connections`, `loadConnectionsSummary` | Superseded → PROC-21 |
| PROC-17 | What should the connections map watch first, and how should a break reach her? | Money first (price, bill-to, change fee, join fee, carryover); a red connection **fails the build**; and every box says whether a test already guards it, rather than only listing what is unguarded | 2026-08-26 | `connections/manifest.js`, `connections.test.js` | Standing |
| PROC-18 | What else should the connections map carry to be usable? | All four, in this order: **list what it does NOT watch** (so a green page is never read as "the app is fine"), **the remaining ten spines**, **the broken count on the Project nav item** so a break is visible from anywhere, and **when the map was last rebuilt** | 2026-08-26 | `NOT_WATCHED`, `paintConnectionsBadge`, `builtFrom` | Standing |
| PROC-19 | How often is `system-map.md` brought up to date? | **Every time**, in the same change — not periodically. "system map should be every time." ⚠ Updated, never regenerated wholesale: it is the document written to be trusted over her own memory, so losing detail in a rewrite is the expensive failure | 2026-08-26 | `CLAUDE.md` §9.9 item 6, `system-map.md` header | Standing |
| PROC-20 | The map cannot cover 36,476 touch points — so what does it watch? | The fields **most likely to fail**, in six named areas: *"quotes, invoices, costumers, schedule, and routes"* plus *"oh and warehouse"*. ⚠ Ranked by how many separate places WRITE each field, because that is the shape every bug this week had — one rule, several writers, one out of step. 82 such fields exist; the ranking put `needsLightBuild` where the real hole was before it was used | 2026-08-26 | `connections/manifest.js` batch 3 | Standing |
| PROC-21 | The connections map is inside admin — but WHERE inside admin? | **Its own item in the sidebar, directly under Checklist.** "can we make connection it's own tab under checklist?" — asked after she went looking for it on main and reported *"I don't see it on main"*. It WAS on main, as the fourth sub-tab of a panel whose sidebar entry reads **Checklist**, so the word Connections appeared nowhere until you were already inside it. ⚠ Supersedes PROC-16 on placement only; that ruling's answer — inside admin rather than a file you open — is why it is here at all. ⚠ A reference nobody can find is a reference nobody reads, which for a map whose entire job is to be looked up costs the whole feature. ⚠ It also gets its own nav badge, which retires the shared one: two facts sharing the Checklist badge needed a painter both sides called or the loser was invisible, and the loser would have been the break | 2026-08-27 | `panel-connections`, `paintConnectionsBadge`, `connections.test.js` §5 | Standing |
| PROC-23 | The connections map is a tree. Is that the right shape? | **No — a grid, plus a second view for the rules.** She sent a mockup (`connections/mockup.html`) with the instruction "build to it, do not redesign it": fields down the side, the record each is stored on, and the eight places it lands across the top; filled square writes, outlined reads, red declared-and-missing, amber found-and-never-declared. ⚠ THE SECOND VIEW IS THE HALF THE TREE COULD NOT DO — the grid finds broken WIRING; rules find a rule that was quietly wrong from the day it was written, which no extractor can detect. Neither catches the other's kind. ⚠ AND THE RULES ARE NOT AUTHORED THERE. Her own brief: "Humans write rulings; behaviour is derived." Every block is a row of the questions map, parsed at build time — writing the code's behaviour out as prose would be a second copy of the codebase, stale in weeks and 300+ hours to review | 2026-08-27 | `connections/grid.js`, `connections/rules.js`, `connections/mockup.html` | Standing |
| PROC-24 | If I answer the same question twice in different ways, which answer do you follow? | **The most recent one.** "can we make it a rule if I answer the same question twice in different ways than the most recent one is the one to trust." ⚠ This REPLACES stop-and-ask for a contradiction between two of HER OWN rulings — CLAUDE.md §2 said "Do not pick the newer one" and that half no longer applies to her answers. It is unchanged for two numbered R- rules colliding, which is a rulebook fault only she can fix. ⚠ Q-023 is what prompted it: it sat open for six days because two of her rulings disagreed and nothing was allowed to choose. ⚠ THE COST, ACCEPTED: a newer answer can be about a NARROWER case than the one it displaces, given without knowing what the older one protected — six rows here already reverse an earlier one, and the map's own note says the superseded reasoning "was sound when it was made and reads convincingly to whoever finds it first". So the older row is kept and marked, never deleted, and applying this rule is SAID OUT LOUD rather than done quietly. ⚠ AND IT ONLY WORKS ON WHAT IS WRITTEN DOWN: two answers can only be ordered if both carry a date, which makes a missing or wrong Decided column a correctness fault now, not untidiness. | 2026-08-28 | `docs/RULES.md` R-024; CLAUDE.md §2; questions-map.test.js checks the Decided column parses | Standing |
| PROC-25 | When you answer by clicking one of the options I offered, how does that go in the map? | **In my words — the option she clicked.** "quesition map can be answered in your words with optional choice it just which ever choice we clicked on." ⚠ The options are MINE by construction, so demanding a quotation of her there would mean INVENTING one — the exact harm the quote rule exists to prevent. Written `Chose: "the option"`, so a later reader can tell a selected option from something she typed. ⚠ AND IT MUST NAME WHAT IT BEAT: "Chose: keep it simple" alone is not an answer, because nobody can later tell what she was choosing BETWEEN, and a choice read without its alternative is how a narrow answer gets applied broadly. Two quoted options, the one taken and one it was taken over. | 2026-08-28 | `questions-map.test.js` — the own-words check accepts either form, and a row saying `Chose:` must carry two options | Standing |
| PROC-26 | Should updating the system map and the questions map be required before merging, the way passing tests is? | **Yes — a gate, not a reminder.** "make sure its in just like you have to test everytime before merging you have to update the system map and the question map." ⚠ This is CLAUDE.md §6 applied to itself: a `read` rule violated twice must be promoted to `code`, and §9.9 item 6 drifted exactly that way — system-map.md described `needsLightBuild` wrongly for five days with nothing going red. ⚠ THE TWO HALVES ARE NOT EQUALLY ENFORCEABLE and are not pretended to be: a source change requiring a system-map change is decidable from git; a RULING is a conversation, and R-023 already records that no check can see one. What IS checkable is the case that actually happens — a commit message quoting her, which means a decision was acted on, so the row belongs in the map rather than in a commit nobody will search. ⚠ AND BOTH ACCEPT A STATED REASON (`No system-map change:` / `No ruling this change:`) deliberately: a gate with no way past it gets satisfied by a junk line added to system-map.md, leaving the document noisier AND the check defeated. The choice it forces is between a sentence and silence. | 2026-08-28 | `docs-current.test.js`; `npm run test:docs`, in the npm test chain and in tests.yml | Standing |
| PROC-22 | PR #14 is decaying and its answer (Q-023) is unanswered. What now? | **Bring the one file that only exists there onto main, unwired, and leave the decision open.** "Go ahead with the first one." ⚠ THE DECAY WAS NOT ABOUT THE FILE. `js/grid.js` is 718 lines of pure arithmetic — no DOM, no Firebase — and conflicts with nothing on main; what was decaying was the SIX WIRING COMMITS around it, against an admin.html that has moved 159 commits since. Copying the file costs nothing and stops the clock on the part that was never at risk from being wrong, only from being lost. ⚠ IT IS NOT AN ANSWER TO Q-023 and must not be read as one — main still builds the season from towns, and wiring the grid means redoing work she changed afterwards (the crew split, One Man Installs, both printed sheets). ⚠ AND AN UNWIRED FILE WITH NO TEST ROTS, which would waste the rescue: `grid-parked.test.js` runs it on every build. Same shape as `js/options.js`, which has been a declared-but-unwired spec since 2026-08-21 | 2026-08-27 | `js/grid.js`, `grid-parked.test.js`, Q-023 | Standing |

## Fixes

| # | Question | Her answer | Decided | Where it's proved | Status |
|---|---|---|---|---|---|
| FIX-01 | Who schedules fixes this season? | The office, not the crew | (chat) | not in repo yet | Standing |
| FIX-02 | How does a new fix surface? | A System inbox note with a link to the customer card. The photo can come from either side. Park-then-destroy with an undo; the note disappears when it's done; the house photo is never touched | (chat) | not in repo yet | Decided — not built |

---

## Open questions live somewhere else

⚠ **Do not start a second list here.** `docs/open-questions.md` already owns open
questions, as `Q-` entries, under R-020 — and its own header already says every answer
becomes a map entry so it is never asked twice. **This file is that destination.**

So the loop is: an `intent` question is raised as a `Q-` entry → Addie answers it → the
`Q-` entry's **Resulting map change** line names the row it created here → the row here
carries the answer. `questions-map.test.js` checks both ends of that link.

Two things are outstanding and are *not* `Q-` entries yet, because neither has been
raised as one. If either needs a decision from Addie rather than a plan from Claude, it
should be raised there first:

- The `ROUTES` router on HEAD's `index.html` — wanted, or dropped? Blocks uploading the
  restored `e67dc8b` files.
- The two retired importers' add branches and `needsLightBuild` — plan written, approval
  pending before any file changes.

---

## What was deliberately left out

Kept out on purpose, so nobody adds them back thinking they were missed:

- **Facts about the code** — the 260 ft bin cutoff, phone digits as the invoice key, which
  function writes what. Those live in `system-map.md` and are derivable by reading the code.
- **Technical constraints that were never a choice** — a carousel is impossible in email,
  EmailJS returning OK is not delivery, the Firestore emulator can't be downloaded in the
  sandbox. Those live in `CLAUDE.md` with their reasoning.
- **Conventions** — branch names, deploy order, patch discipline. `CLAUDE.md` owns those.

If one of those turns into a judgement call — she picks between two technically valid
answers — *that* becomes a row here.

## Measure Roof — the sky-view rebuild (2026-08-27 / 28)

⚠ THIS WHOLE SECTION REPLACED A DESIGN, not a detail. The tool measured on TWO
pictures — one set of points seen from above and from the street — and the owner
ended it: *"abandon ship this is not working."* The superseded rows are kept
because their reasoning is exactly why the new answers are right.

| # | Question | Her answer | Standing | Where it's proved | Status |
|---|---|---|---|---|---|
| MR-01 | Which picture is the roofline measured on? | Sky view, and only sky view. Street View is for the photograph and for reading a grade. *"using sky view instead ... and the street view is just for the picture"* | 2026-08-27 | Suite 170; the sky click is the only thing that places a corner | Standing |
| MR-02 | Sky view cannot see height. How is the climb counted? | By PEAKS. *"we say how many peaks, so we just insert it there are and add on how many extra feet based on the type of peak"*, and which peak is *"between which two dots is it located ... and then it bases it off of the distance between the two points and the grade"*. The extra feet are Pythagoras on that grade | 2026-08-27 | `rmPeakExtraFraction`, `rmPeakSpanFeet`; suite 170 | Standing |
| MR-03 | Does a grade apply itself the moment you let go? | No. It ASKS. *"once you click down and then let go it asks if that was good if not it lets you do it again but if it was good it closes that page"* | 2026-08-27 | `rmGradeYes` / `rmGradeNo`; the grade reaches a peak through the yes and no other route | Standing |
| MR-04 | Must both sides of a gable be measured? | No. *"you should only need to measure the grade on one side and we will assume both sides of the triangle have the same grade"* | 2026-08-27 | `rmPeakExtraFraction` doubles one side | Standing |
| MR-05 | Can a dot be moved, clicked or dragged after it is placed? | No. *"dragging and clicking old dots or editing them in any way on either perspective is no longer an option other than backspace"*, and *"make sure dots can be on top of each other"* | 2026-08-27 | Suite 170; markers are `clickable:false`, nothing rejects a close dot | Standing |
| MR-06 | What does Street View show? | Its OWN dots, placed there and visible only there. Sky-view dots never appear on it. *"it should not show the dots from sky view and should also let you draw your own dots seperate from sky view"* | 2026-08-28 | `rmStreetDots`; suite 170, six sabotages red-checked | Standing |
| MR-06a | *(superseded)* Should Street View show anything at all? | Nothing — picture only. *"the only reason we have the street view is so we can make the picture"* | 2026-08-28 | — | Superseded → MR-06 |
| MR-07 | The controls were spread over several cards and needed scrolling past the picture — how should they sit? | One panel, capped at the height of the picture beside it, scrolling inside itself. *"mush them together so i dont have to scroll"* | 2026-08-27 | `.rm-tools` cap; Save is deliberately NOT pinned — it covered the tools | Standing |
| MR-10 | Recentre did nothing and full screen could not be clicked — should both work? | Both are real controls now. *"the recentre button doesnt fix sky view and you cant click full screen"* — Recentre frames what has been traced and clears a stuck pane; each picture gets its own full-screen button, because Google's is drawn under the sheet that catches measuring clicks | 2026-08-28 | `rmRecentreBtn` handler and `.rm-fs`; suite 170, six sabotages red-checked | Standing |
| MR-09 | The picture jumped every time a dot went down. What should it do? | Nothing. *"whenever i click a dot the screen frantically moves making it not user friendly"* — so the bar holds a fixed height, the changing lines are held to one line, and the Save block is reserved rather than revealed | 2026-08-28 | `rm-panel:not(.rm-commit)` height cap and `rmRenderCommit`; suite 170, five sabotages red-checked | Standing |
| MR-08a | *(superseded)* The ×2.9 pricing multiplier was compensating for a measurement fault that is now gone. Does it stay? | It stays, and its SIZE is a dial she turns — 2.9 → 1.45 → 1.3 on 2026-08-28. It is not a correction any more, it is the RATE — $2 a foot is what is advertised and a 60 ft house cannot be done for $120. *"we might want to adjust the 2.9 and if we do ill let you know but for now lets keep it"* | 2026-08-28 | `RM_FEET_MULTIPLIER`; Q-024 carries the argument | Superseded → MR-16 |
| MR-11 | Enter ended a strand in the sky view and did nothing in Street View. Should it work there too? | Yes. *"when I click enter on street view that should also mean end of strand"* — street marks had no strand at all, so every dot joined to the one before it and marking the top of the house then the bottom drew a line straight across the picture | 2026-08-28 | `rmEndStreetStrand`, `rmStreetBand`; suite 170 RUNS it, 8 checks | Standing |
| MR-12 | What is a foot actually charged at, per difficulty? | **$1.85 easy, $2.00 medium, $2.20 hard.** *"it should be $2 a foot for a medium house, 1.85 for a easy and 2.2 for a hard"* — they were 0.85/1/1.25 of a $2 baseline, so easy was $1.70 and hard $2.50. Written as MULTIPLIERS (0.925/1/1.10) so Per Foot Pricing stays the one dial she changes | 2026-08-28 | `RM_DIFFICULTY_RATE`; suite 170 asserts the three dollar figures | Standing |
| MR-13 | Should the size of a house make it Hard? | **No — size is out of the grade entirely.** *"hard [needs] to be fully based on how many strands, steepness, and walkability not based on size at all"* — the price is feet x rate, so a big house already costs more for being big; grading it up as well charged twice for one fact | 2026-08-28 | `RM_BIG_JOB_FT` retired; suite 170 proves footage cannot change the grade | Standing |
| MR-14 | Should the difficulty change by itself as peaks are measured? | Yes. *"the difficulty can automatically change as you find the grade for peaks"* — a grade she measured off the street photo beats the one Google's satellite model guessed. ⚠ But NEVER over a difficulty the office picked by hand: a re-cut that overwrote their choice would be the tool arguing with them about the price | 2026-08-28 | `rmRegrade`, `rmMeasuredGrades`, `rmGradeTouched`; suite 170 | Standing |
| MR-15 | How should difficulty be decided? | A SCORED system across her three axes. *"create a very advanced rating system with what ive already said and what you think would be best now"* — steepness dominates (it decides whether somebody stands or hangs), then walkability (height and how many separate sections), then strands (time, not risk). ⚠ Two of her OLDER rulings are deliberately preserved in the weights: one soft signal is noted and not charged for, and a roof steep over a third of its area is Hard | 2026-08-28 | `gradeRoof`; the archetypes in suite 170/129 ARE the specification | Standing |
| MR-16 | What is a real foot actually charged at, and what does the invoice say? | **$2.30 per REAL foot on a medium house — and the invoice still says $2.00/ft.** *"change it to $2.30 a real foot on medium and adjust accordingly for hard and easy"*, with *"the length of a foot is the only variable so still label it as $2 a foot"*. So the rate box and her difficulty table are both fixed, and `RM_FEET_MULTIPLIER` is the dial: 1.15 x $2.00 = $2.30. Easy $2.13, hard $2.53. ⚠ The same inflated figure is SAVED as the footage, so it also sizes bins and bundles — narrowed by the drop from 1.3, not removed | 2026-08-28 | `RM_FEET_MULTIPLIER`; S255 pins the effective RATE, not the bare number | Standing |
| MR-17 | While measuring, a left drag places dots and the sheet covers the pane — so how do you look around or move? | **Hold the RIGHT button.** *"if you want to look a different direction and change where you are looking on street view you hold down right click just like roblox studio and if you want to move in sky view you hold down right click."* Street View follows the mouse (Roblox), which is the opposite of Google's own left drag; the sky view grabs the world, because that is what ITS left drag does. All three pictures, the grade screen included | 2026-08-28 | `rmNavPaneAt`, `rmNavDrag`; suite 170 RUNS the gestures, 10 sabotages red-checked | Standing |
| MR-18 | Should the right-click menu still appear on the pictures? | **On a click yes, after a held drag no.** *"you only get the right click pop up if you click there but if you hold the right click pop up doesnt come up"* — suppressing it outright was the first version and took away a menu nobody asked to lose. A few pixels of hand jitter still counts as a click (`RM_NAV_CLICK_SLOP`), and exactly one menu is suppressed per drag | 2026-08-28 | `rmNavMenuAfterDrag`; suite 170 runs click, drag, jitter and the second click | Standing |
| MR-19 | The right-drag look-around lagged behind the hand. How fast should it turn? | **Exactly mouse speed.** *"street view doesnt properly move at the same speed as the mouse which bugs me, correct it to mouse speed"* — it was degrees-per-pixel, the field of view spread evenly across the width, which under-turns by about 21% at any ordinary drag because a perspective picture does not give every pixel the same angle. It uses the camera's own arithmetic now, the same focal length and arctangent that put a mark back on screen | 2026-08-28 | the `focal`/`bearing` pair in the mousemove handler; suite 170 works the expected angle out from the camera rather than from the code | Standing |
| MR-20 | The look-around still trailed her hand after the perspective fix. Why, and what fixes it? | **Windows mouse acceleration — so it reads RAW mouse input now, the way a game does.** *"the screen still lags behind my mouse so its still not right"*, then *"go into my settings and see how fast my mouse moves and adjust it to that"*. Her settings were READ, not guessed: `MouseSensitivity 10` (plain 1.0x, nothing to scale) and `MouseSpeed 1` — **Enhance pointer precision is ON**. With acceleration the cursor is not a fixed multiple of the hand: it under-runs on a SLOW drag, which is the drag you make measuring a roof, and no fixed multiplier can correct that because the error depends on speed. Pointer Lock with `unadjustedMovement: true` bypasses it entirely. ⚠ The sky view is deliberately NOT locked — it is grabbing a real point on a map and wants the real cursor | 2026-08-28 | `rmNavRequestLock`, `rmNavDrag.rawX`; `RM_LOOK_SPEED_DEFAULT` is the dial if the rate ever needs taste (and `RM_LOOK_SPEED_KEY` is where a person's own setting is kept). ⚠ THIS ROW NAMED RM_LOOK_SENSITIVITY (no backticks — it is being quoted, not pointed at) UNTIL 2026-08-29, which has never existed in the code — a standing ruling pointing at nothing, found by checking every anchor in the map against the source rather than by anybody reading it | Standing |
| MR-21 | Raw input still felt too slow. How fast should the picture turn? | **Her dial, defaulting to 2.5x her hand.** *"still uncomfortably slow, it just slows my mouse down"*. Two things were wrong and only one was taste: raw movements are the MOUSE'S OWN units and were being divided by devicePixelRatio as though they were screen pixels, a silent fifth-slower brake at her 125% zoom; and tracking the hand exactly is the correct BASELINE, not a comfortable look speed — games turn several times faster. After three goes at guessing the number it is a **Look speed** slider in More options, saved per machine in localStorage, never synced — a comfortable speed belongs to a person and a mouse, not to the business | 2026-08-28 | `rmLookSpeed`, `rmWireLookSpeed`; suite 170 proves doubling it doubles the turn | Standing |
| MR-22 | Where do the tool buttons belong? | **All at the top, in their own bar — the pictures are last.** *"all the buttons at the bottom of the page should not be there i want it to be like excel with all the tools at the top and its in its own bar thing."* ⚠ This SUPERSEDES the earlier layout ruling that put the pictures SECOND, between the tools and the money — that one is kept because its reasoning still stands: the pictures twice fell through to a catch-all order and landed below everything at y=1261. Every row is numbered by hand now, and the pictures carry the highest number, so an unnumbered row can no longer land under them | 2026-08-28 | the order block in the overlay CSS; suite 170 asserts each row's number and that nothing is left below the pictures | Standing |

| MR-23 | "Attach to Quote" did nothing. What should it do? | **Take the picture itself.** *"in measure roof we have a button that says attach to quote, where it should attach the picture to the quote that doesnt happen, fix that."* It could only ever upload what *Add to Quote List* had already staged, so it sat DISABLED under a house that had just been measured — and a greyed-out button reading "Attach to Quote" is indistinguishable from a broken one. Nothing staged is now a reason to CAPTURE whichever view you were last working in, not a reason to refuse | 2026-08-29 | `rmAttachShots` (the `if(!rmShots.length)` branch), `rmRenderStaged`'s `canCapture`; suite 282 | Standing |
| MON-49 | With the rule off, ~953 read Confirmed and the whole season filled. Which half of that was wrong? | **Turning the rule off was.** *"anyone who is confirmed is scheduled but if one person is confirmed there should be one person on the schedule."* ⚠ THE ORIGINAL COMPLAINT WAS NEVER "schedule everybody" — it was that a row said **Confirmed** while every scheduler in the app had already dropped that customer. The badge was lying, and there are two ways to stop it lying: schedule everybody, or let the badge say **Pending**. [[MON-47]] took the first and it was the wrong one. ⭐ The second only became possible hours earlier, when `seasonBadgeKey` gained its Pending state and started delegating to `isOutForSeason`. So `SEASON_ELIGIBILITY` is back to `confirmed-only` — her 2026-08-27 hardcoding — and the ~951 who have never replied now read **Pending — no RSVP yet** instead of a false Confirmed. One confirmed, one scheduled; her invariant holds in both directions and the badge and the scheduler still cannot disagree. ⚠ WHAT SURVIVES FROM MON-47 is everything except the constant: the three-state badge, the delegation, the derived Pending, and the filter. Those are what make this answer workable at all. | 2026-09-01 | `SEASON_ELIGIBILITY`, `seasonBadgeKey`; arrears-hold.test.js runs the invariant over 32 combinations — both rule states × both money states — 4 sabotages red-checked | Standing |
| MON-48 | The badge decides who is scheduled — how do you list the people it applies to? | **A Season Badge filter in All Customers.** *"there is now way to filter who is confirmed fix that"*. Any / Confirmed / Pending / Maybe Next Year, sitting beside Route Status because since [[MON-47]] Confirmed IS the route answer. ⚠ THE BADGE IS WORKED OUT ONCE PER ROW and both the filter and the pill read that one answer — asking twice is two answers to one question about one customer, which is the shape of the bug MON-47 was built to close. ⚠ AND IT IS NAMED IN BOTH HOUSEKEEPING LISTS, which is what the checks are really for: this file already records both failures by name — a select left out of the change-listener list never repaints until you touch another filter, and one left out of Clear Filters stays applied while the list "stays mysteriously short". Neither surfaces as an error. | 2026-09-01 | `allCustFilterSeason`, `renderAllCustomersTable`; run-all.js, 5 sabotages red-checked | Standing |
| MON-47 | ~~The Confirmed badge says one thing and the scheduler does another — 951 of 956 customers unscheduled. Which decides?~~ | ~~**The badge decides, and a broken rule turns it to Pending by itself.** *"it should look for anyone who is confirmed and put them in schedule … make sure they cant have the confirmed tag if they are breaking a rule so if you break one of the rules they automatically change the badge to pending mainly just the havent paid for last year"*, and *"you shouldnt have to manually add them to pending"*. ⚠ MEASURED FIRST, ON HER OWN BOOK: 956 customers — **951** held out with *no RSVP yet*, 2 for owing from 2025, 1 back next year, and exactly **one** scheduled. ⚠ AND THE RULE WAS LIVE BECAUSE A **TEST SEND** STAMPED `rsvpSentAt`, which is what arms it; the gate did exactly what it was built to do and could not know nobody had really been asked. ⭐ So `SEASON_ELIGIBILITY` is back to `all-but-maybe-next-year` for the testing weeks — *"the no rsvp yet is the testing faze but doesnt mean anything as far as who should be scheduled"*, repeated when checked: *"if its no rsvp that shouldnt effect it because we still need to test if its no rsvp so tht is irrelevant"*. ⚠ THIS DOES NOT RETIRE [[MON-22]] OR THE 2026-08-27 HARDCODING — those are about the season AFTER the RSVP has genuinely gone out, and turning the rule back on is one line, asserted by its own check so moving it is deliberate. ⭐ `seasonBadgeKey` DELEGATES TO `isOutForSeason` rather than deciding for itself, so **Confirmed and in-the-season are one fact** and no row can read Confirmed while every scheduler has already dropped that customer. Pending is derived, never stored — paying the bill moves the badge on its own. ⚠ Maybe Next Year stays its own answer rather than collapsing into Pending: *"if they end up in recycle that means they didnt confirm and same with back next year so you dont need to worry about that"*.~~ | 2026-08-31 | `SEASON_ELIGIBILITY`, `seasonBadgeKey`, the season cell in `renderAllCustomersTable`; arrears-hold.test.js runs the invariant over 14 combinations, 6 sabotages red-checked | Superseded → MON-49 |
| MON-46 | Should the Unpaid tag work out which season a debt came from, or just assume? | **Assume 2025, and centre the whole box on that.** *"assume if they havent paid for a previous season its always 2025"*, then, shown the **Which season** box that #257 had added hours earlier: *"we actually want to use that just modify this … so it assumes 2025 season and adjust everything youve done to be centered around that"*, and *"by default assume they owe the same for this season as last season on the unpaid for last year"*. ⭐ ONE CONSTANT, `ARREARS_ASSUMED_SEASON`, governs all of it: the badge always reads **Unpaid 2025**, the Which season box OPENS on 2025 instead of blank, and a season left blank SAVES as 2025 so the data carries the assumption rather than the badge guessing over it. A season somebody typed is never overwritten. ⚠ SHE WAS SHOWN THE COST AND TOOK IT: a debt carried out of THIS season is a 2026 debt, and while the constant says 2025 the tag will call it a 2025 one. Every debt on the book today did come from 2025, and the "Unpaid last season" wording on year-less notes was the ambiguity she was actually objecting to. Moving it next year is one line. ⚠ AND THE AMOUNT IS SUGGESTED, NOT FILLED IN — the one place this stops short of her words, deliberately. A real value in that box means every ordinary save of any customer, opened to fix a phone number, writes a debt of their whole year's price onto them AND holds them off the schedule until it is paid. It is a placeholder plus a one-click **Same as this season** button instead. | 2026-08-31 | `ARREARS_ASSUMED_SEASON`, `houseArrearsTag`, the Edit Customer arrears box; arrears-hold.test.js, 7 sabotages red-checked | Standing |
| MON-45 | ~~All Customers shows Confirmed or Maybe Next Year. What about people who have not paid for an earlier season?~~ | ~~**A second tag under the same badge, and the year comes off the debt.** *"we need a seperate tag for people who havent paid for 2025 can you just add another one under the same badge that says unpaid 2025."* ⭐ It reads **Unpaid 2025** today because 2025 IS the year on those arrears notes — but the year is taken from the note (`houseArrearsYear`), never typed into the row. A hardcoded 2025 is wrong the moment a season turns, and wrong in the direction that matters: it would tell the office a 2026 debt was a 2025 one. That is the same reasoning as [[MON-34]], applied to the badge. ⚠ IT ASKS THE HOUSE-LEVEL PAIR, so a house on somebody else's bill is judged by the bill it is actually on and can never name a different person or year from the money figure the Route column prints beside it. ⚠ IT SHOWS ON THE MAYBE NEXT YEAR ROWS TOO — somebody sitting the season out who still owes for the last one is precisely who has to be rung. ⚠ AND IT DELIBERATELY OVERLAPS the Route column's "owes $X from 2025": that line answers *why this house is not on a day* and goes as soon as they are scheduled, this one answers *what do they still owe* and does not. Both read the one helper.~~ | 2026-08-31 | `houseArrearsTag`, the season cell in `renderAllCustomersTable`; arrears-hold.test.js runs the wording, 7 sabotages red-checked | Superseded → MON-46 |
| MON-50 | If a customer opens the card form and then switches which amount they are paying, why does it still show the old one? | **The buttons come down and go back up.** *"if I push on 2025 payment than do credit/debit dropdown but than decide to switch to full payment it does not automatically changed to full payment. It still shows 2025 payment."* ⚠ THE BUG WAS AN ASSUMPTION WRITTEN DOWN AS A COMMENT: *"PayPal builds its order when the button is pressed, not now — so nothing needs re-rendering here."* True of the plain PayPal button, which calls `createOrder` on click. **False of the card button**, which opens an inline form and calls `createOrder` at THAT moment — so from then on the form is bound to an order that already exists, for the amount chosen before it opened. ⭐ THE FIX IS A TEARDOWN, NOT A BIGGER VARIABLE: nothing in the browser can revise an order PayPal has already created, and it must not be able to — the amount is the server's decision. `resetPaypalButtons` closes the instances, empties both containers and re-renders, so the next press creates a fresh order. ⚠ EVERY CONTROL THAT CHANGES THE AMOUNT CALLS IT, not just the one she hit — the tip buttons had the identical bug, and fixing one would have been half a fix. ⚠ A TYPED TIP RE-RENDERS ON `change`, NOT `input`: rebuilding on every keystroke would close a card form somebody is filling in. ⚠ AND A `close()` THAT THROWS MUST NOT ABANDON THE TEARDOWN — a half-cleared panel is a dead card form with no way to pay | 2026-09-01 | `resetPaypalButtons`, `paypalButtonInstances`; `arrears-hold.test.js` runs it against a fake SDK | Standing |
| MON-51 | Should the one-off backfill be run to carry 2025's unpaid bills? | **No — there is nothing to carry.** *"I'm not going to do carry cause we did not have this website last year there is nothing to carry."* ⚠ SO THE BUTTON IS NOT A PENDING TASK and nobody should chase her to press it. The snapshot Start New Season saved holds the invoices as they stood at the reset, and for a book that only started on this site there was no 2025 balance on any of them. ⭐ THE MECHANISM IS NOT WASTED, IT IS DORMANT: nothing carries today, so the hold releases everybody, **Schedule › Owes from last year** is empty and the two-button payment choice never appears — and at the END of this season Start New Season carries anyone who has not paid for 2026 onto the 2027 bill by itself, with no button at all. ⚠ THE ONE PART THAT IS USABLE NOW is Edit Customer › Owed from a previous season (MON-36), for putting an old debt on one customer by hand. ⚠ AND THE BACKFILL IS KEPT rather than removed: it is idempotent, it writes nothing without a typed CARRY, and a season reset run before the carry shipped is exactly the case it exists for — deleting it would only mean rebuilding it the next time that happens | 2026-09-01 | `arrearsBackfillPlan`; Invoices › Start New Season | Standing |
| MON-53 | The RSVP goes out by email. How do the customers with no email on file get asked? | **A link to text them, one each.** *"make an rsvp text link for everyone that doesnt have an email on file."* ⭐ NOTHING NEW WAS NEEDED ON THE CUSTOMER'S SIDE, and that is worth knowing before anybody builds a page for it: `#/payment?token=…` with no rsvp parameter signs them in, and the FIRST block on that page is **"Are you having lights this season?"** with Yes / Back next year / Not this season. So one short link IS the whole RSVP — which is what a text needs, because a text cannot show buttons. The email needs three links only because it *is* the buttons. ⚠ THE TOKEN IS KEYED TO THE RECORD, NOT THE PHONE. `getOrCreatePortalToken` finds a customer by phone with a `.find()`, and seventeen numbers in the real book are shared by two households — through that door the second household's "own" link would sign them into the first one's portal: their address, their price, their answer to change. ⚠ WHO IS ON THE LIST MIRRORS THE EMAIL'S OWN AUDIENCE (`audienceNeverAsked`, `effectiveRsvpStatus`), so the two lists cannot drift about one person; somebody who has already really answered is left off, but a bare `yes` with no reply behind it — an import, or the one written at conversion — still gets asked. ⚠ Somebody who replied STOP is SHOWN but has no button: dropping them silently is how a person goes missing with no reason on screen. ⚠ And drawing the list mints no tokens — a panel that writes to the book by being looked at is the thing nobody suspects. | 2026-09-02 | `rsvpTextTargets`, `rsvpTextTokenFor`, `rsvpTextMessageFor`, the card in Automation Emails; run-all.js suite 291, 7 of 8 sabotages red-checked (the eighth expected and documented) | Standing |
| MON-52 | What should the portal show after somebody pays off last season, and what if the card window is still open on the old amount? | **The 2025 option disappears, the payable figure becomes what is left, and a window left open is called out.** *"how much the full amount should show as after they pay 2025. And 2025 unpaid payment should disapear."* ⭐ THE FIRST TWO WERE ALREADY RIGHT AND ARE NOW PINNED BY A RUN, not by reading: `renderPayChoice` rebuilds on every read of the invoice — including the one straight after a payment — so it hides the chooser once the debt is gone, and `portalPayableNow` falls back to the whole balance rather than to the cleared arrears. That fallback is the subtle half: the scope is still `arrears` at that moment, so a naive read returns 0 and the customer is shown nothing to pay. A PART payment leaves the option up for the remainder. ⚠ THE THIRD IS THE MONEY RISK: `resetPaypalButtons` is meant to close an open card window, and normally does — but a window that survives is still holding an order for the amount chosen BEFORE, and completing it charges that while the page shows the new figure. `paypalFlowOpen` (set in `onClick`, cleared in `onCancel`/`onError`/`onApprove`) is what lets the page say so. ⚠ THE FLAG IS READ BEFORE THE TEARDOWN, which is what clears it — read after, it is always false and the warning could never appear. ⚠ AND BACKING OUT REBUILDS THE BUTTONS, so the next press is priced from what is chosen now rather than from the attempt they abandoned | 2026-09-01 | `renderPayChoice`, `portalPayableNow`, `paypalFlowOpen`; `arrears-hold.test.js` runs the renderer through each state | Standing |
| MR-39 | Three peaks with pitches and `0 ft across` are on a quote before any measuring has started. Where from? | **The last house.** *"A – B 0 ft across … E – F 0 ft across … I – J 0 ft across … this is there before i even start measuring."* ⚠ FIRST, THE COMMONEST CAUSE, and it was the cause here: **an admin page left open since before the fix**. [[MR-38]] made the dots and the peaks clear together, so on the current file "peaks present, dots gone" cannot happen at all — seeing it means the tab is running yesterday's `admin.html`, and Ctrl+Shift+R is the whole of the fix. ⭐ BUT IT FOUND A REAL SECOND PATH. `rmReset`, which is what runs when the tool opens on a quote, resets a dozen per-house things by hand and the dots and peaks were not among them; the only thing emptying them was `rmLoadAddress`, on a LATER TICK and only when the quote HAS an address. So the previous house's marks are on screen for as long as the map takes to load, and on an address-less quote they simply stay — counting that house's footage into this one's price. ⚠ THE FIX IS A CALL, NOT A THIRTEENTH LINE: `rmReset` now calls `rmForgetLastHouse`, whose own note says it is "the one place to add to when something new is remembered". Adding to `rmReset`'s hand-written list is how this one got missed. | 2026-08-31 | `rmReset` → `rmForgetLastHouse`; suite 257, 3 sabotages red-checked | Standing |
| MR-38 | Reopening a measured quote shows the peaks with their pitches but `0 ft across`, adding nothing to the price. Why? | **Because a peak is a pair of corner NUMBERS, and the corners were never saved.** *"the peaks are not adding anything because i left the page then came back, but the dots are still there so it should've included the peak addition into the price but it didnt."* ⚠ ONLY THE RUNS WERE EVER WRITTEN DOWN. Reopening put the lines back on the map — which is why the dots looked to be there — while `rmCorners` came back EMPTY, so `rmPeakSpanFeet` returned 0 for every peak; `rmCornerLabel` turns an index into a letter without needing the corner to exist, which is how three peaks still read A–B, E–F and I–J on a house whose dots had all gone. ⚠ AND `rmPeaks` WAS CLEARED NOWHERE IN THE FILE, which is the worse half: a gable measured on one roof stayed in the list when the tool opened on the next one, pointing at corner numbers that now meant somebody else's dots. ⭐ The dots and the peaks are saved with the drawing and restored before anything reads a total; a peak whose corners did not come back is DROPPED rather than shown spanning nothing; the peaks are emptied everywhere the dots are; and taking a dot back off now drops the peak on it and shifts the rest, instead of silently re-hanging a gable between two different corners. ⚠ A corner run is REBUILT from the dots rather than restored beside them, or the same footage lands on the price twice. | 2026-08-30 | `rmMeasurementDoc`, `rmRestoreMeasurement`, `rmPeaksAfterCornerRemoved`; suite 257, 15 sabotages red-checked | Standing |
| MR-37 | Which picture goes on the quote — the roofline from above, or the photograph of the house? | **The street view, always.** *"when you dot the sky view the picture of that gets uploaded as well but we just want the street view to be uploaded."* ⛔ Attach used to GUESS from where the marks were, and that guess was right about where the WORK was and wrong about what the PICTURE is for: the roofline is traced overhead and nowhere else, so "follow the marks" answered *sky* for every properly measured house — while the quote's photograph is what the customer and the crew look at to recognise the place, which an aerial does not do at any price. ⚠ It follows her own 2026-08-28 ruling through ([[MR-06]] — the street picture "should not show the dots from sky view"): the sky view is where the house is MEASURED, the street view is the PICTURE. ⚠ **Capture Sky View is gone** — its only job was to put a picture on a quote, and leaving it would be a door that stages a photograph the attach then has to refuse. The sky view itself is untouched. ⚠ The aerial survives ONLY where Google has no street photograph at all, and then it says so on the picture's own label; refusing outright would leave a quote with no picture, which is a worse answer than a labelled one. | 2026-08-30 | `rmAttachShots`'s `which`, `rmStageCapture`'s label; suite 282 §8, 6 sabotages red-checked | Standing |
| MR-36 | A peak reported 49% and added 2.9 ft. Does that arithmetic add up? | **The sum was right; the number was unreadable.** She followed it with *"i think it did the math as if its a 4 degree angle not 49 or something"*, which is exactly the misreading: 49% GRADE is 26.1°, and 26.1° across a 24.7 ft span really does add 2.81 ft — but read as forty-nine DEGREES the same peak adds 12.95 ft, four and a half times as much. ⚠ NOTHING WAS FIXED IN THE ARITHMETIC, deliberately: changing a correct sum to match a misreading would have been the expensive mistake. What changed is that every pitch now leads with the ANGLE, the percentage is named as a *grade* wherever it still appears, and the peak note SHOWS ITS WORKING — the rake length against the span — so the figure can be checked rather than taken on trust. ⭐ And it says how to overrule it, because a grade read off a photograph tends to come out shallow and she knows her own gables ([[MR-34]]). | 2026-08-30 | `rmGradeSay`, the `rmGradeYes` handler; suite 284 runs both readings of "49" | Standing |
| MR-35 | Full screen shows one pane or neither. How do you see the map and the photograph at the same time, both big? | **The container goes full screen, not a pane.** *"give a third button in the top right of sky view where you can instead of full screen do half and half sky view and street view so you can see where you need to place."* Full screen was all or nothing and both ends were wrong for placing a corner: full-screening the sky view takes the photograph away — and the photograph is how you know WHICH corner you are looking at — while leaving full screen gives both back at the height the ribbon leaves them, a couple of hundred pixels each on a laptop. The two panes already sit side by side in one row, so the third button simply gives that ROW the whole screen, 50/50, stacking instead of squeezing on a tall narrow one. ⚠ It SWAPS rather than refusing: pressed while one pane is already full screen it goes straight to half and half, because that comparison is the whole point of it. | 2026-08-30 | `.rm-both` in the sky pane head, `.rm-panes:fullscreen`; suite 284 | Standing |
| MR-34 | A 24 ft peak at 45 degrees should add 10 ft. Why was the tool adding less? | **Because the pitch was measured on the glass, not in the world.** *"24 feet 45 degree angle would add 10 feet almost exactly meaning an extra $20 to do a peak but your system doesnt adjust it nearly as much as it should."* ⚠ HER ARITHMETIC AND THE FORMULA BOTH AGREED — two rakes at 45° over 24 ft are 2 × (12 / cos 45) = 33.94 ft, and `span × (1/cos − 1)` returns 9.94. What was wrong was the GRADE going in: the drag measured rise over run in SCREEN PIXELS, and the grade panorama is deliberately aimed UP at the roof, which compresses the apparent slope of a line above the camera. The error is always in the same direction — too shallow — so a true 45° rake read back 87–94% and the peak added 7.9–9.0 ft. ⭐ The dragged pixels are now rays intersected with the gable's OWN vertical plane, which the two dots naming the peak already define, so the answer no longer depends on where the camera stood. ⭐ And the pitch can simply be TYPED (45, or 12/12, or 100%) — she knows her own gables, and a typed angle carries no projection error at all. ⚠ A peak with no pitch of its own now SAYS it is using the roof average rather than showing that figure as if it were this gable's | 2026-08-30 | `rmGradeFromGable`, `rmOnGablePlane`, `rmPitchToGrade`; suite 283 runs her 24 ft / 45° case and four camera pitches | Standing |
| MR-33 | Recentre is in the toolbar, and the toolbar is not on screen in full screen. Where does it go? | **A second one inside the sky pane itself.** *"can you add a recentre button even when full screened on sky view."* Full screen is requested on the PANE, so only that pane's own children are on the glass — and losing the house is exactly what happens when somebody is zoomed in and full screen, which is the one moment the button for finding it again was gone. It sits in the pane's head beside the full-screen button, sharing its style so two pane controls cannot drift into looking like two kinds of thing. ⚠ ONE FUNCTION, TWO DOORS: `rmRecentre` is at module level and both buttons call it — a second copy of "put the camera back" is the copy that stops matching. ⚠ The press is stopped from falling through: the head lies over the map, so an unstopped click would place a corner where the button was. ⚠ And it changes NO measurement — it moves two cameras and takes the covers off — which is what makes it safe to put where a thumb can find it by accident | 2026-08-30 | `rmRecentre`, `.rm-recentre` on both buttons; suite 170 | Standing |
| MR-32 | How big should a dot be, when a dormer has a dozen edges a few feet long? | **Smaller than the smallest thing anybody measures with it.** *"the dots look a little messy when you do something like a dormer and it has a ton of little edges can you make the dots smaller and cleaner."* A dot comfortable on a long eave is WIDER than a dormer edge, so it hides the thing it is pointing at: the street dot went r6 → r3.4, the sky dot 4 → 2.6, the joined line 5 → 3.2, the labels 11px → 9.5px. ⚠ ONE SET OF CONSTANTS, because FOUR renderers draw these marks — the street SVG, the sky markers, a traced run's handles and the fallback picture — and separately tuned numbers is how two views start disagreeing about how big a dot is. ⭐ AND THE LABELS THIN THEMSELVES OUT: two letters drawn on one spot are a smudge, so in a cluster the dots ALL stay and only the overlapping labels are dropped. ⚠ It undoes itself on zoom, which is what makes it safe — a letter is hidden at this magnification, never taken away — and it fails towards showing them, so no map or no origin draws every one | 2026-08-30 | `RM_DOT_R` and its neighbours, `rmThinLabels`, `rmSkyPxPerM`; suite 282 runs the thinner over a cluster, a ridge and an off-screen mark | Standing |
| MR-31 | Told the flag weakens the browser, she asked for it anyway. Where does it go? | **Into a SEPARATE browser that only opens the admin, never her normal Chrome.** *"k fix that"*, after the trade was spelled out — so it is her decision and it is built, and this row supersedes MR-30's "not built" clause. ⚠ THE FLAG CANNOT BE SCOPED TO A SITE — `--auto-accept-this-tab-capture` applies to the whole browser it launched, so ANY page open in that window could photograph the tab unasked. Scoping is done with a separate `--user-data-dir` instead: `C:\Users\lanil\HLU-Admin-Chrome`, launched by `Highlighting Utah Admin.bat` on the Desktop. Her everyday Chrome is untouched and still asks. ⚠ IT ALSO HAD TO BE ITS OWN PROFILE FOR A SECOND REASON: one Chrome process serves every profile on this machine, so a flag added to a shortcut is silently IGNORED whenever Chrome is already running — a separate user-data-dir forces its own process, so the flag is always actually in effect rather than intermittently. ⚠ VERIFIED BEFORE IT WAS BUILT, not assumed: without the flag `getDisplayMedia` never resolved (the prompt sat there); with it, capture started in 781 ms and reported `displaySurface: browser`. ⚠ Cost: one sign-in to the admin in that browser, once. To undo: delete the .bat and the folder | 2026-08-30 | not in the repo — it is a launcher on her machine; the page code is unchanged and still works either way | Standing |
| MR-30 | Can the share-this-tab question be permanently allowed? | **No permanent grant exists — so it is asked once a SESSION instead of once a house.** *"is there anyway i can permanently allow the question it asks."* Chrome has no site permission for screen capture and will not add one on request: a page that could photograph itself unasked could photograph whatever else is in the tab. What IS ours is how long the answer is kept, and it was being thrown away when the tool closed. The stream now lives as long as the admin page does, so one answer covers a morning of quotes. ⚠ The off switch is Chrome's own Stop button, which is the right place for it — that fires `ended` and the next capture asks again. ⚠ And nothing is shared speculatively: the only thing that ever asks is a capture the office pressed for. ⚠ There IS a Chrome launch flag that skips the prompt (`--auto-accept-this-tab-capture`). It was offered with that warning and NOT built — **that last clause is Superseded → #MR-31**, she asked for it the same day | 2026-08-30 | `rmEnsureShotStream`, and the absence of any `rmStopShotStream()` call; suite 282 | Standing |
| MR-29 | The dots were on the picture and still not on the HOUSE. How should the picture be made? | **Photograph the screen.** *"the issue is when it uploads i think the dots remain as code but i want the computer to actually take a picture and move it to the customers picture without downloading it because it all stays in the website."* ⚠ SHE DIAGNOSED IT, and it is why MR-24, MR-27 and MR-28 each fixed something real and none of them landed the dots on the house: every version FETCHED A FRESH PHOTOGRAPH from Google and re-drew the marks onto it from their stored directions, so the dots were only ever as right as the arithmetic lining that new photograph up with the one on screen — panorama, heading, pitch, field of view, aspect, any one of them off and they sit beside the house. There is nothing to line up if the picture IS the screen. `getDisplayMedia` hands the page a live video of its own tab; one frame is cropped to the pane and uploaded. ⚠ Nothing touches the disk, which is the other half of what she asked. ⚠ It costs one permission click per house and there is no way round that — a page cannot photograph itself unasked, by design — so the stream is kept while the tool is open and stopped when it closes. ⚠ The fetched path stays as the fallback for a browser that will not share, and SAYS the dots on that one are worth checking | 2026-08-30 | `rmGrabPane`, `rmEnsureShotStream`, the shot branch of `rmCapture`; suite 282 | Standing |
| MR-27 | The dots STILL were not on the uploaded picture. Should the marks be on by default? | **Yes — ticked by default.** *"when the picture uploads the dots dont go with it which is the whole point."* MR-24 drew the marks but left the box UNTICKED, so the crop screen showed a clean photograph and the clean one is what got attached — which from the office's side looks exactly like *"a different google map entirely just in the same spot"*. ⚠ This SUPERSEDES her 2026-08-25 default (*"I don't want red lines showing here after I'm done measuring"*) and that reasoning is kept, because it is why the CLEAN copy still exists: back then the lines were burnt into the JPEG and poisoned the `original` too. Nothing is burnt in now — the clean photograph IS the picture's `original` and the marks ride along as markup — so unticking the box, or Mark Up → Clear → Save, still gives the clean house back | 2026-08-30 | `rmCrop.lines: true`, the `checked` attribute on `#rmCropLines`, `rmReset`; suite 282 | Standing |
| MR-28 | Is the captured street picture the same photograph the dots were placed on? | **It is now — it is asked for by panorama id.** Her diagnosis: *"when you go to capture street view its a different google map entirely just in the same spot."* `location=` asks the static service to find the NEAREST panorama to a point, and the arrow keys walk between panoramas — a mark is a direction from ONE camera and means nothing from the next one down the street. It also asked for a field of view the service silently caps at 120, so a zoomed-out pane got a 120° photograph with every mark laid out for 180°. Both closed, and a picture with none of her marks on it now SAYS so on the crop screen instead of looking like a tidy photograph | 2026-08-30 | `pano=` in `rmCapture`, `rmCaptureFov`/`rmZoomForFov`, `rmMarkTallyText`; suite 282 runs the fov round trip and the sentence builder | Standing |
| MR-24 | Should the dots you placed be on the attached picture? | **Yes — and the photograph underneath still stays clean.** *"make sure the placed dots moves with it."* Only `rmRuns` was ever drawn onto a capture, and a street mark stopped being a run on 2026-08-28, so a whole dotted elevation could be attached with nothing on it. ⚠ This does NOT supersede her 2026-08-25 ruling (*"I don't want red lines showing here after I'm done measuring"* — which has no row of its own; it is recorded only in `CLAUDE.md` and in the code): TWO copies are attached — the marked one is what shows, the untouched one is the photo's `original` — and the marks also ride along as ordinary markup shapes, so Mark Up → Clear → Save gives the clean house back | 2026-08-29 | `rmCaptureMarks`, `rmCaptureShapes`, the two uploads in `rmAttachShots`; suite 282 runs the projections and the crop fractions | Standing |
| MR-25 | Where do the feet and the price go? | **Onto the quote AND onto the customer behind it.** *"also make it so the feet and price attaches to the customer as well so that automatically goes in the system."* `measuredFeet`, `housePrice` and `numberOfBins` are written to the linked customer and the bill is re-summed through `syncPayerInvoice` (the payer's bill where the house bills elsewhere). ⚠ ONLY where the quote carries a `convertedToCustomerId` or `existingCustomerId` — guessing a customer by name or phone is how a book gets duplicates. ⚠ And the customer NUMBER is reported when it no longer fits the footage, never rewritten: a number already given out is on a bin in the warehouse | 2026-08-29 | `rmPushToCustomer`, called by Attach and by all three Save buttons; suite 282 | Standing |
| MR-26 | When should the tool close? | **By itself, once everything has attached.** *"after everything happens it should close it automatically after everything attaches."* Which is why one press has to BE everything — closing on a press that only did the picture hides the half that did not happen. It closes AFTER the numbers, and a partly failed upload reports instead of closing | 2026-08-29 | the tail of `rmAttachShots`; suite 282 asserts the ORDER, not just the close | Standing |
| QT-21 | An existing member approves a re-quote and is never shown the details form, so the card sits in Awaiting Response. Where should it go? | **Straight to the schedule if nothing about the house changed; to the warehouse if it did, and then not scheduled for 48 hours.** *"Okay this is a problem and as long as they kept details the same that should just be put in schedule after we price them but if they did get changes on houses than that gets sent to warehouse and isnt scheduled until 48 hours after sent to warehouse."* ⚠ WHY IT IS STUCK TODAY: `quoteStage` returns the Ready-to-Convert folder only on approvalStatus approved AND either formCompleted or approvedByOffice, and a member is deliberately never shown the install-details form — they get "anything changing this year?" and then "Perfect, you're all set", which sets nothing. So nothing marks them done and the office must press Mark Approved by hand. Measured by RUNNING quoteStage over the real record shapes, not read. ⭐ HER RULE SPLITS ON WHETHER GLASS CHANGES, which is the same split the re-quote apply popup already makes (`requoteBuildChoice`: nothing / top-up / recycle-and-rebuild) — a price-only re-quote queues no build, and that is exactly her "kept details the same". ⚠ AND THE 48 HOURS IS THE EXISTING LOCK, NOT A NEW ONE: `lightsLockedUntil` already holds a house off the routes for 48 hours after a charged colour change, and the route sweep already honours it. This says it must ALSO start when a re-quote sends work to the warehouse. ⚠ DO NOT reuse `formCompleted` to fake it — that field means the customer filled the form in, and writing it when they did not would make the Ready-to-Convert folder lie about where the details came from. | 2026-08-31 | `quoteStage`, `quoteMemberKeptDetails`, `scheduleHoldUntil`; schedule-hold.test.js | Superseded → #QT-22 |
| QT-22 | How long is a house held off the schedule after it is sent to the warehouse, and on what clock? | **72 hours, counting working days only.** *"Okay lets do 72 hours instead of 48 hours then. And this is only bussiness hours not counting weekends."* Asked which of two readings she meant, she chose **three working days** — the clock runs all 24 hours of Mon-Fri and pauses at weekends, over **72 working HOURS** (~8 working days), which in October would be most of the season. She also chose to pause it on **Thanksgiving**, which comes free from reusing `isWorkingDay` — the one rule the crew calendar already uses. So Friday 2pm lands Wednesday 2pm. ⚠ SUPERSEDES QT-21's 48 hours, and widens it: she said "requote is same thing", so the hold now starts wherever a build is NEWLY queued — a re-quote topping up, a re-quote rebuilding, or a colour, wire or timer change — not in the re-quote branch alone. ⭐ AND IT IS A SEPARATE FIELD FROM `lightsLockedUntil` ON PURPOSE: that one decides whether a colour change is FREE (`withinFreeWindow = lockedUntil > now`), so stretching it to 72 would quietly widen the fee-free window and charge the $30 less often — a money change she never asked for. The two also now have different reasons: the old 48 was "so they can change their lights again", this is "so the bundle exists before a crew is sent". ⚠ AND SHE CONSIDERED GATING ON THE WAREHOUSE MARKING IT DONE INSTEAD — better in principle, since the bundle existing is the real fact — and chose the timer after being told the cost: a house the warehouse never ticks off would never be scheduled, silently, whereas a timer always expires. | 2026-08-31 | `scheduleHoldUntil`, `isHeldFromRoutes`; schedule-hold.test.js | Standing |

Last updated: 2026-09-04 — REF-01 to REF-06 named here for the first time: they landed on main with the referral-link work and were never written into this line, so it read as though nothing had been decided about a member's own link or the $25 that follows it. The rows themselves were always there; only this summary was short. 2026-09-03 — SCH-44, SCH-45 and SCH-46 added (the schedule reads the forecast and will not send a crew to a town at 35° or below while somewhere warmer wants hanging; a house the crew did not reach goes to the front of its own tier; and the office can move one customer up by hand, ahead of new hangs, without moving their month). SCH-46 records a DELEGATED placement — "ill let you decide" — so it is the row to change if that reading is wrong; MON-63 added (the set-up fee is $25, from one place instead of twelve); MON-62 added (a charge carried to next season shows under their fees, and can be waived); 2026-09-03 — MON-59 and MON-60 merged in from a parallel session and renumbered (their MON-58/MON-59 collided with MON-58 already on main): the not-billed-yet notice became a pop-up, then was scoped to the RSVP Approve flow alone; MON-57 keeps a forward pointer to both. MON-58 added (a Confirmed customer who owes from last season stays held: money wins, and the arrears test already sits ahead of the confirmed one); RS-46 added (one RSVP stamp per row — the yellow season badge — and the season is switched inside the customer, not from the row); MSG-08 added (a folder that messages are filed into automatically cannot be deleted); RS-45 added (the two test builders make the two kinds of customer, so the RSVP can be tried on an old one); RS-44 added (only a customer who already has a gate code is asked about one; narrows RS-29); RS-43 added (one RSVP control in Edit Customer, not two — the radio that was overruling the dropdown is gone); MON-57 added (the portal says when a bill is due); MON-56 added (the × is on discounts as well as fees); MON-55 added (waiving last season's carried debt); MON-54 added (how waiving a late invoice fee works); MSG-07 added (cancellations and the member portal get folders, and the System tab gets sections); RS-42 added (the office speaks the RSVP's own three answers); RS-41 added (the top-up fills a template somebody left empty); RS-40 added (paying in the portal moves the office screen, and asks them to RSVP on the spot); RS-39 added (the unpaid-last-season email does not send itself); RS-38 added (why the Sides tab drew an empty card); RS-37 added (what the office sends somebody who owes and has not RSVP'd); RS-36 added (an unpaid customer is held out of the portal until they pay); RS-35 added (the gate code is asked as a pop-up inside the member portal); 2026-09-02 — MON-53 added (an RSVP link to text, for everyone with no email); 2026-09-01 — RS-32 added (the invisible outline button on the RSVP cards, which also blocked reporting a changed gate code); RS-31 added (a yes goes straight into the member portal; the gate-code step stays, RS-29 reaffirmed); MSG-06 added (the daily route digest folds away in the System tab, and the nav badge stops counting it); SCH-39 added (Recalculate stops a record claiming a day it no longer has); SCH-38 added (a plan row that is not a customer comes off the season); MON-49 added (Confirmed means they replied again; MON-47 superseded); MON-48 added (filter All Customers by the season badge); 2026-08-31 — MON-47 added (the Confirmed badge is the gate, and an unanswered RSVP stops deciding while she tests); MON-46 added (assume the 2025 season, and centre the arrears box on it; supersedes MON-45); MON-45 added (an Unpaid <year> tag under the season badge); MR-39 added (opening the tool forgets the last house); SCH-37 added (a street in a town box built a crew-day and the sweep fought it every fifteen minutes); QT-21 added (a re-quote an existing member approves goes straight on, without the details form) — MR-38 added (the dots and the peaks are saved, and a peak never outlives its dots); MR-37 added (the quote gets the street photograph, never the aerial); MR-36 added (the sum was right, the number was unreadable); MR-35 added (both pictures, half and half); MR-34 added (a peak's pitch is measured in the world, or typed); MR-33 added (Recentre inside the pane, so full screen has it); MR-32 added (mark sizes, and labels that thin out); MR-31 added (the flag, in its own browser; supersedes MR-30's "not built" clause); MR-30 added (asked once a session); MR-29 added (the capture is a photograph of the screen); MR-27 supersedes the 2026-08-25 clean-capture default. QT-22 added (the schedule hold is 72 hours on working days only, and is a separate field from the free-colour-change window so a scheduling change cannot quietly alter what customers pay); QT-21 superseded by it.
