# Operating protocol

Read this every session. It is short on purpose. The numbered domain rules live in
`docs/RULES.md` — read that before any change that touches data, options, money,
or a destructive operation.

---

## The one-line version

**Never guess, never silently choose, never leave a field unconnected.**
When two rules disagree, or a rule is missing, stop and ask.

---

## 1. Before finishing any change

Check it against `docs/RULES.md`, `docs/data-map.md`, and `js/options.js`.

If the change adds or modifies a field, option, collection, or state, verify all three:

1. something **writes** it
2. something **reads** it
3. it is **declared** — in the option registry if customer-facing, in the map otherwise

If any of the three is missing, stop and ask.

**A field in the code but not in the map is an error, not a detail.**

---

## 2. Precedence — when rules collide

Rules will eventually contradict each other. Resolve in this order, highest first:

| | Tier | Beats everything below it |
|---|---|---|
| 1 | **Customer correctness** | Nobody gets something they didn't order, or misses something they did |
| 2 | **Irreversibility** | Nothing destructive runs without a preview and a confirmation |
| 3 | **Data integrity** | No orphans, no dead ends, no silent drift |
| 4 | **Consistency** | One source of truth, generated not hand-written |
| 5 | **Convenience** | Everything else |

**Two rules in the same tier that contradict: STOP AND ASK.** Do not pick one.
Do not pick the newer one, the more specific one, or the one that's easier.
A same-tier contradiction means the rulebook is wrong, and only Addie can fix it.

When it happens, say exactly this much:

```
RULE CONFLICT — R-004 vs R-011
  R-004 says business constants live in exactly one file.
  R-011 says generation beats verification.
  Here, generating the pricing table would duplicate the constants.
  Both are tier 4. Which wins here, and should the rulebook record the exception?
```

Then wait. The answer becomes an amendment to `docs/RULES.md`.

---

## 3. When to ask

**Ask when:**

- a new field has no consumer, or a new record type has no parent
- a state transition has no exit
- a form saves something the map doesn't know about
- two rules contradict (section 2)
- the change touches **Start New Season, the payment ledger, or nightly billing**
- a customer-facing option would reach fewer than all six destinations
- you're about to do something no rule covers and the wrong choice would be expensive

**Don't ask about:** styling, copy, layout, or anything with no data path.
**Don't ask what you can read.** If the code answers it, read the code.

**How to ask:** one consolidated batch at the end of a change, not three
interruptions mid-work. Every question cites the rule, map, or registry entry
it comes from — *"the map says an approved quote produces a customer, and this
path doesn't"* — never generic prompting.

---

## 4. Never guess

If it can't be resolved from the code, append it to `docs/open-questions.md`.

- **intent** — only Addie can answer (business rules, policy, what *should* happen)
- **factual** — resolvable by reading code, so resolve it; it should never reach her

**An unanswered `intent` question on a customer-facing data path blocks the change.**
Do not ship around it. Do not pick a default and note it. The whole point of this
system is that guessing is how holes get made.

Every answer becomes a map, registry, or rule entry — so it's never asked twice.

---

## 5. Proposing new rules

This is expected work, not an interruption. Propose a rule when:

- **the same decision gets made three times** the same way — that's a rule nobody wrote down
- **a near-miss happens** — something almost shipped wrong, and a rule would have caught it
- **an answered question generalizes** — Addie's answer applies beyond the case that prompted it
- **you find yourself wanting to ask** something a rule could have answered

Format, appended to the `## Proposed` section of `docs/RULES.md`:

```
### P-00X · proposed 2026-08-21
Rule: <one sentence, testable, in the imperative>
Why: <what prompted it — the actual incident or repetition>
Would have caught: <the specific failure>
Might wrongly block: <the honest cost — every rule has one>
Enforcement: code | hook | read
Tier: 1–5
```

Proposed rules are **not active.** Do not follow them until Addie moves them into
the numbered set. If she rejects one, it stays in the file marked rejected with
the reason, so it doesn't get re-proposed.

Do not propose more than three at a time. A flood gets ignored, which is worse
than proposing nothing.

---

## 6. Rule promotion

Every rule declares how it's enforced:

- **code** — a test or audit fails. Cannot be forgotten.
- **hook** — a hook checks it on stop. Reliable.
- **read** — Claude must read and honor it. **Drifts.**

**A `read` rule that gets violated twice must be promoted to `code` or `hook`.**
Note the second violation, propose the promotion, and say what would enforce it.

Text alone is not enforcement. It's a reminder that decays over a long session.

---

## 7. Amendments

Any change to `docs/RULES.md` — new rule, exception, reorder, retirement — gets
logged at the bottom of that file with the date and the reason. Never silently
edit a rule. The history is what makes the rulebook trustworthy.Small, explained commits. One logical change per commit, message in plain English.
Work on the branch for the area you are touching. There is a standing branch per part of the admin portal (created 2026-08-13), so parallel work — the owner's and her sister's included — doesn't pile onto one file:
  tab/customers   Add a Customer, All Customers, Bulk Updates, Danger Zone
  tab/quotes      Quote Requests: pricing, Send Email, convert, nudges, quote email settings
  tab/messages    Customer Messages: inbox, folders, System notices
  tab/routes      Routes and Schedule: generate, save, print, fix and removal routes, calendar
  tab/warehouse   Warehouse and Customer Numbers: build queue, recycle, bins, number pool
  tab/invoices    Invoices, Dashboard finance, Per Foot Pricing — anything touching money
  tab/automation  Automation and Automation Emails: nightly invoicing, templates, SMS, weather
  tab/employees   Responsibilities and Time Logs: staff, crews, timecards, payroll export
  tab/website     Public site content: Reviews, Gallery, Hero Images, FAQ, Site Settings
Bring the branch up to date from main first (git merge main), work, run the gates, then open a PR against main and merge it — ⚠ main is protected and rejects a direct push, see §1. These branches are long-lived — don't delete them after merging. Anything that doesn't fit one of them, or spans several, goes straight to main or onto a one-off claude/... branch.
Keep the Project To-Do checklist truthful. Any change that alters what a checklist test (TEST_SEED, in js/test-seed.js since 2026-08-14 — it used to be inline in admin.html) describes — a renamed button, a moved feature, changed behavior — needs that test's wording fixed and its version number bumped in the same change, or it silently goes stale (see §2 and §7). When a change retires a UI term a test used to describe (a renamed button, a removed label), add it to RETIRED_CHECKLIST_TERMS in run-all.js in the same commit — that's what actually fails gate B if this rule gets skipped, not just this sentence.

⭐ MINIMIZE MANUAL TESTING — the checklist is only for what a human or a live environment must verify (owner's instruction, 2026-08-17: "update testing, it should minimize since you are testing too"). On 2026-08-17 the seed was trimmed from 210 to 108 (⚠ IT IS **125** AS OF 2026-08-25 — measure it, `node -e` over TEST_SEED, do not trust this sentence; it said 108 for eight days while rows were being added): every row the automated suite already proves — the invoice/credit/fee maths, number & bundle logic, season reset, schedule capacity, quote/route data-flow, sync logic, and the five Member Portal flows the Playwright specs now cover — was removed. What stays is real PayPal/email/Twilio/cron, printed-sheet and mobile appearance, the crew-phone workflow, live Firebase permissions, and the admin CRUD the suite barely touches. Two standing rules follow from this:
  - ⭐ Do NOT bump a test's version to force a Retest for behaviour the automated suite already covers and that your run passes — that is exactly the double work the owner asked to stop. Reserve the version-bump→Retest path for tests only a human or a live environment can confirm. (The suite passing IS the sign-off for the covered ones.)
  - ⭐ When you add coverage, add it to the automated suite (run-all.js / money-parity / a Playwright spec), NOT as a new manual checklist row — unless the thing genuinely can't be automated (a real charge, a real email, a physical crew step, a visual/print check). A change that retires a feature removes its seed row; the sync now deletes a removed row on next login whatever its score (see projShouldPruneTest, changed 2026-08-17 — the seed is the single source of truth for the seeded range), so removing a row is deliberate: only do it when the test is truly retired or genuinely covered.
⭐ THREE ROWS ADDED 2026-08-25 (215, 216, 217), and each one is on the list because it CANNOT be automated, not because it is important. §0's rule was applied first and it removed most candidates: the fix-sheet column, the bin counts, the build badges and the colour rules are all proved by the suite on every run, so none of them got a row.
  - **215 · Switch the season to answered-Yes-only.** The mechanism is proved by `npm run test:season`; what is left is the DECISION — whether enough people have replied — which no test can make. It also asks her to try the switch BEFORE the RSVP is marked sent and confirm it refuses, because that guard is the one standing between one click and an empty season.
  - **216 · Read the options list and say what is missing.** The only detector for an option we sell and never wrote down. A missing row cannot fail an audit — every check compares a DECLARED option against where it must reach — so this is un-automatable by construction, permanently. Reads `docs/what-we-can-record.md`.
  - **217 · Get the soft-light houses switched before the list is lost.** Physical work with a real deadline: soft now counts as Warm White everywhere, the houses stay findable because `allCustFilterLights` reads the RAW record, but a master-sheet sync that rewrites one of those rows destroys that house's place on the list for good. The row's job is to get the list written down OUTSIDE the system before that happens.
  ⚠ AND THE COUNT IN THIS FILE WAS EIGHT DAYS STALE when they were added — it said 108 while the seed held 122. Measure it.
Don't stop to ask unless you truly have to — see §5 for the short list of things that genuinely need the owner. Default to doing the work on a branch and reporting it.
1. Stack & deploy commands (your automation primitives)
Three static HTML files + Firebase backend + Netlify hosting. No build step for the HTML.
File	What it is
index.html	Public site + the customer Member Portal
admin.html	Office dashboard (~1.5MB; two inline scripts — a plain one and a <script type="module">, which is the app itself). ⚠ Panels load their own data when opened — see §2, "Panel data loads on open". Do not add a new load*() to initData() without reading that first.
js/test-seed.js	The Project To-Do checklist's manual tests (TEST_SEED) — **125 on 2026-08-25**, and ⚠ MEASURE IT rather than believing this line, which sat at 108 for eight days while rows were added — trimmed from 210 on 2026-08-17 to only what a human/live environment must verify (see §0); moved out of admin.html on 2026-08-14. Loaded by a dynamic import() inside runProjectTestSync, so it is fetched only when the checklist syncs (right after login), never blocks the dashboard rendering, and is cached separately from the page — an admin.html change no longer re-sends 217KB of test list. ⚠ A failed fetch is caught and reported on screen; do not remove that catch, because the only caller swallows errors and the checklist would silently stop syncing.
⚠ The Nudge quote email has FOUR senders and two renderers. The quote card, the "Nudge everyone shown" bulk button and Automation Emails → Preview & Send all render it in the browser through `applyQuotePhotoBlock` in admin.html; the nightly automatic nudge renders it on the server, inline in `runQuoteNudgeBatch` in functions/index.js. Both place `{{photo}}` where the template asks for it. Change one and change the other in the same push — Suite 33 of run-all.js runs both against the real shipped Nudge body and fails if they disagree. Until 2026-08-17 the bulk button and Automation Emails skipped the browser renderer entirely and mailed customers a literal "{{photo}}".
⭐ ONE set of approve/maybe/decline buttons, however many photos (owner's instruction, 2026-08-17: "just one approved, Maybe later, Decline"). Both renderers used to repeat the three buttons on the far side of a stack of two or more photos, so "Approve" was never far down a phone — `repeatQuoteButtonsServer` on the server and a matching block in admin.html. The owner read it as a mistake and both are gone. Do NOT reintroduce it as a fix for a long email: it was deliberate once and was deliberately removed. Suite 33 fails if a second set comes back on either side.
js/money.js	The invoice and bin rules, pulled out of admin.html so they can be unit-tested (computeInvoiceStatus, statusClass, enrollmentYearOf, custInvoiceKey, cnBinsForFeet, fmtMoney, CN_DOUBLE_BIN_FEET). Imported by admin.html's module script — native browser modules, still no build step. ⚠ functions/index.js keeps its own copy of the invoice maths for the nightly run: change a rule in one and change it in the other, in the same push. The SAME applies to `applyLightChange` / `applyLightChangeServer` (the colour-change fee, the 48-hour window and where the $30 lands), added 2026-08-21 — `money-parity.test.js` runs both copies of BOTH rules side by side and fails the build the moment they disagree.
employee.html	Crew/Warehouse Portal
functions/index.js	Cloud Functions (Firebase v2, Blaze). Node 22 runtime, firebase-functions 7.x, firebase-admin 13.x as of 2026-08-13. ⚠ Do NOT bump firebase-admin to 14 as a routine update: 14 removes the namespaced API (admin.firestore, admin.auth are undefined there) and this file uses it in ~30 places, including admin.firestore.FieldValue.increment on deposits and tips in the PayPal capture path. That migration to the modular API is its own job with its own testing. Keep engines.node in functions/package.json matching node-version in .github/workflows/deploy-functions.yml.
firestore.rules / firestore.indexes.json	DB security + indexes
run-all.js	Main test suite (see §3) — needs `npm install` once at repo root (installs jsdom; package.json/package-lock.json are committed). Run it with `npm test`, which also runs the two below.
money-parity.test.js	Proves the browser and server copies of the invoice maths still agree. See §9.2 — this is the one that stops the office and the nightly billing run disagreeing about a bill.
verify-syntax.js	Verification gate A (inline JS parses, <div> tags balance). `npm run verify`. Lives at the REPO ROOT alongside run-all.js, not in a scripts/ folder.
selector-contract.test.js	Checks every #id a browser spec uses still exists in the page it drives. `npm run test:selectors`. No browser needed — see §9.3.
⭐ THE WAREHOUSE BUILD SHEET IS ONE PAGE PER COLOUR GROUP (2026-08-24). Addie chose this over splitting by badge. A colour-and-wire group IS the pile somebody pulls from, so two people can build at once without sharing paper; splitting by BADGE cuts across the piles and sends each person to every shelf.
  ⚠ THE PAGE ORDER IS DECIDED BY `whSheetRowsForBuild`, NOT BY THE PAGER. It emits blocked rows before any group, so walking its rows in order already puts "Waiting on light colours" first and keeps every other page in the tab's order. A sort in the pager was written and REMOVED: it could never fire, and a red-check proved it.
  ⚠ EACH PAGE COUNTS ITSELF, not the morning — a page handed to somebody building one pile needs THEIR numbers — and ends "sheet X of Y", because once a stack is handed out the one thing nobody can tell from a single page is whether they hold all of them. ⚠ parseInt, NOT Number: the Bundles cell carries "+3" and "3 est" markers and Number("3 est") is NaN, which silently drops that house.
  ⚠ `whOpenPrintWindow` TAKES SECTIONS NOW, an array of {title, summary, tableHtml}, one per page. The recycle sheet passes an array of one rather than keeping a second code path alive for the old shape. ⚠ THE PRINTING TAB'S SHEET IS NOT SPLIT — it keeps its own columns and its own single-table shape, because merging the two sheets drops the extra-bins note (see the entry below). 9 sabotages red-checked.
⭐ EVERY SHEET SAYS HOW MANY BINS (2026-08-25). Addie: "Everyone needs to know how many bins there are for each house so bin # and costumer # does matter."
  ⚠ THIS REVERSES HER OWN TRIMMING OF 2026-08-24, and the old reasoning is kept because it was sound at the time: "all warehouse people should know 5000 means 2 bins so not necessary to put how many bins on there", then "just put how many bins will be needed if it is more than 2 bins." That made the count CONDITIONAL (`printExtraBinsNote`, only past two bins), so a house on a REGULAR number said nothing at all and reading the sheet meant knowing the 5000 rule.
  ⭐ TWO SHEETS GAINED A Bins COLUMN: the Printing tab's build sheet and the RECYCLE sheet. The crew sheet and the Warehouse tab already had one. ⚠ The recycle one matters on its own — somebody sent to fetch a two-bin house was told WHICH bin to look for and never how many were out there, so it came back half collected.
  ⚠ TWO DIFFERENT NUMBERS ON ONE RECYCLE ROW, DELIBERATELY, and the headings spell it out: "Bin # to find" is the number PAINTED ON THE BOX, "Bins" is HOW MANY of them. That collision has already put a wrong column on a sheet once.
  ⚠ `whBinsForHouse` IS THE ONE ANSWER — the same count the crew sheet and both build sheets read. A second way of counting bins is how two sheets start disagreeing about one house; a red-check that gave the printed sheet its own arithmetic is caught.
  ⚠ AND `printExtraBinsNote` IS GONE, not left as a helper nothing calls — a column and a note saying the same thing on one row is noise, and the audit block that used to test the note says so in as many words. ⚠ A COLUMN CHECK PROVES ONLY THE HEADING: a red-check deleting the recycle row's cell went straight through until a fill check was added beside it. 6 sabotages red-checked.
⭐ THE TWO BUILD SHEETS LIST THE SAME PEOPLE, AND KEEP DIFFERENT COLUMNS (2026-08-24). Addie: "everyone on the warehouse tab should be printed."
  ⚠ THE PRINTED LIST WAS A SECOND OPINION AND IT KEPT PEOPLE FOR EVER. `printNeedsBuildList` matched `needsLightBuild || chargeNewMemberFee === true || requoteAppliedAt` while the tab asks `needsLightBuild` alone — and the other two are STAMPS, not flags: the warehouse clears `needsLightBuild` when the bundle is made, nothing ever clears the other two. So every new member and every applied re-quote stayed on the printed Needs Building sheet months after their bundle was built, and anybody working off that paper makes a second set for a house that already has one. One flag now, the same one the tab asks.
  ⚠ BUT THE COLUMNS STAY DIFFERENT ON PURPOSE, AND THAT IS NOT DRIFT. She trimmed the printed sheet herself, so bins ride inside the bundles cell there (`printExtraBinsNote` — "just put how many bins will be needed if it is more than 2 bins") and have a column of their own on the tab. ⚠ DO NOT "tidy" this by merging the two sheets into one: that was tried on 2026-08-24 and REVERTED, because it silently dropped the extra-bins note — main's options-audit caught it. Only WHO is on the two lists has to agree; `build-reason.test.js` asserts that, and asserts the note survives.
build-reason.test.js	⭐ WHY A BUNDLE IS BEING BUILT (added 2026-08-24). `npm run test:reason`. Its own file per R-018. Addie: "There should be a badge by each person on warehouse that say's new, Old-Rebuild or Member Poral or Request" — NEW is a new quote, OLD-REBUILD an existing member who moved or extended their house, MEMBER PORTAL a change they made themselves, REQUEST one we typed in after a call, email or text. The badge leads the chips in `whHouseFactsHtml`, the ONE function the build groups, the blocked block and the add-on rows all call.
  ⚠ NOTHING RECORDED WHERE A COLOUR CHANGE CAME FROM, and that had to be built first. Two of her four badges are the same event from opposite directions, and the portal and the office both wrote `lightsChangedAt` and nothing else — so the badge would have rendered one answer for everybody and looked authoritative doing it. `lightsChangedVia` is stamped at all THREE write sites: `portal` on both of `portalSave`'s writes, `office` on the Edit Customer save. Change one, change the others.
  ⚠ AND `requoteKind` LIVES ON THE QUOTE, NOT THE CUSTOMER. The badge reads it off the customer record, which had no way to reach it — so a house that MOVED and a price that was CORRECTED were indistinguishable. It is copied across in the same write as `requoteAppliedAt` when the re-quote is applied, taken from `quotesCache`, never re-derived by comparing addresses (the guess `showApplyRequoteChoice` was deliberately moved off on 2026-08-21). A blank kind writes NOTHING, because a blank stored where an answer goes reads as an answer.
  ⚠ THE ORDER IS THE RULE, most-work-first: NEW → OLD-REBUILD → MEMBER PORTAL / REQUEST. Somebody who moved AND picked new colours is an OLD-REBUILD; reading it as a colour change sends the warehouse to make one bundle for a house that needs the lot.
  ⚠ AND THERE IS A FIFTH BADGE SHE DID NOT ASK FOR: CHANGED. Every colour change made before 2026-08-24 carries no source, so those say CHANGED rather than being assigned one of her four on a coin toss. Do not "tidy" it away by defaulting to one of the real four.
  ⚠ A PRICE-ONLY RE-QUOTE GETS NO BADGE. It changes no glass and queues no build, so badging it OLD-REBUILD would describe work nobody is doing.
  ⭐ AND THE BADGE IS ON PAPER TOO. Addie: "I need paper to carry badge too." `whBuildReasonLabel` renders it in words, sharing `whBuildReasonKey` with the chip — one rule, two renderers, because a coloured pill and a black-on-white sheet cannot share a renderer but must never make different claims. It is a **Why** column on BOTH build sheets, beside Type on the warehouse tab's: Type says WHAT the row is, Why says where it came from. ⚠ Blocked rows KEEP their badge — those are the ones somebody has to chase. ⚠ Buffer stock carries NONE: no customer, so no claim. 18 sabotages red-checked.
fix-sheet.test.js	⭐ WHAT THE CREW IS TOLD IS WRONG (added 2026-08-25). `npm run test:fix`. Its own file per R-018. Addie, 2026-08-21: "were not using the employee portal this year... we are only printing on schedules and warehouse."
  ⚠ THAT RULING RESCUED THE GATE CODE, THE SIDES AND THE OUTLET INSTRUCTION onto paper, because all three had only ever lived in the crew portal. THE FIX NOTE AND THE FIX PHOTO WERE IN THE SAME POSITION AND WERE MISSED, so a Fixer Route sheet named the houses and said NOTHING about what was broken at any of them. The office types "two strands out on the front peak" into `fixNote` and photographs the fault; both render on the Routes SCREEN, which nobody in a van is looking at. The crew drove out holding a list of addresses and had to ring in or guess.
  ⭐ A **What's wrong** COLUMN, and the fault photo in its OWN block under its OWN heading. Never merged with the new hangs' block: one heading reading "New hangs on this sheet" over a close-up of a dead strand is the heading lying about half its contents, and the two answer opposite questions — a new hang's photo says THIS IS THE HOUSE, a fix photo says THIS IS THE PROBLEM. Both can legitimately appear on one sheet (a new member can need a fix), which is exactly why they must not share a caption.
  ⚠ A FLAGGED HOUSE WITH NO NOTE PRINTS "?", NEVER BLANK. Blank reads as nothing-wrong-here on a row that is on the sheet BECAUSE something is, and the crew skips it. "?" is this sheet's own established word for nobody-said (`printYesNo`, and the master sheet's own Up Plug column) — not a second vocabulary.
  ⚠ BOTH GATED ON `needsFix`, NOT ON THE NOTE OR THE PHOTO ALONE. `hlxMarkJobDone` clears the flag and the note together, so a leftover note or photo on a mended house would send somebody back to it.
  ⚠ THE COLUMN IS DECIDED BY THE ROWS, NOT BY `day.isFixRoute` — `printCrewColumns` never reads a day at all, and a check asserts that. A house flagged for a fix can sit on an ordinary install day, and the crew standing in front of it should still be told; testing the day kind would miss exactly that. It also means no empty column on ~950 install rows, which is the owner's own Notes-column argument about width and the photo note's argument about ink.
  ⚠ THERE ARE TWO CREW-SHEET PRINTERS PLUS A THIRD TABLE, and the bins work the day before proved what happens when only one is guarded — a red-check deleting the column from the OTHER build sheet passed, because the only check written was about `PRINT_COLUMNS`. `printDaySheet` (whose in-neither-crew's-city block is the third table), `printCrewSheetPage` (a crew's own sheet, and Print Today). Each is named in the gate.
  ⚠ AND EVERY COLUMN IS CHECKED TWICE — that it appears, and that a record FILLS it. A column check proves only the heading; that is the miss from 2026-08-25 recorded in the bins entry above. 16 sabotages red-checked, three of which did not apply on the first pass because the script used `\n` against a CRLF file — the §7 trap, in the red-check itself.
warehouse-colours.test.js	⭐ WHAT COLOUR A HOUSE IS (added 2026-08-24). `npm run test:colours`. Its own file per R-018. Three faults in one afternoon, all the same shape — something reading ONE of the two fields a colour lives in, or ONE of the two vocabularies it can be spelled in. Every one was invisible from the source and obvious the moment both sides were RUN against the same input.
  ⭐ COLOURS LIVE IN TWO FIELDS AND EVERY READER MUST READ BOTH. `rbDetectColorsAndPattern`, which the master-sheet sync writes through, only fills `lightsDescription` when a colour REPEATS — a repeat means an alternating pattern where the ORDER matters. An ordinary house comes back with the colours in `lightColors` and the description EMPTY. Four warehouse readers tested the description alone, so every ordinary house the sync added was called blocked, had NO BULBS ordered for it (`computeColorDemand` — the costly one), and was left out of the pending count. `houseLightsText` is the one answer and all five callers ask it, `printLightColor` too. ⚠ THE DESCRIPTION WINS WHERE THERE IS ONE, because it carries the order and the list does not. ⚠ A FIXTURE MUST BE SHAPED THE WAY THE SPLITTER REALLY WRITES ONE — one carrying both fields passes whether the fix is there or not, so the gate RUNS the splitter and feeds its own output in.
  ⭐ ONE VOCABULARY, TWO NORMALISERS. The import read `RB_COLOR_ALIASES` (ww, w, warm, pure, p, pw, r, rr); `whColorsFromWords`, which decides the warehouse GROUP, knew only the nine full names — so "ww" and "Warm White" were two piles for one build. `WH_COLOR_WORDS` is built once from both. ⚠ LONGEST MATCH FIRST is load-bearing. ⚠ The unknown-word guard is unchanged and is the point: one unknown word and the text is kept exactly as typed, as its own heading somebody can correct.
  ⭐ HER RULINGS, 2026-08-24 — do not change one without her: a repeated single letter is a COUNT (`rr` is two reds, `bbb` three blues), but WW and PW are INITIALS and must not count up; `rrgg` reads the same as `rr/gg` (`rbLetterRun`, r/g/b/o ONLY — w and p are excluded because `wwrr` is genuinely ambiguous); anything multi-something is Multi (`rbLooksMulti`, a rule not table rows); `pur` → Pure White (NOT Purple — the one I would have got wrong); `clear`/`cool white`/`bright white` → Pure White; `orng`, `pnk`, `blu`, `grn`, `rainbow` → Multi; plurals → the singular. ⚠ `mc` is SKIPPED for good: "just skip mc".
  ⭐ SOFT IS WARM WHITE, reversing her 2026-08-19 ruling — she was asked and told the cost. ⚠ THE SWITCHING LIST SURVIVES: `allCustFilterLights` matches /soft/i against the RAW record, never through the table, and the two colour checkboxes are untouched. ⚠ THE ONE REAL LOSS: a sheet sync that rewrites one of those rows replaces the label and that house drops off the list for good. ⚠ `soft(recycled)` is a KEY in its own right, or the note reader turns it into "Warm White (recycled)" — its own heading, which would not merge.
  ⭐ A SET SORTS, A STRAND KEEPS ITS ORDER (`whOrderColors`). Since rr means two reds, order carries information: rrgg and rgrg are the same four bulbs and two different builds, and sorting flattened both to one heading. ⚠ AND NORMALISING IS IDEMPOTENT — `whSplitAllKnown` tries the whole description before treating a trailing bracket as a note, so "Red, soft(recycled)" merges while "Warm White (every third bulb)" is left alone.
  ⚠ employee.html CARRIES ITS OWN COPY OF ALL OF IT and had none of the aliases, so the crew screen and the office grouped the same house differently. run-all.js compares all six pieces line for line and this gate RUNS the crew copy against the office one. 16 sabotages red-checked.
docs/what-we-can-record.md + .html	⭐ THE LIST ADDIE READS TO FIND WHAT IS MISSING (added 2026-08-25). Generated by `node docs/build-what-we-can-record.js` from `js/options.js` — never hand-edited. It answers the one question no check in this repo can: **is anything sold today that has no row at all?** Every gate here works by comparing a DECLARED option against the artifacts it must reach, so an option nobody ever declared cannot fail anything. It is invisible by construction, permanently, and the only detector is somebody who knows the business reading the list.
  ⚠ ONLY THE PROSE IS WRITTEN BY HAND. Which options exist, what each is called, what sets the price and where each answer has to reach are read out of the registry every run — "how many feet of roofline you are lighting" is the only part a machine cannot derive.
  ⚠ AND IT REFUSES RATHER THAN DRIFTING. Add or remove an option in the registry and the generator STOPS, naming the line to write. A list that silently omitted a real option would read as a complete answer to the exact question it exists to help her ask — worse than no list.
  ⚠ THE GROUPING IS DERIVED, NOT TYPED: eleven options the customer is asked, three we work out ourselves (bins, difficulty, the one-time note). Change where an option is asked and the grouping follows on the next run.
  Settled and recorded there so they are not asked again: only MEASURED FEET sets the price (sides raise a re-quote because the FOOTAGE changes — they are not a second price input), and where each answer must reach is frozen in the AGREED map in options-audit.test.js.
js/options.js	THE OPTION REGISTRY — one list of the 14 things a customer can ask for and which of eight artifacts each must reach. Derived from the code and corrected by the owner 2026-08-21; the working is in docs/option-registry-draft.md. ⚠ NOT WIRED YET (plan §3.3): nothing imports it, so it is the SPEC and the artifacts are still hand-written — a change here does not reach a screen. R-001 and R-003 rest on it.
season-state.test.js	⭐ WHERE EACH RSVP ANSWER ENDS UP (added 2026-08-22). `npm run test:season`. One table, six rows, five lists: for each thing a customer can say — Back Next Year (through the RSVP link OR badged by the office), No (either way), cancelled to the archive, moved, ordinary — which of Contact 2027 / the Yes tab / in-the-season / the build queue / the recycle queue do they land on. Its own file per R-018. ⚠ It RUNS the real predicates lifted out of admin.html, never a copy: the failure it exists to catch is two lists answering "is this customer in the season" differently, and a second opinion written in the test would agree with itself and prove nothing. ⚠ AND IT CHECKS THE WRITERS TOO — a queue reading `needsLightRecycle` correctly is worth nothing if nothing sets it, which is exactly how the Contact 2027 tab shipped reading a field nobody wrote. Seven sabotages red-checked.
  ⚠ THE THING IT WAS BUILT FROM: on 2026-08-22 SIX places decided "is this customer out for the season" by reading `d.maybeNextYear` — and `portalRsvp` writes `rsvpStatus: 'backnextyear'` with NO flag. So every customer who answered Back Next Year through the RSVP link stayed fully in the season: routed, scheduled, and built for. `isOutForSeason` now reads both, and `whBuildQueueGroups`, the warehouse colour totals, `computePendingHouseCount`, `whHouseBuildStatus` and `printNeedsBuildList` all ask it rather than the flag. That last one had no season rule at all, so the printed sheet listed people the screen beside it had already dropped.
options-audit.test.js	The blocking gate for the registry. `npm run test:options`. Its own file rather than a run-all.js section, per R-018. ⚠ It proves the registry is COHERENT, not that any screen obeys it. A frozen AGREED map holds the owner's destination answers, because a red-check proved audit() cannot catch a destination being quietly dropped — `consumers` is the declaration, so deleting one is coherent and wrong. Change that map only when she changes her mind.
playwright.config.js	Browser test config. Serves the repo statically on :4173, chromium, no retries.
test/fixtures.js	The ONE set of fake customers and invoices (§9.5).
test/firebase-stub.js	Fakes Firebase and BLOCKS every real backend call (§9.4).
test/*.spec.js	Browser specs. `npm run test:browser`. NOTE the folder is test/ (singular) in this repo; playwright.config.js and selector-contract.test.js accept either test/ or tests/, so a rename will not break them.
.github/workflows/tests.yml	CI: TWO jobs — "Fast suite (no browser)" and "Browser tests (Playwright)". ⚠ CORRECTED 2026-08-22: this line has twice said the browser job was removed on 2026-08-14. It was, and then it was restored. Both jobs run, both gate, and both are REQUIRED status checks on main — so nothing merges past a red one. Reasoning at the top of that file. See §9.14.
(quote-card.test.js	REMOVED 2026-08-14 — it tested the pre-P1 quote card, and Suite 5 of run-all.js covers the current one. Full reasoning in §3 gate B.)
system-map.md	Plain-English map of the whole app — regenerate per §6
Firebase project id: highlighting-utah (billing on).
Deploy — each surface is separate. Netlify does NOT deploy Firebase, and Firebase does NOT deploy the HTML.
bash
# HTML (index/admin/employee): merge to main → Netlify publishes in ~1 min
# ⚠ CORRECTED 2026-08-22 — YOU CANNOT PUSH TO main. A repository rule rejects it:
#     GH013: Repository rule violations found for refs/heads/main.
#     - 2 of 2 required status checks are expected.
# The two required checks are the GitHub Actions jobs "Fast suite (no browser)"
# and "Browser tests (Playwright)", and they only run through a PULL REQUEST, so
# a direct push can never satisfy them however green the suite is locally. The
# line that used to be here (`git push origin main`) has not worked for a while.
# The route to production is therefore: branch → push branch → open a PR → both
# checks go green → merge. Netlify publishes from main once the merge lands.
git add index.html admin.html employee.html && git commit -m "..."
git push -u origin <your-branch>          # then open a PR against main and merge it
# Cloud Functions:
firebase deploy --only functions
# Firestore security rules:
firebase deploy --only firestore:rules
# Firestore indexes:  (⚠ offers to DELETE console-made indexes not in the JSON — say NO unless the file is known complete)
firebase deploy --only firestore:indexes
⭐ FUNCTIONS DEPLOY THEMSELVES — DON'T RUN IT BY HAND (verified 2026-08-13). `.github/workflows/deploy-functions.yml` deploys Cloud Functions automatically on any push to main that touches functions/** or firebase.json, using the FIREBASE_SERVICE_ACCOUNT repo secret. So the correct way to ship a change spanning the website and the functions is ONE push to main: Netlify publishes the HTML and the workflow deploys the functions, both from the same commit. That is exactly the "ships as ONE push" rule in §0, and it happens for free. Running `firebase deploy --only functions` by hand at the same time races the workflow — don't, unless the workflow is failing.
Watch the run at github.com/addieflips/Highlighting-Utah/actions (it also prints which service account it deployed as, and that account's real IAM roles, which is what to read first on a permissions error). To confirm a deploy landed without opening a browser: `firebase functions:list --project highlighting-utah`.
The local `firebase` CLI IS installed and logged in (v15.26.0, highlightingutah@gmail.com, project highlighting-utah) — verified 2026-08-13. An earlier version of this file said it wasn't and that deploys had to go through Firebase MCP tools; that is out of date. Use the real CLI for read-only checks (functions:list, projects:list) freely. Reserve hand-deploys for when CI is broken.
firestore:rules and firestore:indexes are NOT covered by the workflow — those still need a hand-run of the commands above.
Prereqs a human sets up once: git push credentials for github.com/addieflips/Highlighting-Utah. functions/ needs `npm install` locally if you ever hand-deploy (it fails with "Cannot find module 'firebase-functions'" until node_modules exists); CI does this itself. If a deploy fails with an auth error, stop and tell the owner — do not try to work around it.
⚠ Hand-deploys on this Windows machine fail at the analysis step, 2026-08-13: `firebase deploy --only functions` dies with "User code failed to load. Cannot determine backend specification. Timeout after 10000" before uploading anything. The source is fine — this is a handshake bug between the CLI and this machine. Discovery boots functions/ on a local port and GETs /__/functions.yaml; run that by hand (cwd=functions, `FUNCTIONS_CONTROL_API=true PORT=8401 ./node_modules/.bin/firebase-functions <abs path to functions>`, then curl 127.0.0.1:8401/__/functions.yaml) and it answers 200 in under a second. Raising FUNCTIONS_DISCOVERY_TIMEOUT does NOT help — nothing is slow, the CLI just never connects. The fix that works: save that manifest as `functions/functions.yaml`, which the CLI reads instead of doing the port handshake at all. DELETE it straight after the deploy and never commit it — it is a frozen snapshot, and if left in place later function changes are silently not picked up. All of this is only needed when CI is broken; the workflow above has no such problem.
⚠ Verify what is actually on disk immediately BEFORE a hand-deploy, not just before writing the code. On 2026-08-13 a deploy shipped the old file because a parallel Claude session switched the checkout to another branch in between — the deploy reported "Successful update operation" for code that did not contain the fix. A shrinking/growing upload size in the deploy log is a useful corroborating signal.
⚠ Historic note, 2026-08-08: a Cloud Functions deploy via the Firebase MCP tool once ran 45+ minutes with no output and never resolved. The GitHub workflow above is the answer to that — it runs in a clean environment and either succeeds or fails visibly. If you are ever waiting on a hand-run deploy with zero progress for ~10 minutes, treat it as suspect and say so rather than waiting indefinitely.
After pushing admin.html, always check the browser console for Render failed: lines — that names any broken render immediately (renders are individually wrapped so one failure no longer kills the rest).
2. Architecture in one screen
Full plain-English map lives in system-map.md (regenerate it — see §6). Essentials:
jobAddresses is the customer record; almost everything hangs off it.
Lifecycle: public quote (quotes) → office prices it → customer approves → convert to customer (jobAddresses) → Measured Feet drives bin count / number series / warehouse bundles → warehouse builds (needsLightBuild) → route (scheduledRoutes, snapshot of stops) → crew installs → nightly invoice email (invoices) → PayPal/Venmo payment → RSVP "no" → recycle → customer number returns to availableCustomerNumbers.

  ⭐ RECYCLE WITHOUT DELETING — SOMEBODY WHO MOVED (added 2026-08-20). Owner: "I need a way to recycle ashley wray without deleting her because she just moved and I already requoted her so give me a button to recycle her, but then also put her in the needs to be built section in the warehouse." Every route to the recycle queue until then ended with the customer being REMOVED — right for somebody who said no, exactly wrong for somebody who moved. Edit Customer — **Recycle Their Lights — They Are Staying** sets `needsLightRecycle`, `needsLightBuild` and `recycleKeepingCustomer` and touches nothing else.
    - ⭐ SPLIT INTO TWO BUTTONS (2026-08-21). Owner: "for this actually dont make it one button have one button for build and another for recycle." **Recycle Their Old Set** sets `needsLightRecycle` + `recycleKeepingCustomer`; **Build Them A New Set** sets `needsLightBuild` and clears `buildTopUpFromFeet`. Somebody who MOVED needs both and presses one then the other — which does exactly what the single button did. ⚠ But the combined one could not express either alone: a house whose old set is already back needs only a build, and a customer sitting the season out needs only a recycle, and neither had a button. ⚠ Each must NOT quietly do the other's half, or the pair is useless for the two cases they were separated for. ⚠ Each still mirrors its write into `jobAddresses` — the panel repaints from the cache, not from Firestore. The no-colours warning moved to the BUILD button, where it belongs.
    - ⭐ AND AN ADD-ON HAS TO LOOK LIKE AN ADD-ON ON THE SHEET. Owner: "when it builds an add on it should somehow indicate to the warehouse that what they built needs to go into a bin that already exists." The **When built, put into** column already named the bin, but the row still read `Type: House` with a Feet of `+120` — and Type is the column an eye scans down. A bundle handed to somebody who thinks they are building a house gets a NEW bin made for it, and then two bins wear one number. Three signals now: `Type: ADD-ON`, a Notes line reading GOES INTO THE BIN THEY ALREADY HAVE (their own note still travels with it), and a badge on the warehouse tab beside the Timer and wire chips. The Printing tab's copy says `EXISTING BIN — <name> #<num>` so the two sheets cannot drift.
    - ⚠ IT IS ITS OWN FLAG, AND IT HAS TO BE. The tempting shortcut is "recycling AND building means moving" and it is WRONG: adding a customer sets `needsLightBuild`, the warehouse clears it only when the bundle is actually made, so somebody who answers NO before the warehouse reaches them carries both flags and would silently stay in the season. That is the one mistake here that sends a crew to a house with no lights for it. `isOutForSeason` reads the explicit flag.
    - ⚠ AND `Mark Recycled` NEEDS THE MATCHING BRANCH. It deletes the customer by design (see the entry below); a mover sent down that path is the very customer the other button was pressed to keep. The branch clears both flags, keeps the number, and RETURNS before the removal path.
    - It never pools the number (they still hold it), never touches the invoice, the route or the RSVP, and says so on screen when there are no light colours on file — the build queue skips a house with no `lightsDescription`, so they would go on one list and silently not the other.
  ⭐ DELETING A RE-QUOTE DELETES THE QUOTE AND NOTHING ELSE (made explicit 2026-08-20). Owner: "also make sure that when i delete a requote they dont get deleted as a customer." It never did — `attachDeleteHandlers` only ever removed the document named on the button — but the message said "Delete this item?" on a card showing somebody’s name, address and price, which is a question nobody can answer safely. It now names which of the two is going.
    - ⚠ AND IT WITHDRAWS THE QUESTION THE QUOTE WAS ASKING. The portal sets `seasonStatus` to `needs_changes` (or `address_changed`) when it raises a re-quote, and that is resolved by ANSWERING the quote. Delete the quote and the customer sits in Needs Changes for ever with nothing left anywhere to clear it. `QUOTE_RAISED_STATUSES` is deliberately just those two — anything else was put there by something that is not this quote.
    - Suite 106; twelve sabotages red-checked. ⚠ TWO PASSED AT FIRST because the flags appear TWICE in that handler — once in the write and once in the local `jobAddresses` mirror (the panel repaints from the cache rather than re-reading Firestore). Matching the text once stays green with either half deleted; the checks count both.
  ⭐ A POOLED NUMBER SOMEBODY STILL HOLDS IS NOT AVAILABLE (2026-08-20). Owner: "rachel oslund is number 479 but still in customer numbers is this Available — Regular". A number enters the pool when a customer is removed and leaves it when it is handed out — but it can also leave WITHOUT the pool being told (typed onto a record by hand, or assigned by an import), and the entry is then a lie that puts the same label on two bins. `cnFreePool` is the single answer to "which are free"; `cnShadowedPool` is the rest, named on screen so a missing number reads as an answer rather than a bug. ⚠ `cnNextAvailable` ALREADY FILTERED and the list beside it did not, so the two sat there contradicting each other — that is the bug, and it is why both read one rule now. ⚠ SHADOWED, NOT DELETED: the entry stays, so the number frees up again on its own if that customer is later removed, and a render is the wrong place to be deleting anything (Find conflicts still offers to clear them properly). ⚠ IT FAILS SAFE WHILE LOADING: before `jobAddresses` lands nothing looks held and the pool shows in full — offering a taken number is a real mistake, holding a free one back for a moment costs the next number up. Suite 95.
Measured Feet is the highest-leverage field — one number drives four things (bins, number series, bundle count ceil(feet/40), auto-price). Double-check it. Bins go up in 260s: a house needs another bin for every 260 ft, so up to 260 is 1 bin, 261–520 is 2, 521–780 is 3, and so on (`cnBinsForFeet`, `CN_DOUBLE_BIN_FEET` = 260 in js/money.js). Some UI/doc text still says "200 ft rule" — 260 is the number the code uses, and that boundary has never moved; what changed on 2026-08-15 is that the count no longer stops at 2. There are still only TWO customer-number series, so anything past one bin takes a 5000-series number — test `numberOfBins >= 2`, never `=== 2`, or a 3-bin house is handed a regular number and labelled for a bin that does not exist.
⭐ THE MEASURE TOOL SAVES THE FEET AND THE PRICE IN ONE PRESS (2026-08-25). Owner, on the quote card: “I think it fills out price but I want it to fill out price and feet.” She was right, and it was not a misreading — the tool had TWO commit buttons in TWO panels, and only one of them was gold. `rmUseFeetBtn` sat as a plain outline button at the bottom of Distance Results and wrote `estimatedFeet` plus the drawing; `rmUsePriceBtn` was minted inside `rmRenderPrice`’s innerHTML, was gold, sat two panels lower, and wrote `quotedPrice` plus the grade. Neither touched the other’s field. The gold one reads like the finish line, so it is the one that got pressed, and the house ended up priced with no footage.
  - ⚠ THAT STATE DOES NOT ERROR, WHICH IS THE WHOLE PROBLEM. `quoteFeetOrEstimate` divides the price by `perFootRate`, takes a 5% safety margin off, and hands that back as if it were measured — and THAT figure then sizes the bins at 260 ft, chooses between a regular and a 5000-series number, and counts the bundles at 40 ft. The warehouse builds to a number nobody measured, and the only thing on screen saying so is small grey text reading “no measured feet on file”. Owner, earlier and still the rule: “I need no guessing I need feet to be correct.”
  - **What exists now.** One gold `rmCommitBtn` in a Save block below the Price panel. `rmCommitPayload(feet, price, grade, measurement)` is a pure function — its own function precisely so a test can RUN it rather than match its source, the same reason `rmSavePayload` is one — and it returns `estimatedFeet` and `quotedPrice` in a SINGLE object. ⚠ ONE `updateDoc`, NEVER TWO AWAITED WRITES: two writes can half-succeed and the half that survives is a priced house with no feet, which is the exact state this closed.
  - ⚠ THE GRADE GOES INSIDE THE DRAWING WHEN THERE IS ONE, AND BY DOTTED PATH WHEN THERE IS NOT. A `measurement` object and a `measurement.difficulty` dotted key in the same write is refused by Firestore outright — and the footage rides in that write, so getting this wrong loses both numbers, not just the grade. Assigning a whole `measurement` object when there is no drawing to carry would flatten a drawing saved earlier to one field.
  - **The old buttons MOVED, they were not removed.** `rmUseFeetBtn` and `rmUsePriceBtn` keep their ids and their handlers and are now the “Save the feet only” / “Save the price only” links inside the same block — somebody pricing a house by hand still needs to save footage without a price. ⚠ `rmUsePriceBtn` IS NO LONGER MINTED INSIDE `rmRenderPrice`. Built into that innerHTML it was destroyed and re-created on every render, which is why it used to have to re-attach its own listener each time; the Save block’s shell is now static and only its TEXT is updated, so the three handlers bind once. Do not rebuild that block with innerHTML — it would take the listeners with it.
  - **The block shows what the feet decide** — bins, number series, bundle count — above the button, via `cnBinsForFeet` and `FEET_PER_BUNDLE`. ⚠ NOT A SECOND COPY OF 260. The last time two screens each held their own copy of that number, one of them said 200.
  - Suite 268 in run-all.js runs the payload builders against the real shipped code. Red-first: 13 failures on unmodified main, and the two no-regression guards passed on old code as they must. 4857 → 4887 passed, 0 failed.

⭐ THE DIFFICULTY GRADE SAVES ITSELF ON CHANGE (2026-08-25). Owner: “when we assign easy,medium,hard that will automatically save right?” It did NOT. The only listener on `#rmDifficulty` called `rmRenderResults()` — a redraw, no write — so a grade the office set by hand reached Firestore only as a passenger on one of the commit buttons. Set a house to Hard, close the tool without pressing either, reopen it, and it was back to whatever Google’s roof model had suggested (`rmRoofGrade`). `rmSaveGrade` now writes it on change.
  - ⚠ THE GRADE ONLY, NEVER THE PRICE. `quoteEstimatedPrice` applies the saved grade, so this moves the ESTIMATE on the quote card immediately — but `quotedPrice` is the number the customer is asked to APPROVE, and a dropdown must not change what somebody has been quoted. `rmPriceIsStale(saved, current)` surfaces the gap in the Save block instead. ⚠ IT RETURNS `null` FOR “nothing to compare”, not `false`: a quote that has never been priced must not read as out of date, or every fresh quote opens shouting about a price that does not exist. And `typeof 0 === 'number'` — a falsy check there would let a zeroed quote hide.
  - ⚠ IT IS A DOTTED-PATH WRITE for the same reason as above, and it never calls `alert()`: it runs off a dropdown, not off a press, so a dialog would interrupt somebody who never asked to save. The line under the block reports it; the buttons still guarantee it.
  - ⚠ AND IT MUST LIVE AT MODULE LEVEL. Written next to the commit handlers it was a scope error waiting for the first grade change — the `#rmDifficulty` listener lives inside `openRoofMeasure` and the handlers do not, so `rmSaveGrade` was simply not defined at the call site. It closes over `db`, `quotesCache` and `rmQuoteId`, all of which are module level anyway.
  - ⚠ ONE EXISTING CHECK CHANGED SHAPE, IT WAS NOT WEAKENED. S129 proved “nothing saves until a button is pressed” by finding the words `Use ` inside `rmRenderPrice` — that is, by finding the BUTTON’S LABEL in the function that drew it. The button moved, so the label left, and the old match would have failed on code that is right. It now asserts the guarantee directly: that function contains no `updateDoc`/`setDoc`/`addDoc`, and a press-to-save path exists. This is the §7 slow-fuse shape — a check pinned to where a string happens to sit rather than to what must be true.

⭐ THE SATELLITE PICTURE IS LINED UP WITH GOOGLE'S ROOF MODEL (2026-08-25). Owner, on the third go at one complaint: “This is still wrong. What are other solutions we can look at to get this right?” The two fixes before it were both about HEIGHT and the fault is POSITION, which is exactly why neither of them moved it.
  - ⚠ GOOGLE'S AERIAL IMAGERY IS NOT SHOT FROM STRAIGHT OVERHEAD. On the house that prompted this you can see a strip of the west WALL and its shadow down the side of the roof, which is only possible if the camera was off to one side. Anything with height is therefore drawn displaced from its own footprint; measuring that strip against the width of the house put it at about SIX FEET. Google's ROOF MODEL — where every height and every Street View projection comes from — is in true positions. So a gutter traced on the picture you can SEE lands outside every roof face, falls back to the eave, and draws into Street View beside the house.
  - ⚠ AND NO FORMULA CAN ANSWER IT. Google does not publish the angle its satellite was at, so working the shift out rather than measuring it would be the guess she has ruled out twice (“I need no guessing I need feet to be correct”). It is measured off TWO CLICKS she makes herself — the same physical spot in each view. The difference IS the shift. `RM_ALIGN_MAX_M` (8 m) refuses a pair too far apart to be one spot, and nothing moves when it refuses.
  - ⭐ `rmSkyShift` MOVES EVERY SKY CLICK ONTO THE MODEL, inside `rmMapPixelToWorld` — the ONE funnel every sky click goes through, tracing and dot-picking alike — and BEFORE the roof height is read. That order is the point: a displaced click lands outside every face and falls back to the eave, a corrected one lands inside the face it belongs to. ⚠ A second route from a map pixel to a world point would bypass all of it.
  - ⚠ WHAT IT LINES UP IS THE PICTURE AGAINST THE MODEL, not against the ground. The Street View click gets its depth from that model, and the model is what draws the lines and supplies the heights — so agreeing with it is what makes the two views agree with each other.
  - ⚠ LINES ALREADY TRACED FROM ABOVE MOVE WITH IT, BY THE DIFFERENCE ONLY. They carry the displacement, so leaving them would line up every FUTURE click and leave the existing ones as wrong as before — which reads as the button not working. A `surface: 'street'` run NEVER moves: its position came from the camera and the model, the thing being lined up TO. Heights are re-read after the move; a point that still finds no face keeps the height it had rather than falling back to a guess.
  - ⚠ THE FEET WERE NEVER WRONG BECAUSE OF THIS, and saying so matters or the next person goes looking for a pricing bug. A level gutter traced from above measures true however far the picture is displaced — both ends move together and it cancels, which is the whole reason eaves are traced up there. What was wrong was the picture, and any run that CLIMBED. ⚠ One real fault did ride on it: with one end inside a face and the other outside, the two ends got different heights, so a flat gutter read as a rake — slightly inflated feet, and flagged as a guess when it was not.
  - ⭐ AND IT REMEMBERS, so this is a once-a-street job (`settings/measureAlign`). Owner: “is there a way you can make sure everything is lined up perfectly when you bring up the pictures so there isn't a ton of readjusting”. The shift belongs to the PHOTOGRAPH, not the house — same flyover, same lean — so a house within `RM_ALIGN_INHERIT_M` (a mile) of one that was measured opens already corrected. ⚠ INHERITED IS NOT MEASURED AND SAYS SO ON SCREEN; applied silently it would be a guess wearing a measurement's words. ⚠ AN INHERITED OFFSET IS NEVER WRITTEN BACK AS A FRESH SAMPLE, or one house's answer copies itself down the street gaining authority at every step with nothing left saying where it came from. ⚠ A house that was measured keeps its own answer — the saved drawing carries the offset it was traced under.
  - ⚠ THE SAVED PATHS ARE ALREADY CORRECTED, so `rmRestoreMeasurement` SETS the offset and never re-applies it — pushing it through `rmSetSkyOffset` would shift the whole drawing a second time and put it twice as far off as the picture ever was. `null`, not `{e:0,n:0}`: “nobody lined this house up” and “somebody did and it needed nothing” are different answers and only one is a reason to go and check. Cleared in BOTH `rmForgetLastHouse` and `rmReset`; the remembered neighbours survive.
  - Suite 269 in run-all.js RUNS the real functions — every claim here is about where a point ENDS UP, which a text match cannot see. 21 sabotages red-checked. ⚠ TWO MISSED ON THE FIRST PASS AND BOTH WERE THE SABOTAGE, NOT THE CHECK: the height-read line is spelled identically in `rmRefreshHeights`, which sits EARLIER in the file, so `.replace()` broke that function instead and the pass reported a MISS against a check that was never in danger. Anchor on the line above it.
  - ⚠ AND `extractFn` DROPS THE `async` KEYWORD, which is written down in §5 and cost this suite a run anyway: `rmAlignOnLoad` arrived as a plain function full of bare `await`, a parse error that kills the whole suite as one unattributable crash. Suite 269 lifts its own with an indexOf helper that tries `async function NAME(` first.
  - ⚠ TWO SESSIONS NUMBERED A SUITE 268 AT THE SAME TIME, for the second time this week. Both are kept and this one is 269. The conflict boundary cuts the first block off BEFORE its closing brace, so restore that rather than borrowing the other side's — the error surfaces as “Unexpected end of input” hundreds of lines from the cause.

The one correct money formula, everywhere: owed = (install + removal + changeFees) − credits − deposit, floored at 0. As of 2026-08-08 this is actually true everywhere in admin.html and functions/index.js — it wasn't before (see §4 history).
⭐ REMOVAL IS INCLUDED IN THE PRICE, AND `removal` IS DELIBERATELY UNSETTABLE (settled with the owner 2026-08-21). This was on the hole list as "removal never billed" and it is NOT a hole — the public FAQ in index.html says "Post-season removal is included in every package", so charging a fee when a takedown is marked done would bill customers for something they have been told is free. Owner, asked directly: "No we don't need that we'll just add on a fee if we need that."
  - ⚠ SO DO NOT WIRE `removalDone` TO MONEY. Marking a takedown complete touches no invoice, on purpose.
  - ⚠ AND DO NOT "FIX" THE FIELD EITHER. `removal` is read in ~20 places, summed into the balance, printed as its own line item on the invoice document, carried across Start New Season, and defended by a comment recording a real bug where opening an invoice wiped it — but EVERY write of it in the whole app is a preservation (`removal: x.removal`, `keptRemoval`, `existing.removal || 0`). Nothing can give it a value, and that is the settled answer, not an omission. An exceptional charge goes on as a fee instead.
  - The suite's `invoice-doc: a removal charge shows as its own line item` check stays. It guards the document builder for a value that can only arrive from an import or a hand-edited record — rare, but a customer seeing a Total that does not add up is exactly what it exists to stop.
  - Fixed in passing: the Add Customer footnote promised "Deposit and removal fee can be added later from the Invoices tab", which was half untrue — there is no removal box anywhere. It now mentions only the amount paid, which really is settable (`.ie-paid`).
Two separate $30 fees: new-member fee (added once by the nightly function, flagged newMemberFeeApplied, folded into install) and the light-change fee (changeFees / changeFeeNotes, added by portalSave when a member changes colours late — tracked as its own field, removable independently).
Portals reach protected data only through Cloud Functions (portalLookup/portalSave/portalRsvp/portalInvoice/quoteRespond/publicQuoteLookup) which use Admin SDK and bypass rules. A field the client reads must be in that function's read whitelist or the customer never sees it.
Automatic: nightly invoices (sendNightlyInvoices, 7 PM Mountain, 0 19 * * *), portal colour change → warehouse queue, late colour change → $30 fee, RSVP "no" → recycle flag, customer edits → upcoming route resync, carryover credit.
A crew-day is one town, PLUS nearby towns when it cannot fill itself (changed 2026-08-16). Still 20 per crew and 40 per working day — those numbers have not moved, and the ⚠ correction above about 20-vs-40 still stands. What changed is the tail of the season. Owner: "prioritize doing the most houses in a day as possible… it should always favor having all 20 in the same town and only stretch to nearby towns if the day isnt full for a crew." A crew-day is still seeded with one town and fills from that town first; only if it is still short of 20 does it top up from towns whose centre is within `NEARBY_TOWN_MILES` (8), nearest first. A day its own town can fill never borrows. Measured on eight towns with five houses left each — 40 houses, exactly one working day — this went from 4 working days to 2.
  ⭐ THE DAY IS THE UNIT, NOT THE TOWN (rewritten 2026-08-17). Owner: "we want to start with the two cities that have the most clients and work our way down... if lehi has 140 houses heriman has 120 and AF has 119 then today is Lehi and heriman but tomorrow is Lehi and AF." `planNewCrewDays` used to walk TOWN by town — take the biggest, give it days until its queue emptied, move on — which is why a season opened Lehi-Draper, Lehi-Draper, Lehi-Draper with the first town holding every early slot. It now walks DAY by day: on each working day each free crew is sent to the town with the most houses still waiting, and the count is redone before the next day, so a town just worked drops down on its own. Verified against the owner's own numbers: Oct 1 = Lehi+Herriman, Oct 2 = Lehi+American Fork.
    - ⚠ "Most clients" means most houses ALLOWED on that date. Counting every waiting house instead lets a town of 100 November houses win an October day and then place nobody.
    - ⚠ Ties break ALPHABETICALLY, not by object order, so the same input always produces the same plan. Suite 47 feeds the same towns in both orders and requires an identical result.
    - ⚠ When crews are free but nobody is allowed yet, it JUMPS to the first allowed date rather than stepping through the calendar. Stepping gives the same answer for a short gap — which is why the first version of that test proved nothing — but exhausts the 400-spin horizon on a long one and silently drops the houses. Suite 47 uses a date ~430 working days out for exactly that reason.
  ⭐ AT MOST ONE OTHER TOWN PER CREW (changed 2026-08-17). Owner, reading a real Oct 1 day that held Lehi, Draper, American Fork, Herriman, South Jordan, Alpine and Pleasant Grove at once: "each crew is only doing one other city — we have one crew doing Lehi American fork and another doing Heremin Draper, but we dont have one doing American fork, saratoga springs, and Lehi." The top-up used to walk every neighbour in range until the day was full; it now picks exactly ONE. A day nobody can drive is worth less than a day that is short.
  ⭐ AND IT PICKS THE MOST URGENT NEIGHBOUR, NOT THE NEAREST. Same message: "we shouldnt be doing anyone who said any until oct are done." Among neighbours in range with houses allowed on that date, the one whose best waiting house has the lowest priority number wins; distance breaks a tie only (nearbyTowns already returns them nearest-first). A neighbour with October houses therefore beats a closer one holding only "Any" houses. ⚠ Suite 43 deliberately places the October town FARTHER than the Any town — with them the other way round, "most urgent" and "nearest" give the same answer and the check proves nothing.
  ⭐ NEW HANGS OUTRANK EVERYTHING (added 2026-08-17). Owner: "the very top priority is new hangs, so if a new hang requested Any they are the first chance we get in Oct and if they said Nov we do them the first chance we get in Nov." `houseInstallPriority(h, custData)` returns 0 for `chargeNewMemberFee === true`. ⚠ CORRECTED 2026-08-25: this line said that was "the same flag the New Hang badge on Routes reads, so the two cannot drift", and it is NOT. The badge is `isNewMemberHouse` → `closedQuoteFor`, which looks for a CLOSED QUOTE, not the fee box — two different questions, deliberately (see `printIsNewHang`, whose own note explains why the union is the honest rule for a crew photo). They CAN disagree and routinely do: Ashley Wray badges NEW with the fee box unticked. Nothing depends on them agreeing, so this is a documentation error rather than a bug — but do not write a check asserting the two match, and do not "fix" one to read the other. Order is then new hang (0), October (1), no preference (2), November (3). ⚠ This does NOT move a November house into October: `houseAllowedFrom` still gates that, so a new hang who asked for November sits at the head of the queue being skipped over every October day and is taken first on the first November one — which is what was asked for. The customer record has to be PASSED IN (`houseInstallPriority(h,d)`); without it every house looks like a returning one.
  ⭐ AND NOBODY IS HUNG BEFORE THE MONTH THEY ASKED FOR — enforced on the SAVED PLAN, not only at build time (added 2026-08-17). Owner: "under no circumstance do we do anyone in Nov i october we just wait until Nov instead." The builder always honoured this; two other things did not. The season is saved in `routeSchedule`, so a day built before somebody changed their mind keeps them on it — and the customer sync now pulls a changed month across, which leaves a house sitting on 1 October holding the word NOVEMBER. `enforceInstallTiming` runs on every sync and MOVES them: to the earliest existing day they are allowed on that still has room, preferring one already working their town so honouring a date does not break the one-other-town rule instead. ⚠ A house already ticked `done` is history and is never moved; a house with nowhere to go stays visible and is reported rather than dropped; the 40-a-day cap is never broken to honour a date.
  ⭐ THE DRIBBLE AT THE END IS SWEPT UP (added 2026-08-18). Owner, reading a real tail — "Mon Nov 23 / 15 left, Tue Nov 24 / 2 left, Mon Dec 7 / 6 left, Wed Dec 9 / 2 left — thats not good we should fit as many of those into other days as possible, just stuff those guys into another day." Twenty-five houses over four days across three weeks is four mornings of loading the truck. `packTailCrewDays` runs over the finished plan, cheapest move first: (1) a crew-day holding less than half a crew rides an earlier DATE that has a crew standing idle — nothing mixed, one date gone; (2) houses join a crew already going to their town, or fill a crew’s one spare town slot with a neighbour; (3) a crew already holding two towns may take a third NEARBY one, but ONLY where the source day disappears entirely; (4) a crew-day of ≤3 that is the last thing on its date may be handed to a crew up to 30 miles off. Measured on the real 955-house sheet: 34 working days → 31, season ends 30 Nov instead of 2 Dec.
    - ⚠ NOBODY IS EVER MOVED LATER than the day they already had. Both routes into the packer (whole-crew-day and per-house) need their own copy of that guard, and of the allowed-date guard — a sabotage of either is invisible unless a fixture exercises that specific route. Suite 51 has one for each.
    - ⚠ THE THIRD TOWN IS BOUGHT WITH A DAY, NOT SPENT FREELY. Each thin day is planned in FULL before anything is committed: if every house found a home the day dissolves and the mixing is accepted; if one is stuck the day survives anyway and only the tidy moves are kept. Without that gate a crew picks up a third town for nothing.
    - ⚠ "Just stuff those guys into another day" is permission to mix TOWNS, not to ignore DISTANCE. The two-town rule is still the builder’s and the builder still keeps it; Suite 27 now asserts the raw build (`{pack:false}`) separately from the swept plan so neither half can drift.
    - ⚠ The walk examines EVERY crew-day including the earliest. It used to stop at index 0 — which can never find a target and so looked safe — and that made a fixture that happened to sort a leftover first pass straight over it.
  ⭐ THANKSGIVING (added 2026-08-18). Owner: "everyone labeled Thanksgiving and before thanksgiving we need to do as close to thanksgiving as possible, also we will not be working thanksgiving day." Two things. `isWorkingDay` now excludes the holiday — that is the single place the calendar is walked, so it never enters a plan rather than being built and removed. The date is computed from the year, never written down. And "November - Before Thanksgiving" was reading as a plain November house because the words start with NOV, so those customers were hung on 1 November, three weeks before the holiday they asked about, using early-November days people with no deadline could have had; they now open `PRE_THANKSGIVING_DAYS` (7) before Thanksgiving and outrank everybody except a new hang inside that window.
    - ⚠ PRE_THANKSGIVING_DAYS IS A SAFETY MARGIN, NOT A GUESS AT THE NICEST DAY. Seven days is five working days, ~200 slots. Tightening it to two or three puts them closer to the holiday and pushes anybody who does not fit PAST it — the one outcome the label exists to prevent. Suite 52 asserts the floor so nobody tightens it without meeting that argument.
    - ⚠ Before and after Thanksgiving are OPPOSITE answers in nearly identical words, and this file has already had that bug once (the importer rewrote "November - Before Thanksgiving" to "After Thanksgiving" because it contains "thanks"). Any new match on the word needs a test that the two do not collapse.
  ⚠ THIS WAS LOST ONCE AND RE-APPLIED (2026-08-19). A parallel session pasted admin.html over a read taken before these two commits, so the priority and prefKey changes vanished while everything around them survived. Recovering it was a MERGE, not a revert: her paste also FIXED THE SAME 5000-character notice problem a different and better way (summarising with `report.over.slice(0, 5)` rather than truncating at write time), so that half of mine was dropped rather than restored — a tie goes to the other session. Only the strictly additive half, which she had not touched, was put back. See §0: merge, never paste.
  ⭐ AND A NAMED DAY IS A WAIT, NOT A HURRY (changed 2026-08-19). Owner: "make a somewhat priority to do it in the time frame they asked, if they ask for a specific day though that is not top priority we just do it the next time we have a chance for when they ask." `houseInstallPriority` returned 1 for a named day — the same tier as October — so 11/9 was treated as urgent. Waiting for a date is not being in a hurry. It returns 2 now (the no-preference tier), which is safe precisely BECAUSE `houseAllowedFrom` still holds them to the day: they cannot go early, and from their date they take their turn. A new hang who named a day is still 0.
  ⭐ AND THE ROUTES SIDE HAD NEVER HEARD OF ONE (fixed 2026-08-20). Owner: "we have a situation where the system cant read every format so we need to make sure the system can tell what all these mean", over a column of `1-Nov`, `11/1`, `11/9+` and `10/28+`.
    - ⚠ EVERY ONE OF THOSE ALREADY PARSED, and still does. Do not go looking for a parser bug. `prefSpecificDate` has read all four spellings for a while and the Schedule tab honours them (`houseAllowedFrom` holds the house to the day, `houseDeadline` deliberately gives it no ceiling).
    - THE FAULT WAS WHERE IT WAS DECLARED: inside the schedule module, so only the season could ask it. `earliestAllowedInstallDate`, `isInstallPrefLocked` and `installTimingBadge` each matched the five standard wordings and nothing else, so a customer who wrote `11/9+` read as NO PREFERENCE — the nightly fill could put them on a crew route on 1 October while the season was carefully holding them to 9 November, and their badge said "Normal Schedule". Two halves of the app disagreeing about one customer is worse than either being wrong alone: whichever runs last wins and neither says a word.
    - The parser now lives in shared scope above the widget, and `prefNamedFloor(pref, year)` is the one helper all three call. Suite 103 asserts the PLACEMENT (parser declared before `const MARKUP=`) as well as the behaviour, because moving it back inside the widget makes the customer side quietly blind again.
    - ⚠ A FLOOR, NEVER A CEILING. The plus means the customer named their EARLIEST day, not their last, and a bare `11/1` is read the same way on purpose. Turning a named day into a deadline would push somebody PAST a day they asked for. ⚠ AND IT ONLY MOVES THE DATE LATER: a day already gone by leaves the floor at today rather than dragging it backwards and re-opening somebody already done.
    - ⚠ A BARE MONTH IS NOT A DAY. `NOV` must never come back as a date or every November customer gets pinned to one square on the calendar. Asserted; a sabotage adding a month-only fallback is caught. Suite 103; seven sabotages red-checked.
    - ⚠ AND `prefKey` WAS THE REAL BUG SHE SAW. Owner: "there are two people who entered their preferred date in a wrong format including 11/1/2026 and 1-Nov which obviously will be flagged and wont work correctly." The FORMATS were fine — `prefSpecificDate` has read both for a while. `prefKey` was not: neither string starts with OCT or NOV, so both fell through to "any" and the Schedule filed somebody who asked for 1 November beside the people with no preference at all. It now derives the month from `prefSpecificDate`.
    - ⚠ THAT IS A COMPARISON KEY, NOT A REWRITE. The periodic sync compares timings through prefKey and leaves the plan's own wording alone, so "11/1/2026" and "November" agree about the MONTH without either being flattened into the other — which is what SCHEDULE_SYNC_FIELDS already warns about. The DAY still governs when they go out.
    - ⚠ A FIXTURE FOR THIS MUST USE HER TWO ACTUAL VALUES. A test on "11/9+" passes before and after, because that one was already being read.
  ⭐ A PREF DATE CAN NAME AN ACTUAL DAY (added 2026-08-18). The column is not only OCT/NOV/ANY/THX: 14 customers hold "45962" — how Excel stores 1 November, a date cell read raw — plus "11/9+" ×4 and "10/28+". All were falling through as "no preference" and being offered the first day of the season. `prefSpecificDate` reads serials and 11/9+, 1-Nov, Nov 1, always in the SEASON’s year (the serials say 2025 because that is when the sheet was typed). ⚠ A bare NOV deliberately does NOT become a date — pinning every November house to one day would be far worse than ignoring it. ⚠ Unreadable values ("prepaid") are treated as blank, never as an error, so one junk cell cannot stop a house being scheduled. ⚠ The customer record only holds the five standard wordings, so a named day lives only on the plan and the periodic sync must not flatten it back to "November".
  ⭐ OCTOBER IS A DEADLINE, AND THE TOWN PICK IS URGENCY-FIRST (added 2026-08-18). Owner, reading a 26 November day holding three customers who had asked for October: "we need to get everyone who requested Oct done in Oct none in November and if any in Nov it should be Nov 1st but only if theres literally no other way." Ordering INSIDE a town was never the problem — October was already first in every town's queue; it was the TOWN that was late. A town is worked when its head-count wins and everything on it goes out that day, so a town of five was not reached until November and took its October customers with it. On the real sheet: nine towns of ≤6 holding eight October customers. The day-by-day walk now sends each free crew to the most URGENT town, and among equally urgent ones to the biggest.
    - ⚠ THIS DOES NOT UNDO "THE TWO BIGGEST TOWNS GET EACH DAY". Through most of October every town still has October houses, so the urgency ties everywhere and head-count decides exactly as before. It only bites once the big towns have no October left. Suite 53 asserts both directions.
    - ⚠ A FIXTURE FOR THIS NEEDS TWO BIG TOWNS. With one, the second crew has nothing else to do and picks the small town on day one anyway, so head-count-alone looks identical and the check proves nothing. That fixture was vacuous on the first pass.
  ⭐ houseDeadline — THE LAST DAY A HOUSE MAY BE HUNG (added 2026-08-18). `houseAllowedFrom` says the earliest; nothing said the latest, so nothing could tell a starting gun from a deadline. October → 31 Oct; Before Thanksgiving → the holiday; everything else '' — including a named day like "10/28+", where the plus is the customer saying "not before then". Only the tail sweep reads it, and only to decide whether somebody may be moved to a LATER day.
    - ⚠ FAIL SAFE: a caller that does not supply `until` gets NO later moves at all, rather than every house looking deadline-free. Without that guard an October customer lands on 9 November silently — it happened once in the measuring harness. `mayMoveLater = !!o.until`.
    - ⚠ A named day never creates a deadline, and the guard sits AHEAD of the month branch to catch "Oct 28", which starts with a month word AND names a day. The two on the real sheet ("10/28+", "11/9+") never reach that branch, so the guard protects a spelling the office has not used yet.
  ⭐ A WASTED MORNING IS MEASURED PER DAY, NOT PER CREW (changed 2026-08-18). Owner: "6, 8, and 11 houses every day is really bad... we cant just be waisting time." A day costs a morning whether it holds four houses or forty, so the rescue triggers on the DAY's total (`wastedDay`, 12) rather than one crew's share — the first version ("a crew-day of ≤3 alone on its date") missed a date holding two crews of two, and one crew holding four. And it is ALL OF IT OR NONE: a crew-day that half empties still costs the same morning while making four other routes longer, so moves are committed only where the crew-day goes completely. A partial sweep once left ONE house holding 28 October.
    - Measured on the real 962-house sheet after all of the above: 221 October customers, ZERO finishing after 31 October; 34 working days → 28; nothing in December; nothing on Thanksgiving.
    - ⚠ Levan, and in the live book Smithfield/St George, are single customers 100–300 miles out. No scheduling rule makes those efficient — they will always be a near-empty day or a long detour. That is a business decision, not a bug to fix.
  ⚠ A SAVED PLAN PREDATES ANY RULE CHANGE. The season lives in Firestore, so changing the builder does nothing to days already on screen until somebody presses ⚙ Recalculate everything (it was called Rebuild days until 2026-08-20 — see the entry below). `crewDaysOverTownLimit` counts saved days holding more than four towns (two crews × own + one other) so the office can be told to rebuild rather than wondering why the new rule "did not work".
  ⭐ THE TOWN RULE BELONGS TO THE CREW, NOT THE DAY (settled 2026-08-20). Owner: "the rule isnt one day can have 4 citys its one crew can have 1 dominant city with some flakes in another city so one crew can go in a max of 2 cities but we need to try to prioritze a crew being in one city but if thats not possible they can also be in one nearby city", then: "1 city for a crew would be 4th or 5th in priority though but the second city being a neighboring city is mandatory not a priority." Read this entry and the sub-bullets below before touching any of it.
    - **Two cities a crew is the CEILING. A neighbouring second city is a RULE. Staying in one city is a nicety that loses to almost everything** (she puts it 4th or 5th). `MAX_TOWNS_PER_CREW = 2`; `maxTownsPerDay()` is derived from it and is only ever a summary. ⚠ A DAY-LEVEL COUNT CANNOT EXPRESS THE RULE: four towns split three-and-one passes any "no more than four on a day" test while one crew drives three cities.
    - ⚠ `dayCrewTowns` SHARED OUT EVERY TOWN WITH NO CAP AT ALL, so a day holding six towns gave one crew THREE. That is what 27 October actually was. The builder had already been taught the rule and it changed nothing anybody could see, because THIS is where a season day’s crews are decided and it is what the panel, the printed sheet and the route generator all read. Fixing the builder without fixing this fixes nothing.
    - `townsAreNeighbours(a, b, day)` asks it the same two ways the builder does, in the same order: the office’s typed list (Scheduling settings) wins, then the towns are measured — same eight miles, same `haversine`. Measured from the CUSTOMER records, because the plan’s own house rows carry no coordinates. ⚠ A town the office HAS listed, but not against this one, is a settled NO — not a gap to fall through to the tape measure.
    - ⚠ IT ANSWERS NO WHEN IT CANNOT TELL. An unknown pairing leaves those houses unassigned and visible (`unassignedHousesFor` reports them and they still print on the day) rather than quietly loaded onto a crew that may be driving fifty miles for them. If a real pair keeps coming up unassigned, add it to the nearby-towns list — do not soften this.
    - "Prefer one city per crew" needs no code of its own: a crew with no town yet is carrying zero, so it wins the load-balance on its own. That is how it gets honoured without being given a priority it does not have.
    - ⚠ EVERY FIXTURE FOR THIS NEEDS THREE TOWNS. With two crews and two towns each crew simply takes its own as a dominant city and the neighbour test is never consulted — the first versions of three of these checks had exactly that shape and proved nothing. And a fixture where all the towns are close cannot test the CAP, because nearness alone holds the line: Suite 101 uses five mutually-neighbouring towns for that. Suite 101; nine sabotages red-checked.
    - Suite 77 (the crew sheet that printed EMPTY) now supplies a nearby-towns list, because its Draper sits 46 miles from its Lehi and Alpine and can no longer be a second town on distance alone. The two-town case it exists to cover survives; the pairing is just made legal the way the office would make it.
    - ⚠ I TWICE WROTE THAT THE BUILDER COULD NOT MAKE SUCH A DAY. THAT WAS WRONG, and it sent two rounds of work into the timing sweep. `planNewCrewDays` scored a crew’s THIRD town as merely worse than its second (`score = towns.length < 2 ? 1 : 2`, whose own comment said "or the third"), and the near-empty-day rescue checked the MILES to a town but never how many towns the day already held. The live book settled it: 27 October had Cottonwood Hts + Holladay + Midvale on one crew and Murray + Santaquin + Payson on the other. Own town, plus one borrowed, plus one more, on BOTH crews.
    - ⚠ AND THE GUARD HAS TO COUNT THE TOWNS THE PLAN IS ABOUT TO ADD. The rescue plans every house first and commits only if the whole crew-day empties, so `tgt.towns` still reads as it did before planning began. A guard against that alone is right for one house and wrong for two: Payson and Santaquin each see the same single spare slot and both take it. `addTowns[]` tracks it, exactly as `extra[]` already did for the house count.
    - The timing sweep was a SECOND way in, and is fixed too: its fallback was `sameTown || room[0]` — the earliest day with a free slot, no regard for towns. Now `sameTown || roomy` and nothing else: if no day is under the limit the house is NOT moved, it is reported as stuck, and the office decides. A version that crowded as a last resort shipped for about an hour and was overruled by "no exceptions". ⚠ It runs on the five-minute customer sync, long after the rebuild, so anything it breaks reappears minutes after a Recalculate.
    - `maxTownsPerDay()` (`CREWS_PER_DAY * 2`) is the one answer, shared by the sweep and by `crewDaysOverTownLimit`. `dayTownCount` counts TOWNS, not houses. The warning no longer claims the days were "built before the current rules": naming the wrong cause costs a rebuild every time and teaches the office that the button does not work.
    - ⚠ THE FIXTURE IN SUITE 22 STILL DOES NOT REPRODUCE THE SIX-TOWN DAY, and says so in its own comment. Sabotaging either builder guard leaves it green. Two things were wrong with the harness and are now fixed — it supplied neither `haversine` nor `NEARBY_TOWN_LIST`, so `nearbyTowns` returned [] and EVERY check in that suite ran against a builder with town-mixing switched off; and fixture houses carried no `stop` coordinates, which is what town centres are measured from. Both are fixed and borrowing now happens, but the queue ordering still never drives it down the failing path. The guards are asserted directly as well, which is weaker and is not pretended otherwise. **If you can make that fixture fail on the old code, do it and delete this paragraph.**
    - Its fallback was `sameTown || room[0]` — the earliest day with a free slot, with no regard at all for the towns already on it — so a legal four-town day quietly became five or six. ⚠ AND IT RUNS ON THE FIVE-MINUTE CUSTOMER SYNC, long after the rebuild: Recalculate everything laid the season out correctly and the next sync undid it. That is why the warning kept coming back on its own and why pressing the button looked like it did nothing.
    - Now `sameTown || roomy || room[0]`. A day already working that town still wins (the crew is driving there anyway); failing that it takes the earliest allowed day that is still UNDER the limit. ⚠ IF NOTHING IS UNDER IT, THE HOUSE STILL MOVES and the day is reported in `crowded` — a customer hung in a month they ruled out is worse than a crowded day, but doing it silently is how this stayed invisible.
    - `maxTownsPerDay()` is now the ONE answer (`CREWS_PER_DAY * 2`), shared by the sweep and by `crewDaysOverTownLimit`. It used to be the number 4 written into the check, so setting the office to one crew a day moved the builder and left the check behind. `dayTownCount` counts TOWNS, not houses — five houses in one town is the emptiest kind of day and exactly where a stray should go.
    - ⚠ The warning no longer says "built before the current rules". Naming the wrong cause costs a rebuild every time and teaches the office that the button does not work.
    - Suite 98, and Suite 46 now lifts the two helpers instead of stubbing them. Nine sabotages red-checked.
  ⭐ WHICH TOWNS ARE ACTUALLY CLOSE (added 2026-08-20). Owner: "right now you have too many cities marked as close to each other, we dont want every city bordering each other we want citys that are actually close so what I will say is no one city can have more than two cities listed, as for example lehi would be saratoga and american fork... sometimes it might be hard to figure that out because you have really small cities that need to have a listed neighbor too but the city already has two big cities next to it, so in those cases they can have 3 neighbors."
    - ⚠ THERE WAS NO LIST AT ALL. `settings/scheduling.nearbyTowns` was empty, so every pairing fell through to the eight-mile tape measure — which on the Wasatch Front is most of the valley. That is the "too many cities" she is describing, and it is why a crew could be handed towns that merely happen to be within eight miles rather than ones you would actually work together.
    - `DEFAULT_NEARBY_TOWNS` now ships in the page: 30 towns, TWO each, written as a CHAIN rather than a mesh (Lehi — American Fork — Pleasant Grove — Lindon — Orem, and so on down the freeway). A chain is what "one dominant city plus the one next door" looks like written out. An empty settings list falls back to it; anything the office types replaces it wholesale.
    - ⚠ IT IS SYMMETRIC ON PURPOSE, and Suite 102 enforces that. `townsAreNeighbours` accepts a pairing named from EITHER side, so a one-sided entry still works — and quietly makes the two-per-town count untrue for the town on the receiving end.
    - THE ONE EXCEPTION is exactly the case she described: Vineyard is small and sits between Orem and Provo, both of which already have their two, so OREM CARRIES A THIRD. It is the only three in the list and Suite 102 asserts that — "in those cases they can have 3" is about a small town with nowhere else to go, not a licence to round everything up. Eagle Mountain, Vineyard and Levan keep a single neighbour because there genuinely is only one.
    - ⚠ A FAILED SETTINGS READ FALLS BACK TO THE BUILT-IN LIST, NOT TO `{}`. The older rule still holds (a previous read must not be left standing, or towns get paired off a list nobody can see) but resetting to empty is not neutral either — empty means the tape measure. Suite 102; six sabotages red-checked.
  ⭐ ONE MAN INSTALLS (added 2026-08-20). Owner: "if putting 4 towns in a day make it so we end up with like 4 houses a day stretched across 4 days list those as one man installs", and "create a tab for one man installs... it should be in sync so as the season goes if we get more one man installs or get less then its in sync and it gets rid of them in that tab or adds them." Refusing to crowd a day has a price and this is it: a handful of houses in a town nobody else is going to, which the rescue may no longer sweep onto somebody else’s sheet. That is not a failure to hide, it is a day that wants one installer.
    - `oneManMaxHouses()` is HALF A CREW DAY, derived from `MAX_STOPS_PER_ROUTE` rather than typed, so changing the crew size moves it. `isOneManDay` excludes an EMPTY day (nothing for one man to install) and excludes takedowns and fixer routes — a fixer route is already one person in one van.
    - ⚠ NEVER STORED. `renderOneMan` asks `oneManDays()` at draw time and keeps no list of its own, which is the whole of "in sync": a day that fills up leaves the tab on the next draw and one that empties joins it. A stored flag would be right on the day it was written and wrong every day after. Suite 99 proves this by RUNNING the renderer three times with the season changed underneath it — a source check for the `oneManDays()` call is not enough, because a red-check that wrapped it in a cache kept the call and froze the tab.
    - ⚠ The reschedule control is per HOUSE. The change handler reads `data-move` as a house id and calls `moveHouse`; a day-level select renders perfectly and does nothing at all when used, which is worse than not offering one.
    - ⚠ ADDING A PANE MEANS ADDING IT TO `syncTabs`. It walks a list of pane ids and sets `.hidden` on each: leave the new name out and the pane never hides, and sits underneath whichever tab is showing. (Removing one without trimming the list throws on load instead — both directions bite.)
    - HER PRIORITY ORDER, checked against what the code already does and found to MATCH, so nothing was reordered: "first priority do the house when the person wanted or close to it, next priority is new hangs first and then next priority is calculate in a way that gets rid of 1 man installs." Timing is a HARD GATE (`houseAllowedFrom` / deadlines), new hangs are first WITHIN what timing allows (`houseInstallPriority` returns 0 for `chargeNewMemberFee`), and the near-empty-day rescue is last and now bounded by the town rule. ⚠ Do not "fix" this by making new hangs outrank timing outright — see the 2026-08-17 entry above, which is the same rule stated from the other end.
  ⭐ THREE SIZES OF DAY (changed 2026-08-20, hours after the entry above). Owner: "any days with under 20 houses but over 8 should be one crew and one crews are fine what we want to minimize the most is one man days we would rather one crew", then "on one crew days you should be able to pick which crew."
    - **8 or fewer = one man. 9 to 19 = one crew. 20+ = two crews.** ⚠ EIGHT IS HERS AND IS NOT A FORMULA. The entry above shipped it as half a crew-day, which made it ten and quietly called two perfectly good one-crew days one-man days. `ONE_MAN_MAX_HOUSES = 8`. The UPPER edge IS derived, because "under 20" means under a crew day.
    - ⚠ A ONE-CREW DAY IS ONE SHEET. `dayCrewHouses` puts everything on the chosen crew and leaves the other empty, so the panel, the printed sheet and the route generator all agree there is one run. Splitting fourteen houses into seven and seven sends two crews out for a day one of them could do, which is the waste she is describing.
    - ⚠ SIZE ALONE IS NOT THE QUESTION. A crew is its own town plus at most one other, so a light day over THREE towns still needs two crews however few houses are on it — merging it hands one crew exactly the sheet the town rule above exists to prevent. `dayCrewCount` checks both.
    - ⚠ AND IT COUNTS THE WORK THE CREWS ACTUALLY HOLD (`dayAssignedHouses`), not every house on the date. A stop in a town neither crew works is already reported by `unassignedHousesFor`; counting it here would send a SECOND crew out over a house nobody has agreed to drive to, turning one honest sheet of twelve into two thin ones. `dayCrewTowns` reads `day.houses` directly, so asking it from there cannot loop back.
    - ⭐ `day.soloCrew` IS THE ONE THING ABOUT A DAY’S CREWS THAT IS STORED, and it has to be — there is nothing to work "which crew" out from. The town split is still derived every time it is asked; that rule has not changed. ⚠ `serialize` and `hydrate` name day fields EXPLICITLY, so a field left out of either list silently does not survive a reload. A day that stops being a one-crew day ignores the stored value rather than losing it, so filling a day up and emptying it again does not forget the choice.
    - ⭐ THE RESCUE NOW ONLY SAVES ONE-MAN DAYS. `wastedDay` was a flat 12, so days of nine, ten and eleven — perfectly good one-crew days — were taken apart and their houses posted to crews further away than the borrowing rule allows. It is `ONE_MAN_MAX_HOUSES + 1` now. This is "we would rather one crew" expressed as a number.
    - The day list badges each install day `1 MAN` or `1 CREW`: a one-man day and a two-crew day look identical in a list of dates, and the difference is who has to be rostered. Suite 100; nine sabotages red-checked.
  ⭐ A FULL CREW-DAY, EVERY TIME — AND DO NOT REBUILD WHAT WAS REMOVED (settled 2026-08-20). Owner, after two different attempts at a tidier tail were built and shown to her: "actually no we dont want to fill up 1 man days to 15 because we want to prioritize everyday having 40 houses more than the ending days having 15 because the one man days will fill up so it just needs to recalculate it so it turns to 1 or two crew rather than staying one man."
    - ⚠ TWO MECHANISMS WERE BUILT HERE AND BOTH WERE REMOVED. Do not re-add either without asking her again: (1) RESERVING flexible people in a town so its late openers had company, and (2) TRIMMING a crew-day so the leftover reached fifteen. Each produced a neater tail and a slower start, and the start is what she cares about — forty houses a day early is how the season does not fall behind.
    - A SHORT DAY AT THE END IS NOT THE BUILDER’S PROBLEM TO SOLVE, because it fills itself: crews do not finish every house every day, the leftovers roll forward into `movable`, and the next Recalculate lays them out again. Owner: "every day scheduled for one crew should fall at the end of the season but they likely will pick up more because every crew wont finish all their houses everyday."
    - What survives from all of it, and is the useful half: the near-empty-day rescue still fires only on genuinely one-man days (`wastedDay = ONE_MAN_MAX_HOUSES + 1`), which is the mechanism that turns a short day into one or two proper crews on the next recalculation; and One Man Installs still LISTS them so the office can see them. Suite 22 carries checks whose only job is to fail if either removed mechanism comes back.
    - Stated for the record, since it was asked in these words: the rule is EVERY CREW OVER 8, AIM 15 PER CREW — but aiming is all it is, and it never costs a full early day. A day is two crews, so a full day is forty and a full crew is twenty.
  ⭐ THE CREW GOES WHERE A FULL DAY IS, NOT WHERE THE MOST HOUSES ARE (added 2026-08-20). Owner: "we want to fill up everyday to 40 so then the days with less than 40 fall at the end, it should only not be like that if enough people requested a later date", and "we dont want one man crews until the end of the season."
    - ⚠ HOUSES WAITING IS NOT A DAY’S WORTH. The crews were sent to the two BIGGEST towns, which is right until a town runs low: twelve left somewhere with no neighbour free makes a crew-day of twelve, while ten next door to a town holding thirty makes twenty. `fillableCount` asks what a town can PRODUCE — its own allowed houses plus what ONE available neighbour could lend, capped at a crew-day — and the pick goes urgency first, then that, then houses waiting.
    - ⚠ EARLY IN THE SEASON IT CHANGES NOTHING: every big town answers "twenty", they all tie, and the tie breaks on houses waiting exactly as before. It only decides anything once towns run out, which is where the short days were appearing. And a neighbour the OTHER crew has already taken lends nothing, so it must not make a town look fillable.
  ⭐ A ONE-CREW DAY NEEDS ITS TWO TOWNS TO BE NEIGHBOURS, NOT MERELY TWO (fixed 2026-08-20). The owner caught this on the live book: 18 November came out badged ONE CREW holding Draper and Springville — forty miles apart, thirteen houses, one sheet. `dayCrewCount` counted the towns and stopped there; two is the right ceiling and counting was the wrong test. It asks `townsAreNeighbours` now. Two crews with one town each is the correct answer for a pair like that EVEN THOUGH thirteen houses would fit on one sheet.
    - ⚠ A FIXTURE FOR ANY ONE-CREW BEHAVIOUR NEEDS TOWNS THAT REALLY ARE NEIGHBOURS. The sandboxes have no customer coordinates to measure between, so they supply a nearby-towns list exactly as the live page does. Six sabotages red-checked.
  ⭐ A NAMED DAY, ONCE OPEN, GOES AHEAD OF THE PEOPLE WHO DO NOT MIND (added 2026-08-20). Owner: "for people who have exact dates try to do it close to then, having it as a floor is good but try to do their house the next possible chance after that but thats not high on priority." ⚠ A FLOOR ALONE ONLY SAYS NOT BEFORE: somebody who asked for 9 November was then ordered BEHIND every Any house, because those open on day one of the season and sort earlier by `from` — so the one person who named a day waited longest of anybody.
    - It is a TIEBREAK INSIDE THE TIER, which is what "not high on priority" means. It never jumps a tier: an October house still outranks a named day in November. Among named days it stays earliest-first, so somebody already past their day is not overtaken by somebody whose day has only just arrived.
    - ⚠ `rebuildSeasonDays` SETS `named` FROM THE REAL SPELLING (`1-Nov`, `11/1`, `11/9+`), and every fixture in Suite 22 hands the flag in ready-made — so none of them notices if that stops happening, and then no real customer is ever flagged however good the sort is. Asserted separately. Nine sabotages red-checked, including two that were vacuous first time: a twenty-house fixture where the whole town fits in one day proves nothing about ORDER.
  ⭐ THE PLAN IS TWO ROUTES A CREW CAN DRIVE, AND ONE BUTTON MAKES ALL OF IT (added 2026-08-20). Owner: "we need to add the route generator into schedule because we need it to print in the right order, it should create two routes one for each crew and it should automatically generate the route when they click rebuild days or refresh towns, also those two buttons just need to be combined because I want one button that just makes everything recaluculate but not recalculate houses within two days cause weve already printed those houses so it needs to keep track of the time US mountain time to make sure if a day is within 48 hours it is unchangable."
    - THE BUILDER SAYS WHICH HOUSES SHARE A DAY; NOTHING SAID WHAT ORDER THEY WERE DRIVEN IN. The panel, the printed sheet and the CSV all showed whatever order the builder happened to place them in. `generateDayRoutes` orders each crew's houses through `reorderFlatStops` — the SAME nearest-neighbour-plus-2-opt the crew's own Routes tab uses — and writes the day back crew 1 then crew 2. ⚠ A second orderer written here would eventually disagree with that one, and the one nobody looks at is the one on the paper.
    - ⚠ THE ORDER IS INSIDE A CREW, NEVER ACROSS THE DAY. Ordering the day as one list interleaves the two towns and sends each crew into the other's. ⚠ AND A FIXTURE FOR THIS MUST INTERLEAVE THE TOWNS ON THE GROUND: with them in two separate clumps, one list gives the SAME answer as two, so a check against clumped towns passes whether the split is honoured or not — the first version of Suite 77 did exactly that and a sabotage deleting the split went straight through it.
    - ⚠ THE CREW SPLIT IS STILL NOT STORED. Only the ORDER is written back; `dayCrewTowns` still derives who holds what, every time it is asked. See the entry above for why storing it breaks an iced-off day.
    - ONE BUTTON: **⚙ Recalculate everything** (`recalcBtn`) replaces ⚙ Rebuild days and ⟳ Refresh towns. They were always pressed together and IN THIS ORDER — a rebuild groups by TOWN, so rebuilding before pulling the corrected towns across lays the whole season out from towns somebody has just fixed. One press syncs the customers, rebuilds, generates, and says what all three did. Still one press to undo (`preRebuild` is taken before any of the three run).
    - ⚠ 48 HOURS, IN MOUNTAIN TIME, IN THREE PLACES: `rebuildSeasonDays` (EITHER that or the older `pinHorizon` locks a day — whichever is longer; a lock that shortens is a printed sheet that moves), `generateAllRoutes`, and the periodic customer sync. Re-ORDERING a printed sheet does the same harm as moving a house off it. `routeDayIsLocked` is measured to the day's START, because the truck is loaded the night before.
    - ⚠ THE PERIODIC SYNC RE-ORDERS WHAT IT MOVED. `enforceInstallTiming` drops a house on the END of its new day, so the day it landed on is no longer in driving order. Only when a house actually changed day or town — a changed phone number does not alter a route, and re-ordering the season every five minutes would.
    - FIXED ON THE WAY PAST: a crew sheet for a crew holding TWO towns printed EMPTY. `crewSheetRows` filtered against `crewCityFor`, which JOINS a crew's towns into one string, while the button offering the sheet counted `crewHousesFor`. A crew day is one town plus at most one other, so that was every mixed day the builder makes. Both ask the same question of the same list now, matched by POSITION rather than by id — an imported plan row need not carry an id at all.
    - Suite 77 RUNS the generator, the crew sheet builder and the panel's own grouping against fixtures. Seven sabotages red-checked.
  ⭐ THE CREW SPLIT IS EVENED, BUT ONLY BACK TO THE CAP (changed 2026-08-20). Owner: "each crew should have 40 houses but for some reason one crew gets 1-19 and the other crew get 20-40 the problem with that is one of those lists are 19 houses and the other is 21" — asked which rule she meant: "we want two crews two towns 20 for each 1-20 and 21-40". `dayCrewHouses` splits by town as before and then hands houses BACK from a crew that is over `crewCap()` to one that is under it; `crewHousesFor` reads that, so the panel, the printed sheet, the CSV and the route generator all see the same split.
    - ⚠ IT IS A HAND-BACK, NOT A LEVELLER. It stops the moment the big pile is down to the cap and never hands houses to a crew already at it. Nineteen and twenty-one is one house moving. A day of thirty and four STAYS twenty and fourteen — levelling it to seventeen and seventeen drags thirteen houses over a town line, which is the opposite of one-crew-one-town, and an earlier version of this did exactly that. Twenty-five and nineteen comes out twenty-four and twenty: on an overloaded day the crew that was already over stays the one that is over, rather than the loop swapping the overflow back and forth until both are over.
    - ⚠ HISTORY, so nobody "fixes" this back: until 2026-08-20 there was deliberately NO balancing code, and this note used to say so. It was checked against the real builder first — 14 in Lehi and 19 in Herriman already came out as 14 and 19, and what stretched the season was one-crew-one-town, not a split. The hand-back exists because she reported the 19/21 sheets, not because even numbers are a goal in themselves.
    - Suite 93 runs the split against fixtures. Six sabotages red-checked, including the `crewCap` fallback: with `MAX_STOPS_PER_ROUTE` gone the cap must still be twenty, or the hand-back silently never fires.
  ⚠ THE THING THAT MAKES IT SAFE. `routeCityOf` reports only the MAJORITY town of a route, and `stopProblem` evicts any stop whose town does not match — on autoBuilt days, which is what these are. So a mixed-town day MUST record every town it is allowed to hold (`towns` on the scheduledRoutes doc), `townsByRoute` must read it, and `stopProblem` must test membership rather than equality. Break any one of those three and the sweep evicts the borrowed houses on the next pass while the builder puts them back on the one after — build, evict, rebuild, for ever. Suite 27 red-checks all three.
  Two rules survive and are asserted, because they are cheap to lose in a packing change: never more than 20 on one crew, and never two crews in the same town on the same day — where a BORROWED town counts, which the crew→town map alone cannot see, so every town worked on a date is tracked separately.
⭐ WHO PAYS FOR WHOM — one bill, several houses (added 2026-08-19). Owner: "We need a way for members to see who exactly they are paying for this year if they have multiple bills in member portal on the RSVP email and we need a section were we can see it in costumers as well." A payer can be billed for several houses — a parent paying for a child, a landlord paying for tenants, a cabin as well as a house. That has always worked on the back end and has never been SAID anywhere, so the customer read a figure bigger than the house they were looking at and the office had to search a phone number to find out why. Three surfaces answer it now: the portal panel (`renderPortalHouses`), the `{{houses_block}}` email token which the RSVP send appends by itself, and Customers → **Who Pays for Whom**.
  - ⭐ THE GROUP HAS TWO HALVES AND EVERY SURFACE MUST USE BOTH. A house with `billToPhone` set joins that payer's bill; a house with NO billToPhone whose `custInvoiceKey` matches is ALREADY on that invoice with no field set anywhere. `syncPayerInvoice` sums both. A billToPhone-only scan finds the first half, so two houses on one phone read as separate on the office screen while the customer's portal — which reads the invoice — lists them together. `billingGroupsByPayer` in admin.html is the one place that decides; `billedHousesByIds` / `billedHousesByKey` in functions/index.js are the server pair.
  - ⭐ WHOSE NAME GOES ON A SHARED BILL: THE LOWEST CUSTOMER NUMBER WINS — the longest-standing account, and so the likeliest payer. One function, `payerHouseOf` in admin.html, and `runInvoiceBatch` in functions/index.js mirrors it. Four Anderson houses share 8013721805 (Heather #14, Brit / Dani #20, Loren #27, Ryan #972); seventeen numbers in the real book are shared this way. It used to be a `.find()`, so the bill was addressed to whichever house came back first and editing any sibling silently changed the name. The point is less which rule than that it is STABLE: the same houses give the same answer whatever order they arrive in. Where nobody has a number it falls back on the document id, never arrival order.
  - ⚠ AND THE FIX WAS FIRST WRITTEN IN ONLY ONE OF THE THREE PLACES THAT ASK (2026-08-20). It went into `syncPayerInvoice` that morning; `billingGroupPayer` and the server's nightly run were left as `.find()`. The owner reported the result the same day: the money list said Brit / Dani while Who Pays for Whom said Heather, about the same four houses. A rule about who gets billed cannot have two implementations — `billingGroupPayer` is now a one-line call to the shared rule, and Suite 83 runs all three callers against the same fixture.
  - ⚠ AND THE STORED INVOICE NAME IS STALE UNTIL SOMETHING RESYNCS THAT PAYER. That is the second half of what the owner was looking at, and fixing the rule alone would have changed nothing on her screen: `invoices/8013721805` still held the name the bug put there, and nothing was going to rewrite it until an unrelated edit happened to touch one of those four houses. `invoiceDisplayName` derives it from the houses at render time instead, falling back to the stored name (never to blank) when an archived invoice has no group left. The money list and its search box both use it. Nothing was written to the database to correct the old names — they correct themselves on screen, and in the document the next time anything syncs.
  - ⚠ A HOUSE THAT SAID NO IS NOT ON THE BILL AND MUST NOT BE NAMED. It is out of the total, so naming it prints a list that does not add up to the amount underneath it — which is worse than saying nothing.
  - ⚠ THE BILL NAMES ITS OWN HOUSES. `portalInvoice` returns them, resolved from the invoice's own `billedHouseIds` — the list the total was summed from, so the rows and the amount cannot drift. It used to come from `portalLookup` alone, and only the token link and the email sign-in go through that: the ordinary phone-and-surname sign-in calls the invoice straight away, so for most customers the panel could never appear at all.
  - ⚠ NAMES, NOT ONLY ADDRESSES. Two streets do not tell a parent which of their children is on the bill. That is the whole question.
  - ⚠ THE EMAIL BLOCK GOES ONLY TO THE PAYER. A house billed to somebody else pays for nothing, and sending it the group would name a landlord's other tenants and their prices to a person who is not paying for any of them.
  - ⚠ AND ONLY WHEN THERE IS MORE THAN ONE, everywhere. "This bill covers 1 property" is a paragraph and no information.
  - ⚠ DO NOT ADD `where('phone','==',digits)` TO THE SERVER FALLBACK. Stored phones are not all digits-only — the office types "(801) 555-0123" and the import keeps it. That is the same mistake that quietly duplicated the whole book once (see §5). `billedHousesByIds` avoids the question entirely.
  - Suite 5 §5.9 and Suite 69 of run-all.js cover it, and both RUN the code rather than matching text — the panel is rendered against a fake DOM and the grouping is executed against a fixture. Red-checked with six sabotages.
⭐ THE SCHEDULE TAB NOW MARKS CUSTOMERS COMPLETE, AND SO CAN TRIGGER BILLING (changed 2026-08-21, Job 2). Owner: "Schedules should mark routes as complete automatically. However that does not mean takedown is done", and, asked whether that meant one stop or a whole day: ticking ONE stop marks that customer complete.
  - ⚠ IT USED TO BILL NOBODY. Ticking a house set `done` on the plan document and nothing else — the tab's own footer said so in as many words. The nightly run bills on `completed` on the CUSTOMER, so with the crew portal out of use this season, working the season off Schedule invoiced no one at all. That footer has been corrected; it now says what the tick actually does.
  - ⭐ `hlxMarkJobDone(target, kind, done, opts)` IS THE ONLY WAY A JOB IS MARKED DONE. Five doors call it: the Schedule tick, Schedule All done, the leftover flow, Mark Done on a Routes stop, and the customer-row dropdown. A second copy of what "done" means is the copy that stops matching the others — which is exactly what the dropdown was.
  - ⭐ INSTALL, TAKEDOWN AND FIX ARE INDEPENDENT EVERYWHERE. Nothing may nest one under another. The customer-row dropdown wrote `removalDone: completed ? removalDone : false`, so unticking Install Complete silently cleared the removal too, and greyed the Removal box out whenever the install was unticked. Both are gone.
  - ⚠ THE DOCUMENT ID DECIDES WHICH CUSTOMER, never the customer number. `houseFromCustomer` builds `cust-<id>`; `hlxResolvePlanHouse` reads it and only falls back to number-then-phone for imported CSV rows that have no id. A customer number goes back to the pool on a recycle and is handed out again, so a stale plan row holding #479 can resolve to a different household — and marking THEM complete bills the wrong person. A `cust-` id that matches nobody returns null rather than falling through to the number.
  - ⚠ TAKEDOWN ROWS NOW CARRY `srcId`. `seedTakedowns` copies name, address and number off the install house, so every takedown tick used to depend on that same reused-number guess. Fixed at the source rather than guessed at the other end. Houses round-trip through serialize/hydrate wholesale (`Object.assign({},h)`), so it survives a reload without being named in a list — unlike DAY fields, which are explicit.
  - ⚠ THE PLAN'S OWN `done` FLAG IS STILL WRITTEN AS WELL. That is two copies of one fact, and Job 3 is what removes the stored one by deriving it. Until then every counter, the crew split, the near-empty-day rescue, One Man Installs, the printed sheets and the CSV all read the plan flag, so dropping it now would blank all of them.
  - ⚠ `applyLeftoverPicks` WRITES BOTH DIRECTIONS, UNCONDITIONALLY. The office is stating the day's final truth, so houses the crew missed are marked NOT done as well. A change-guard ("only write when the plan flag moved") looks like a cheap saving and silently leaves out exactly the houses that were changed from Routes or the customer row since — and a house left ticked that the crew never reached is invoiced that night.
  - ⚠ ALL DONE ASKS ONCE AND NAMES THE COUNT; the single tick is silent. Unticking a house whose invoice has already gone warns first, because unticking cannot un-send it — `invoiceEmailSent` is only ever cleared by Start New Season.
  - ⚠ THE FIX KIND DOES NOT TOUCH THE FIX PHOTO. That is Job 4 (park-then-destroy), undecided and unbuilt; destroying a Cloudinary asset with no undo behind it is not something to slip in here. And nothing in any kind touches the HOUSE photo, which prints on the new-hang crew sheets — its own check exists for that.
  ⭐ AND WHETHER A HOUSE IS DONE IS THE CUSTOMER'S ANSWER, NOT THE PLAN'S (Job 3, 2026-08-21). Job 2 left TWO copies of one fact — the plan's `done` flag and the customer record — and they disagree the first time somebody uses the other screen: mark a house complete from Routes or the customer row and Schedule still showed it outstanding, counted it in "left", put it back in the movable pile and printed it on a crew sheet.
    - `derivedDoneFor(h)` reads three DIFFERENT fields, because they are three different questions: install → `completed`, takedown → `removalDone`, fix → `!needsFix`. ⚠ A fixture for this must make those fields DISAGREE for the house being checked — the first version had a fix row whose customer was both `completed` and not `needsFix`, so swapping the fix rule for the install rule gave the identical answer and the check stayed green.
    - ⚠ THE STORED FLAG IS THE FALLBACK, NOT THE SOURCE, and returning `false` instead of `null` for "nobody behind it" would blank an imported season: an imported CSV row never matched anybody, so most of the plan has nothing to derive from. It also means the screen is untouched while `jobAddresses` is still loading, rather than showing every house as not done for a second after login.
    - ⚠ REFRESHED ONCE PER RENDER, AND THE TWO DOZEN READERS ARE LEFT ALONE. `refreshDerivedDone()` runs at the top of `renderAll` — before `renderStats` counts what is left and before `scheduleSave` writes the plan back — so the crew split, the 20/20 hand-back, the near-empty-day rescue, One Man Installs, `unfinishedOn`, the leftover flow, the progress bar, the CSV and every print sheet keep reading `h.done` and know nothing about any of it. Rewriting two dozen call sites would have been the same behaviour with two dozen chances to miss one.
    - ⚠ THE SAVED VALUE IS A CACHE, NOT A SECOND SOURCE. `renderAll` ends in `scheduleSave`, so the plan keeps a mirror of the customer's answer — overwritten from the customer before every read, and recomputed on the first render after a reload. What it buys is a sane fallback for a house that later stops resolving.
    - ⚠ A TICK MIRRORS INTO THE LOCAL `jobAddresses` CACHE BEFORE THE WRITE IS AWAITED. The Firestore write is asynchronous and the tick is now derived, so without the mirror `renderAll` re-derives from the stale cache and the tick visibly springs back — and the office presses it again, putting two writes in flight. Same optimistic mirror the rest of admin already does, and it must be BY KIND or ticking a takedown looks like it did nothing.
    - ⚠ AND IT NEEDS AN ID INDEX. This runs once per house per render; resolving `cust-<id>` with a `.find()` over ~1,000 customers is ~955,000 string comparisons a render — the exact shape that once locked the screen up on every keystroke. `custById` joins the other customer indexes in `rebuildCustomerIndexes`.
    - Suite 127. Seven sabotages red-checked, one of which caught the vacuous fix-kind fixture above, and one of the suite's own arithmetic checks caught me assuming a missing `needsFix` reads as not-done when `!undefined` is true.
  - Suite 126, plus the leftover suite. Eighteen sabotages red-checked, and three of my own checks were caught being vacuous by that process: two matched `confirm(` in the source and stayed green when the whole guard was disabled with `if(false && …)`, and one used a regex that never matched the real markup. All three are behavioural or precisely scoped now.
The Schedule tab is NOT the crew's routes (clarified 2026-08-16; it does now read and write customer records — see above). `#scheduleHost` is a shadow-DOM planner saving to `routeSchedule`; the crew's days are `scheduledRoutes`, built by the reconcile sweep. `buildSeason` groups houses by the date column of an imported CSV, so the plan mirrors the spreadsheet and a crew-day rule change does NOT move its days. Two consequences worth knowing before promising the owner anything:
  - It kept its own copy of every house, so a town corrected in All Customers never reached it. ⭐ WIDENED 2026-08-17 (owner: "the schedule should check for changes in all customers periodically to update for changes"): `syncHousesFromCustomers` now pulls town, **install timing**, name, address, phone and notes across, driven three ways — on any customer change (via `safeRender('scheduleSync')`, so it follows the panel-deferral rule), when the Schedule tab is opened, and on a 5-minute timer as the backstop for a tab left open all day. ⚠ A BLANK on the customer never wipes what the plan holds, and timings are compared by MEANING (`prefKey`) not spelling — "OCT" and "October" are the same thing, and treating them as different would rewrite every house on every tick and toast at the office for ever. It matches on customer number then phone, NEVER on address: `custByAddrKey` is keyed on address AND town, and the town is the field known to be wrong, so it would miss exactly the houses it exists to find.
  - ⭐ AND IT ADDS PEOPLE IT HAS NEVER HEARD OF (added 2026-08-20). Owner: "rachel oslund is a new customer but when i click recalculate everything rachel oslund is not put into the schedule, the list of customers needs to be synced to both of these." Every sync above WALKS SEASON and corrects houses that are already there, so a customer added in All Customers after the import had nothing to correct and literally no way onto a day — the only route in was Replace imported file, which throws away every tick and pin. `customersMissingFromSeason` finds them, `houseFromCustomer` builds the house, and `rebuildSeasonDays` pushes them into the movable pile.
    - ⚠ IN-FOR-THE-SEASON IS NOT RE-DECIDED THERE. It asks `isOutForSeason`, the one definition the route generator and the nightly fill already share. A second opinion living in the schedule is how those two start disagreeing about who is coming.
    - ⚠ ALREADY-HERE MEANS ANYWHERE IN SEASON, including days inside the 48-hour lock and days already worked — matching only the movable pile hands somebody a SECOND day while the crew already has them on paper. And a TAKEDOWN OR FIX DOES NOT COUNT: both are copies made from an install house, so somebody holding only a takedown still needs hanging.
    - ⚠ THE HOUSE ID IS DERIVED FROM THE CUSTOMER (`cust-<docId>`), never a counter. Rebuilds run over and over; a counter hands the same person a fresh id every time, and the house ticked off on Tuesday stops being the house on the plan on Wednesday. `findHouse` compares as strings, so the prefix cannot collide with the plain 0, 1, 2 the CSV importer hands out.
    - ⚠ IT MUST RUN BEFORE the `if(!movable.length)` early return, or a season whose only outstanding work is one new customer reports "every house is done" and stops — which is the exact silence she reported.
    - Suite 96. Fourteen sabotages red-checked across this entry and the two below.
  ⭐ THE PRINTING TAB (added 2026-08-20). Owner: "lets make a tab for printing, a main button for print today, which would be the warehouse list (for two days from now) and both crew lists for tomrrow morning (should be on a seperate page) so it prints 3 papers", plus print recycles, needs-building, warehouse, crew 1, crew 2 and color changes.
    - ⚠ THE PER-DAY BUTTONS IN SCHEDULING STAY. Owner: "in schedule we want to keep the feature where we can print individual days and I can print any day in schedule so keep that their." `printDaySheet` and `printCrewSheet` are untouched; this tab is the morning routine, not a replacement. A check exists whose only job is to fail if they are removed.
    - ⚠ PRINT TODAY USES TWO DIFFERENT DATES ON PURPOSE, and it is the thing most easily got wrong: the crews get the NEXT working day, the warehouse gets the one AFTER, because the warehouse builds ahead of the van. And "tomorrow" means the next day the crews actually work, not tomorrow’s date — printed on a Friday the crews’ next day is Monday, and asking for Saturday hands the office three blank sheets. Asserted by RUNNING printToday with the page builders stubbed: a source check for `printWarehouseDay()` passes while the page is built from the crew’s day, because the call is still there on the line above doing nothing.
    - ⚠ EVERY LIST IS NUMBERED FROM 1 AND ENDS IN A BLANK COLUMN, and both live in `printTableHtml` so no list can forget. Crew two restarts at 1 — their sheet is their day, not a filtered copy of crew one’s numbering. Columns per list are fixed by `PRINT_COLUMNS` and each one is asserted by name.
    - ⭐ A REQUOTE IS A NEW HANG, A COLOUR CHANGE IS NOT (for the photos). THE ORDER OF THE TWO LINES IN `printIsNewHang` — `lightsChangedAt` tested BEFORE `chargeNewMemberFee` — was the whole rule while a colour change set `chargeNewMemberFee` (2026-08-19 to 2026-08-21). ⚠ SINCE 2026-08-21 A COLOUR CHANGE NO LONGER SETS THAT FLAG (see the fee entry below), so the ordering is now harmless rather than load-bearing. IT IS KEPT ANYWAY, deliberately, and so is the check that asserts it: every customer whose colours were changed through the portal between those two dates still CARRIES `chargeNewMemberFee` from the old model, and testing the fee first would print a photo of a house the crew has already hung for every one of them. Do not "tidy" the order away.
    - Photo sizing is about ink and eyesight: `height:1.5in`, `width:auto`, `max-width:3.5in`, on a landscape page — a tall crop of a wide house is mostly sky and driveway. Captions sit UNDER the picture and nothing is absolutely positioned, which is exactly how a caption ends up printed across somebody’s garage. A new hang with no photo on file is skipped rather than shown as a broken frame.
    - ⚠ ADDING A PANE MEANS ADDING IT TO `syncTabs` (see the One Man Installs entry below — same trap, twice). Twenty-three sabotages red-checked; two passed at first because they matched a string that also appears elsewhere in the same block, so both are behavioural now.
  - ⭐ THE NEW MEMBERS TAB IS GONE (removed 2026-08-20). Owner: "because we just want it to be in sync we shouldnt need the new members tab in schedule anymore." It answered "did this new customer make it onto a day", which was a real question only while the plan could learn about somebody through a re-import. ⚠ `syncTabs` WALKS A LIST OF PANE IDS AND SETS `.hidden` ON EACH: taking the pane out of the markup while leaving `members` in that list is `getElementById` returning null, a TypeError on load, and a blank Schedule tab with nothing on screen to say why. The New members COUNT stays on the stats bar. Suite 97.
  - ⭐ ONE BUTTON, FINALLY (2026-08-20). `Reload saved` and `Replace imported file` are gone too — owner: "get rid of the button that says reload saved because we dont need that and replace imported file I dont even know what that does". ⚠ That request was made once and lost once: a parallel session’s one-button change removed Rebuild days and Refresh towns but left these two, and the branch that had removed them was dropped in favour of theirs. The import screen is still reachable when there is no saved plan, so a first import still works. Suite 97.
  - ⚠ "After Thanksgiving" was read as NO PREFERENCE and offered the first day of October — the one month those customers ruled out. It matches neither `OCT` nor `NOV`, and the master sheet writes it `THX`, so this was live. `houseAllowedFrom` now holds it to the day after Thanksgiving and `houseInstallPriority` sorts it last. Fixed 2026-08-17 while widening the sync, because syncing the timing across would have spread it further. "November - Before Thanksgiving" still starts with NOV and stays a November house.
  - A pinned day shows as SET and holds its exact date while everything re-flows around it. Owner: "a day should only be set if the day falls within the next two business days." `effectivePin` enforces that; the pin is ignored, never deleted, and starts holding again on its own once the date is close. Fix routes are exempt — a fixer route IS its pin and is laid out separately.
A corrected town only moves a house on a day the scheduler BUILT (`autoBuilt`). Hand-built days are left alone on purpose — a route somebody assembled may deliberately carry a house from the next town over. So "I fixed the town and Routes still has them on the old day" is expected behaviour on a hand-built day, not a bug; say so rather than hunting for one.
Panel data loads on open (added 2026-08-16, after the owner reported the admin page eating memory). initData() used to call every load*() at login, opening 39 permanent Firestore listeners and holding every collection in the browser whether or not that tab was ever clicked — two years of clock-ins, every card transaction, every employee record and every website image, loaded to look at a quote. It now opens 15; the other 24 wait until their panel is first opened, via PANEL_DATA / panelDataGroup() / ensurePanelData(), hooked into switchToAdminPanel(). Nothing was removed — each panel loads exactly what it always did and keeps it for the session, just later.
  Three things to know before touching it:
  - It is grouped, not per-panel, because the data is shared. The Dashboard's finance figures read the time logs (allTimeLogsCache) and the card transactions (ccTxnCache), so opening Dashboard pulls the money AND people AND timelogs groups. Adding a panel that reads another panel's cache without adding that group draws a confident $0 where a real number belongs — worse than being slow. Check the cache's read sites, not just its panel.
  - The badge loaders stay eager on purpose: loadQuotes, loadMessages, loadEmployeeNotes, loadProjectTests, loadNightlyHealth. A sidebar badge has to be right before anyone clicks anything, so it cannot wait for its panel.
  - Suite 21 of run-all.js enforces all of it — every loader named in the map is a real function, every panel key is a real nav panel, every group asked for is handled, both wiring points exist, the badge loaders are still eager, and no deferred loader has crept back into initData(). Those checks exist because a typo in the map fails ONLY when someone opens that tab, and shows up as a silently empty panel rather than an error.
Panels DRAW on open too, not just load (added 2026-08-17, after the owner said the page was still using too much memory). Lazy data fixed the listeners; this is the other half. `renderJobAddressPanels` fires twenty-one renders on every customer change, and several build a row, an `<option>` or a Google marker PER CUSTOMER — for panels nobody had opened. Measured on the real book of ~1,031 houses that is roughly 122 elements per customer across the deferred renders, about 125,000 elements, plus a map marker each. `safeRender` now checks `RENDER_PANEL`: if that render's panel is not on screen the function is REMEMBERED instead of run, and `flushPendingRenders` runs it the moment the panel is opened (from `switchToAdminPanel`, after the panel is marked active, so anything the render itself triggers sees an open panel).
  Four things to know before touching it:
  - The DATA is untouched. `jobAddresses` still updates on every write, so every figure is correct the instant its panel draws. Only the drawing waits — the same trade the render coalescing already makes.
  - ⚠ A label with no entry in `RENDER_PANEL` ALWAYS draws. That is the safe direction: an unmapped render costs memory, a wrongly mapped one shows somebody stale numbers. Five are deliberately left eager (`cityDropdowns`, `etPreview`, `rsvpMemberSelect`, `rsvpRecipients`, `rsvpPreview`) because their output could not be pinned to exactly one panel.
  - Only the NEWEST pending render per label is kept. Replaying an older closure would draw figures that have already been superseded.
  - No sidebar badge goes through this. `badgeQuotes`, `badgeMessages`, `badgeProjTodo` and `badgeHealth` come from their own eager loaders, and Suite 38 asserts no deferred render writes any of them.
  Suite 38 enforces the rest: every panel named in the map is a real panel AND has a nav item, every label is a render that really exists, the five heaviest renders are actually deferred, and the behaviour itself is run against a fake DOM. Same reasoning as Suite 21 — a typo here does not throw, it just leaves a tab blank for ever.
Pasting the master sheet TURNS THE NAMES ROUND by itself (added 2026-08-17). Owner: "excel is last first but the website is first last". Excel says "Cattani Julie", the site stores "Julie Cattani", every time — so a fresh paste ticks the `rbFlipNames` box rather than leaving somebody to find a checkbox they did not know about, and the report says it did and shows the first three names as they will be SAVED.
  - It ticks ONCE per paste (`rbSplitSheetIntoBoxes._flipAutoSetFor` holds the sheet text it last did it for). Unticking and pressing the button again therefore sticks — without that, a sheet that really is First Last could never be imported at all. The state hangs off the function because the suite lifts the splitter out and runs it alone.
  - Businesses come out mangled by the flip ("Lehi Vision Care" → "Vision Care Lehi") and nobody spots one row in a thousand, so `rbLooksLikeBusiness` names them in the report. They are NOT excluded — the office may want the business listed like everyone else, and guessing either way is worse than saying so. The FIRST word is ignored because on a Last First sheet that is the surname slot: Mrs Church is a person, and flagging every one of her trains people to ignore the warning. On the real sheet it flags exactly two, both genuine.
⭐ BULK UPDATES IS RETIRED IN PRACTICE — USE THE SHEET COMPARISON (settled with the owner 2026-08-21). Owner: "It should update people that are already there but sometimes it misses people and adds new ones but we should no longer use bulk so this is no longer a concern." The everyday path for getting the master sheet and the website to agree is Bulk Updates → Use my master sheet → Compare → Sync (`rbApplyTickedAdds` and the ledger), which shows every difference for review before anything is written. The raw paste-the-columns importer stays in the page and still works; it is simply not the tool to reach for.
  - ⚠ AND THAT IS WHY TWO KNOWN GAPS IN IT WERE DELIBERATELY NOT FIXED. Neither `rbImportBtn` (Bulk Updates) nor `ibImportBtn` (Invoice Bulk Update) sets `needsLightBuild` when it ADDS a customer, so somebody created that way carries their colours and their wire and never appears in Needs Building — no bundle is made and the crew arrives at a house with nothing for it. That is exactly hole B, which WAS fixed for the sheet sync. It is one line each; what stops it being a one-line change is that both write hundreds of records per press, and a matcher that misses (a blank street column is what produced the ~944 duplicates) would queue the entire book for building on top of duplicating it. If either tool is ever brought back into regular use, fix this first and test the add path on its own.
  - ⚠ "SOMETIMES IT MISSES PEOPLE AND ADDS NEW ONES" IS THE DUPLICATE FACTORY, said by the owner in as many words. See §5 for the diagnosis — the guard `if(!existing && !street){ failed++; continue; }` was added afterwards and only covers the blank-street case.
Bulk Updates imports a fixed batch of rows at a time and REMEMBERS the place (added 2026-08-17). Owner: "we need to cut it up because it crashes at 250 so we need to go 250 at a time then refresh but it needs to remember", then the same day: "change it to 150 instead of 250 at a time". `BULK_CHUNK_SIZE = 50` — it went 250 → 150 → 50 in one afternoon as each size still used too much, so treat it as a dial the owner turns, not a settled number. One constant; the loop bound, the "next N rows" button and the progress line all read from it, and Suite 39 asserts its exact value so a change is deliberate; the loop runs `startAt`..`stopAt`, then writes the pasted columns, the cursor and the running totals to localStorage (`hu.bulkImportJob.v1`), reloads the page — the reload is the point, it is what hands the memory back — and shows a banner offering Continue. Finishing or cancelling deletes the saved job.
  - ⚠ ANYTHING THE IMPORT READS THAT IS NOT ONE OF THE TEXT BOXES HAS TO BE CARRIED IN THE JOB. Learned the hard way on 2026-08-17: the name flip is a checkbox, batching reloads the page, and the job saved only the eighteen `rbAreaIds` boxes — so the first batch wrote "Julie Cattani" and every batch after it wrote "Cattani Julie". At 50 rows a batch that is fifty right and nine hundred wrong, and it looks exactly like the flip never working. `job.flipNames` now travels with it, and Suite 41 walks every `getElementById` in the import AND in its per-row helpers (`rbName`, `rbCol`, `rbHeaderOffset`) and fails on any input that is neither a saved box nor in the job. Scanning the loop body alone would have missed it — `rbFlipNames` is read inside `rbName`.
  - ⚠ It REFUSES to run when `jobAddresses` is empty. Everything decides existing-or-new by looking there, so an empty list means every row is added as a new customer and the whole book duplicates. That is the one mistake with no cheap undo, so it is a hard stop before the loop, not a warning.
  - A saved cursor is only used when the boxes still hold the SAME paste (`bulkJobFingerprint` = row count plus the first and last three identifier values) and the same `BULK_IDENTIFIER`. A cursor from a different paste points into the wrong list; a mode change would match half the rows one way and half the other.
  - The boxes are NOT cleared while a job is unfinished, or a refresh loses the half that has not run. They are cleared, along with the whole-sheet box, only on completion.
  - A save that fails (quota, private mode) returns false and the page does NOT reload — it says to press Import again instead. Reloading with nothing saved would lose the run.
  - Suite 39 covers all of it, including corrupt/half-written storage being read as "no job" rather than throwing on page load. Red-checked with nine sabotages.
  Still unbounded, and deliberately left alone: the invoices and ccTransactions queries fetch every record ever, with no date window (timeLogs already caps at TIMELOG_WINDOW_MONTHS = 24). Windowing them would cut memory further but would stop older records appearing in those panels — that is the owner's call, not a silent change.
Project To-Do checklist: TEST_SEED (a plain-English test list in js/test-seed.js — it lived inline at the end of admin.html until 2026-08-14, when it was moved out because it was 217KB, 12.8% of that page, re-downloaded on every admin load and re-sent after every deploy) auto-syncs into the projectTests collection on every login — new tests get added, any test whose version number moved on since it was last scored flips to Retest (its old score and notes are kept, not lost; same-version wording tweaks just reword in place for free), and (changed 2026-08-17) any seeded row you REMOVE from TEST_SEED is deleted from the checklist on the next login whatever its score, because the seed is the single source of truth for the seeded range (projShouldPruneTest). One-off tests the owner adds by hand (num ≥ 1000) are never in the seed and are never touched. This only stays accurate if TEST_SEED itself is kept current — the sync can't detect that a button got renamed or a feature moved, it only ever pushes whatever is currently written there. Per §0, don't force a Retest for something the automated suite already covers, and add new coverage to the suite rather than as a new manual row. Whenever a change affects what a test describes, update that test's steps/expected and bump its version in the same commit.
3. Verification gates (must pass before every commit)
Run these and paste results into the commit/PR description.
A. Inline-JS syntax + <div> balance (catches the two most common breakages):
bash
npm run verify
This runs verify-syntax.js at the repo root. It parses every inline <script> in the three HTML files, parses functions/index.js, and checks that <div> opens equal </div> closes in each file. Exits non-zero on any failure, so it is safe to chain in CI.
It replaced a python3 heredoc that used to be pasted in here. Same checks, but it is Node-only (no python dependency on Windows or in CI) and it checks classic scripts and <script type="module"> blocks SEPARATELY. That separation matters: concatenating them forced the classic code to be parsed in strict mode, where a few legal-but-old constructs are syntax errors — that could report a failure that does not exist in a real browser.
An unbalanced <div> silently swallows every panel below it, which is why the count is printed even when it passes.
B. Test suite.
bash
npm test
That runs all three gates in order — verify (gate A), the money parity test, then run-all.js — and stops at the first failure.
⚠ CORRECTED 2026-08-14: the old instructions here told you to copy the suite into a tests/ subfolder via a shim (mkdir -p tests && cp run-all.js ...). That is no longer needed and has not been for some time — run-all.js resolves its own root by looking for admin.html next to itself, so `node run-all.js` works directly from the repo root. The shim still works, it is just pointless. Ignore any older note saying the suite is broken from the root; it is not.
Needs `npm install` once at repo root (jsdom — package.json and package-lock.json are committed). Without it, Suite 5's quote-card rendering checks silently skip instead of running — don't mistake that for a pass.
GAP lines are known disconnects (non-blocking, self-heal to PASS when fixed). Real regressions show as FAIL. A change is only clean if it introduces no new FAIL and doesn't turn a PASS or a closed gap back into a GAP/FAIL.
Baseline on 2026-08-14, after the whole audit shipped (§1–§6): 703 passed, 0 failed, 3 notes, 1 known GAP (two customers sharing a phone share one invoice key — the colorComboKey gap has since closed). Plus 9 on gate A, 17 on the selector contract, and 12 on money parity.
An earlier line here read "232 passed … 2 known GAPs" and was overwritten back to that once by a parallel session's edit to this file. Trust a fresh `npm test` over any number written down here; if the two disagree, the file is what is wrong.
Browser suite (`npm run test:browser`): 13 passed, 0 failed, re-run 2026-08-21. ⚠ It IS in CI — this line said "run by hand — it is not in CI" and that was stale; see §9.14 and §9.15. There are no known-red specs.
⚠ AN EXTRACTION SUITE CAN BE PASSING ON A FUNCTION IT NEVER SUPPLIED, AND ONLY A MACHINE WITHOUT node_modules FINDS OUT (2026-08-19). Several suites lift a function out of admin.html by name and run it in a `new Function` sandbox. Suite 68 was running `quoteAwaitsCustomer`, which calls `quoteAlreadyACustomer` — a helper the extraction list never named. It passed anyway, and passed on CI, because the jsdom-gated suites `eval()` their slices of admin.html into the GLOBAL scope, and one of those slices happened to carry the helper. The sandbox was reaching a global left behind by an unrelated suite.
  - Run it with no node_modules and jsdom is absent, those suites skip, the leak stops, and Suite 68 dies with ReferenceError BEFORE the summary — so `npm test` exits non-zero with no failure list at all. That reads like "the suite is broken" rather than "one name is missing from a list", which is exactly how much time it costs.
  - ⚠ SO A GREEN RUN DOES NOT PROVE A SUITE SUPPLIED ITS OWN DEPENDENCIES. If you add a helper that an extracted function calls, add it to that suite's extraction list in the same commit; if it reads module-level state (a cache, a `let`), declare it in the sandbox preamble. Never "fix" one of these by stubbing the helper — a stub makes the branch untestable while reporting green.
  - ⭐ THERE IS NOW A GUARD FOR THIS, added 2026-08-22 after it bit three times in one day (`houseDeadline`, then `nextInstallDayFor`, then `seasonYesUpdates`). `sandboxDeps(code, src, provided)` in run-all.js scans an assembled sandbox for names it CALLS that the source file defines as functions and the sandbox was never given; `assertSandbox(...)` turns that into a named FAIL listing them. The run finishes instead of dying, and the message says exactly which name to add to which list. ⚠ IT REPORTS, IT DOES NOT AUTO-WIRE — several harnesses stub on purpose, and silently substituting the real function would change what a test proves with nobody deciding to. That is also what keeps "LIFT, NOT STUB" a decision rather than a default. ⚠ A METHOD CALL IS NOT THIS BUG: `dt.getDay()` is Date's, and admin.html happens to define a `getDay` too, so the first version flagged a sandbox that was fine. Names preceded by `.` or `?.` are skipped. ⚠ It is wired into the two sandboxes that have actually broken (portalRsvp, enforceInstallTiming); add `assertSandbox` when you build a new one. And `suite()` now remembers its title, so "an async suite crashed" finally names the suite.
  - ⚠ AND `npm install` REALLY IS REQUIRED, whatever a green run says. Without it jsdom is missing, every render test silently NOTEs itself away, and what is left is quietly leaning on evaluation order. Baseline for comparison: de7bfa8 was 2155 passed / 0 failed WITH node_modules, and a crash without it.
⚠ Suite 10 of run-all.js is ASYNC — it EXECUTES syncPayerInvoice against a fake Firestore rather than reading it as text, because a regex cannot catch an undefined variable and a text-only check is exactly what let the `forTotal` crash ship for a day. Anything async must be pushed onto `pendingAsync`; the summary awaits that list before printing. A check that scores after the summary can never fail the build.
RESOLVED 2026-08-14 — `quote-card.test.js` is gone, and this is the reasoning, because the previous note here said in as many words "do not fix it by deleting it".

That instruction was written to stop the coverage being thrown away for convenience. The coverage was not thrown away: by the time it was removed, Suite 5 of run-all.js already tested the SAME cards, against the CURRENT markup, on every commit. The old file could not be repaired into something useful, only into a duplicate — and §9.1 says not to duplicate a check across the three systems.

What it was actually asserting: roughly twenty of its checks drove the Detail Form block on the quote card (`.qdLightColors`, `.qdGateCode`, `.qdWantsMailed`, `data-savequotedetail` …). That block was deliberately REMOVED by the P1 work — customers fill those details in on their own portal now (§4). Suite 5 asserts its absence with "no Detail Form left on the card". So the old file was not broken-but-correct; it was describing a design the owner had replaced, which is exactly the trap §5 warns about.

Every other check in it was verified to have an equivalent in Suite 5 before removal — one card per quote, pricing collapsed by default, no approval buttons when unpriced, Add Photo, approval link, convert-to-customer, copy-quote-email, the hostile-input escaping set, missing-field fallbacks, no duplicate ids, each card wired to its own quote, and the comma parsing (Suite 4). Exactly ONE had no equivalent — "Estimated Price line shown" — and it was ported into Suite 5 first, in the same commit.

If you want it back, `git show 644a3c2^:quote-card.test.js` has it. Do not restore it without first checking whether Suite 5 already covers what you want, and remember it targets the pre-P1 card.
C. Element-ID sanity (a lesson learned the hard way): every element ID referenced in JS must exist in the HTML, there must be no duplicate IDs, and after any big HTML deletion re-check the <div> balance (gate A). This is folded into Suite 1 of gate B above. ?. optional chaining hides missing markup as a silent no-op — don't trust "no error" as "it works."
4. Work queue history (P0-P3, completed 2026-08-08)
The P0-P3 items from the 2026-08-07 audit are done and deployed (HTML side confirmed live via Netlify; the Cloud Functions half of P0 hit the deploy issue noted in §1 and needs the owner to confirm it actually landed — check the PayPal charge amount against a real discounted/fee'd invoice, or check the Firebase Console function source directly). Summary, for context on anything that references "the P0 fix" etc.:
P0 (money bug, changeFees not carried everywhere) — found to be much larger in scope than originally described: not 5 places but ~20+, including the PayPal charge total, the portal invoice read whitelist, the printable/emailable invoice document builder (buildInvoiceDocHtml — was missing the fee as a line item entirely), the email-recipient payment-status filter, the "All Customers" table filter, the Excel exports, and several bulk-tool status computations. All fixed; computeInvoiceStatus and the raw balance formula now consistently include changeFees everywhere they're computed.
P1 (quote-card UX) — Detail Form block removed from admin.html's quote cards (customers now fill it in on their own portal after approving, via a Firestore rules change already live — see §8). Pricing & Approval panel converted from native <details> (which lost its open state on every re-render) to a manual ▸/▾ toggle backed by a module-level Set (quotePricingOpenIds) that survives re-renders. Feet/lights inputs switched from a 700ms input-debounce to save-on-blur.
P2 (test harness) — run-all.js was crashing on a colorComboKey extraction (that function is genuinely gone from admin.html, not renamed — left as a documented GAP, not restored). Several checks were stale (a fixed 9000-char extraction window that the Edit Customer save handler had grown past — now slices to the next real anchor instead of a magic number; a couple of renamed-variable and removed-feature mismatches; a CRLF-vs-LF banner search). jsdom is now actually installed and Suite 5 runs for real against the current (P1) card markup — it was silently skipped before, so nobody had noticed it was testing removed structure.
P3 (three small bugs) — fixed: warehouse buffer stock (warehouseExtras) now decrements quantity instead of deleting the whole doc on a tick. Fixed: Add/Edit Customer's "One-Time Notes" field was writing a plural oneTimeNotes field that nothing downstream read — unified to the singular oneTimeNote field that route stops, the crew portal, and completion-clearing already use. NOT a bug, despite the original P3 description: frontPhotoUrl reads in admin.html are the live manual-photo-upload feature that replaced the retired Street View lookup (confirmed by a code comment in index.html) — do not remove them.
Two new gaps found during the 2026-08-08 audit — both resolved same-day after checking with the owner, no longer open:
Invoice Bulk Update (admin.html, ibImportBtn) used to always zero removal and deposit for every row, even when an invoice already existed with a removal charge or a recorded payment. Owner confirmed the tool isn't currently run against existing invoices, but it's now guarded anyway (preserves existingInv's removal/deposit/credits/changeFees rather than trusting that stays true). Promoted from gap() to a real check() in run-all.js.
Switching a customer's bill-to used to never move an outstanding light-change fee onto the new payer's invoice. Owner said the fee should move over automatically — it now does (captured off the old invoice before it's zeroed/deleted, folded onto the new payer's invoice after syncPayerInvoice rebuilds it). Promoted from gap() to a real check() in run-all.js.
Ongoing / needs owner (do NOT automate blindly)
Assign customer numbers to ~962 customers — needs the master sheet pasted by the owner and the #5012 duplicate resolved (Staci Cosby vs Liz Frome). Use Customer Numbers → Assign in Bulk → Check First (dry run) before Assign.
Route-card cleanup, warehouse/crew printable split, New-Hang badge for crews, payment history — see the older handoff for specs (not re-verified in this pass).
5. Default to acting. Owner's standing instruction (2026-08-13): "I never want it to need a human."
Do all of this without asking: edit any file; run the gates; commit; push to main (Netlify publishes the HTML, and the GitHub workflow deploys the functions from the same commit — §1); deploy firestore:rules / firestore:indexes with the local CLI, which is installed and logged in; create branches; regenerate system-map.md; write and repair tests; investigate and fix failures. Do NOT hand-deploy functions — pushing does it (§1).
Where you would once have stopped to ask, prefer: do the safe reversible thing, do it on a branch, verify it with the gates, and report what you did and why. A branch costs nothing and can be deleted; stopping costs the owner a turn.
What genuinely cannot be automated is now very short, and none of it is process friction — it is information or authority that exists only with the owner:
  - Data only she has. ⚠ CORRECTED 2026-08-17 — the master sheet does NOT have to be pasted in. There is a local copy — ⚠ PATH AND FILENAME CHANGED 2026-08-19, see the entry below — at `C:\Users\lanil\OneDrive\Documents\2026 Client List.xlsx` (first tab, 962 customers: CU #, Name, Address, City, Zip, Phone, Email, Notes, Wire, Lights, Timer, $$, Pref Date, # Feet), mirrored under OneDrive, plus `2026 Customer Numbers.xlsx` (a plain CU# → name list). There is no xlsx library installed, but an .xlsx is a zip of XML and ~110 lines of Node (`zlib.inflateRawSync` + the central directory + sharedStrings) reads it fine — two traps if you write one: a greedy attribute match on `<c …/>` eats the self-closing slash and swallows the following cells, and empty cells are simply absent from the XML so you must index by the column letter in `r=`. Check the file's date before trusting it (that copy was last saved 2026-07-29) and spot-check it against whatever she pastes; on 2026-08-17 fifty rows agreed exactly and only three phone numbers had been added since. What is still genuinely hers alone: anything she has changed and not saved.
  - Decisions with no right answer in the code. Which of two customers keeps number 5012. Whether a feature that vanished was removed on purpose or by accident. Ask ONE clear question with the options laid out, then act on the answer — don't ask twice, and never ask something the code or git history can answer.
  - Spending real money. A live PayPal checkout charges an actual card.
  - Deleting customer data at scale. "Delete All Customers" and "Start New Season" are irreversible and hit every record. Prepare them, dry-run them, report exactly what they would do — then let her press the button.
  - ⭐ DUPLICATE CUSTOMERS, and the tool for them (added 2026-08-17). On 2026-08-17 Health Check reported 1,923 customers against 962 on the master sheet, with 944 numbers each held by two records of the SAME name — the book had been imported over itself. Danger Zone → `Duplicate customers` groups them (by customer number, else by name AND address together), scores each copy by what it CARRIES (`dupAssets`: invoice, route stop, address, pin, phone, email, feet, price, notes), keeps the richest, and lists the lot before touching anything. Deleting needs DELETE typed, exactly like Delete All Customers.
    - ⚠ THE SAFETY ARGUMENT IS THE SUPERSET RULE, not the weights. A group is REFUSED the moment any copy holds something the keeper does not — one copy has the invoice and the other has the route means real work dies either way, and that is the owner's call. Because of that refusal, every group that survives has a keeper holding a superset of its losers, so nothing is ever thrown away. A red-check proved the asset WEIGHTS can be gutted with no test noticing, and that is correct: once the superset rule holds the weights only order ties. Suite 45 asserts the superset directly.
    - ⚠ The customer number is NOT returned to `availableCustomerNumbers`. The copy being kept still holds it; pooling it would hand a live number to somebody new. And the deleted copy comes off any upcoming route first, or the crew is left with a stop pointing at a customer who is gone.
    - Re-running IS the resume: a run that dies half way finds fewer the next time. A failed invoice read aborts the whole thing, because a missing invoice makes a record look empty and therefore deletable — exactly backwards.
  - ⭐ HOW THE ~944 DUPLICATES WERE ACTUALLY MADE (diagnosed 2026-08-18, from the owner's own clue: "the bad duplicates have their address titled: , UT and the good duplicated dont have a price"). `buildFullAddress('', '', '')` returns exactly `", UT"` — a blank street, a blank town, and the UT it always appends. So those records were written with NO street and NO town. Bulk Updates matches through `findExistingAddressMatch` (BULK_IDENTIFIER `'phone+address'`), and with a blank street that match cannot succeed, so every row fell past "update this customer" into "add a new one" — a second copy of the whole book, each with `", UT"` and a price on its invoice. The guard that now refuses it (`if(!existing && !street){ failed++; continue; }`) was added AFTERWARDS in `96e9f63`. If you ever see a mass duplication, check what the identifier column was and whether the match could have succeeded at all.
    - ⚠ A SECOND FACTORY, live until 2026-08-18: Invoice Bulk Update creates a customer for anyone it cannot find, and looked them up with `(a.data.phone||'') === phone` where `phone` is digits-only and the record holds what the office typed, `"(801) 706-7225"`. False for every customer who has a phone, so it added another address-less copy EVERY RUN. Every other matcher in the app strips punctuation first — if you write a new one, use `custByPhoneDigits`.
  - ⭐ MERGE, DON'T DELETE (added 2026-08-18). Owner: "if you can make it so they rather merge more than just delete", "use name and CU#". Danger Zone → `Merge duplicates (by name)` — `findMergeableCustomers` groups by NAME (the broken copies have no address, so an address could never group them; the customer number SPLITS a group rather than forming one, so a copy that never got a number still travels with its twin), then `mergeFieldsFrom` fills the keeper's BLANKS from the spare and deletes the spare. This is the case the scored tool refuses by design — each copy holds something the other does not — which is why that one could never clear this book.
    - ⚠ Only ever fills a gap; nothing the keeper already has is overwritten, so a merge can add information and cannot destroy any.
    - ⚠ `", UT"` counts as NO address (`dupAddressIsEmpty`), so the copy with the real house always wins — and the address family (street/city/state/zip/address/lat/lng) moves as ONE unit, or a street from one copy and a pin from the other put a pin on the wrong house.
    - ⚠ The keeper is written BEFORE the spare is deleted. The other order can lose the very field the merge exists to rescue.
    - Refuses for a human: two customer numbers under one name, the same name at two real addresses (a landlord or a family), or more than one copy already on a route. Keeps the routed copy where the address does not decide it, needs MERGE typed, returns no number to the pool.
  - ⚠ "MISSING OR INSUFFICIENT PERMISSIONS" CAN MEAN THE DOCUMENT IS TOO BIG (fixed 2026-08-19). Owner, from her error log: "Could not raise the reconcile note: Missing or insufficient permissions" — failing quietly for weeks. It is not an auth fault. `firestore.rules` caps `messages` at 5000 characters on CREATE, the reconcile note grows a line per hand-built day and a clause per retired one, and a full season rebuild sails past it. Firestore reports ANY rule that says no as "insufficient permissions", which is why it read as a login problem for so long. If a write fails that way and the user is definitely signed in, check the rule's CONTENT conditions before the auth ones.
    - ⚠ THE CAP IS NOT THE BUG AND MUST NOT BE RAISED. Creating a message is a PUBLIC write — that is how the website contact form works with no login — and the size limit is what stops anybody posting a novel into the office inbox. The note is trimmed instead. ⚠ CORRECTED 2026-08-19: this was `sysNoteBody()` at `SYS_NOTE_CAP` = 4000 for about an hour. A paste of admin.html from a stale read (`7cc2358 "Update admin.html"`) removed both, and the follow-up `86eae01 "Update run-all.js"` removed the checks that would have caught it — so the suite went green over the loss. The trim that is actually in the file is inline in `noticeRoutesReconciled`, as `trimNote`, cutting the body to 4500. Trust the file: neither `sysNoteBody` nor `SYS_NOTE_CAP` exists any more.
    - ⚠ WHAT DOES NOT FIT IS COUNTED, NOT CUT. A note that quietly loses half the sweep is worse than one that says it did. The intro and the closing warning ("nobody has been told about any date that moved") always survive the trim, and a note that already fits is passed through untouched — an apology line on every note trains people to ignore it.
    - ⚠ AND A TOAST IS NOT A RECORD. The same paste replaced the short System note with a `toast()` alone. A toast is gone the moment the office looks away, and this is the LAST step of a sweep that has already rewritten real routes — "days moved and nobody knew" is the exact failure the block exists for. Restored 2026-08-19, with the toast KEPT alongside it: the toast is the half that gets noticed immediately, the note is the half that keeps. Suite 70 red-checks both halves separately so neither can be dropped again.
    - ⚠ AND THERE IS A ONE-LINE FALLBACK. It failed silently for weeks and the office heard nothing at all; if the long note is refused for any reason, a bare "N thing(s) changed, open Routes" goes instead. The sweep HAVING HAPPENED is the part that has to survive.
  - ⭐ ONE SYNC BUTTON, FINISHED PROPERLY (2026-08-19). Owner asked for "a sync button at the bottom" — singular — and it shipped with two, because adding a customer and editing one are different writes and merging them in a hurry would have meant two report writes overwriting each other. The unhurried version: the add loop was lifted out of its button into `rbApplyTickedAdds(statusEl)`, which RETURNS COUNTS and writes no report; `rbSyncAllBtn` runs it, then the diffs and spares, then writes ONE sentence covering all three.
    - ⚠ ADDS RUN FIRST. Folding in a spare copy DELETES a record; doing that before the adds have read their own list is asking for trouble.
    - ⚠ "Nothing was ticked" is only true when NEITHER half had anything (`!list.length && !added.ran`). Returning early on an empty diff list would silently discard adds that had just run in the same press.
    - ⚠ `extractFn` SEARCHES FOR "function NAME(" AND SO DROPS THE async KEYWORD. A check for `/async function .../` against a lifted body always fails; check the async against the whole file and the ordering against the slice. This suite has been caught by that three times now, and the third time looked different: lifting a whole ASYNC module (the workbook writer) produced a body full of bare `await`, which is a parse error, so the entire suite died as one `an async suite crashed` with no clue which suite or which function. Suites 74—76 lift their own with a two-line helper that tries `async function NAME(` first. ⚠ That helper uses indexOf and NOT a regex, deliberately: a backslash does not survive every route into this file, and an escape that quietly degrades gives you the same unattributable crash.
  - ⚠ A MESSAGE THAT IS IN THE SOURCE IS NOT A MESSAGE ON THE SCREEN (learned 2026-08-19, the hard way). Owner: "griner is a duplicate here but its not finding her as a duplicate still." She was right and the DETECTION was fine — `onSheetRow` and `sheetWhere` were both set correctly. The bug was one line of render order: the default `right = rbLedgerCell('')` sat at the END of the website-only branch, after the two blocks that fill it, so both messages were built and then overwritten with "— nothing —".
    - EVERY CHECK I HAD MATCHED THE SOURCE TEXT of those messages, and every one of them passed, because the text was all present and correct — it simply never reached the screen. Reproducing the bug afterwards failed exactly TWO checks, both of them new ones that EXECUTE `rbRenderLedger` against a fixture and read the HTML it produces.
    - So: when a check is about what somebody SEES, run the renderer. A regex over the file proves the words exist, which is a different claim and a weaker one. This is the third time in one day a text-only check has been green over code that could not run (see also the sort disabled by an if(0), and the price assignment behind an if(false)).
  - ⭐ A SPARE COPY IS TICKED BY DEFAULT, AND THE NAME RULE IS WHY THAT IS SAFE (2026-08-19). Owner: "and so when it syncs it should automatically delete dupes." I had them unticked; she asked twice, so a plain Sync now clears them. The safeguard is not a checkbox, it is HER OWN RULE: "two different names are never duplicates though unless one is surname first name and the other is first, surname." `dupNormName` SORTS the words, so Griner Lauren and Lauren Griner are one key while a real typo (Chafffetz against Chaffetz) is not. A ticked row is therefore always the SAME NAME on a keeper the sheet itself found — never a guess about which of two people this is. Twelve existing checks hold that sort in place; removing it fails all of them.
    - ⚠ REJECTED, and worth recording so nobody rebuilds it: grouping duplicates by phone/email/street as well as name. It sounds more thorough and it is wrong here — measured on the real book, 17 phone numbers are held by two people and 14 of those are genuine households, a parent paying for a child's house. Two different names are never one customer.
    - ⚠ Automatic is fine; invisible is not. The rows are still listed before anything is pressed, and unticking is one click. The three write-side safeguards are unchanged: merge before delete, off the routes first, and a keeper that is known and is not the spare.
  - ⭐ THE COMPARISON IS ONE TWO-COLUMN LEDGER, AND IT CAN FOLD IN A SPARE COPY (added 2026-08-19). Three stacked blocks became one ranked list: what the website says on the left, what the sheet says on the right. Rank 0 MANUAL FIX (both sides differ), 1 customer on one side only, 2 address, 3 price, then `RB_FIELD_RANK`. Two buttons decide every manual row at once — Favour my sheet ticks them, Favour the website unticks them — and they touch ONLY the manual rows, because a gap has no second value to prefer.
    - ⭐ "NOT ON THE SHEET" USED TO BE A LIE. Owner spotted it from the names: a list of Last-First names on website records. Checked against the real book — Aiono Shelley, Anderson Ryan, Burton Corinne, Byrd Whitney, Cardon Howard, Chafffetz Julie, Fry Tom are ALL on the sheet with full addresses. Their website record holds only a town, so no row can reach it and the sheet row matched their OTHER copy. "Nobody reached this record" and "this person is not on the sheet" are different statements and only the first was true. Every website-only record is now checked against every sheet name through `dupNormName`, which SORTS the words — so Last-First and First-Last are one key.
    - ⭐ AND A SPARE CAN BE FOLDED IN ON SYNC. Owner: "whichever one is wrong should be listed as a customer that doesnt exist... if they are both equally right then it just deletes one of them when they sync it." I pushed back once, she asked again, so it is built. THREE SAFEGUARDS ARE THE DESIGN, not decoration: (1) MERGE BEFORE DELETE — `mergeFieldsFrom` writes everything the spare carries and the keeper lacks onto the keeper FIRST, and only ever fills blanks; the other order loses the field the merge exists to rescue. (2) OFF THE ROUTES FIRST, or the crew keeps a stop pointing at a customer who is gone. (3) THE KEEPER MUST BE KNOWN and must not be the spare — it is the record the sheet row already found, so which of two is real is never a guess; a row without one gets no tick at all.
    - ⚠ NEVER TICKED BY DEFAULT. Every other row in this list is reversible by editing a field back; this one is not.
    - ⚠ The number is NOT returned to `availableCustomerNumbers` — the keeper still holds it, and pooling it would put two houses on one label. Suite 58 checks that with COMMENTS STRIPPED, because the rule is written down in the code and a plain search finds the explanation and calls it a violation.
    - ⚠ A fixture for the spare-copy case needs TWO website records for one person, the good one reachable by street and the spare holding only a town, with the spare named the other way round. With one record the sheet row simply matches it, nothing is left over, and the check passes whatever the code does.
  - ⭐ WHAT THE COMPARISON PUTS FIRST (added 2026-08-19). Owner, in three messages: "it should be sorted from most important to most different — at the top would probably be customer that exists on one but not the other and then next would probably be the address then the price", then "actually the very top priority should be things that require manual fixes that would fail to sync like if they both have an address but theyre different and I need to decide which ones right", then "and they should be labeled manual".
    - The second message is a DIFFERENT AXIS from the first and it wins. A CONFLICT (both sides hold a real value and they disagree) is not a harder gap — no rule can settle it, and the value it would overwrite is the one somebody in the office typed most recently. Conflicts sort above every gap whatever the field, carry a MANUAL tag, are counted in a banner at the top, and come UNTICKED. Ticking one takes the sheet’s value; leaving it keeps the website’s. That one checkbox IS the decision.
    - Inside each tier, : address, price, feet, CU#, town, phone, email, name, timing, then zip/wire/timer/eaves/notes. Ordered by what goes wrong when the field is wrong, with the two she named leading.
    - ⚠ THE ARRAY IS SORTED, NOT THE DISPLAY.  finds each ticked row by its INDEX in , and treats everything past the 400 drawn as ticked because it was never shown. Reordering at render time hands those two rules a different list than the one on screen — the wrong customer synced, silently. A text-only check for the sort passed happily with an  in front of it; Suite 58 now proves the order by RUNNING the collector, with the conflict deliberately placed on the least important field and the gap on a more important one.
    - ⚠ A conflict past the 400 drawn is excluded from the "rides on the default" rule. Conflicts sort to the top so in practice they are always drawn — but the failure is overwriting a value nobody chose to overwrite.
    - PRICE IS NOW COMPARED (`housePrice` from the `$$` column, via `rbNormalizeMoney`). It was not before, so a house being charged the wrong amount was the one difference this tool could never show. ⚠ Junk reads as BLANK, never 0 — blank is never a difference, and reading it as zero would offer to empty every price the sheet did not fill in.
  - ⭐ THE MASTER SHEET MOVED, AND THE OLD ONE HAS A BROKEN PHONE COLUMN (2026-08-19). THE FILE TO USE IS `C:\Users\lanil\OneDrive\Documents\2026 Client List.xlsx` — OneDrive → Documents, top level, NOT inside the Highlighting Utah folder. Owner, asked which of two: "use the new one".
    - ⚠ DO NOT USE `...\Highlighting Utah\Highlighting Utah\2026\2026 Master List.xlsx`. Its Phone column is detached from its rows: every customer carries somebody else’s number. It has been renamed to `2026 Master List (BROKEN PHONE COLUMN - do not use).xlsx` so it cannot be picked by accident. Kept, not deleted — it may hold edits the good file lacks, and Liz Frome #5012 is in it and not in the new one.
    - ⚠ CAUSE CORRECTED 2026-08-19 BY THE OWNER: "the only reason the new sheet was different is cause i already fixed it." So the phone numbers in the newer file are HER CORRECTION, not the surviving half of a bad sort. The evidence below still stands and still picks the right file — the conclusion never depended on the cause — but do not repeat the sort story as fact.
    - HOW THAT WAS ESTABLISHED, because it matters more than the conclusion. The two files hold the same people in different row orders, but the Phone column is POSITIONALLY IDENTICAL in both — the signature of a sheet sorted without that column selected. Which one broke was settled by a household test: a phone shared by two people should be one household (same surname or same address). New file 14 of 17 shared phones are a household (82%); old file ZERO of 13, every repeat landing on two strangers. The control was EMAIL, which travelled with the person in both files and scores 87% in each — so the test itself is sound and Phone is the one column adrift. `scratchpad/whichphone.js` is that script.
    - ⚠ WHY THIS WAS WORTH STOPPING FOR: phone digits are `custInvoiceKey` / `invoiceKeyFor`, so they decide which customers share one invoice. Syncing the broken file would have put the wrong number on ~955 customers and regrouped who gets billed together. Nothing was written — the comparison only ever offers differences for review — but the saved file handle pointed at the broken file, so the next Compare would have offered exactly that.
    - ⚠ NEVER TRUST A MASS DIFFERENCE WITHOUT TESTING THE TOOL FIRST. "954 of 955 phones differ" was read as a broken comparison before it was read as broken data, and checking took one row. The reverse mistake — believing it straight away — would have been a bug report about the reader.
    - The saved connection has to be re-picked (Bulk Updates → Use my master sheet) once per computer, since the handle points at the old path.
  - ⭐ THE MASTER SHEET CHOICE TRAVELS; THE PERMISSION CANNOT (added 2026-08-19). Owner: "I want it to be that in the admin when I select the master sheet it permanently remembers it no matter what computer I'm on." Two things were tangled in that and only one of them can move. THE CHOICE — which workbook, how many rows, when it was connected and from which machine — is information, so it is written to `settings/masterSheet` and is the same everywhere for ever. THE PERMISSION to read a file on a particular disk is a browser rule (File System Access API): it is per-origin, per-device, and no code carries it across machines. So a new computer still needs ONE press.
    - What that buys: a machine with no handle now says WHICH file to pick and how many rows it should have, instead of sitting blank. Blank is what let "2026 Customer Numbers.xlsx" be connected in the first place.
    - ⚠ ORDER IS THE SAFETY ARGUMENT: the unusable-workbook guard runs BEFORE the choice is written. Remembering a wrong pick once sends every other computer to the same wrong file, and it now looks authoritative. Suite 66 asserts the ordering, not just that both exist.
    - ⚠ A failed write is logged, never thrown. The sheet IS connected on this computer either way; sending somebody back to the picker over a failed note is noise.
    - ⚠ THE REAL FIX IS NOW POSSIBLE and was not before. Reading the file from OneDrive over the network needs a Microsoft app registration, which a personal Microsoft account cannot have (tested 2026-08-18: 400/401/403 on every anonymous route). On 2026-08-19 the owner moved to a Microsoft 365 WORK account, which has a directory and can register one. If that is ever set up, the per-computer step disappears entirely.
    - ⚠ THE LOCAL PATH IS ABOUT TO MOVE. `C:\Users\lanil\OneDrive\...` was the PERSONAL OneDrive of lanilani@yahoo.com (cid bd69143d88216923), unlinked 2026-08-19. The business folder is moving into the work account's `OneDrive - <Org>` folder. When it does, the saved handle breaks and "Use my master sheet" has to be pressed once per computer again — and the path in this file and in any scratchpad reader needs updating in the same pass.
  - ⭐ WHO THE SHEET COMPARISON MAY OFFER AS A CUSTOMER (added 2026-08-19). Owner, shown four nameless rows and five uncontactable ones in the same list: "no names should not show here cause they just arent a customer", and "they are only for the excel sheet and should not show here either". Two rules, both in `rbCollectMissingCustomers`.
    - A ROW WITH NO NAME IS NOT A CUSTOMER. Verified on the real master sheet: 94 rows of the 1,056 hold a bare phone number and nothing else — no name, no address, no email — a call list kept at the bottom of the same file. ⚠ They carry a PHONE, so the no-phone-and-no-email rule below could never reach them, and the old guard (`if(!who && !street && !phone)`) needed all three empty, so every comparison offered them and ticking one would have created a customer with no name at all. A fixture for this must give the nameless row a phone or the sheet-only rule answers first and the check proves nothing.
    - ⚠ COUNTED, NOT SILENTLY DROPPED, and `read` is NOT incremented for them. A paste whose Name column was never mapped makes every row nameless; without that, the tool would report a confident "everybody is already on the website" instead of tripping the "nothing to compare" refusal.
    - AND SHEET-ONLY PEOPLE ARE COUNTED, NOT NAMED. The block used to list up to 20 of them with number and address. That turns a decision the owner has already made back into a to-do list on every single run, and nobody re-reads a paragraph they have already agreed with. The count stays so the rows are still accounted for — a row that just disappears is one nobody can check.
  ⭐ THE SHEET AND THE WEBSITE SIT SIDE BY SIDE, AND ONE BUTTON SYNCS (added 2026-08-19). Owner: "I want the two columns side by side, the left side is things in website not in excel and right side is things in excel not in website with a sync button at the bottom", and "we dont need a button, we should make it so theres only one sync button because anything I dont want synced will be manually fixed". `rbRenderLedger` builds ONE ranked list of rows, each with a left cell and a right cell, so a difference is read across rather than hunted for in two lists.
    - THE ORDER IS THE POINT. `rbLedgerRank`: 0 a CONFLICT — both sides hold something and they disagree — labelled MANUAL FIX and never pre-ticked, because a machine picking between two addresses will eventually pick the wrong one and nobody will know which; 1 a customer on one side only; 2 address; 3 price; then `RB_FIELD_RANK`. Owner: "the very top priority should be things that require manual fixes that would fail to sync like if they both have an address but theyre different and I need to decide which ones right".
    - ⚠ ONE SYNC BUTTON, NOT ONE PER ROW. Anything she does not want synced she fixes by hand first, so a per-row button is a second way to do the same thing and a second thing to keep in step.
    - ⚠ A DUPLICATE IS CALLED A DUPLICATE. Owner: "instead of all those words where it says not on the sheet just call it duplicate." And two DIFFERENT names are never duplicates — owner: "two different names are never duplicates though unless one is surname first name and the other is first, surname" — which is the master sheet’s own format, so the comparison flips the name before it compares and never after.
    - ⚠ THE RENDER ORDER BIT ONCE. A `right = rbLedgerCell('')` default sat at the END of its branch and overwrote every message built above it, so a customer the code had correctly found as a duplicate rendered blank — reported by the owner as "griner is a duplicate here but its not finding her as a duplicate still", which is what a working detector plus a broken renderer looks like from outside. The tests now RUN `rbRenderLedger` rather than matching its source.
  ⭐ NEW CUSTOMERS ARE WRITTEN BACK INTO THE SHEET, IN PLACE (added 2026-08-19). Owner: "I want new quotes to be able to have their info put into the first open row in excel, and include all the info", and "it should not redownload the excel and edit it though it should update that exact file". Downloading a second copy is how a business ends up with nine master lists and no master list. `hlxAppendRowsToSheet(rows, tabName)` opens the saved handle, edits the workbook and writes the same bytes back.
    - AN .XLSX IS A ZIP OF XML, and the browser can do all of it: `hlxUnzipAll` reads, `hlxDeflateRaw` (CompressionStream 'deflate-raw') and `hlxCrc32` rebuild, `hlxZip` repacks, and `hlxSheetPartFor` walks workbook.xml to rels to sheetN.xml so a tab is found by NAME. No library is installed and none is needed.
    - ⚠ ATTRIBUTES MUST BE DOUBLE-QUOTED. `hlxRowXml` built `<row r='7'>` at first: valid XML, opens in Excel, and our own reader could not see the rows — so the verify step refused every write and the failure looked like a permissions problem. Found by EXECUTING the shipped function against a real file, not by reading it. `String.fromCharCode(34)`.
    - ⚠ AND THE $$ COLUMN NAME KEEPS BEING EATEN. Written through a shell it collapses to one character, and it did so in admin.html AND in the test in the same pass — so the test agreed with the bug and passed while 394 of 400 real customers came back with no price. Both sides now build it with `String.fromCharCode(36,36)`. Anything holding a $, a backslash or a nested quote goes into a file written with the Write tool, never through a shell. The same trap put literal BACKSPACE characters into run-all.js where a word boundary was meant, and that test then silently filtered nothing while passing.
    - VERIFY BEFORE WRITE: the new bytes are re-read and the appended rows re-found before the file is replaced. Proved three ways on the real 962-row workbook — Excel opens it, the file got SMALLER (298KB against 352KB, the original had slack), and 17 of 17 columns survived a round trip.
  ⭐ WHAT EACH COLUMN OF THE MASTER SHEET MEANS (settled with the owner 2026-08-19). Guessing from the header is how three of these were got wrong.
    - `$$` IS THE STANDARD PRICE WITH NO FEES. Owner: "just put the standard price no fees in excel that will be website only." Set Up Fee is left BLANK by us. Fees live on the website; the sheet holds the plain number.
    - `Up Plug` IS A YES/NO AND IT IS THE EAVES QUESTION — "preference on using outlet in eaves". The real column holds four values (112 yes, 98 "?", 61 no, 3 y) and no descriptions at all, so it fills `useEaves`. ⚠ The first version read it as WHICH outlet: it wrote the word "yes" into the note the crew reads and ticked Specific Outlet for anything non-blank — backwards for the 61 who said NO, and inventing an answer for the 98 who wrote "?". Every test passed. A read-only dry run over the real column is what caught it. "?" is not a no: blank leaves the record alone, which is the honest answer to a question mark.
    - WHICH outlet only ever appears in NOTES. Owner: "sometimes they give plug or outlet information in notes and if they do that should go to the outlet preference we already have set up in all customers." `rbOutletFromNote` finds eleven real ones, e.g. "use lower outlet by door w/ timer".
    - `Misc` IS A DRAWER, and `rbMiscParse` empties it: bill-to, how the bill travels, gate codes, the side count, prepaid. ⚠ ANYTHING LEFT OVER GOES TO NOTES rather than being dropped — owner: "if the misc column says anything I havent mentioned that should be put in notes section of the all customers when we sync the two."
    - ⚠ AND IT IS WRITTEN THE WAY PEOPLE TALK. "text the bill" has a word in the middle; "cc bill to Jennalesa@gmail.com" has a prefix in front and an EMAIL where a name should be. Both were being filed as leftover notes by patterns that were too tight. Owner: "make sure you format it in many formats so it doesnt flag if its not exactly Gate Code: or anything else like that." A payer given as an email is kept as `billToEmail` and preferred by `rbResolveBillTo` — an address is exact where a name is a guess.
    - GATE CODES ARE IN MISC OR IN NOTES, and the letters are stripped. `rbGateCodeFromText` reads "gate code 4412", "code: 4412", and the bare `#0754` the office actually types. Owner: "sometimes gatecodes are just formatted: #0754". Owner again: "also sometimes notes will hold gatecodes so we need to look for that in misc and notes".
    - PREPAID CAN BE IN ANY COLUMN — the owner said so — so `rbRowSaysPrepaid` scans several, including Pref Date, where "prepaid" was already known to turn up. It sets the tag to PAID: "in customers it says if they are paid or unpaid, change their tag to paid."
    - STILL UNMAPPED, deliberately: `Set Up Fee`, `Yes`, `Recycle`, `2027`, `Color Changes`. Recycle, Color Changes and Contact 2027 are their OWN TABS in the workbook (`HLX_STATE_TABS`), so a customer is appended to the tab rather than having a column set.
  ⭐ SIDES OF THE HOUSE ARE A NUMBER, NOT FOUR NAMES (changed 2026-08-19). Owner: "in the website its called front left right and back side, we need it to say 1, 2, 3, or 4 sides of the house so then it can just be connected and we dont have to guess if its the left or right side." The sheet says "3 sides"; front/left/right/back was a guess nobody could confirm and could not be matched to it. The quote form and the portal are radios 1 to 4.
    - ⚠ `houseSideCount` STILL READS THE OLD ARRAYS. Every existing customer holds something like `['front','left']`, so the count is `filter(Boolean).length` capped at 4 and history keeps working without a migration. `houseSideWords` renders it for the crew.
    - ⚠ CHANGING SIDES STILL RAISES A RE-QUOTE and the server route is untouched: `section === 'sides'` compares counts (`asCount`) and the 48-hour lock still holds.
  ⭐ ONE BILL, TWO CUSTOMERS (added 2026-08-19). Owner, on a Misc that says bill somebody else: "first we check if the person we bill to is even a customer of ours", and "yes they should have one member portal and on invoice but still remain two seperate customers". `rbResolveBillTo` looks the payer up by EMAIL first and then by name; a payer who is not a customer is left as a note rather than invented. `billToPhone` is what joins them, so the who-pays-for-whom screen and `custInvoiceKey` already do the rest.
  ⭐ THE COLOUR-CHANGE FEE IS BACK, AS ITS OWN LINE, AND IT STACKS (changed 2026-08-21). Owner: "new member fee and change light fees are seperate but should get charged seperetly for this so if new member changes lights again after 48 hours of being a new member than they should get charged for light change and new member fee which would put them at 6[0] dollars. So new member should not be assigned on routes within 48 hours."
    - ⚠ THIS REVERSES THE 2026-08-19 DECISION, which said the opposite ("get rid of color change fee and just make it new customer fee") and which this line used to record as settled. A colour change writes `changeFees` + a `changeFeeNotes` entry again and does NOT set `chargeNewMemberFee`. The two $30 fees are separate: a new member who changes colours outside their window pays both, $60.
    - ⚠ AND THE FEE WAS NEVER ACTUALLY BEING CHARGED under the old model — that is what prompted the rewrite. `portalSave` ended with `updates.chargeNewMemberFee = true`, and `updates` is written to Firestore SIXTY-THREE LINES EARLIER, so the flag landed on an object nobody saved again; it is not in `PORTAL_READ_FIELDS` either, so it never reached the browser. Every colour change a member made in their own portal charged nothing for two days while the portal told them in red that $30 had been added to their balance. Every check passed throughout, because they all read the source as text and the text was there.
    - ⭐ ONE RULE, TWO COPIES, ASSERTED IDENTICAL. `applyLightChange` in js/money.js and `applyLightChangeServer` in functions/index.js. Change one, change the other, ship them in the SAME push — `money-parity.test.js` runs both over ~1,100 combinations and fails the build if they ever disagree, exactly as it does for `computeInvoiceStatus`. It also asserts the answers are RIGHT, not merely equal: two copies wrong in the same way agree perfectly.
    - ⭐ THE FREE WINDOW AND THE ROUTE LOCK ARE THE SAME 48 HOURS, and one field says so: `lightsLockedUntil` on the CUSTOMER. Owner: "48 hours will be from when they become a costumer and we won't schedule them within that 48 hours so they can change there lights again if they choose", and "if an old costumer or a new costumer outside the 48 hour window changes lights than they should not be scheduled for another 48 hours." Two events open a window: BECOMING a customer, and a charged change. A change inside a window is free and does NOT extend it — a window that renews on every free save never closes and they are never scheduled at all.
    - ⚠ THE WINDOW MOVED OFF THE INVOICE. It used to be `lastLightChangeFeeAt` on the invoice document, which cannot work now: a brand new customer's window has to exist before any invoice does. `lastLightChangeFeeAt` is still stamped, because `portalInvoice` hands it to the portal as `lightChangeFreeUntil`, but it no longer decides anything. `lightsLockedUntil` is in `PORTAL_READ_FIELDS` so the portal can warn correctly BEFORE a save.
    - ⚠ ONLY ADD CUSTOMER OPENS A WINDOW ON JOINING. Six places create a `jobAddresses` document; the other five — Bulk Updates, the sheet sync, Invoice Bulk Update and the two test-record builders — must never set it. Importing the existing book of ~960 customers is not 960 people joining: locking them all takes the WHOLE season out of the scheduler for two days, and it looks like the scheduler being broken rather than a flag being set. Suite 117 counts the writes, matched on the VALUE rather than one syntax — the first version counted only the object-literal form and a red-check that added an assignment-style one to the bulk importer sailed straight through it.
    - ⭐ WHERE THE $30 LANDS DEPENDS ON WHETHER THE BILL HAS GONE. Owner: "if invoice has already been sent out but they change there lights after invoice is sent out than the 30 dollars will be charged for next season but if invoice hasn't been sent out than it will be charged on there current invoice." Not sent (`invoiceEmailSent` false) → `changeFees` on the current invoice. Already sent → `carryoverCharge` + `carryoverChargeNotes` on the CUSTOMER, collected by `runInvoiceBatch` next season.
    - ⚠ IT HAS TO BE ON THE CUSTOMER, NOT THE INVOICE. Start New Season zeroes `changeFees`, `changeFeeNotes` and `lastLightChangeFeeAt` on every invoice, so a charge parked there is DELETED rather than carried; it touches only `completed` / `invoiceEmailSent` / `scheduled` / `scheduledDate` / `assignedCrew` on the customer, which is exactly why `carryoverCredit` already lives there. This is that same mechanism pointing the other way. ⚠ And `runInvoiceBatch` sums it across the whole payer GROUP, not off the payer alone — the person who changed their colours is often not the person who pays.
    - ⚠ THIS IS ALSO WHY NOTHING HAS TO RE-OPEN A SENT BILL (known hole F). `invoiceEmailSent` is only ever cleared by Start New Season, so a fee added to an already-sent invoice would sit there and never be posted to anybody. Sending it to next season is the answer that reaches the customer.
    - ⭐ THE OFFICE IS ASKED, NOT CHARGED SILENTLY. `askLightChangeFee` is a popup, not a `confirm()`, because there are THREE answers: charge, change without charging, and cancel. Waiving is for the office correcting its own typo and must be available AT THE POINT OF SAVING — nobody goes back to undo a fee later, so "waive it afterwards" means "charge them by mistake". ⚠ WAIVING STILL SETS THE WINDOW AND STILL RAISES THE NOTE: the lock is about the crew, not about money. ⚠ Cancel saves NOTHING at all, not even the rest of the form, which is why the popup runs before any write.
    - ⚠ A FIRST-TIME COLOUR IS NOT A CHANGE. Both the old and the new value must be non-empty. Getting this wrong charges somebody for filling their own colours in and sweeps every new customer onto the Color Changes sheet — which has happened once already, with twelve people. The old server code tested only that the value DIFFERED, so it charged them.
    - ⚠ THE BROWSER HAS NO TRANSACTION. `portalSave` wraps the decision in `db.runTransaction` so a double submit cannot add $30 twice; Edit Customer has no equivalent and two people are in admin every day, so the invoice is re-read immediately before the fee is written. That write is LAST in the handler, after `syncPayerInvoice`, which rebuilds the invoice from the houses — a fee written before it is overwritten.
    - Suites 108 (office, run against a fake DOM and fake Firestore) and 117 (server, run against a fake Firestore), plus money-parity. Twenty-five sabotages red-checked across the three.
  ⭐ WHO IS A NEW MEMBER: BY QUOTE, NOT BY DATE, NOT BY BULK — AND IT EXPIRES (settled 2026-08-21). Owner: "since this is new and bulk report runs all the time we should not do new members by dates right now. We should do it by who become a costumer through quotes and not through bulk", then: "if someone came from quotes this year but we are in 2027 than they are no longer a new costumer."
    - ⚠ THE $30 JOIN FEE WAS BEING CHARGED EVERY SINGLE SEASON, and this is the fix that matters. It is one-time, guarded within a season by `newMemberFeeApplied` on the invoice — which Start New Season deliberately sets back to false. But NOTHING anywhere cleared `chargeNewMemberFee` on the customer (a search of admin.html and functions/index.js for a write of false or null returned zero hits), so the guard was released every year while the flag stayed true and the nightly run charged the join fee again the night their lights went back up. Start New Season now clears it, in the SAME write as the rest of the customer reset — a separate write can fail on its own and leave the guard released with the flag set, which is the overcharge back in a shape nobody would look for twice.
    - NOT BY DATE, and that was already true: `looksLikeNewMember` reads `chargeNewMemberFee` and nothing else. The old enrollment-year guess said "new member" for essentially the whole book after a bulk import — ~945 people, one $30 fee each. A check exists whose only job is to fail if `createdAt` returns to that decision.
    - NOT BY BULK, and that was already true too: only TWO places write the flag, the Add Customer box and the Edit Customer box, both a person ticking something. No importer writes it. Now COUNTED, scoped to the bulk region — a file-wide search finds the two checkboxes and every reader, so it would pass with an importer writing it.
    - BY QUOTE: `quoteChargesSetupFee(d)` — the office's own `chargeSetupFee` answer if they gave one, else "charge it unless this is a re-quote against an existing customer". ⚠ It tests `!== undefined` rather than truthiness in BOTH directions: read as a plain boolean it would silently re-charge every quote the office had deliberately UNticked. ⚠ It was FOUR copies of that expression (showAddCustomerFromQuote, the quote card checkbox, the card renderer, the automatic path) — four copies of a rule about $30 is three chances for the card to promise one thing and the invoice to do another.
    - ⚠ AND THE CLEANUP I FLAGGED WAS NOT NEEDED. Customers were thought to be carrying `chargeNewMemberFee` wrongly from the 2026-08-19 colour-change model; they are not. That write never reached Firestore (see the entry above), so the bug that lost the money also prevented the contamination. Do not go looking for records to clean.
    - Suite 10a and the `logic` suite. Eight sabotages red-checked, two of which caught MY OWN checks being vacuous: the dry-run check searched for a word that survived the sabotage, and the "same write" check used a proximity window that a genuinely separate write still satisfied. Both are behavioural or structural now.
  ⭐ SOFT LIGHTS KEEP THEIR OWN NAME SO THEY CAN BE FOUND (added 2026-08-19). Owner: "so then we can find them later cause we need to switch their lights", and "no new houses can get soft you can only see that as even an option in the admin portal". `p` is pure, `w` is warm white, a bare `white` means BOTH, and `soft` becomes `soft(recycled)`. All Customers has a Lights filter: "On soft (needs switching)" is the work order (twelve houses on the real sheet) and "No colours recorded" is who still has to be asked. Keeping the label without a way to search it is a label, not a list.
    - ⚠ COLOURS LIVE IN TWO FIELDS AND BOTH MUST BE READ. `rbDetectColorsAndPattern` moves a REPEATED colour out of `lightColors` and into `lightsDescription`, because a repeat means an alternating pattern where the order matters — so an alternating soft house has an EMPTY colour list. Reading only the list misses them, and the same mistake calls them undecorated on the no-colours filter. A red-check caught that second half.
    - ⚠ AND THE ALIAS TABLE HAS TO SUPPORT A LIST. `white` maps to two colours; a parser that only read single values dropped it silently, so the tests were checking a table the app did not have.
  ⭐ THE READERS WERE ALL GREEN AND THREE OF THEM WERE WRONG (2026-08-19). Every rule above was covered by passing tests before `scratchpad/dryrun.js` ran them read-only over all 960 named rows and printed what a sync WOULD do. That found the Up Plug inversion, "text the bill" and the email payer. A test proves the code does what it was written to do; only real data says whether that was the right thing. Run the dry run before any sheet-wide change, and keep it read-only.
  ⭐ THE CUSTOMER LIST IS FOUR SHEETS, NOT ONE (added 2026-08-20). Owner: "in the excel 2025 it syncs to customers but if they are in color change or contact 2027 it wont detect them and so information wont go back and forth between website in excel, if they are located in both the 2025 sheet and contact 2027 then dont add a second customer but we need to make sure that they are still synced as well." `hlxWorkbookRowsAllSheets` reads Yes, Color Changes, Recycle and Contact 2027 alongside the 2025 sheet. A customer moved to a side sheet used to look like somebody who had LEFT the business, so the comparison offered to add them back as new and no edit ever travelled either way.
    - ⚠ LINED UP BY HEADER NAME, NEVER BY POSITION. All four carry the same columns today — checked against the real workbook — which is exactly why this needs a test: one column inserted into one sheet shifts every value one place along and nothing complains. A red-check swapping name-lookup for position passed until a fixture put the side sheet's columns in a different order.
    - ⚠ AND THE NUMBER NEVER STANDS ALONE. The first version keyed a row on the customer number, with a comment arguing the office controls them. The real workbook disagreed inside a minute: Liz Frome sat on Recycle holding #5012, which Staci Cosby also holds on the 2025 sheet, so Liz read as already present and was dropped — and she was the ONLY named row on any side sheet, so the merge reported a confident nothing-to-do. This file already said it: "A NUMBER NEVER OUTRANKS A NAME THAT DISAGREES." A collision deletes a customer silently; two copies of one person is a mess somebody can see.
    - A row carrying ONLY a name still matches, but only when that name is unique. Six names on the real sheet belong to two different rows, and the rule for that is already written down a few hundred lines up: two candidates is not a weaker match, it is no match. An ambiguous one stays visible.
  ⭐ "PAID" ANYWHERE IS A TAG, NOT A NOTE (changed 2026-08-20). Owner: "we are using this as a test right now so youre right they havent paid for 2026 but we still want them to change to paid, and we dont want it in notes we want their tag to change, if it says paid anywhere they should just be marked as paid for this year." `rbRowSaysPrepaid` reads "paid" as well as "prepaid", the season in the cell is deliberately ignored, and `rbMiscParse` CLAIMS the cell so it stops arriving as prose nobody acts on while the customer goes on showing as owing. On the real sheet: Byrd, Crespo, Williams, Henry.
    - ⚠ THE GUARDS MATTER MORE THAN THE MATCH. Marking somebody paid who is not writes off money the business is owed, and nobody chases an invoice that says settled. "unpaid", "not paid", "paid?", "to be paid" and "needs to be paid" are all still not paid. The word boundary is load-bearing too: Misc and Notes carry street names, and a substring match turns any word ending in those four letters into a receipt.
  ⭐ A CELL THAT IS ONLY A NUMBER IS A GATE CODE (changed 2026-08-20). It used to require a marker, reasoning that "51500" could be a price. Asked what her two bare numbers were, the owner said "assume its gate codes". ⚠ Only when the WHOLE cell is digits, and 3 or more: a number inside a sentence is a price or a house number far more often, and a bare "3" is a side count.
  ⭐ A THIRD RSVP TAG: UNANSWERED (added 2026-08-20). Owner: "right now there is a tag for maybe next year, and another for confirmed, add a third for unanswered", and "right before we RSVP we need to move everyone to unanswered", and "no one should have that tag yet cause we still need to test". Blank means nobody ever asked them; unanswered means we asked THIS season and they have not replied. Keeping them apart is the whole point — after a reset nobody is carrying last year’s Yes. `rsvpStatusLabel` is the one place that names a status, because three screens spelled them out by hand and the one that got missed would have shown the new tag as "Pending", the very status it exists to be distinguished from.
    - ⚠ ADDING IT BROKE TWO WORKING THINGS, BOTH SILENTLY, AND NEITHER HAD BEEN RUN. (1) Automation Emails' Pending audience was `!rsvpStatus`, so after "move everyone to Unanswered" the RSVP would have matched ZERO people — on the one send that needs the whole list, and a send to nobody looks exactly like a send that worked. `etRsvpAnswered` answers that question in one place now. (2) The other session's "approving a quote marks a member in for the season" was guarded on blank only, so it would have stopped marking ANYONE from the first reset onwards — producing exactly the complaint it was written to fix. Any new state has to be walked through every `!rsvpStatus` in the file.
    - The reset button touches the ANSWER and nothing else. `needsLightRecycle` is work the warehouse has already done, and `maybeNextYear` is a decision rather than a reply; both survive. That is also why a reset cannot drag somebody out of the season back into it — `isOutForSeason` reads the flag, not the answer. Type-to-unlock, like every other mass write here.
  ⭐ CONFIRMED IS A SHEET, NOT A COLUMN (added 2026-08-20). Owner: "we dont need to map confirmed in excel either that will be done in the website but everyone who is confirmed after we RSVP will need to go to the Yes tab in excel not the 2025 one", and "we are mapping contact 2027 just not in a column we have a tab in excel for that, same thing with color change, same thing with recycle". Her workbook holds nine sheets: 2025, Sheet1, Yes, Hanging, DONE, Color Changes, Available #s, Recycle, Contact 2027. Set Up Fee, Set Up and Confirmed are deliberately never mapped as columns.
    - ⚠ ANSWERED YES, NOT ASSUMED YES. Converting a quote writes `rsvpStatus: 'yes'` at creation with no `rsvpRespondedAt` — the office knows they want lights, but nobody has asked them about this season. On the status alone, the first press of Update customer info would have emptied most of the customer list onto the Yes sheet before a single RSVP had gone out. The tab tests `rsvpRespondedAt` as well, which only works because every route that takes a REAL answer stamps it — and the office dropdown did not until this was found.
  ⚠ THE CONTACT 2027 TAB READ A FIELD NOTHING WRITES (fixed 2026-08-20). It tested `rsvpStatus === "maybe_next_year"`; that is the QUOTE field (`approvalStatus`). On a customer it is `backnextyear`. Worse, `portalRsvp` writes the STATUS ALONE while only the office button also sets `maybeNextYear`, so every customer who answered Back Next Year through the RSVP link matched neither half and never reached the tab. The test that should have caught it asserted the invented name while titled "the three tabs read flags that actually exist", and a fixture written the day before copied the value out of the code under test instead of out of the writer. Both RUN the predicates now, against records shaped the way the real writers write them.
  - ⭐ RE-QUOTES ARE THEIR OWN FOLDER, AND CONVERTING ONE UPDATES (added 2026-08-19). Owner, asked a question that killed a worse plan: "so will we have a place seperate from quotes named requotes cause if thats the case we dont need to remove them we just need it to update them after they confirm their new price." The plan until then was to DELETE the customer and let the re-quote convert them back in — which signs them out of their portal mid-change, has to pull them off any route they are already on, and leaves their invoice homeless until somebody finishes the quote. A folder costs one tab and none of that.
    - ⭐ TWO KINDS OF RE-QUOTE, SAID RATHER THAN GUESSED (2026-08-21). Owner: "in requote I should have the option to have requote for addition to house, or for change address." They are different jobs: an addition means more feet at the same house and the old set stays in its bin (top-up); a change of address means they MOVED — old set back, new one built, re-geocode, route stops now wrong. `askRequoteKind` asks when the re-quote is raised and stores `requoteKind`. ⚠ `showApplyRequoteChoice` used to infer this by comparing the quote's address with the customer's — a guess about two typed strings, so "Red Cedar Ln" vs "Red Cedar Lane" read as a move that never happened. The stated kind wins; the address comparison survives only as the fallback for cards raised before this existed.
    - ⭐ AND A THIRD: JUST THE PRICE, WHERE THE WAREHOUSE DOES NOTHING (2026-08-21). Owner: "you can also get a requote because you just changed the price and when that happens nothing gets added to warehouse but it gives you the option to explain why you changed the price." The other two both imply work — an addition builds the extra, a move recycles and rebuilds — but a price change is the same house, same lights, same feet, same street. Doing either would put a real bundle on a real shelf for no reason. `requoteKind: 'price'` pre-selects a **Nothing** answer in the apply popup, and `requoteBuildChoice = {mode:'none'}` touches neither queue on save. ⚠ It is OFFERED, not forced: the office may raise it as a price change and then find something else as well, so the other two stay pickable and override it. ⚠ And 'none' is not 'no answer' — the office WAS asked and said the house had not changed, so existing flags stay exactly as they are.
    - ⭐ CHANGING THE PRICE IN EDIT CUSTOMER ASKS WHETHER IT GOES BACK TO THEM. Owner: "it gives you the option if you want to requote or not requote", and "there should be a spot for explaing why you're requoting them but it isnt a required field." ⚠ Feet and address still raise one WITHOUT asking and that is right — the house is measurably different. A price typed on its own is not: a typo, a discount agreed on the phone, a rounding. `askPriceRequote` is a popup, not a `confirm()`, because a dialog has nowhere to type the optional reason. It only asks when nothing else has already decided — the feet/address raise one anyway, and during a conversion the new price IS the answer to the re-quote in hand, so asking would never terminate. A note typed and then answered "just change it" is dropped, since no card exists to carry it.
    - ⭐ BUILD TEST CUSTOMER (Quotes) AND CREATE TEST PERSON (Customers). Test · 3853912235 · highlightingutah@gmail.com · $250 · 125 ft · Warm White. One button on the Quotes tab rather than five, building a card that lands in whichever section is open — `testQuoteFieldsFor` mirrors `quoteStage`/`quoteFolder`, and Suite 113 checks the round trip rather than the field values, so the two cannot drift. The Re-quotes one builds a test CUSTOMER first and links it, because converting only updates when the quote points at a live record. ⚠ Every test row carries `isTestRecord: true`, the address says "not a real house", and the test person takes NO customer number from the pool — handing a real bin label to a record somebody is about to delete is how a live number goes missing.
    - ⚠ AND THE TEST ADDRESS IS A REAL ONE: 209 S 850 W, Lehi (2026-08-21, owner's own instruction). This replaced a deliberately fake "not a real house" address, and the trade is worth knowing: the fake one could never be geocoded, so a test record could never be routed, scheduled or mapped — which is most of what a test record is FOR, and it would have behaved differently from a real customer at exactly the moment somebody was testing routing. The test person is geocoded on creation for the same reason (routing reads lat/lng, not address text), guarded so a geocode failure still creates them with `needsGeocode` set rather than losing the record. ⚠ The NAME and `isTestRecord` are now the only things marking a test row, and it can reach a crew sheet — delete them when finished.
    - ⚠ A QUOTE CANNOT BE CREATED ALREADY PRICED, EVEN BY STAFF. `firestore.rules` lets ANYBODY create a quote — that is how the public form works — and stops the obvious abuse with a deny list: `status == 'new'`, and no `quotedPrice`, `approvalStatus`, `convertedToCustomerAt` or `quoteArchived`. There is no `request.auth != null ||` escape on create, so a signed-in admin is held to the same rule. Every real flow already obeys it (raise new, price afterwards); the first Build Test Customer did not, and failed on ALL FIVE tabs with "Missing or insufficient permissions" — the Quotes tab because the fields carried no `status` at all, the rest because they carried a price and an answer. It now creates plain and stages with `updateDoc`, which is staff-only and unrestricted. Suite 113 reads the banned field list OUT OF firestore.rules rather than copying it, so the check cannot go stale when the real rule is tightened.
    - ⭐ AND ONE BUTTON TAKES THEM ALL BACK OUT (Customers → Bulk Updates, 2026-08-21). Owner: "a button in bulk updates for deleting all test customers from customers and quotes and invoices and routes and schedule and put their number back in the system", plus "it should also delete test customers from anywhere in the warehouse." Sweeps `jobAddresses`, `quotes`, `invoices`, `archivedCustomers`, `warehouseExtras`, `scheduledRoutes` stops and the `routeSchedule/plan` document. ⚠ Dry run FIRST — CLAUDE.md §5 — and the delete button only ever removes what that report showed; re-finding at delete time would take rows she never saw. ⚠ Numbers go back to the pool BEFORE anything is deleted: a customer document can be rebuilt by the button that made it, a number lost out of the pool is invisible until somebody is handed a bin label already on a shelf. ⚠ Each piece is guarded on its own — half a sweep looks finished and is not — and what failed is counted. ⚠ It deliberately does NOT use `hlxRemoveCustomerToRecycle`, which archives them and puts them on the Recycle sheet: right for a real customer whose lights must come back, wrong for a row that never had any.
    - ⚠ AND "TEST" IS THE PHONE PLUS THE NAME, NEVER THE NAME ALONE. `isTestRecordData` accepts `isTestRecord: true`, or the test phone (3853912235) AND the name Test together. A test customer walked through the real flow spawns records nothing stamped — an invoice keyed on their phone, a route stop copied off them — so identity has to count; but there is a real customer called Test waiting to happen, and deleting them would take their invoice and route too.
    - ⚠ AND AN ARCHIVED RECORD NESTS THE CUSTOMER, which the sweep missed entirely (2026-08-21). Owner, after running it: "Done. 6 things cleared. but the tests in the recycle are still there." `hlxRemoveCustomerToRecycle` writes `{customer: d, archivedAt, archivedBy, reason}`, so the name, the phone and `isTestRecord` all live ONE LEVEL DOWN. Asking `isTestRecordData` about the outer document showed it no name and no phone, so it said no to every archived row and skipped the whole collection — and the recycle queue is mostly archived rows (`whArchivedPending` unwraps it the same way), which is exactly where she could still see them. Reads `raw.customer || raw` now: a row written before that shape existed is flat, and reading only the nested half is the same bug pointing the other way. ⚠ A check that the word `archivedCustomers` appeared in the sweep passed the whole time this was broken; Suite 116 runs it against both shapes.
    - ⚠ AND THE INBOX IS SWEPT TOO. A test customer walked through the real flow leaves messages behind — a Light Colour Change from the portal, an RSVP note — and NONE of them carries `isTestRecord`, because the portal wrote them and knows nothing about tests. Left there they are junk in the one list the office reads every morning. Matched on the phone AND the name, like everything else: a real customer's message is somebody asking for something, and deleting one loses a request nobody else has a copy of.
    - ⚠ AND THE REPORT USES THE WORDS ON THE SCREEN IT IS ABOUT. Owner, reading "Would delete: 1 invoice" with two Test rows on the recycle queue in front of her: "it fails to see it needs to delete a recycle." It was in fact failing to find them at all (the nested-archive bug above), but even once found, "2 archived records" is not obviously the two rows on the Recycle screen. It says "entries on the recycle list" and "messages in the Inbox" now.
    - ⭐ AND THE BUILD TAB CAN BE ASKED ABOUT ONE HOUSE. Owner, having pressed Build Them A New Set: "i clicked build ashley wray but shes not here." The list is five collapsed headings, so "she is not here" and "she is here, inside the third group" look identical until every one is opened — and "not on the list" has four different causes with four different fixes (nothing queued it, sitting the season out, already built, or queued with no colours). `whHouseBuildStatus` answers by asking the REAL `whBuildQueueGroups` rather than re-deciding — a second copy of those rules is how a screen starts confidently contradicting the list beside it — and `whBuildStatusText` says what to do about each.
    - ✅ CHECKED, at the owner's request: deleting a real customer ALREADY returns their number. `hlxRemoveCustomerToRecycle` writes it back to `availableCustomerNumbers` with its `type` (which `cnNextAvailable` and the Customer Numbers panel both filter on) and `releasedFrom`, deletes their invoice, and pulls them off upcoming routes. Suite 116 holds that true — and note the first version of that check was VACUOUS: wrapping the write in `if(false)` left the text in place and it still passed. It is scoped to the branch now.
    - ⭐ THE NUMBER ON THE BIN IS NOT ALWAYS THE NUMBER ON THE RECORD (2026-08-21). Owner, looking at Ashley Wray in the recycle queue: "this shows the updated cusotmer number not the old one, we need the old one in the recycle section because thats how they find it." She re-measured at 300 ft (two bins) so she moved from #894 to the 5000-series; the bin on the shelf still says 894, because nobody has been to it. `binLabelNumber` records what the bin says, `whBinNumberFor` is the one place that answers "which number do I look for", and the recycle queue, the warehouse print sheet and the Printing tab's recycle list all read it. Written ONCE — two moves before the set comes back and the bin still wears the first label — and cleared by Mark Recycled (the bin is empty) and by both Mark Done buttons (a new bin exists under the current number).
    - ⭐ RECYCLING IS A NUMBER AND A NAME, NOT A BUILD. Owner: "they dont need to know the color here they just need the customer number and name." The recycle queue was grouped by pattern and wire like the build queue and read as though it were one ("Warm White — White wire, Need 1"). Nobody makes anything here; they walk to a shelf, find a bin by its number and bring it in. One flat list ordered by name, and the printed sheet lost its Pattern column.
    - ⚠ AND THE BIN NUMBER IS CORRECTABLE BY HAND, because `binLabelNumber` is only stamped when a number moves FROM NOW ON. Everybody whose number already moved has no label recorded and falls back to the number on their record — the wrong one — and that includes Ashley Wray, the customer the whole thing came from. A fix that cannot reach the case that prompted it is not finished. There is a "bin says" box on each recycle row; blank clears it, non-digits are refused, and typing their own number back stores nothing rather than a label that reads as a move. It writes to `archivedCustomers` or `jobAddresses` depending on which the row came from — the same trap Mark Recycled already branches for.
    - ⚠ THE FIRST VERSION OF THAT BOX HAD NO HANDLER. The patch adding the listener silently did not apply, so it rendered an input that saved nothing — identical on screen to a working one, and `npm test` stayed green. Suite 112 now checks the control is listened to and writes the field the list reads. Owner has asked for this class of check by name: "just make sure to double check that if i click a button the function that is supposed to happen actually does."
    - ⭐ AND CONVERTING MOVES THE NUMBER WITHOUT ASKING. Owner: "when I click convert to customer, if the number changes that happens automatically so I dont have to manaully change the number, but before that happens they put it in recycle while it still has the old number." The confirm existed for ONE reason — "their bin is physically labelled with this number" — and `binLabelNumber` now handles exactly that, so the change is no longer silent, just no longer a question; it is announced in a toast naming both numbers. ⚠ Only on a conversion (`requoteBeingConverted`). An ordinary edit that happens to change the footage still asks, because moving a live customer's number underneath them unprompted is a different thing.
    - `quoteFolder(d)` sits beside `quoteStage(d)` and diverts an OPEN re-quote to the Re-quotes tab at every stage. ⚠ CLOSED IS STILL ONE FOLDER: history is looked up in one place, and splitting it means checking both every time and finding it in neither when the guess is wrong. ⚠ And a re-quote is in EXACTLY one folder — subtracted from the ordinary tabs rather than shown in both, because a card in two places is a job each of two people assumes the other did.
    - `showConvertQuoteChoice` now looks the customer up FIRST and hands a live one to `showApplyRequoteChoice`, which opens the Edit Customer form with the new price and footage filled in. ⚠ IT REUSES THAT FORM RATHER THAN WRITING ITS OWN UPDATE: the form already re-derives the bin count, warns when the number series no longer matches, rebuilds the invoice, re-syncs saved route stops and queues the warehouse. A fresh write path would have to do all of it again and would be the copy that quietly falls behind. ⚠ The lookup is against the LIVE list, not the `existingCustomerId` field alone — a customer really can have been deleted since, and that one case does still get built.
    - ⚠ TWO THINGS HAPPEN ON SAVE AND BOTH MATTER. `requoteBeingConverted` holds off the "feet changed → raise a re-quote" rule (applying a re-quote whose whole point was new footage would otherwise raise another for the same change, and the office never reaches the end of the list), and closes the quote as answered. The close is its OWN block, NOT part of the feet/address branch: the commonest re-quote changes only the PRICE — the sides are already on the record by the time the quote is raised — so folding it in would leave the card open for ever in the ordinary case. Suite 65 checks the four-space indent for exactly that.
    - ⭐ AND A RE-QUOTE CAN BE PRICED FROM THE POPUP THAT REFUSES IT (2026-08-20). Owner, on Ashley Wray: "it says its not priced, and it doesnt let me fix the issue from here so theres no way I can convert customer in this situation." Two things had to be true at once. (1) The price box on the quote card starts on a number worked out from the footage but SAVES NOTHING until the office clicks out of it, so the header reads "not priced yet" with 600 sitting in the box — a straight contradiction, and she read the number and believed it was priced. Both suggestion branches say so now. (2) `showApplyRequoteChoice` read `quotedPrice` alone while `showAddCustomerFromQuote` has always fallen back to the feet estimate, so the same quote converts at $600 as a new customer and refuses as a re-quote. Same quote, two paths, two answers.
    - ⭐ NOTHING CHANGING IS A REAL ANSWER, AND MUST NOT BE A DEAD END (2026-08-21). Owner: "if they change address then they go to requote but if the price doesnt change from what it was it wont let you convert them cause something has to change... to the system it looks like nothing changes." Three separate holes, all in the same chain. (1) Raising a re-quote CLEARS `quotedPrice` on purpose — it is what `quoteStage` reads — and keeps the old number on `requoteFrom`. The apply popup ignored `requoteFrom`, so an ADDRESS re-quote, where the money is not moving at all, arrived with an empty box and a held button. It now falls back to their last price, AFTER the feet estimate: if the footage moved, their old price is the one number known to be wrong. (2) On the card, `change` only fires when a human edits the value, and the box starts on their old price — so a re-quote returning at the SAME price had nothing to type, no event, and stayed unpriced for ever. It commits on blur when nothing is stored, and there is a **Use $X — unchanged** button for anyone who never touches the box. (3) The apply step filled in the price and the feet and NOTHING ELSE, so a re-quote raised because somebody MOVED opened their record still showing the old street: save it and the address never moved, the geocode never reran, and saved route stops still pointed at the old house — while the card closed itself as answered. The new address and town are filled now, and only when the quote really carries a different one.
    - The popup now carries one editable price box, pre-filled with the saved price, else the feet estimate, else blank; the button is held while it is blank (and the handler checks again, since a disabled button is still reachable by keyboard); and whatever is used is written back onto the quote as well, because the approval page and the quote email read the quote. A saved price still beats the footage — recalculating could bill them something they never agreed to — and $0 is a price, not a blank. Suite 107 runs the real function against a small fake DOM; the popup was a summary with nothing to type into, which is exactly why there was no way out of it.
    - ⚠ AND AN ARROW POINTING AT THE SAME NUMBER IS A CHANGE THAT DID NOT HAPPEN. Ashley's popup read "Price $600.00 → $600.00": she is already on $600 and that re-quote is about the footage, not the money. Glanced at, an arrow reads as a rise. The price line now says unchanged / up $X / down $X, filled in by `refreshGo` rather than in the markup so it follows the editable box; the footage line says unchanged too, which is the commonest case, since a re-quote answered by a price alone carries the feet straight through.
    - ⭐ AND THE POPUP ASKS WHAT THE WAREHOUSE DOES (2026-08-20). Owner: "after doing a requote it should give the option recycle old build new or build what they dont already have... it should take the number of feet they used to have and subtract it by the new number of feet... and should say to add it to their bin." Two radio choices. **Recycle old, build new** writes exactly what the Recycle button in Edit Customer writes — one way of saying a thing, not two. **Build only the difference** sets `needsLightBuild` and `buildTopUpFromFeet`, and leaves the recycle queue alone.
    - ⚠ IT STORES WHAT THEY ALREADY HAVE, NOT THE DIFFERENCE. Storing 120 would go stale the moment the footage is edited again — re-quote 300 to 350 and the sheet would still say build 120. `houseBundleNeed` does the subtraction fresh every time and is the ONE place that answers "how much to build": the warehouse tab, the warehouse print sheet and the Printing tab's Needs Building list all read it. It stops being a top-up when the feet come back to at or below what is in the bin — a build of zero, or of minus forty, is not a thing anyone can make.
    - ⚠ TOPPING UP IS ONLY OFFERED WHEN THERE IS SOMETHING TO TOP UP (known old feet AND more new ones), and the default follows the ADDRESS: a re-quote carrying a new address is somebody who moved, so their old set is coming back and recycle is right even when the sums would allow a top-up. When it is not offered, the reason is printed where the second button would have been. Ashley Wray has no footage on file at all, which is her own "for this one it will be recycle old build new".
    - ⭐ SUPERSEDED 2026-08-21 — THE ADD-ON IS ALWAYS OFFERED AND ITS FOOTAGE IS TYPED. Owner, looking at a popup showing only Nothing and Recycle: "we also want there to be one thats build the add on so dont build the whole thing again but build however many feet the addition was", then "just have it make you type how many feet instead of calculating." ⚠ It used to appear only when the sum came out — a known old footage AND a bigger new one — so the commonest case of all, somebody adding a side to a house whose feet were never recorded, was refused the one answer that fits it, and the office was left choosing between rebuilding the whole house and doing nothing. ⚠ AND IT NO LONGER GUESSES: the difference between two footages is only the size of the addition if both were measured the same way and neither has been corrected since, and a pre-filled number is one nobody re-reads. ⚠ It is never pre-picked, because its box starts empty — picking it for her and then refusing the button is a worse start; recycle stays the default whenever the house changed. ⚠ Picking it with a blank box is REFUSED, and so is an add-on as big as the whole house (that is a full rebuild wearing the wrong label, and storing it would tell the warehouse somebody already holds zero feet) — both name the answer that does fit. ⚠ It is still STORED as what stays in the bin (`totalFeet - addOn`), so one field and one subtraction, and correcting the footage later still corrects the build.
    - ⚠ AND A FINISHED BUILD IS NOT A TOP-UP ANY MORE. Both Mark Done buttons and the Recycle button clear `buildTopUpFromFeet`; left set, every future build for that house would read as "they already have 180 ft" for ever.
    - ⭐ WHOSE BIN THE BUNDLE GOES IN IS A COLUMN, NOT A NOTE IN THE BLANK ONE. Owner suggested writing "when built put into ashley wray #909" into the blank column, then: "or I dont know if you have a better idea for this because its a tricky situation." The blank column is where the warehouse ticks a row off, so an instruction there takes away the place to mark it done. **When built, put into** is its own column on BOTH build sheets — `WH_BUILD_COLUMNS` (Warehouse tab) and `PRINT_COLUMNS.build` (Printing tab) — empty on every ordinary row so the ones that need it stand out, and it survives the paste into Excel the sheet is built for. A red-check deleting it from the warehouse one PASSED, because the only check written was about PRINT_COLUMNS; there are two build sheets and both need guarding.
    - ⚠ APPLYING A RE-QUOTE IS TWO STEPS AND THE SECOND ONE WAS SILENT (2026-08-20). Owner: "I converted her but she hasnt left the requote secion", then "shes not in the recycle section", then "shes not in the build section." All three are one thing — the popup only OPENS the record; nothing is written until Save Changes is pressed there. Pressing the big gold button reads as having converted them, so closing the form afterwards looked like finishing rather than abandoning. Cancel now says so by name and tells her how to finish; the Save button is re-worded to "Save Changes — finishes the re-quote" while one is in hand, and `requoteRestoreSaveLabel` puts the plain wording back from BOTH endings.
    - ⭐ AND THEN IT STOPPED BEING TWO STEPS AT ALL (2026-08-21). Owner: "after I fill everything out I just want to convert them... I just want it to happen after I fill the stuff out and that doesnt happen." ⚠ THE SECOND PRESS EXISTED FOR ONE REASON AND THAT REASON WAS GONE: it was there because the customer-number rule could need a human answer (keep the old number or move to the 5000 series), and applying silently would have decided that for her — but converting now moves the number by itself, so the question is already answered by the time the button is pressed. **Apply it to <name>** is the primary; **Open their record first** keeps the review path. ⚠ IT STILL GOES THROUGH THE EDIT CUSTOMER SAVE, by opening the form, filling it and pressing THAT form's own button — the form re-derives the bin count, rebuilds the invoice, re-syncs route stops, queues the warehouse and closes the quote, and a write path of its own here would be the copy that quietly falls behind. Both paths go through one `applyRequote(applyNow)`, and both buttons are held while the price is blank.
    - ⭐ AND THE CARD SAYS "APPLY RE-QUOTE", NOT "CONVERT TO CUSTOMER", when the quote still points at a live customer. Owner: "I think the issue might be that its converting them rather than updating them." The button had said Convert since before re-quotes existed, and it is the only wording she sees BEFORE the popup — so the popup then had to talk her out of what the button had just told her. ⚠ Checked against the live book, not `existingCustomerId` alone: a customer really can have been deleted since, and building them again IS converting. Same test `showConvertQuoteChoice` makes before it diverts, so the label and the behaviour cannot disagree.
    - ⭐ AND THE REAL CAUSE WAS A SWALLOWED ERROR (2026-08-20). Owner: "everything is still not working", then "the quote wont dissapear and warehouse isnt being updated." The Edit Customer save handler is ~36,000 characters and its outer catch was `statusEl.textContent = 'Something went wrong — try again.'` with NO console line. Any throw anywhere in it produced five grey words, nothing saved, the re-quote still open and the warehouse never told — indistinguishable from a button that does nothing, and impossible to diagnose over the phone. It now logs the error and shows the actual message.
    - ⚠ AND THE NUMBER-POOL WRITE COULD LOSE THE WHOLE SAVE. When the footage moves a customer across the number series (Ashley: #894 → 5000-series at 300 ft), two writes to `availableCustomerNumbers` run BEFORE the customer is written and the first was unguarded. Every other pool write in that handler is already wrapped for exactly this reason — "a pool write that fails must not undo a save that already worked" — this branch was the one that was not. Reproduced exactly: on the old code one failing pool write gives all three reported symptoms and reports nothing.
    - ⭐ SUITE 108 RUNS THE SAVE HANDLER, it does not read it. Every existing check on that handler read it as TEXT and every one passed. Running it against a fake page and a fake Firestore is what proved the logic was right and the fault was a hidden runtime error. Same lesson as Suite 10 and `syncPayerInvoice`: a regex cannot catch a write that throws.
    - ⭐ MARKING A QUOTE APPROVED YOURSELF IS THE OVERRIDE FOR NEVER EMAILING IT. Owner: "there should be an override where I dont have to email it to them, and that override is if i manually click the they approved for them." Ready to Convert meant approved AND `formCompleted` — right when they answered through the portal, since the form is how the details arrive; wrong when the office took the answer on the phone, since no form is coming and on a re-quote the details are already on the record. `quoteStage` now accepts `approvedByOffice` in place of the form. It is RECORDED when Mark Approved is pressed, not inferred from "approved but never sent" — a quote can be sent and then approved by phone, which is the same override — and it is taken back if the answer is later changed.
    - ⭐ AND A RE-QUOTE CARRIES THE QUOTE'S LIGHT COLOURS ONTO A CUSTOMER WHO HAS NONE (2026-08-21). Owner: "Ashley and Rachel should have lights in warehouse so we can build them." `showAddCustomerFromQuote` has always carried colours across — its own comment spells out why: without a description the customer is saved with `needsLightBuild` false and never reaches the build queue. `showApplyRequoteChoice` carried the price and the footage and NOTHING else, so somebody quoted with colours and holding none came out the far end still holding none. `requoteLightsToCarry` fills the gap.
    - ⚠ AND ONLY INTO A BLANK. A re-quote is usually about price or footage, not colour; writing the quote's colours over a customer who already has some would quietly re-colour a house nobody asked to re-colour, and `lightsChangedAt`, the warehouse and the printed sheets all key off that change. The wire colour follows the same rule. When neither the record NOR the quote has colours, the popup says so on the spot rather than letting the warehouse find out days later.
    - ⚠ RED-CHECK SCRIPTS MUST VERIFY THEIR OWN RESTORE. One run left admin.html sabotaged, the next script read that as its baseline, and every "restore" wrote the broken version back — `npm test` stayed green because the sabotage was in a branch the suite reached only through the tests that had been written for it. Diff against git before trusting the tree, and assert the file is byte-for-byte back after each pass.
    - ⚠ AND CANCEL WAS LEAKING THE WAREHOUSE CHOICE. It cleared `requoteBeingConverted` but not `requoteBuildChoice`, so abandoning Ashley's conversion would have written her recycle-and-build flags onto the next customer anybody saved.
    - ⭐ A HOUSE FLAGGED TO BUILD WITH NO LIGHT COLOURS USED TO VANISH FROM THE BUILD TAB. `whBuildQueueGroups` filtered on `!d.lightsDescription`, so with nothing else waiting the tab said "Nothing needs to be built right now" while somebody sat flagged and invisible — and the Printing tab's Needs Building list has never had that filter, so the two screens disagreed about the same customer. They now come back in `blocked[]`, get their own block at the TOP of the tab (it is the only thing there nobody in the warehouse can act on) with an Add colours button, and go on the printed sheet as type Blocked with the reason in Notes. They are deliberately NOT grouped by a blank pattern, which would mix them into real builds.
    - ⚠ AND THE BUILD FLAG ITSELF WAS BEING WIPED BY A BLANK COLOUR FIELD (2026-08-21). Owner: "big problem, she went to the recycle but not to the build." `addrUpdates.needsLightBuild = newLightsDescription ? … : false` — so EVERY save of a customer with no colours cleared it. Ashley has none. Converting her set both queues in one write; the next save of her record, for any reason at all, cleared the build while leaving `needsLightRecycle` alone (that one is only touched on an RSVP change). Recycle yes, build no. The tail is now `: !!item.data.needsLightBuild` — blank colours mean the build cannot be DONE yet, not that it is not OWED, which is the whole reason the Waiting on light colours block exists. A build is finished by Mark Done or cancelled by Back Next Year, never by a field being empty.
    - A failure closing the card does NOT fail the save. The customer is already right; a card closed by hand is visible, whereas rolling the save back silently undoes the new price.
    - Fixed in passing: the feet-changed card said "over 200 ft" while `CN_DOUBLE_BIN_FEET` has always said 260, so 210 → 250 ft was told in red it needed a second bin it does not need and 190 → 265 was told nothing at all. It also promised the old number went back to the pool, which it does not — the customer holds it until somebody reassigns them, and a pool told a live number is free hands it to the next new customer.
  - ⭐ APPROVING IS SAYING YES TO THE SEASON (added 2026-08-19). A returning member who approved their re-quote had agreed to the PRICE, but `rsvpStatus` stayed blank — so they still read as "pending" and were chased by an RSVP email asking them to confirm a season they had just confirmed. `quoteRespond` now sets `rsvpStatus: 'yes'` on approval.
    - ⭐ REVERSED 2026-08-22 — APPROVING IS A YES EVEN OVER A RECORDED NO. Owner, asked which way and told the trade in full: "go with option 2." THE OLD RULE, kept here because it is the argument against and nobody should restore it by accident: approving only marked somebody whose RSVP was blank or Unanswered, because a recorded "no" was a DELIBERATE answer and one inferred from a price should not outrank it — a re-quote can go to somebody who has already said no, to correct a figure or to price next year, and reading that approval as "I am back in" puts them on a crew day when all they agreed to was the number. That cost is real; she took it, because in practice a re-quote goes to somebody she is trying to bring back and the alternative is flipping every one of them by hand. ⚠ THE LATEST ANSWER STILL WINS — this is one more way to say yes, not a lock. A no arriving afterwards, by link or by office, overrides it. She asked that specifically. ⚠ AND IT HAD TO DO THE WHOLE JOB. The old branch set `rsvpStatus` alone, which was safe ONLY because it never ran for somebody who had said no; now that it does, a status-only write would leave them in the season with `needsLightRecycle` still set — in, and queued to have their lights pulled apart, at the same time. ⭐ SO THERE IS ONE SERVER RULE FOR WHAT A YES DOES: `seasonYesUpdates` in functions/index.js, shared by portalRsvp and quoteRespond. It sets the status, stamps the reply, cancels a queued recycle, re-queues a build ONLY when the recycle actually happened, and marks a rejoiner (`rejoinedForSeasonAt` + `cameBackThisSeasonAt`) when they were out. ⭐ AND IT CLEARS `maybeNextYear` (and `maybeNextYearAt`) when one is set — owner, 2026-08-22: "we shouldn't have to clear a badge to get someone updated. That badge should update once they approve it." ⚠ THAT REVERSES THE LINE THAT STOOD FOR AN HOUR, which said the badge was the office's alone. The argument was answering the wrong question: the badge does not record an opinion the office holds ABOUT somebody, it records what they SAID, and the office set it from what it had been told at the time. A newer answer from the customer supersedes an older one taken on their behalf — and left standing it meant somebody who had actively re-committed sat out the season until a human noticed. ⚠ ONLY WHEN ONE IS SET: writing false onto every yes touches ~960 records for nothing and writes a decision where none was made. `season-state.test.js` RUNS it.
    - ⚠ ONLY FOR SOMEBODY WHO IS ALREADY A CUSTOMER. A new lead has no season to be in yet — they join when the office converts them.
    - ⚠ AND `quoteMatchesExistingCustomer` RETURNS THE RECORD, NOT A BOOLEAN. "Is this a member" and "which member" are different questions and the second one is what lets them be marked. It returns null (never `false`) when there is no match — mixing the two in one return type is how a caller ends up testing the wrong thing, and a test caught exactly that here.
  - ⭐ AN RSVP NEVER GOES TO SOMEBODY WHO HAS NEVER HAD LIGHTS (added 2026-08-19). Owner: "lets just tick new members for now that have come in so they don't get an RSVP but any new people should automatically get ticked and we shouldn't have to do anything." "Will you be getting lights hung AGAIN this year?" is nonsense to a first-year customer and invites a "no" from somebody who was never asked a sensible question. Choosing an RSVP template now sets Automation Emails' **New vs returning** filter to Returning by itself, so new customers are not in the list at all and Select All cannot reach them.
    - ⚠ THE SEPARATE RSVP SENDER IS DEAD UI. `rsvpRenderRecipientList`, `rsvpPopulateMemberSelect` and their send handler are all in admin.html and NONE of their markup exists — every `rsvp*` id is in `KNOWN_MISSING_IDS` in run-all.js, so those functions return on their first line. Reading them and concluding "the RSVP sender has no filters and pre-ticks everyone" is a mistake made in this repo on 2026-08-19, twice, and reported to the owner as fact before it was checked. The RSVP email is an ordinary template sent through Automation Emails, which has the full filter set. Suite 72 asserts the markup still does not exist, so if it is ever built the assumption is re-examined rather than silently going stale.
    - ⚠ A DEFAULT, NOT A LOCK. The dropdown still works, so an RSVP genuinely meant for everybody is one click away — and the recipient count line says the exclusion happened. Automatic is fine; invisible is not.
    - ⚠ AND IT MUST NOT OVER-MATCH. `etTemplateIsRsvp` keys on the rsvp button/link tokens or a folder named RSVP. Matching more widely would quietly drop new customers off EVERY send, which is the opposite failure and far harder to notice — a quote email in particular absolutely does go to somebody who has never had lights. Suite 72 red-checks that direction specifically.
  - ⭐ AN EXISTING MEMBER IS ASKED WHAT IS CHANGING, NOT HANDED THE NEW-CUSTOMER FORM (added 2026-08-19). Owner: "I need the members that already exist not to go to the form once they are created. It should just show a message similar to Do you want anything changed with your lights this year? If they say yes they will go straight to there member portal if they say No than a cute message will come up." Approving used to drop everybody into the install-details form (colours, wire, timer, notes). A member already has all of it on file, and admin's own quote card has warned the office about this case for a while — "Already a customer… do not Convert to Customer again" — so this is the half the CUSTOMER sees. `quoteRespond` returns `alreadyMember` and `offerMemberChangeChoice` asks the question instead.
    - ⚠ THE LINK MUST BE AN EXPLICIT ONE, NEVER A PHONE MATCH. The obvious implementation looks the quote's phone up in `jobAddresses` and it is WRONG here: 17 numbers in the real book are shared and 14 of those are two genuinely different houses (a parent paying for a child's place). A brand-new house quoted against a parent's phone would be told it was already a member and would never be asked for its install details at all. Only `convertedToCustomerAt` (staff-only — `firestore.rules` forbids a public create from setting it; this is the owner's "once they are created") and `existingCustomerId` on a record that still exists (a re-quote) count. Suite 69 red-checks the phone join specifically.
    - ⚠ AND "YES" NEVER SIGNS THEM IN FROM THE QUOTE LINK. A `quoteToken` is generated in the visitor's OWN BROWSER when the public quote form is submitted, so anybody able to submit that form knows one. Upgrading it into a portal session is the exact account takeover closed in `portalLookup` on 2026-08-14. `quoteRespond` returns a boolean and the contact the quote already carries — never a `portalToken`. A login already saved on that device is a different thing and is used; otherwise they sign in normally, last-name checked and rate limited, with the contact box filled in.
    - ⭐ WHICH DOOR THEY CAME IN BY DECIDES, and the PORTAL door was the one actually broken (2026-08-19). Owner: "if they approve in there member portal they should not have the 2 steps just will log them into there member portal and they will be updated to approve", and "new members that have not been converted to a costumer yet should still be given the form". The portal's own Approve button ended with `window.location.hash = '/quote-details'` — the new-customer form — with NO `?token=` on the end, and `handleQuoteLink` only runs when the router sees a token. So the entire member flow was bypassed and the form simply rendered, however good the email route was. A member testing from their own portal saw the form no matter what. It now branches on the same `alreadyMember` the server returns: a member stays put and is shown the approved message; anybody not yet converted still gets the form, because that is the ONLY place colours, wire and timer are collected.
    - ⚠ SO THE TWO-STEP IS THE EMAIL ROUTE ONLY. Inside the portal we already know who they are, so there is nothing to ask. Suite 70 asserts the member branch comes FIRST and that it RETURNS, leaving the redirect reachable — deleting that redirect to "simplify" loses every new customer's install details, and a red-check proves the suite catches exactly that.
    - ⚠ ASKED BEFORE THE `formCompleted` BRANCH, deliberately. A member who once filled the form in would otherwise be told "nothing else to do" and never be asked the one question we actually want answered.
    - ⚠ A KNOWN DISCONNECT, recorded as a GAP rather than papered over. admin.html decides the same question a THIRD way — `quoteAlreadyACustomer`, matching ADDRESS plus phone-or-email (address as well as contact, which is what keeps a parent and child apart). The server deliberately does not copy it: that would put a second copy of the address normalisation on the far side of the wire, which is the duplicated-maths problem `money-parity.test.js` exists for. The cost is that BOTH writes marking a quote converted are best-effort, so a failed one leaves a real member still being shown the form. The office is toasted at the time and closing the card by hand fixes it.
    - ⚠ The suite proves the mechanism; it cannot prove your REAL converted quotes carry the mark. That is what checklist test 201 is for, and it is the only new manual row — everything else went into Suite 69.
  - ⭐ A RE-QUOTE IS NOT A NEW LEAD (added 2026-08-18). Owner: "we shouldnt need a flag that says needs requote the customer should just appear in the requote section", "their customer number should be held only if the # of feet stays below 260 with the additional side, but we should be able to find what their old # was no matter what and their should be a clear way to tell this isnt a new quote so we dont accidentally build the house again", and "if they go above 260 feet hold what you can". A member who changes how many sides of their house are lit is re-quoted: the portal writes a quote and the customer is marked `seasonStatus: 'needs_changes'`. Three things follow.
    - ⚠ THERE IS NO SEPARATE FLAG, deliberately. A `needsRequote` field was added and removed the same day: the quote IS the record that they need re-quoting, and a second field saying the same thing is a second thing to keep in step — the one that goes stale is the one nobody is looking at. Suite 63 fails if `needsRequote` comes back.
    - ⚠ THE QUOTE CARRIES THE WHOLE OLD RECORD, not just the number: `existingCustomerNumber`, `existingMeasuredFeet`, `existingNumberOfBins`, `existingLightsDescription`, `existingLightColors`. The re-measure is the whole point of the re-quote, so whether the old number still fits CANNOT be decided when the quote is opened — only when it is priced. Carrying everything is what makes "hold what you can" possible; carrying the number alone leaves whoever prices it rebuilding from nothing.
    - ⚠ AND THE CARD SAYS SO IN WORDS. A re-quote that reads like a first-time quote gets a second set of lights built for a house that already has some, so the quote card shows a warning box — do not build this house again, here is what is on file, under `CN_DOUBLE_BIN_FEET` everything stays as it is and over it they need another bin and a 5000-series number. The 260 is NAMED, never typed in, so it cannot drift from the bin maths. Suite 64 runs the "On file" strip against a record rather than grepping for the field names: a grep still passes when the line is left standing but can never print, which is the shape this dies in.
  - ⭐ EXACT DUPLICATES ARE A SEPARATE, SAFER BUTTON (added 2026-08-18, REPLACED the same day by the merge above — `dupFingerprint`/`findIdenticalCustomers` are gone). Owner: "create a button that delete duplicates, meaning everything is the same." Danger Zone → `Exact duplicates` groups records by `dupFingerprint` — EVERY key, sorted, with a blank field counting as a missing one — and deletes the copies that are identical to the one kept. Because they are identical there is nothing to weigh and nothing that can be lost, which is why this one needs no superset argument. It keeps THE COPY THE CREW IS GOING TO (deleting takes a record off its upcoming route, so keeping an unrouted copy would silently cancel a stop), refuses a group with two copies on routes, never treats empty records as copies of each other, returns no number to the pool, and still needs DELETE typed. No invoice read: identical records produce an identical invoice key, so both already point at the same invoice.
    - ⚠ Compare EVERY key, never a chosen list — a list stops protecting whatever field is added next, and the whole safety of the button is that "everything is the same" means everything.
    - ⚠ Sorting the keys is load-bearing. Two documents can hold the same fields in a different order; without the sort it finds nothing, which reads as "no duplicates" rather than as a bug.
    - ⚠ THE TWO DUPLICATE TOOLS ARE SPELLED ALIKE. `if(list.length < 2) return;` and the `input.value.trim().toUpperCase() !== 'DELETE'` lock appear in BOTH, so a file-wide check passes with one tool's guard deleted — scope any check for either to that handler's own slice. Suite 42 needed the same fix from the other direction: it anchored on `go.addEventListener('click', onceAtATime`, a shape the new button also has, and being earlier in the file the new one captured its slice and failed a check that was actually fine.
Everything else: just do it.
Before "restoring" anything that looks deleted, check WHEN it went and what replaced it. On 2026-08-13 a suite of 36 failures looked like one regression and was three things: a feature genuinely half-removed by a generic "Update index.js" commit, a button whose handler stayed wired while its markup vanished, and two tests describing designs that had been deliberately replaced the following day. Restoring the last two would have undone the owner's own newer work. `git log -S"someIdentifier" -- file` answers this in seconds.
Never: hand-edit Firestore documents blindly; delete indexes on a rules/index deploy prompt unless the JSON is known complete; push a money change to only one surface; assume a field is dead code without checking every read/write site.
6. Standing task — regenerate the System Map + audit (do this at the start of a fresh session unless told otherwise)
Pull main. Read the actual files (don't trust this doc or memory).
Write system-map.md (plain English an owner can use): the full lifecycle; where each field is entered→stored→read (call out the ones that drive several things, e.g. Measured Feet); every Firestore collection one-liner; the three portals; the money model; everything automatic; and an "if X isn't working, check Y" section at the end. (Done 2026-08-08 — regenerate rather than trust it forever; re-verify anything load-bearing before acting on it if it's been a while.)
Audit using the map: fields collected-but-never-read or read-but-never-written; values that should update in several places but update in one; possible orphans (deleted customer on a route, invoice with no customer, number held by nobody); the same number computed twice that could disagree. For each: what breaks, who notices, how bad.
Add a gap() check to run-all.js for every gap found (the helper reports without failing the build and self-heals to PASS when fixed). Then report and ask before fixing production behaviour. When writing a gap() check's "fixed" condition, make sure it can't false-positive against unrelated code nearby — a naive substring/regex test can accidentally match a fix for a different bug that happens to sit in the same block (this happened once during the 2026-08-08 pass; caught before it shipped).
7. Hard-won lessons (verification traps)
Indexes/rules in the JSON/rules files are not deployed until you run the firebase deploy for them — a perfectly correct file can still throw failed-precondition. Netlify never touches Firebase.
firebase deploy --only firestore:indexes offers to delete console-made indexes absent from the file — say no unless the file is complete.
Scan for duplicate element IDs, not just missing ones (getElementById returns the first — a whole modal was once unreachable).
Missing markup fails silently under ?. optional chaining. Verify IDs exist in HTML, not just that JS runs without error.
Check <div vs </div> balance after any big HTML deletion; run node --check on extracted inline scripts.
Watch for undefined variables after a rename (grep the new identifier inside string concatenation).
str_replace/edits need exact whitespace and real characters — the files use literal em dashes (—), not &mdash;, in places. ⚠ LINE ENDINGS — MEASURE, DO NOT BELIEVE THIS SENTENCE. As of 2026-08-19 admin.html is **LF, not CRLF**: `wc -l` returns 31,637 and `grep -c $'\r' admin.html` returns **0**. On 2026-08-17 this file said CRLF-on-every-line and gave a line count of 26,946; both were true of some checkout at some point and neither is true of this one. That is the third time the claim has flipped, which is the whole point of the rule below — the answer has no memory, so nobody's written-down version of it is worth anything. Earlier the same day this file said the opposite — "pure LF, returns 0" — and that was wrong when it was read; a sabotage script written against LF matched nothing and reported success. That is exactly the failure mode either version of the claim causes, so the only safe rule is the last clause: **run `grep -c $'\r' admin.html` yourself, write `\r?\n` in every regex, and assert the anchor count before writing.** The Edit tool is immune to all of this — it matches the literal bytes — so prefer it over a rewrite script whenever the change can be expressed as one.
Firestore's long-poll Listen/channel "Fetch failed" console line is normal reconnection noise — not a bug.
A collection missing from firestore.rules is denied by default and fails silently in a listener — check the rules file first when a list renders empty.
A field can look orphaned from one read site and still be live from another — grep every read AND write site before calling something dead code (see §9 in system-map.md, frontPhotoUrl).
A fixed-length character-window extraction in a test (e.g. "next 9000 chars after this anchor") will silently go stale as the real code grows past it, turning true passes into false FAILs. Prefer slicing to the next real structural anchor over a magic number.
A Project To-Do checklist test's wording drifts the same way any other doc does — it doesn't auto-detect that a button got renamed. On 2026-08-08 five Admin quote tests (#19/20/30/31/33) still told the reader to open "the approval link" hours after that button had been renamed to "Send Email" in the same session; the owner caught it by actually reading a rewritten test's steps, not by any automated check. Whenever a change touches something a test describes, update+version-bump that TEST_SEED entry in the same change (see §0).
8. Current security note
The quotes collection is currently open to unauthenticated scoped updates (Option A) so the customer approve/detail flow works without a login. Price and all other fields stay staff-only. This is a deliberate, owner-approved tradeoff, unchanged in this pass. If future work re-introduces the function-based quote flow, re-tighten this rule to staff-only update and confirm the portal still saves through the functions.
9. Automated testing — rules, conventions, and how the pieces fit
Added 2026-08-14. Read this before writing any test or any selector.
9.1 The three test systems and what each is for
There are three, deliberately. Do not merge them and do not duplicate a check across them.
run-all.js (repo root) — structure, wiring, and logic read straight out of the source files. Fast, no browser. This is where "does admin.html still call this function", "do these two files agree about a field name", and the js/money.js unit tests live. Biggest suite, run on every change.
money-parity.test.js (repo root) — one job: prove the browser copy and the server copy of the money maths still agree. See §9.2.
Playwright specs (tests/) — real browser, real DOM, real click handlers, fake database. For user flows: filling the quote form, logging into the portal, editing an invoice amount, marking a house done. Slower, so keep it to flows that matter.
Project To-Do checklist (Firestore projectTests, TEST_SEED in js/test-seed.js) — the owner's manual sign-off list — 125 rows on 2026-08-25, trimmed from 210 to 108 on 2026-08-17 and grown since (⚠ measure it, see §0). NOT automated and not replaced by any of the above; it holds only what a human or a live environment must verify. When a Playwright spec covers a checklist test, put the checklist id at the front of the spec title — test('t11 — portal shows a PayPal button', ...) — so the two point at each other instead of drifting into two separate truths.
9.2 The money parity rule (the most important test in the repo)
The invoice maths exists TWICE and cannot be shared, because one runs as a browser ES module and the other runs on Node inside Cloud Functions:
  js/money.js          computeInvoiceStatus()        what the office sees
  functions/index.js   computeInvoiceStatusServer()  what the customer is actually billed
  js/money.js          custInvoiceKey()             which invoice a customer files under (admin)
  functions/index.js   invoiceKeyFor()              the same decision, server side
money-parity.test.js lifts all four out of the real files, runs them side by side over ~13,000 money combinations plus blank/text/broken inputs, and fails the moment the two disagree. It does not care what the formula IS — only that both copies say the same thing — so it keeps working when pricing rules change.
If you change one of these formulas, change the other in the same commit. If the parity test fails, the office screen and the nightly billing run disagree about someone's bill. Do not push. Do not "fix" it by editing the test.
If a rename makes the test unable to find one of the four functions, it FAILS loudly rather than skipping — that is intentional. A test that cannot find its target must never report green.
9.3 Selectors — how to target elements in a browser test
Prefer, in this order:
1. An existing id. admin.html is full of stable ids and gate C already checks every id referenced in JS exists. Use them: page.locator('#invoiceAmount').
2. data-testid, added only where there is no stable id, and only on elements a test actually touches. Add them AS YOU GO, not in a big sweep — a mass edit of a 21,000-line file is risk without benefit (owner's decision, 2026-08-14).
3. Visible text, for buttons whose label is the thing being tested.
Never use CSS descendant chains (.panel > div:nth-child(3) input) or XPath. They break on any layout change and produce failures that look like bugs but aren't.
Naming: data-testid="area-thing-action", lowercase and hyphenated — data-testid="invoice-amount-input", data-testid="quote-send-btn". A testid is part of the contract; renaming one is a change that needs its test updated in the same commit, exactly like a TEST_SEED entry.
9.4 Tests NEVER touch real Firebase
Non-negotiable. There are ~967 real customers and real money in that project.
Playwright serves the real HTML with a STUBBED Firebase module. No credentials in any config, any spec, any fixture, or any CI secret used by tests.
The stub must THROW LOUDLY if anything reaches a real Firestore or Auth endpoint. A test that silently falls through to production is worse than no test.
Never point a test at project highlighting-utah. If you ever genuinely need real backend behaviour, that is the Firestore emulator, it is a separate decision, and it needs the owner's say-so first.
This rule outranks getting a test to pass. If the only way to make a test work is to touch real data, the test does not get written.
9.5 Fixtures
One shared fixture file. Do not let each spec invent its own fake customer — they drift and then nobody trusts any of them. The fixtures must include the edge cases that have actually caused problems here: a multi-house billToPhone group, an email-only customer (no phone), a 260ft double-bin house (CN_DOUBLE_BIN_FEET, NOT the 200ft some old UI text still claims), a customer carrying carryoverCredit, and an unrated-difficulty house.
9.6 TDD — red, green, refactor
For a bug fix: write the failing spec FIRST, watch it fail for the right reason, then fix the code. A spec that has never failed has not been shown to test anything.
For a new feature: write the spec alongside the code, not after the fact. "After the fact" reliably becomes "never".
HISTORIC: browser coverage was started on the five known Member Portal failures (checklist tests 9, 11, 14, 15, 17). All are green as of 2026-08-14 (§9.14). New coverage should go where there is still real risk — the money paths and the crew portal — not on re-testing these.
9.7 Flaky tests
A flaky test is fixed or deleted. It is never retried until it goes green, and retries are never added to paper over one.
The reason is not purity: a suite with known-flaky tests trains you to dismiss failures, and then the real one gets dismissed too.
9.8 Speed budget
The whole of `npm test` stays under 60 seconds. If it creeps over, cut or parallelise — a suite slow enough to skip is a suite that gets skipped.
9.9 The standing rule — tests follow every change (owner's instruction, 2026-08-14)
Any change to the website updates its tests IN THE SAME COMMIT. Concretely, all four of these, whichever apply:
  1. the relevant run-all.js / Playwright check
  2. the TEST_SEED entry in admin.html, with its version bumped and a retestReason (§0, §7)
  3. RETIRED_CHECKLIST_TERMS in run-all.js, if the change retires a UI term a test used to describe
  4. money-parity.test.js, if a money function was renamed
Shipping code without its test update is not "finishing early", it is leaving a trap. This is the rule the owner asked for by name — do not skip it.
9.10 Deploy safety — decisions already made, do not re-propose
Canary / percentage rollout with automatic rollback: CONSIDERED AND REJECTED, 2026-08-14, with the owner's agreement. Do not build it and do not suggest it again without new information.
Why it does not fit: the site is three static HTML files on Netlify's CDN. A broken admin.html still returns HTTP 200 — it fails in the browser, not at the server. There is no 5xx spike for an auto-rollback to trigger on, so the whole mechanism would watch a signal that never arrives. On top of that, 5% of the admin audience is a fraction of one person (the owner, her mother, her father, the crew), and Netlify's split testing uses branch affinity, so whoever draws the canary is PINNED to the broken version while everyone else sees a working one — an unreproducible bug report by design.
What is used instead, and is enough at this scale:
  1. Netlify instant rollback. Every previous deploy is retained and republished in one click from the Netlify UI (Deploys → pick a known-good deploy → Publish deploy). This is the "roll back to the safe version" step, available in about fifteen seconds. Reach for it FIRST when something is wrong in production — diagnose afterwards, not before.
  2. Deploy previews. A branch deploy gives a real URL to look at before anything reaches main. At this scale that beats a canary: the person reviewing is the owner, who knows the system, rather than 5% of random traffic.
Firestore rules and Cloud Functions are NOT covered by a Netlify rollback — they are separate surfaces (§1). Rolling back the HTML does not roll back a functions deploy. If a bad change spanned both, roll back the HTML and then revert the functions commit so CI redeploys the previous version.
9.11 Production monitoring — read-only only
Synthetic monitoring of the LIVE site is approved in principle (Phase 5), with one hard limit: checks are READ-ONLY.
Never write a synthetic that exercises a write path in production. The obvious "log in, add to cart, check out" pattern maps here to creating a real quote in the quotes collection — at one run every ten minutes that is roughly 4,300 junk quotes a month landing in the office queue, plus whatever a synthetic payment does to invoice data. A write-path synthetic would need a designated test customer excluded from every list, route, invoice and export; that is its own project and has not been approved.
Safe checks: does the public site load, does the portal login function respond, does admin.html load with a clean console, is the Cloud Function answering.
Cost note (verified 2026-08-14): Checkly's free Hobby tier allows 1,000 browser check runs and 10,000 API check runs a month. A browser check every 15 minutes is ~2,880 runs and overruns the free tier three times over; hourly is ~720 and fits. A light API check every 5 minutes is ~8,640 and fits. So: API checks frequent, browser checks hourly. A scheduled GitHub Actions workflow running the same Playwright specs against production is the $0 alternative.
The highest-value monitor already exists and predates all of this: the nightly billing summary text and the stale-run banner (§6 of system-map.md). Do not let a monitoring project quietly replace them — they watch the thing most likely to silently cost money.
9.12 The commands, in one place
bash
npm install                    # once per machine
npx playwright install chromium # once per machine, ONLY for browser tests (~150MB)
npm test                       # gate A + selectors + money parity + option audit + season state + run-all.js. No browser. ~8 seconds.
npm run test:options           # the option registry audit on its own
npm run test:season            # where each RSVP answer ends up, on its own
npm run test:browser           # Playwright specs. Needs the chromium install above.
npm run test:all               # both
npm run test:browser:headed    # watch a browser test run, for debugging
`npm test` is deliberately browser-free so it always works, on any machine, with no 150MB download. Browser tests are a separate command and a separate CI step. Keep it that way — the fast suite is the one that actually gets run.
9.13 How the browser tests are wired (read before editing tests/)
No change to index.html was needed, and none should be. index.html imports Firebase as ES modules from https://www.gstatic.com/firebasejs/10.12.2/... — Playwright intercepts those three URLs and serves fake modules in their place. The page's own code runs exactly as it does in production; only what sits underneath it is fake.
The entire member portal reaches the backend through ONE helper, callPortalFn() → httpsCallable(), across four callables: portalLookup, portalInvoice, portalRsvp, portalSave. Faking those four plus a couple of settings reads covers the portal completely. That is why tests/firebase-stub.js is short — it is not cutting corners, the app is genuinely that well funnelled. Preserve that funnel; if a new portal feature calls Firestore directly from the browser instead, the stub gets harder and the security story gets worse.
The fixture field names are PORTAL_READ_FIELDS from functions/index.js verbatim — that is the only set of fields portalLookup ever sends to the browser. Change that list in the Cloud Function and change tests/fixtures.js in the same commit, or the tests assert against a shape production no longer returns.
Every spec ends with stub.assertNoRealCalls(). Do not remove it and do not soften the FORBIDDEN_HOSTS list to make something pass (§9.4). One spec deliberately tries to reach real Firestore and asserts it is blocked — that one proves the guard is still alive.
HISTORIC: tests/portal.spec.js was written red-first against the five known checklist failures (t9, t11, t14, t15, t17). All five are green as of 2026-08-14 — see §9.14 for which were real bugs and which were the harness. "Do not weaken a spec to turn it green" still stands; note that t17's decline matcher was corrected from /decline/i (which matched nothing) to the button's real label, "Not Right Now". That is making an assertion accurate, not weakening it — and it was deliberately not "fixed" with an aria-label, which would have broken WCAG 2.5.3 Label in Name.
t11 is worth trying before writing any code: index.html reads paymentProvider and falls back to 'venmo'. The spec runs with it set to 'both'. If that alone goes green, the "no PayPal button" bug was only ever a Firestore setting, not a code defect.
9.14 CI — what gates what (built 2026-08-14)
Two workflows, and it matters which one is the real gate.
.github/workflows/tests.yml runs on every push to main and on every pull request. Two jobs:
  fast-tests — npm ci then npm test (gate A, selector contract, money parity, run-all.js). No browser, about five seconds.
  browser-tests — npm ci, install Chromium, then `npm run test:browser`. About a minute. Uploads traces and screenshots as an artifact when it fails.
  ⚠ CORRECTED TWICE, 2026-08-21 AND 2026-08-22, FROM TWO SESSIONS THAT FOUND THE SAME STALE CLAIM. This said "There is NO browser job in CI. It was removed on 2026-08-14." That has not been true for some time: the job is in tests.yml, it gates, and it is one of the two REQUIRED status checks on main. The deletion argument was sound when the specs were permanently red; the premise changed when they went green, and the header comment in tests.yml records that resolution in full — including the instruction that if they ever go permanently red again the job should be DELETED rather than given continue-on-error back, which is the one thing both sessions agreed was worthless.
  ⚠ SO A RED BROWSER RUN BLOCKS EVERY MERGE, including a money fix. It does not gate the functions deploy (deploy-functions.yml runs its own fast-suite job) and it does not gate Netlify directly — but nothing reaches main without it, and Netlify publishes from main. Budget for that before assuming a red browser spec can be left for later.
  ⚠ THIRTEEN BROWSER SPECS, ALL PASSING — 10 in test/portal.spec.js, 3 in test/quote-link.spec.js. Re-run and re-counted 2026-08-21; this line said "all eight" from 2026-08-14 until then, because specs were added and the number was never updated. (A note dated 2026-08-22 elsewhere still said eight — same staleness, one day younger.) This section previously said five were red on purpose (t9, t11, t14, t15, t17). None are. THREE of the five were never product bugs at all — they were the harness not matching production, and each cost real time to chase into the page before that was spotted:
    t9 / t14 — every invoice fixture omitted name/phone/email, though INVOICE_READ_FIELDS begins with exactly those three; and the stub returned invoice fields FLAT while production returns {found, record} and takes `key`. index.html reads res.record, so every field came back undefined.
    t11 — the fake PayPal SDK had no FUNDING object. The real one does, and index.html reads FUNDING.PAYPAL, so renderPaypalButtons threw and the container stayed EMPTY.
  t17 WAS a real bug and is fixed: the quote approve/decline card only ever rendered when portalInvoice found nothing, so any customer who had ever been billed could not see a new quote. index.html now calls offerPendingQuote() after the account renders.
  THE LESSON, before chasing any red spec into the page: check the fixture and the stub match what production actually returns FIRST (§9.13).
  Get the baseline before blaming your own change — `npm run test:browser` needs `npm install` first, because @playwright/test is a devDependency the fast suite does not pull in.
  (The old "delete the continue-on-error line once they go green" warning is retired with the job it referred to.)
.github/workflows/deploy-functions.yml now has a `test` job that the `deploy` job `needs:`. Cloud Functions do not deploy unless the fast suite passes on that exact commit.
THIS is the real gate in this repo, and it is deliberate. Netlify publishes the HTML from main whether or not CI passed — nothing in GitHub Actions can stop it, because Netlify builds independently. So gating the FUNCTIONS deploy is where a failing test actually prevents something dangerous: nightly invoicing, PayPal capture, and the portal write path all live there. The website's safety net is Netlify's instant rollback instead (§9.10).
The deploy gate runs the fast suite only, not the browser suite. Blocking every functions deploy on a known-red suite would just train everyone to bypass the gate — which is how gates die.
If a functions deploy is ever urgently needed while the fast suite is red: fix the test. It takes seconds to run and there is no legitimate reason to ship money code past a failing money-parity check.

9.15 Browser tests are NOT parked any more — they gate (corrected 2026-08-21, again 2026-08-22)
⚠ THIS WHOLE SECTION DESCRIBED THE OPPOSITE OF THE CURRENT SETUP and is kept, corrected, rather than deleted, because the argument in it is still worth reading — it is the case for deleting the job if the specs ever go permanently red again, which is what tests.yml says to do.
What is true now: the Playwright specs in test/ ARE run by CI, on every push and every pull request, and "Browser tests (Playwright)" is one of the two REQUIRED status checks on main. They are not parked and they cannot be ignored — a red run stops the merge. Do not go looking for a job that isn't there.
What was true when this was written, and is not now: the specs were permanently red, so the job could only ever be noise. All thirteen pass. A green suite that goes red is a signal, not noise. So the test to apply is the PREMISE, not the conclusion.
The paragraphs below are the ORIGINAL reasoning, left intact deliberately: they explain what to do if that premise flips back.
The reason is not that they are bad tests. It is that they need a 150MB browser download, a machine to run on, and a local clone kept in step with GitHub — and keeping those in step is exactly what went wrong repeatedly the day they were built. The owner runs a Christmas lights business, not a dev team. A suite that needs babysitting gets ignored, and an ignored suite is worse than no suite because it trains you to dismiss failures.
Before re-adding the browser job to CI, be honest about one thing: is there a person who will act on a red run? If not, do not re-add it. A permanently-yellow job is noise.
What still runs on every push regardless: npm test — no browser, no setup, a few seconds. That includes money-parity.test.js, which is the one that actually protects money, and the Cloud Functions deploy is gated on THAT job only (deploy-functions.yml runs its own copy), never on the browser job. ⚠ Do not trust any check count written down here; run it.
⚠ RUNNING THEM IN A CONTAINER: if every spec fails at once with "Executable doesn't exist at /opt/pw-browsers/chromium_headless_shell-…", that is the sandbox's pre-installed Chromium not matching the pinned @playwright/test build. It is NOT a product failure and NOT a spec failure — do not go chasing it into the page. Point Playwright at the build that is actually there (a throwaway config in the scratchpad setting `use.launchOptions.executablePath` to /opt/pw-browsers/chromium-<build>/chrome-linux/chrome, with an ABSOLUTE testDir and a webServer cwd, since both resolve relative to the config file). Verified that way on 2026-08-21: 13 passed in 1.6 minutes.
The right way to pick these up is Claude Code, which can run the specs, read the failure, edit the file and re-run without anything being copied between machines by hand.
9.16 What testing costs the owner, by design
Nothing on a schedule. There is no daily or weekly test routine and there should not be one.
Push to GitHub → CI runs both jobs by itself (⚠ corrected 2026-08-22: this said "the fast suite" alone). GitHub is set to email on FAILURE ONLY, so no email means it passed. That is the entire feedback loop.
⚠ But note what changed underneath it: because both jobs are now REQUIRED checks on main, a red run is no longer only an email you can read later — it stops the merge. Nothing reaches the website until both are green.
`npm test` by hand is optional, for when someone wants to know before pushing rather than after.
The live business is watched by the nightly billing summary text and the stale-run banner (system-map.md §6), NOT by any of this. Do not let a testing project quietly displace them.
If a future session is tempted to add visual regression, synthetic monitoring, or anything else that needs regular human attention: ask first, and assume the answer is no unless the owner says otherwise.
